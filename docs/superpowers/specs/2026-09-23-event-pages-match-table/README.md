# Event pages as match tables — design frames

Static mockups from the Claude Design canvas
https://claude.ai/artifact/HtpJvpw5iLpuswGYy9qh7v (2026-09-23), chosen by the
author: summary **strip** (not the scoreboard alternative), "Add video" as the
dual's primary once every line is scored.

- `Main.dc.html` — dual page, line table
- `DualDrawer.dc.html` — dual, singles line drawer (S2)
- `DualDoublesDrawer.dc.html` — dual, doubles line drawer (D1)
- `TournamentDrawer.dc.html` — tournament page grouped by entry, match drawer open

Each is plain HTML with inline styles inside `<x-dc>`; read them for copy,
order and spacing. Sample names and numbers are mockup data. Built by T7–T13
in `.claude/tasks/claude-event-deletion-matches-fc6c61.md`.
