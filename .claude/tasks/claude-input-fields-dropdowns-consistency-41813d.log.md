# Run log — claude/input-fields-dropdowns-consistency-41813d

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add the advField() class helper — done
- **gate:** lint 0 errors (37 pre-existing warnings, none naming adv-field) · `tsc --noEmit` clean, no `.next/` staleness to clear · `npm test` 301 passed · `task-completion-reviewer` VERDICT: pass, all five criteria met with file:line evidence and no out-of-scope change. Guardrails both skipped legitimately: the diff is one new file under `src/lib/ui/`, which is neither a `pipeline-guardrails-reviewer` surface (`src/app/dashboard/`, `src/components/dashboard/`, upload wizard) nor an `rls-boundary-reviewer` one (`src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, any new table/view/query). Surfaces determined from `git diff HEAD --stat` **and** `git ls-files --others --exclude-standard`, since the new file was untracked.
- **changed:** Added `src/lib/ui/adv-field.ts` (111 lines), the sibling of `advButton()`: `advField(kind, size)` returning the canonical boxed and underline field classes, with `AdvFieldKind`/`AdvFieldSize`. Boxed is `rounded-[var(--radius-button)]` (6px) at 36/32px with 13/12px text, mirroring `advButton`'s md/sm so a field lines up with the button beside it. Underline is 34px with a bottom rule that thickens to 2px blue on focus. No focus ring is emitted, and the header records why: `focus.css` is unlayered, silently discards `focus-visible:*` utilities, and already supplies `--focus-ring-field`. The header also records the two-radius-scale trap (`rounded-md`/`lg`/`xl` = 8/10/14px here, not the design system's 8/12px) that made the drift systemic. For the disabled background it substituted the existing `--surface-subtle` rather than adding `--bg-field`, which does not exist anywhere in the repo — a new token would owe an unreviewed hex in the measured WCAG-verified `.dark` block, and `--surface-subtle` is two points of grey away and already the token `advButton()` uses. `colors.css` untouched; the helper has no consumers yet, by design.
- **follow-ups:** ideas for the author to triage through `/task-add`, not eligible to run.
  1. `advField()` deliberately emits no width. `ui/input.tsx` carries `w-full` plus `file:*` and `aria-invalid:*` utilities outside the design-system field spec — T2 should decide explicitly whether those survive rather than dropping them silently.
  2. `size` is accepted but ignored for the `underline` kind, documented in the header. If a second underline tier is ever designed, that is where it goes.
  3. Two `#F7F7F7` literals in `statistics/period-toggle.tsx` and `stat-trajectory-chart.tsx` are pill backgrounds, not fields. They are chrome, out of this feature's scope, and a later batch's grep should not sweep them up.
