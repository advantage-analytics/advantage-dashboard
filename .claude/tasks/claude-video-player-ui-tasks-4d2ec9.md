# Tasks — claude/video-player-ui-tasks-4d2ec9

> Scope: matchId Video tab follow-ups — the "This point" widget, the point-row winner mark, bounce times from the vendor's regular results JSON, and the seek-track set tooltip design

Run one with `/task-next`. To drain the file, loop a plain-text instruction —
**not** `/loop /task-next`, which a scheduled fire cannot invoke:

> `/loop Read .claude/skills/task-next/SKILL.md and follow it exactly — run one task from this branch's queue; do not add, edit, or reorder tasks; then stop.`

Append freely while it runs: the queue is re-read at the start of every
iteration, and the runner only ever rewrites a task's `status:` line.
Mark a task `next` to jump the queue.

Status values: `todo` (eligible to run), `next` (jump the queue), `doing` /
`done` / `blocked` (written by the runner around a dispatch), and `later`
(deferred — `/task-next`'s picker never selects it, so a loop drain skips
straight past it; promote a task to `todo` by hand once it's actually
ready).

## T1 · Rename "Current point" to "This point" and drop the speed column

- **status:** done
- **model:** sonnet
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-this-point.tsx; docs/superpowers/specs/2026-09-20-video-tab-in-shell-h1-design.md (section B, prose only)
- **done when:**
  - [ ] The eyebrow and the section's `aria-label` both read "This point", and the string "Current point" no longer appears in `film-this-point.tsx`
  - [ ] The speed header cell (`Mph` / `Km/h`) and the speed body cell are gone, `WIDE_COLUMNS` has seven tracks (the `44px` track removed, every other value unchanged) and `NARROW_COLUMNS` is unchanged
  - [ ] `unit` / `DistanceUnit` / `formatSpeedValue` are removed from the file and from its call site in `film-tab.tsx` only if nothing else in the file reads them; `npm run lint` and `npm run typecheck` pass
  - [ ] `film-shots.ts`, `point-list.tsx`, `film-court-card.tsx` and `tests/film-shots.spec.ts` are not in the diff — `shotRowCells.mph` stays because the room's court readout reads it
  - [ ] Section B of the H1 spec doc names the widget "This point" and its rail-collapsed column list no longer lists `Mph`, dated as an author decision 2026-09-21
- **notes:** Resolved via trace-route: `/dashboard/matches/[matchId]` → Video tab → `film-tab.tsx` → `FilmThisPoint` in `film-this-point.tsx`. The room's court already titles its point mode "This point" (`film-fullscreen.tsx`, T12), so the rename makes the two agree. No spec asserts on the heading. Keep the `#`, Player, Spin, Stroke, Type, Placement, Result columns and the "added and removed, never re-sorted" rule from the H1 spec.

## T2 · Point-row mark shows the point's winner, not the last hitter

- **status:** done
- **model:** sonnet
- **files:** (guess) src/components/dashboard/matches/match-detail/film/point-list.tsx (the `isYou` line ~451 and the "decisive-player mark" doc comment ~44–49); tests/film-playback-refresh.spec.ts or its fixture tests/fixtures/film-point.ts for the pinning case
- **done when:**
  - [ ] `isYou` in `PointList` is computed from `point.wonByPlayer1 === youIsPlayer1`; `point.player` is not read anywhere in `point-list.tsx`
  - [ ] The file's header comment describes the mark as the point's WINNER — the viewer's `WorkspaceMark` when the viewer won it, the opponent's initials chip otherwise — and says the value is `won_by_player1`, which the Advantage Intelligence derivation folds from consecutive point scores (`derivation/winners.ts`) and the SwingVision parser reads from the export
  - [ ] Both tones are covered by the one change: no second `isYou` computation exists for the dark drawer, and `film-room-drawer.tsx` is not in the diff
  - [ ] A spec renders (or unit-tests through the fixture) a point with `player: "player2"`, `wonByPlayer1: true` for a viewer who is player 1 and asserts the row carries the workspace mark, not the initials chip; the mirrored case asserts the chip
  - [ ] `src/lib/data/match-points-server.ts` is not in the diff: `MatchPoint.player` and `determinePlayer()` stay for their other readers
- **notes:** The room's scoreboard pill (`film-fullscreen.tsx` `wonByYou`) and the "Point went to" filter axis (`filters/types.ts:291`) already use `wonByPlayer1`, so after this the three agree. `won_by_player1` IS the score-derived winner the author asked for: `resolveWinner` in `derivation/winners.ts` walks the ladder / game / set counts between consecutive rallies and deliberately ignores the last stroke's `in` flag. Points flagged `winner_disputed` (22 of 87 on Caden Ace v Matt Goodman) are exactly where the old chip and the new one differ. `useMatchSides()` stays the only source of you/opponent (guardrails §4). `tests/film-playback-refresh.spec.ts` selects rows by `[data-point-id]` and never asserts on the mark, so it needs no change; add the pinning case in the node style if the browser harness is heavier than the assertion deserves.

## T3 · Carry the vendor's bounce_frame to shots.bounce_video_time

- **status:** done
- **model:** fable
- **files:** (guess) src/lib/services/splitstep/derivation/types.ts, parse.ts, ball-paths.ts (move `fitFrameToTime` to a new pure `frame-clock.ts` and import it back), transcript.ts, persist-transcript.ts, src/lib/data/match-points-server.ts (select + `MatchShot`), supabase/migrations/<stamp>_add_shot_bounce_video_time.sql (new), tests/splitstep-derivation.spec.ts or a new node spec, tests/splitstep-ball-paths.spec.ts (import path only), the two specs that build `MatchShot` literals
- **done when:**
  - [ ] `SplitStepStroke` gains `bounceFrame: number | null`; `normalizeStroke` sets it from `raw.bounce_frame`, null when the value is missing, non-finite, the `-9999` sentinel, or less than `raw.frame`. A spec pins each of those four nulls and one accepted value
  - [ ] A pure exported `bounceVideoTimes(strokes)` (in `frame-clock.ts` beside the relocated `fitFrameToTime`, no I/O) returns, per stroke, the fitted seconds of its `bounceFrame` on the SAME clock as `videoTime` (trim offset already included), or null when the frame is null or fewer than two distinct stroke frames exist. `deriveBallPaths` imports the fit from the new module and `tests/splitstep-ball-paths.spec.ts` still passes unchanged apart from any import path. The spec pins: strokes at frames 0/30/60 with `videoTime` 100/101/102 and a `bounceFrame` of 45 on the first give `101.5`
  - [ ] The transcript's shot rows carry `bounce_video_time` and `persistTranscript` writes it; the SwingVision path (`process-match`, `swingvision-parser.ts`) is not in the diff, so imported rows stay NULL
  - [ ] A new migration adds `shots.bounce_video_time real null` with a column comment naming the source (`bounce_frame` fitted through the strokes' frame/time pairs) — nothing else in the DDL — and it is applied to the live project through the Supabase MCP, following `.claude/skills/create-migration/SKILL.md` (RLS on `shots` is unchanged; a column adds no policy). `mcp__supabase__list_tables` output in the run log shows the column
  - [ ] `match-points-server.ts` selects the column and `MatchShot` gains `bounceVideoTime: number | null`; the two specs that build `MatchShot` literals (`tests/film-shots.spec.ts`, `tests/match-video-attachments-db.spec.ts`) carry the field; `npm run typecheck` passes
- **notes:** The regular results JSON already carries `bounce_frame` per stroke (`RawSplitStepStroke`, types.ts:64) — only `trajectories.json` was ever converted, in `ball-paths.ts:137–145`, with the same sentinel and ordering rules; reuse that logic, do not fork it. `videoTime = raw.time + startTimeSeconds` (parse.ts:153) already carries the trim offset, so a fit against `videoTime` lands the bounce on the source-video clock, equal to `shots.video_time`; `toFilmTime` in the room does the one conversion later (T4). Verify column names against the live DB, not `supabase/migrations/` (about 100 behind). Guardrails §2: additive column, no backfill, no mutation of existing rows — existing matches get the value only when the author re-derives them by hand. **No client code and no room change in this task.** Do not run `scripts/splitstep-derive.ts --write` against any live job; a `--job <uuid>` dry run is fine and read-only. This touches the service-role boundary only through `persistTranscript`, which is unchanged in shape; `rls-boundary-reviewer` runs at `/pr-check`.

## T4 · Room placement marks fire at the stored bounce time

- **status:** done
- **model:** opus
- **needs:** T3
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-shots.ts (`ShotStop`), film-fullscreen.tsx (the `courtMarks` memo ~411–439 only), film-court.ts (doc comment on `bounceEventTime` / `BOUNCE_REVEAL_SHARE`), tests/film-shots.spec.ts, tests/film-court.spec.ts
- **done when:**
  - [ ] `ShotStop` gains `bounce: number | null` — `toFilmTime(shot.bounceVideoTime, clock)` when the shot carries one and it is ≥ the shot's own `start`, else null. `tests/film-shots.spec.ts` pins one converted value (with a non-zero clock offset), one null for a missing time and one null for a bounce before contact
  - [ ] The `courtMarks` memo in `film-fullscreen.tsx` builds `TimedShot.bounceTime` as `stop.bounce ?? bounceTimes.get(stop.shot.id)` — the regular-JSON bounce first, the ball-paths match second, and `estimatedBounceTime` (inside `pointMarks`) only when both are absent. Nothing in the keydown switch, the Tab-trap effect, the problem panels or `arm` / `wake` is in the diff
  - [ ] `tests/film-court.spec.ts` adds a case: a shot whose `bounceTime` comes from the stored value draws its bounce mark at that time (opacity 1 at `bounceTime`, absent at `bounceTime − 0.2`) rather than at the 60 % estimate, while its contact mark still appears at `contactTime`
  - [ ] The `BOUNCE_REVEAL_SHARE` provenance comment in `film-court.ts` now says the estimate is the fallback for shots with no stored bounce time and no ball-paths match, and no longer claims "which today is all of them"
  - [ ] `film-tab.tsx`, `film-player.tsx` and `film-this-point.tsx` are not in the diff (the shell has no court; nothing else reads the new field)
- **notes:** Two clocks: `shots.bounce_video_time` is SOURCE-video seconds; `ShotStop.start` and everything the room compares against are FILM seconds (`toFilmTime`, minus `clock.offset`, clamped). Convert in `shotStops()` where `start` is converted, so the room stays on one clock. Contact marks already fire at the regular JSON's `time` (`contactTime: s.start`), so this task changes only the bounce event. `ballAt` / `film-court-ball.tsx` (the moving dot) keep reading the ball-paths file and are untouched. After this lands the author must re-derive a match (`npx tsx scripts/splitstep-derive.ts --job d3bff342-b33a-417a-a332-b5a3192f3f4d --write` for Caden Ace v Matt Goodman) before any existing match shows a stored bounce; that run is the author's, not a criterion. Keep the test hooks `film-room-video`, `data-film-chrome`, `data-shot-id`. Run `tests/film-playback-refresh.spec.ts`.

## T5 · Design the seek-track set tooltip for the shell player and the room

- **status:** done
- **model:** fable
- **files:** (guess) docs/superpowers/specs/2026-09-21-film-track-set-tooltip-design.md (new), docs/superpowers/specs/2026-09-21-film-track-set-tooltip/track-tooltip.html (new frame); read-only: film-track.tsx, film-timeline.ts (`setSegments`), film-player.tsx, film-transport.tsx, chrome-tooltip.tsx, chart-tooltip.tsx, .skills/advantage-analytics-design/SKILL.md (+ `foundations`, `components`, `chrome`)
- **done when:**
  - [ ] The design doc states what a set segment's tooltip shows, with the exact strings for a decided set ("Set 2 · 6–4 · 41 points · 0:38:10–1:12:05" or whatever is chosen), the set in progress, a one-set match, and a match whose rows carry no set score (every `setScore` is `0-0`) — and says which field each string is built from (`FilmStop.point.setNumber`, `setScore` of the NEXT set's first point for a final score, `matches.score` as fallback, `youFirstScore` orientation)
  - [ ] It settles the interaction for both mount points: hover and focus on the shell player's light chrome and on the room's dark transport, what happens while scrubbing (pointer down), on touch, and with `prefers-reduced-motion`; and names the primitive per surface (`ChromeTooltip` vs `DARK_READOUT_CLASS` / `DARK_READOUT_STYLE`) with the reason
  - [ ] The frame draws both surfaces at their real geometry (3 px track, 2.5 px segment pad, the room's transport row) using only design-system tokens and the room's existing white/black alpha literals — no new hex — and passes `npx playwright test tests/design-drift.spec.ts` if that spec scans `docs/`
  - [ ] The doc lists the code changes an implementation task would make (`setSegments` returning set identity, `FilmTrack` prop shape, keyboard reachability of a segment) as a "handoff" section, and ends with an "Open for the author" list of anything it could not decide from the code
  - [ ] No file under `src/` or `tests/` is in the diff
- **notes:** The author asked for this to be designed first. `setSegments` today discards the set identity it splits on (film-timeline.ts:314–318 cuts at each new set's serve and returns bare `{start,end}`), and `FilmTrack` has no hover state at all, so the design has to decide the data contract as well as the look. Read `film-track.tsx` before drawing: the watched share is a CSS gradient split off `--film-t`, the playhead is a transform, and the slider owns ←/→/Home/End with `data-film-own-keys`. The frame files of the two prior handoffs (`docs/superpowers/specs/2026-09-21-video-fullscreen-h2/`, `…-h1/`) are the format to match. The implementation task is NOT queued here on purpose: its criteria depend on this document, and the author reviews it first. When they approve, add it with `/task-add` pointing at the doc.

## T6 · Drop the ball's trail; marks fade in fast and out over 2.5 s

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-court-ball.tsx; film-ball.ts; film-court.ts; film-court-card.tsx; src/app/globals.css; tests/film-ball.spec.ts; tests/film-court.spec.ts
- **done when:**
  - [ ] The moving ball is gone entirely: `film-court-ball.tsx` is deleted, and `film-fullscreen.tsx` loses only its `FilmCourtBall` import and the `<FilmCourtBall …/>` mount inside `<FilmCourt>` (no other line in that file changes); the `children` slot doc comment in `film-court-card.tsx` no longer names `FilmCourtBall` (author amendment 2026-09-22: "remove the white moving ball as well")
  - [ ] `film-ball.ts` drops `ballAt`, `BallAt` and `BALL_TAIL_SECONDS` and keeps `FilmBallPath`, the path parsing and `bounceTimesByShot` (the court's bounce marks still read them); `tests/film-ball.spec.ts` drops the `ballAt`/tail tests and every remaining test in it passes unchanged
  - [ ] `film-court.ts`: `MARK_HOLD_SECONDS` stays 2, `MARK_FADE_SECONDS` becomes 2.5, the "How long a mark stays" comment says a mark is gone 4.5 s after its moment (author decision 2026-09-22); `tests/film-court.spec.ts` re-pins the hold/fade test as `markOpacity(12,10)=1`, `markOpacity(13.25,10)=0.5`, `markOpacity(14.5,10)=0`, updates "a mark is gone five seconds after its moment" to 4.5, and the step test still passes
  - [ ] Marks fade IN: `src/app/globals.css` gains `@keyframes film-mark-in { from { opacity: 0 } }` (no `to`, so it lands on the element's own inline opacity) beside `viz-vt-rise-in`, and every point-mode mark button in `film-court-card.tsx` runs `animation: film-mark-in var(--duration-fast) var(--ease-primary) both` (150 ms) while keeping `MARK_FADE_TRANSITION` for the fade-out; match-mode (`isMatch`) marks carry no animation. No reduced-motion opt-out is added — DS foundations keep opacity-only fades under reduced motion
  - [ ] `use-ball-paths.ts` and the derivation under `src/lib/services/splitstep/` are not in the diff; `useBallPaths` and the `bounceTimes` memo in `film-fullscreen.tsx` are untouched; `npm run lint` and `npm run typecheck` pass
- **notes:** The author's words: "remove the ball traveling line, it should be a fast fade in and then a fade out slightly faster than what it is now". The trail is the SVG tail in `film-court-ball.tsx`; "it" is read as the court marks, the only thing on the court that fades today (2 s hold, 3 s fade, no fade-in — they pop). If the author meant the moving dot, criterion 4 moves to the dot (fade its `visibility` swap into a 150 ms opacity fade) and the mark numbers stay. `ballAt` keeps its head interpolation and its "null outside the span" rule — only the tail goes. The keyframe overrides the `transition` only while it runs, then the 300 ms smoothing of `markOpacity`'s 0.05 steps resumes. Run `tests/film-court.spec.ts`, `tests/film-ball.spec.ts`, `tests/film-shots.spec.ts`.

## T7 · Court readout hangs toward the room, never off its edge

- **status:** done
- **model:** sonnet
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-court.ts (`readoutPlacement`); film-court-card.tsx (`FilmCourtProps`, the readout div ~L408); film-fullscreen.tsx (the `<FilmCourt` mount ~L1310, `boardRest.anchor`); tests/film-court.spec.ts
- **done when:**
  - [ ] `readoutPlacement(x, y, dock?)` takes an optional third argument `dock: "left" | "right"` — which side of the room the card is parked on — and answers `side: "left"` for `dock: "right"` and `side: "right"` for `dock: "left"` regardless of `x`; without `dock` the answer is unchanged (`x > 50` → left), and the `top` clamp (`READOUT_LIFT_PCT` / `READOUT_MAX_TOP_PCT`) is untouched
  - [ ] `FilmCourt` gains an optional `dock` prop passed straight through to `readoutPlacement`; the readout's `READOUT_GAP` geometry, its 168 px width, `DARK_READOUT_CLASS`/`STYLE` and the rest of the card are unchanged
  - [ ] `film-fullscreen.tsx` passes `dock` from `boardRest.anchor`'s column (`top-right` / `bottom-right` → `"right"`, otherwise `"left"`), and no other line in the file changes
  - [ ] `tests/film-court.spec.ts` "the readout hangs opposite the mark and stays in frame" gains four dock cases — `(17, 50, "right").side === "left"`, `(83, 50, "left").side === "right"`, and the two where dock and the x rule agree — with a comment stating the arithmetic: at a right-hand corner (card right edge 24 px from the room's edge) a right-hanging readout starts 10 px past the card and is 168 px wide, so it would end 154 px off-screen
  - [ ] `shots/viz-fullscreen-court.tsx`, `chart-tooltip.tsx` and the Statistics-tab cards are not in the diff
- **notes:** Hover/focus readout on the room court's marks (`film-court-card.tsx` L408–423). The card sits at `courtSlot(anchor, …)` (`board-position.ts:182`) 24 px inside the room, and `readoutPlacement` today picks the side from the mark's half alone, so at a right-hand corner every left-half mark's readout is clipped by the viewport (mirror on the left). The `top` clamp already keeps it inside vertically (70 % of 227 px + 68 px = the box's own height). Keep the `role="tooltip"` div and the `data-shot-id` hooks; `tests/film-board-position.spec.ts` needs no change because `courtSlot` does not.

## T8 · "Clear all" is a labelled control in the list header and the quick menu

- **status:** done
- **model:** sonnet
- **files:** (guess) src/components/dashboard/matches/match-detail/film/point-list.tsx (`LIST_TONE.clear`/`clearIcon` L107–137, the clear button L377–389); film-quick-filters.tsx (both tone branches); tests/point-list-winner-mark.spec.ts + tests/fixtures/point-list-winner-mark-harness.tsx (or a sibling pair on the same pattern)
- **done when:**
  - [ ] The header's clear control in `point-list.tsx` is a text button whose visible label is "Clear all" (12 px `X` glyph then the word; the `aria-label` goes because the text is now the name), still `onClick={clearAll}` and still drawn only while `hasActiveFilmFilters(filters)`; per tone via `LIST_TONE`: light `text-[11px] font-medium text-[var(--ink-600)] hover:text-[var(--ink-900)]` (the Advanced footer's `clear` style, `film-advanced-panel.tsx` L~100), dark `text-white/60 hover:text-white` with the drawer's `mb-[7px]`; 22 px tall, `cursor-pointer`, `focus-visible:shadow-[var(--focus-ring)]`
  - [ ] `FilmQuickFilters` gains a "Clear all filters" row after the Serve group and before "Advanced filters…", in both the `FloatMenu` and `FilmDarkMenu` branches, rendered only while `hasActiveFilmFilters(filters)`; selecting it calls `onFiltersChange(DEFAULT_FILM_FILTERS)` and closes the menu
  - [ ] The header's geometry is otherwise unchanged — 28 px trigger, `matched / total` count on the right, the collapse glyph where a host passes `onCollapse` — and no chips row, badge or second strip appears (DS banned list: accumulating filter chips); the Advanced panel footer's "Clear all" and `EmptyList`'s actions are not in the diff
  - [ ] A spec on the winner-mark harness pattern mounts `PointList` twice: with `DEFAULT_FILM_FILTERS` the header has no button named "Clear all"; with `{ ...DEFAULT_FILM_FILTERS, pressure: "break" }` it has exactly one, and clicking it hands `onFiltersChange` the `DEFAULT_FILM_FILTERS` object
  - [ ] `npm run lint` and `npm run typecheck` pass; `film-filters.tsx`'s header comment ("Clear all is the exception: it resets the draft AND commits") is still true and unedited
- **notes:** Today "clear" is a bare 22 px `X` with only an `aria-label` and no dark tooltip — the DS bans icon-only controls without both, and a glyph beside a trigger that names the cut reads as "close the menu", which is why the author cannot find it. A visible word fixes both. This is the Video tab's list (shell column AND the room's dark drawer — one component, `tone` decides the paint); the Visualizations tab already shows a visible "Clear" in `shots/applied-strip.tsx` and is out of scope. Do not reintroduce per-category chips; the Matches list's one-Filter-button decision applies in spirit here too.

## T9 · Shot rows reveal with a staggered rise when a point opens

- **status:** done
- **model:** opus
- **files:** (guess) src/app/globals.css (beside `viz-vt-rise-in` / `.viz-crossfade-in`); src/components/dashboard/matches/match-detail/film/point-list.tsx (`ShotWellRow` L800); film-this-point.tsx (`ShotRow` L217); a spec (extend the winner-mark harness with `shotStops`/`activePointId`/`onSelectShot`, or a `createLoader()` static render of `FilmThisPoint`)
- **done when:**
  - [ ] `globals.css` defines `@keyframes film-shot-row-in` (`from { opacity: 0; transform: translateY(4px) }` to the resting state) and `.film-shot-row-in { animation: film-shot-row-in 200ms var(--ease-primary) both }`, plus a `@media (prefers-reduced-motion: reduce)` rule under which no transform runs — either `animation: none` (the `.viz-crossfade-in` precedent) or an opacity-only keyframe
  - [ ] `ShotWellRow` (room drawer, 34 px) and `ShotRow` (This point, 40 px) both carry `film-shot-row-in` and an inline `animationDelay` of `Math.min(order - 1, 8) * 25` ms — row 1 at 0 ms, row 2 at 25 ms, rows 9+ at 200 ms — so a 30-shot rally is fully revealed within 400 ms; row heights, column tracks, lit/active classes and `data-shot-id` / `aria-current` hooks are unchanged
  - [ ] The reveal is mount-driven only: no new state, effect or container `key`; rows stay keyed by `shot.id` so stepping to another point mounts fresh rows (which fires it) while a `timeupdate` re-render of a memoized row does not
  - [ ] A spec renders at least one of the two surfaces with a 10-shot point and asserts the class on every row and the delays of rows 1, 2 and 10 (`0ms`, `25ms`, `200ms`)
  - [ ] `npm run lint`, `npm run typecheck` and `tests/film-playback-refresh.spec.ts` pass; nothing else in either component changes
- **notes:** The author invoked `/impeccable animate` — load the `impeccable` skill before writing and, if it argues for other numbers than 200 ms / 25 ms / 4 px / cap 8, keep them within the DS motion scale (`--duration-hover` 200 ms, stagger ≤ 60 ms between siblings per the viz precedent, `--ease-primary`; no bounce, no scale) and say so in the commit body. Both surfaces are on the Video tab of `/dashboard/matches/[matchId]`: the well opens under the playing row in the room's drawer (H2 frame R3) and `FilmThisPoint` swaps its rows in the shell. A `translateY` on rows inside the `overflow-y-auto` scrollers is fine; do not animate height. The film subtree uses no Framer Motion — stay on CSS keyframes like `film-room-drawer.tsx` and the viz rise-in.

## T10 · Court corner geometry and its own stored anchor

- **status:** done
- **model:** sonnet
- **files:** (guess) src/components/dashboard/matches/match-detail/film/board-position.ts; tests/film-board-position.spec.ts
- **done when:**
  - [ ] `board-position.ts` exports `COURT_ANCHOR_STORAGE_KEY = "film-room:court-anchor"` and a pure `courtRest(courtAnchor: BoardAnchor | null, board: { anchor: BoardAnchor; position: BoardPosition; size: BoardSize }, courtSize: BoardSize, room: BoardSize, insets: BoardInsets): BoardPosition` — with `courtAnchor` null OR equal to `board.anchor` it returns exactly `courtSlot(board.anchor, board.position, board.size, courtSize)` (today's stacking, so an untouched viewer sees no change); with a different corner it returns `anchorPosition(courtAnchor, courtSize, room, insets)` (the same four corners, the same `POINTS_TRIGGER_CLEARANCE` at top-right)
  - [ ] `parseBoardAnchor` is reused for the court (exported alias `parseCourtAnchor = parseBoardAnchor`, or a shared `parseAnchor`) — no second list of corner names exists in the file
  - [ ] `courtSlot`, `anchorPosition`, `nearestAnchor`, `clampBoardPosition`, `nudgeBoard` and every existing test in `tests/film-board-position.spec.ts` are unchanged
  - [ ] The spec gains: `courtRest(null, …)` equals `courtSlot(…)` for a top-left board; `courtRest("top-left", …)` with the board also top-left equals the same; `courtRest("bottom-right", …)` with a top-left board equals `anchorPosition("bottom-right", { width: 168, height: 296 }, room, BASE_BOARD_INSETS)`; and `courtRest("top-right", …)` lands at `top: 58` (the trigger clearance)
  - [ ] `npm run lint` and `npm run typecheck` pass; no file under `src/` other than `board-position.ts` is in the diff
- **notes:** Today the card is parked at `courtSlot(boardRest.anchor, …)` (`film-fullscreen.tsx` ~L502–514) — it has no position of its own. This task adds only the geometry T11 wires: a court with no stored corner (or one sharing the board's corner) keeps stacking in the board's column per R6's court rule; a court dropped elsewhere takes that corner with the board's insets. `FILM_COURT_SIZE` is `{168, 296}` (`film-court-card.tsx:134`). Storage read/write stays in the component (T11), the way `film-scoreboard.tsx` owns `BOARD_POSITION_STORAGE_KEY`. Never `foo.tsx` beside `foo.ts`.

## T11 · Court card drags to a corner like the board

- **status:** done
- **model:** opus
- **needs:** T10
- **files:** (guess) src/components/dashboard/matches/match-detail/film/use-corner-drag.ts (new — the pointer/keyboard mechanic lifted out of film-scoreboard.tsx); film-scoreboard.tsx; film-fullscreen.tsx (the court wrapper ~L1296–1333 and the `dock` expression); film-court-card.tsx (a drag handle on the header row + `data-court-anchor`); docs/superpowers/specs/2026-09-21-video-fullscreen-h2-design.md (R6 row + "R6's court rule" paragraph, prose only); tests/film-playback-refresh.spec.ts (+ its harness) for the browser case
- **done when:**
  - [ ] The drag/nudge/settle mechanic in `film-scoreboard.tsx` (pointer capture, the 3 px lift threshold, the ghost, arrows 8/40 px, Space lift/drop, Escape cancel, blur-drops, the polite landing announcement, the localStorage write) lives once in a hook `useCornerDrag({ storageKey, size, rest, … })` in `use-corner-drag.ts`; `FilmScoreboard` uses it with `BOARD_POSITION_STORAGE_KEY` and `tests/film-board-position.spec.ts` + `tests/film-playback-refresh.spec.ts` pass unchanged (board behaviour is byte-for-byte the same to a viewer: same `data-board-anchor`, same `onRest` contract)
  - [ ] The court wrapper in `film-fullscreen.tsx` uses the same hook with `COURT_ANCHOR_STORAGE_KEY`, a resting position from `courtRest(courtAnchor, boardRest, FILM_COURT_SIZE, roomSize, BASE_BOARD_INSETS)`, and a stored corner of `null` until the viewer first drops it — so a fresh viewer sees the court exactly where it is today; the wrapper carries `data-court-anchor` (`"follow"` or the corner) and `tabIndex=0` + `data-film-own-keys` like the board
  - [ ] Only the card's 20 px header row is the drag handle (`cursor-grab`), because every mark and the header's two glyphs are buttons; a click on a mark or a glyph still seeks/swaps/hides and does not lift the card
  - [ ] `dock` passed to `<FilmCourt>` now derives from the court's OWN resting column (its corner when set, otherwise the board's), not `boardRest.anchor` alone; `readoutPlacement` and T7's tests are untouched
  - [ ] A browser spec on the playback-refresh harness opens the room, drags the court header ~600 px right with the mouse, and asserts `data-court-anchor="top-right"` and `localStorage["film-room:court-anchor"] === "top-right"`; `npm run lint`, `npm run typecheck` pass; the H2 spec's R6 row and court-rule paragraph say the court is movable too, dated as an author decision 2026-09-22
- **notes:** The author's words: "I should be able to move the court visual like the scorecard." The spec says the opposite today (R6: "the board is the only movable object", `film-court-card.tsx` header: "carries no drag affordance at all") — this is an author reversal, record it. The board mechanic is entirely inside `FilmScoreboard` (`film-scoreboard.tsx` L~150–330: `drag` ref, `free`/`held`/`landed` state, `settle`, `endDrag`, the pointer/key/blur handlers); lift it, don't fork it. The court's `SETTLE_CLASS` glide (`transition-[left,top] duration-[360ms]`) already exists on the wrapper. Keep the test hooks `film-room-video`, `data-film-chrome`, `data-shot-id`, `data-board-anchor`. Guardrails: UI only, nothing in §2 is touched. `widget-states` hook fires on the dashboard edit — loading/empty/error of the card are unchanged, say so. Run `tests/film-court.spec.ts`, `tests/film-board-position.spec.ts`, `tests/film-playback-refresh.spec.ts`.

## T12 · Double-click no longer exits the room

- **status:** done
- **model:** sonnet
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-fullscreen.tsx (the `<video>` props ~L1214–1222); docs/superpowers/specs/2026-09-21-video-fullscreen-h2-design.md (R1 row ~L190, Doors table ~L246, prose only); tests/film-playback-refresh.spec.ts
- **done when:**
  - [ ] `onDoubleClick={exit}` is removed from the room's `<video>` and no `dblclick`/`onDoubleClick` handler exists anywhere under `film/`; the comment above `onClick={togglePlay}` no longer says "double-click exits" and instead names the three exits that remain (Esc, the transport's minimize, "Back to the report")
  - [ ] `exit`, `togglePlay`, the keydown switch and the transport are otherwise not in the diff
  - [ ] A spec in `tests/film-playback-refresh.spec.ts` opens the room, dispatches `page.locator(ROOM).dblclick()` and then three clicks 50 ms apart, and asserts `page.locator(ROOM)` still has count 1 after 500 ms and the report player (`REPORT`) still has count 0 — the room did not exit; the existing "hands the playhead back" test (Escape exit) still passes
  - [ ] The H2 spec's R1 row and its Doors table drop "double-click exits" / "dbl-click", dated as an author decision 2026-09-22
- **notes:** Root cause is literal: `film-fullscreen.tsx:1222` `onDoubleClick={exit}` on the `<video>`, with `onClick={togglePlay}` at :1214. Two fast clicks fire click, click, dblclick — the third exits. The author's "when I press too fast in succession it exits" is the same event. Removing the handler is the whole fix; the two clicks still toggle twice (pause, then play), which is native player behaviour — if the author wants a click burst to count once, that is a separate decision (a ~300 ms settle window in `togglePlay`) and is NOT in this task. `togglePlay` already swallows the AbortError a quick pause-after-play raises (:641–651). The room's video is `[data-testid="film-room-video"]` (`ROOM` in the spec); the room opens via the "Open the film room fullscreen" button (spec ~L582).

## T13 · Saved points survive a view switch, and the room saves the point just played

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/match-data-provider.tsx (or a new `match-points-provider.tsx` mounted beside it in the (detail) page); src/components/dashboard/matches/match-detail/film/film-tab.tsx (`points`/`pointsRef` L127–134, `handleToggleSaved` L353–383); film-fullscreen.tsx (`toggleSavedActive` L697–702, the `saved` props at L1286 and L1399); tests/fixtures/film-playback-refresh-harness.tsx + tests/fixtures/film-playback-refresh-window.ts; tests/film-playback-refresh.spec.ts
- **done when:**
  - [ ] The optimistic `points` array and its `pointsRef` no longer live in `FilmRoom` (`film-tab.tsx`); they live above `MatchReportWhen` — in `MatchDataProvider` (a `useState` seeded once from the server prop, exposed as `points` + `setPoints` on the context) or a sibling provider mounted in the (detail) page — so unmounting the Video view (`match-report.tsx:140` returns null for an inactive view) and remounting it renders the same saved flags; `handleToggleSaved`'s write logic (insert / unfiltered delete, `23505` counts as landed, revert on any other error) is unchanged in substance
  - [ ] The harness window gains a `remountFilmTab()` seam (unmount only the `FilmTab` subtree, keep the providers) and a spec toggles a bookmark on a row (`[data-point-id]` → the "Bookmark this point" button), calls `remountFilmTab()`, and asserts the same row's button has `aria-pressed="true"`; the mirrored case (a `?bookmarkOutcome=refused` insert) asserts `aria-pressed="false"` after the revert
  - [ ] In the room, the transport's bookmark control and the `S` key act on the last point REACHED (`boardStop?.point`, the same `activeStopAt` the board's score reads), never null once the first point has played — matching the shell, whose `S` and player control already read `active?.stop.point`; the drawer's lit row, the point line and the position counter still read `playingStop` (R7 unchanged)
  - [ ] `film-transport.tsx`'s `if (p.saved !== null)` guard stays (null only before the first point); the board's "· saved" (`FilmScoreboard saved=`) reads the same point the control saves, so the foot and the glyph can never disagree
  - [ ] `npm run lint`, `npm run typecheck`, `tests/film-playback-refresh.spec.ts`, `tests/point-list-winner-mark.spec.ts`, `tests/point-list-clear-all.spec.ts` pass; `supabase/migrations/` and `src/lib/data/match-points-server.ts` are not in the diff
- **notes:** The database is not the problem: `point_bookmarks` has the three T4 policies (INSERT own-row + `visible_match_ids()`, SELECT/DELETE per visible match), no UPDATE, and the 5 live rows (all today, 14:56–15:15 UTC, one user, match `1415029e…`, 87 points, a program match) inserted fine; zero legacy `points.saved` rows were left behind by the backfill. Two UI mechanisms make saves look lost: (1) `film-tab.tsx:128` seeds `useState<MatchPoint[]>(serverPoints)` once, and `MatchReportWhen` UNMOUNTS the Video view on a switch to Statistics/Shots (`match-report.tsx:113–116, :140`), so coming back re-seeds from the page's server render — every bookmark toggled since page load disappears from the UI (it is in the DB; a hard reload brings it back). (2) In the room `activePoint = playingStop?.point` (`film-fullscreen.tsx:355`), null in the padded dead time after a rally, and `toggleSavedActive` (:697) + the transport guard (`film-transport.tsx:141–143`) silently do nothing there — "save that point" pressed just after it ended saves nothing, with no toast by design (R9). The shell reads `active?.stop.point` (last reached), so the two surfaces disagree today. Do NOT fix (1) with `router.refresh()`: it re-mints the playback SAS, the hook's `key` includes the URL, and the `<video>` would reload. `useMatchSides()` stays the only you/opp source. Guardrails §2: no writes to `points`/`matches`. `widget-states` hook fires; the list's three zero states are untouched.

## T14 · Instrument the seek path and pin "a point jump is only a seek"

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-trace.ts (new, pure summariser + attach/detach); film-fullscreen.tsx (`seek` L548–563, the `<video>` props L1199–1253); film-player.tsx (`seekTo` L322–345, the `<video>` L626–657); tests/film-trace.spec.ts (new, node); tests/film-playback-refresh.spec.ts
- **done when:**
  - [ ] A spec in `tests/film-playback-refresh.spec.ts` opens the room, selects three different point rows in the drawer (`[data-point-id]`) 200 ms apart, and asserts: the `/video` poll count for the match is unchanged (`polls` endpoint), the room's `<video>` is the same DOM node (`generation` unchanged — expose it as `data-generation` on both elements), and `el.currentTime` lands within 0.1 s of each row's stop start — a point jump issues no request and no remount
  - [ ] `film-trace.ts` exports a pure `summariseSeek(events)` that turns a list of `{ type: "seek" | "seeking" | "seeked" | "waiting" | "stalled" | "playing" | "error" | "generation"; t: number; time?: number; buffered?: [number, number][] }` into `{ seekToSeekedMs, seekToPlayingMs, waitingCount, stalled: boolean, bufferedAtSeek: boolean, remounted: boolean }`, with a node spec pinning a buffered seek (no `waiting`), an unbuffered seek (`waiting` then `playing`), and a remount mid-seek
  - [ ] Both surfaces attach the trace only when `localStorage["film-room:trace"] === "1"` (read once in a lazy initializer, like `readCourtOn`): `seeking`/`seeked`/`waiting`/`stalled`/`playing`/`error` listeners on the element, `buffered` ranges captured at each `seek`/`seekTo`, the credential host's range requests counted from `performance.getEntriesByType("resource")`, one `console.table` per seek with the summary and `generation`; with the flag off no listener is added and no `performance` call runs (a spec on the harness asserts `performance.getEntriesByType` is not called and no `console.table` fires when the flag is unset)
  - [ ] Nothing about seeking changes in this task: `seek`, `seekTo`, `land`, `settle`, the loop/skip-dead re-seeks and `preload="metadata"` are unchanged in behaviour; `npm run lint`, `npm run typecheck` pass
  - [ ] The task's run-log entry lists, for the author, the exact steps to capture a trace on the real match (`1415029e…`, Caden Ace v Matt Goodman): set the flag, open the room, jump between five points, paste the five tables — that paste is the input to the fix task, which is queued only after it
- **notes:** Load the `vercel-react-best-practices` skill before touching the players (re-render cadence, memo boundaries, no derived state in effects) and `vercel-optimize` only if the trace shows the network side — the credential is streamed straight from Azure Blob (guardrails: proxying breaks range requests), not through a Vercel route, so `/api/matches/[matchId]/video` is a metadata call only. What the code already proves: a point jump is `el.currentTime = target` and nothing else (`film-fullscreen.tsx:548–563`, `film-player.tsx:322–345`) — no `src` change, no `load()`, and the element's `key={generation}` (:1203 / :629) moves only when the credential controller installs a source (`use-attachment-playback.ts:682, :707`) or recovers from a media error (:821–838, `reportLoadFailure` → refresh → remount → `land`). So an on-screen "reload" is either (a) a scheduled credential swap (~2 min before the SAS expires), (b) a seek whose range request failed and recovered, or (c) the media pipeline fetching an unbuffered range of a 1.4 GB file whose `moov` sits at the tail (`trim.worker.ts:137`, T15) with nothing on screen saying so (no `waiting`/`stalled` handler exists on either element). The trace tells the three apart. Two clocks: `ShotStop.start` and every seek target are FILM seconds; leave `toFilmTime` alone. Keep `film-room-video` / `film-player-video` test ids. An alternative reading of "the points reload": the drawer's shot rows re-animate on every point change by design (T9); if the author meant the LIST, not the film, this task is the wrong one.

## T15 · Front-load the moov in the browser remux

- **status:** done
- **model:** opus
- **files:** (guess) src/lib/video/trim.worker.ts (`Mp4OutputFormat({ fastStart: false })` L137 and the comment above it, the moov note L108–110); tests/video-trim-plan.spec.ts or a new tests/video-trim-worker.spec.ts; tests/fixtures/match-video/ (`h264-tail.mp4` / `h264-faststart.mp4` already exist)
- **done when:**
  - [ ] `trim.worker.ts` writes the `moov` before the `mdat` — `fastStart: 'reserve'` (Mediabunny 1.56.2, `node_modules/mediabunny/dist/modules/src/output-format.d.ts:106`) if it works with the positioned `StreamTarget` writes into the OPFS sync handle; OR, if `'reserve'` cannot be used with a copy-only `Conversion` whose total size is unknown up front, the option stays `false` and the comment at L135–136 states the concrete constraint with the d.ts line it comes from (no "cheapest write" claim without the trade-off named) — one of the two, verdictable from the diff
  - [ ] When the option changes: a spec parses the top-level boxes of a worker output (browser harness if OPFS is needed, else a node run of the same `Conversion` against `tests/fixtures/match-video/h264-tail.mp4` into a `BufferTarget`) and asserts `moov` precedes `mdat`; the short-write guard (L116–124) still throws on a truncated write
  - [ ] `decideTrim`, `trim-plan.ts`, `trim.ts` (the OPFS lifecycle) and the "no audio → upload the original" rule are not in the diff; `tests/video-trim-plan.spec.ts` and `tests/match-video-file-step.spec.ts` pass
  - [ ] `docs/ui-revamp-guardrails.md` §1 "Superseded, September 2026" paragraph gains one sentence saying the cut is written faststart (or is not, and why), so the next reader knows which files on Azure are which
  - [ ] `npm run lint` and `npm run typecheck` pass
- **notes:** Every fresh `<video>` — the shell's first load, the room's second element, and each credential remount — must fetch the file's tail before it can seek when the `moov` is last; Chrome does this with two range requests and the frame stays black meanwhile. This fixes only FUTURE uploads: files already in Azure keep their layout, and rewriting them is an ops step (`azure-storage` skill), not this task. Guardrails §2: the SwingVision path and `lib/services/splitstep/**` are untouched; the job's `StartTime 0 / EndTime = cut length` contract does not change with box order. Mediabunny's `onMdat`/`onMoof` callbacks are irrelevant here — do not switch to `'fragmented'`: a fragmented MP4 with no `sidx` seeks WORSE over plain HTTP ranges.

## T16 · The frame says it is catching up: a seeking state on both players

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/film/use-seek-settling.ts (new, one hook shared by both players — `use-corner-drag.ts` is the naming precedent; no `.tsx` twin); film-fullscreen.tsx (the room's `<video>` L1348–1403, its className L1359–1362); film-player.tsx (the shell's `<video>` L644–676, className L654); tests/film-playback-refresh.spec.ts (a hold seam beside `jumpRows` L1046, cases after the T14 block)
- **done when:**
  - [ ] Both `<video>` elements (`film-room-video`, `film-player-video`) carry `onSeeking`/`onSeeked` wired to one hook, `useSeekSettling({ graceMs: 120, generation })` in `use-seek-settling.ts`, and expose `data-film-seeking="true"` only while a seek has been in flight for at least 120 ms and `seeked` has not fired; the attribute is absent otherwise, cleared by `seeked`, reset by a `generation` change (the old element cannot fire `seeked` after a keyed remount), and the grace timer is cleared on unmount
  - [ ] The treatment is opacity only, no new element: while `data-film-seeking` is set the element's class reads `opacity-60` instead of `opacity-100`, through the room's existing `transition-opacity duration-200` (L1360) — `videoReady === false` still wins with `opacity-0` — and the shell's `<video>` (L654) gains the same `transition-opacity duration-200`; the diff adds no spinner, skeleton, text readout, transform or extra DOM node to either player, and `film-transport.tsx`, `film-scoreboard.tsx`, `film-room-drawer.tsx`, `film-trace.ts` are not in the diff
  - [ ] Option A, chrome leads and the film follows: the diff contains no change inside `seek` (film-fullscreen.tsx L688–706), `mark` (L677–686), `land`, `settle`, `onTimeUpdate`'s loop/skip-dead re-seeks (L943, L949), `seekTo` (film-player.tsx L327–354), `pushTime` (L311–320) or either `preload="metadata"`; `mark(target)` / `pushTime(target)` and the `--film-t` write still run synchronously on click, so the drawer's lit row, the board, the transport readout and `savePoint` advance at once while the frame dims
  - [ ] `tests/film-playback-refresh.spec.ts` gains a hold seam installed by `page.evaluate` AFTER `openRoomWithDrawer` (never `addInitScript` — it must not delay `settle`/`land` at mount): it wraps the `HTMLMediaElement.prototype.currentTime` setter to dispatch `seeking` on the element at once and apply the real assignment 400 ms later (so native `seeking`/`seeked` follow it), and three cases: (a) room, hold on — click drawer row `c`; by 200 ms `expect.poll` sees `data-film-seeking="true"` on `film-room-video` AND `[data-point-id="c"][data-playing="true"]` lit; after the hold the attribute is gone and `el.currentTime` is within 0.1 s of 0.45; (b) room, hold off — a `MutationObserver` (`attributeFilter: ["data-film-seeking"]`) installed before `jumpRows` records zero sets across the three buffered jumps (the grace holds); (c) shell, hold on — click a point row in the tab's list (a `[data-point-id]` outside `aside[aria-label="Points"]`) and assert the same present-then-gone sequence on `film-player-video`
  - [ ] `npm run lint`, `npm run typecheck`, `tests/film-playback-refresh.spec.ts`, `tests/film-trace.spec.ts`, `tests/film-motion.spec.ts` pass; `use-attachment-playback.ts` and `film-tab.tsx` are not in the diff
- **notes:** The evidence this rests on — the T14 trace the author captured in the room on match `1415029e…` (Caden Ace v Matt Goodman), six seeks, all `surface: "room"`, `generation: 0`, `remounted: false`, `waitingCount: 0`, `stalled: false`, `seekToPlayingMs: null` (paused throughout), `rangeRequests: 0`: target 2.8 → 9 ms (buffered); 48.87 → 67 ms; 107.57 → 522 ms; 150.13 → 64 ms; 179.43 → 1066 ms; 218.53 → 1212 ms (the last five unbuffered). Nothing reloads or remounts; a point jump is `el.currentTime = target` and nothing else. The "freeze" is a mismatch the code creates: `seek` (film-fullscreen.tsx:688–706) calls `mark(target)` (:700, → `setCurrentTime` → `activeStopAt`/`playingStopAt` :437–444 → board, `savePoint` :488, drawer `activePointId` :1571, transport `currentTime` :1537) and writes `--film-t` (:701) on the click, but the element has `onSeeked` (:1394–1397) and no `onSeeking`/`onWaiting`, so the chrome reaches the new point instantly while the last frame of the old point holds for up to 1.2 s — Chrome fires no `waiting` for a paused seek. The shell is the same shape: `seekTo` (film-player.tsx:327–354) → `pushTime` (:311–320), `onSeeked` only (:672). The 67 vs 1212 ms spread is keyframe distance in the camera recording, preserved by the copy-only remux (T15) — not a client concern. WHY option A: the transport playhead is the viewer's own placement and must move on click; T13 made the `S` key/bookmark read `savePoint = boardStop?.point`, which must name the clicked point immediately, not after `seeked`; the loop (:943) and skip-dead (:949) re-seeks would flicker the row under option B; and `onSeeked` already re-marks with the landed time (:1395), so the chrome converges either way. The dim, not a spinner: the room's own R11 note (:490–497) says the black frame with its chrome IS the loading state, "no spinner, no skeleton"; DS `empty-and-loading.md` reserves skeletons for a fetch and SKILL.md's banned list forbids fake progress; `foundations.md` §Reduced Motion keeps opacity transitions, so no `motion-safe:` gate is needed; 200 ms is `--duration-hover`; 60 % sits beside the existing dims (court glyph 45 %, board 82 %). The 120 ms grace: the 9 ms buffered seek and the shell's `settle` nudge to 0.001 (film-player.tsx:404) must never flash; 120 ms is the `--duration-fast` neighbourhood and under the 150 ms "fast UI response" band. Traps: two clocks — every seek target is FILM seconds, `toFilmTime` untouched; `useMatchSides()` stays the only you/opp source; keep the test ids `film-room-video`, `film-player-video`, and `data-generation`, `data-film-chrome`, `data-shot-id` as they are; the T14 trace attaches its own `seeking`/`seeked` listeners (film-trace.ts) — leave them, the hook is a second listener, not a replacement; `key={generation}` (:1352 / :647) means a remount mid-seek strands `seeking: true` unless the hook resets on `generation`; the `currentTime` setter seam must be installed after the room is ready or it defers `land()`; `widget-states` hook fires on this edit (no fallback, panel or `return null` touched — say so in the log). Load `.skills/advantage-analytics-design/SKILL.md` + `reference/empty-and-loading.md` + `reference/foundations.md` (Animation & Motion, Reduced Motion) and `docs/ui-revamp-guardrails.md` §2 (no writes to `points`/`matches`; nothing here writes). Run `tests/film-playback-refresh.spec.ts` alone first — it webpacks the harness in `beforeAll`; the `(live)` shared-DB specs that trip in a full run are unrelated (reference_live_db_auth_rate_limits).

## T17 · Design follow-or-hold for the film points: the drawer pill and the card's header line

- **status:** done
- **model:** fable
- **files:** (guess) docs/superpowers/specs/2026-09-22-film-follow-hold-design.md (new), docs/superpowers/specs/2026-09-22-film-follow-hold/frames.html (new frame); read-only: point-list.tsx, film-room-drawer.tsx, film-this-point.tsx, film-fullscreen.tsx (the "Points" trigger L1509–1536, `step` L798–808), film-tab.tsx (`active` L262–265, `position` L313–317, the `FilmThisPoint` mount L592–601), src/app/globals.css (`film-shot-well-open` L867–891, `film-shot-row-in` L827–840), .skills/advantage-analytics-design/SKILL.md (+ `reference/foundations.md` Duration Scale L299–316 and Reduced Motion L349–351, `reference/chrome.md`, `reference/components.md`), docs/superpowers/specs/2026-09-21-video-fullscreen-h2-design.md (R2/R3/R7 L191–196, "The drawer's unfolded shots" L223–232)
- **done when:**
  - [ ] The doc states the one shared state `pointFocus: { mode: "follow" } | { mode: "held"; pointId }` in a per-surface table whose rows say, for the room drawer and the shell "This point" card, what HOLDS (the open well / the card's shot rows) and what KEEPS FOLLOWING (the lit `data-playing` row, the board, the court, the position counter, the room's court title); it carries an enter/leave table with every rule from the plan (row or shot click on either surface → `held(thatPointId)` and still seeks; hand scroll of the drawer list → `held(displayedPointId)` from `wheel`/`touchmove`/`pointerdown`, never the `scroll` event, with the follow effect flagging its own `scrollTo`; leave held on the return affordance, `←`/`→`/transport chevrons, or clicking the playing row; room/drawer open-close leave it alone; leaving the Video tab resets to follow) and a "Settled against the code" section that names the well slice (`wellStops`, point-list.tsx L294–297) and `isActive` (L456) as the two reads the state splits, and cites the effect comment at L303–305 ("without fighting a user who is scrolling") as a claim the code does not yet implement (L312–328 has no hand-scroll detection)
  - [ ] The doc's strings table gives the exact pill text (`Now playing · Point 14` with a directional chevron) and the card's header line (`Now playing · Point 14 →`), the `aria-label` for each, the show rule (only while `held` AND playing point ≠ held point), and says which field the number is built from (the position counter's `walkStops` index, film-tab.tsx L313–317, so the pill and the `14 / 87` counter agree — or a stated alternative), plus the singular/edge rows: playing point outside the applied cut, held point filtered out of the cut, no point playing (R7 dead time)
  - [ ] The frame draws four labelled sections at real geometry — (a) drawer following, (b) drawer held with the pill inside the list's bottom edge and the lit row further down than the open well, (c) the shell card held with its header line in the counter's right slot, (d) a reduced-motion note — using the drawer's 320 px / `rgba(13,13,13,0.88)` sheet, 52 px rows on 14 px padding, the well's `rgba(0,0,0,0.28)` + 6% inset hairlines with 34 px rows, the lit row at `rgba(255,255,255,0.08)`, the pill on the "Points" trigger's recipe (28 px, `rgba(13,13,13,0.72)`, 11 px/500 white, `--radius-button`, `data-film-chrome`), and the card's `surface-card` `10px 8px 8px`, `eyebrow`, `size-7` step buttons, 56 px `mono` counter, 40 px rows on `NARROW_COLUMNS`; it links the four real token files under `src/styles/design-system/` like `2026-09-21-film-track-set-tooltip/track-tooltip.html` does, and `grep -o '#[0-9A-Fa-f]\{3,6\}'` on the frame returns only `#FFFFFF` — every other colour is a `var(--…)` token or a white/black alpha literal already present in point-list.tsx, film-room-drawer.tsx or film-fullscreen.tsx (checked by hand: `tests/design-drift.spec.ts` runs `scripts/check-design-drift.mjs`, whose walk root is `SRC = "src"`, so it never sees `docs/`)
  - [ ] The doc has an interaction table with a motion column giving these values and no others: follow-mode scroll = `scrollTo({ behavior: "smooth" })` on the list's own `scrollTop` (no `scrollIntoView`, per the L306–310 rule); pill and header line enter 150 ms opacity + 4 px rise on `--ease-primary`, exit 100 ms opacity; `prefers-reduced-motion` → instant scroll and opacity only; the well fold (`film-shot-well-open`) and the row stagger (`film-shot-row-in`) unchanged; and an a11y block covering keyboard reach of the pill (tab order inside the `aside[aria-label="Points"]`), its `aria-label`, and that the lit row's `data-playing` keeps naming the playing point while held
  - [ ] The doc ends with a "Handoff" section listing the code changes an implementation task would make — state owner in `film-tab.tsx` beside `room`, a `displayedPointId` prop into `FilmRoomDrawer`/`PointList`/`FilmThisPoint`, which element hosts the pill (the scroller is `PointList`'s own `listRef` div, point-list.tsx L433–436, and the drawer has no padding, L98 — the doc must say where the positioning wrapper goes), the scroll-intent listeners and the follow flag, `step` (film-fullscreen.tsx L798) and `handleStep` re-following, the tab-leave reset, and the harness specs — then an "Open for the author" list; and no file under `src/` or `tests/` is in the diff
- **notes:** Deliverable 1 of the approved plan `/Users/cjgimena/.claude/plans/the-following-tasks-cheeky-muffin.md` ("Film points: follow playback, or hold the point you are reading"); modelled on T5 (see its queue block and log entry). The design is SETTLED — do not re-litigate: one state shared by both surfaces, `follow | held(pointId)`; the WELL and the CARD hold, while the lit row, board, court and counter keep following the film; scrolling by hand holds (chat-window precedent); NO per-point dropdown or accordion — one open well stays the rule (H2 R3, "No second list, no Shots tab"); both surfaces (room drawer AND shell card); playback, loop and skip-dead untouched; the state is not persisted. Enter held: a click on a point row or a shot row on either surface (it still seeks); a hand scroll of the drawer list, detected from `wheel`, `touchmove` and `pointerdown` on the list — NOT the `scroll` event, which the programmatic follow-scroll also fires; the follow effect sets a flag around its own `scrollTo` so its scrolls never count. Leave held: the return affordance; `←`/`→` and the transport chevrons; clicking the playing row itself. Re-follow scrolls smoothly to the playing row and unfolds its well. Opening/closing the room or the drawer leaves the state alone; leaving the Video tab resets to follow. Return affordance, drawer: a small dark pill pinned inside the list's bottom edge, never over the held well, "Now playing · Point 14" with a chevron pointing toward the playing row (up/down), shown only while held AND playing ≠ held; styling = the room's "Points" trigger (`bg-[rgba(13,13,13,0.72)]`, 11 px, `--radius-button`, film-fullscreen.tsx L1509–1536); `data-film-chrome` so it fades with the chrome (R2 collapse, the exit fade at L926–934); keyboard reachable; `aria-label` names the point. Shell card: a header line in place of the counter's right slot (film-this-point.tsx L84–105), "Now playing · Point 14 →", same show rule, light-tone styling from the card's existing `eyebrow` (L82) and `var(--ink-600)` (L161). Motion (DS scale): follow-mode scroll `scrollTo({ behavior: "smooth" })` — Chromium eases ~300 ms, no custom scroll-jacking; pill/header line enter 150 ms (`--duration-fast`) opacity + 4 px rise on `--ease-primary` (the 4 px is `film-shot-row-in`'s rise, globals.css L827), exit 100 ms opacity; reduced motion → instant scroll, opacity only (foundations.md L349: "skip transforms, keep opacity"). Chevrons are Lucide at 12 px (chrome.md's chevron size). Format to match: the two prior handoff frame dirs `docs/superpowers/specs/2026-09-20-video-tab-in-shell-h1/` (`B-current-point-T1-T2.html` is the card's own frame) and `…/2026-09-21-video-fullscreen-h2/` (`R-states-R1-R11.html` is the drawer's), and T5's in-repo `track-tooltip.html` for the token-linking head (L16–19) and the state-id/state-title/state-note scaffolding. Geometry verified for the frame: drawer `w-[320px] bg-[rgba(13,13,13,0.88)]` + inset hairline (film-room-drawer.tsx L98), no padding of its own; list dark header `px-2.5 pt-[13px] pb-[3px]` (point-list.tsx L129); dark row `min-h-[52px] px-[14px]` with `playing: bg-white/[0.08]`, `hover:bg-white/[0.06]` (L534–536); well `film-shot-well-open` → `bg-[rgba(0,0,0,0.28)]` + 6% inset hairlines (L771–772), rows `h-[34px] px-[14px]` on `SHOT_COLUMNS`, lit `rgba(255,255,255,0.12)`, hover `0.05` (L835–839); card `surface-card` padding `10px 8px 8px` (film-this-point.tsx L78–79), head `pt-0.5 pr-[5px] pb-2.5 pl-3` (L81), counter `min-w-[56px] text-[11px] mono` in `--ink-500` (L91–94), `ShotRow` `h-10 px-3 gap-x-4` with `bg-[var(--surface-subtle)]` when active (L252–255), `NARROW_COLUMNS` `16px_104px_88px_minmax(0,1fr)_48px` (L44), wide set at `@min-[880px]` (L45–46). Traps: never propose `foo.tsx` beside `foo.ts` in the Handoff (`use-corner-drag.ts` / `use-seek-settling.ts` are the hook-naming precedent); the H1/H2 frame format (eyebrow → `.text-display` → `.text-body` → numbered `state` blocks with `state-id` chips); the frame must render from a plain checkout (relative `../../../../src/styles/design-system/*.css` links, fonts from Google, Lucide from unpkg, nothing bundled); no `src/` or `tests/` file in the diff; `widget-states` hook does not fire (no dashboard component touched). Do NOT draft or queue the implementation tasks (T18+): as with T5, they are queued only after the author reads the doc, and the doc's Handoff section is their criteria source — the plan's expected split (state + drawer; shell card; motion + reduced motion) is a guess for the Handoff to refine, not a commitment.

## T18 · Follow-or-hold state: the pure layer, the drawer's split reads, scroll intent, re-follow on step

- **status:** done
- **model:** fable
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-timeline.ts (the `PointFocus` type and two pure helpers, after `prevStop` L265); tests/film-timeline.spec.ts (EXISTS — extend, do not create); film-tab.tsx (state beside `room` L166–168, reset effect near `filteredPoints` L250–253, props on the shell `<PointList>` L608–629 and `<FilmFullscreen>` L634–661); film-fullscreen.tsx (`step` L798–808, props interface L137–175, the `<FilmRoomDrawer>` mount L1590–1609); film-room-drawer.tsx (four pass-through props L29–52, L109–128); point-list.tsx (`wellStops` L294–297, the comment L302–304 and effect L311–328, the well mount L480–489, `PointRow` `onSelect` L625/L630, `ShotWellRow` L828, props L146–185); tests/fixtures/film-playback-refresh-harness.tsx (`POINTS` L129–137, `boot` L216); tests/film-playback-refresh.spec.ts (new block after the T16 cases, L1359+)
- **done when:**
  - [ ] `film-timeline.ts` exports `type PointFocus = { mode: "follow" } | { mode: "held"; pointId: string }`, `displayedPointId(focus, playingPointId: string | null): string | null` (the held id while held, else the playing id), and `followAffordance(focus, playing: { id: string; index: number | null } | null): { label: string; ariaLabel: string; inCut: boolean } | null` returning `null` in `follow`, `null` when `playing` is null (R7 dead time), `null` when `playing.id === focus.pointId`, `{ label: "Now playing · Point N", ariaLabel: "Now playing: point N — follow playback", inCut: true }` when `index` is a number, and `{ label: "Now playing · not in this cut", ariaLabel: "Now playing: a point outside this cut — follow playback", inCut: false }` when `index` is null; the file stays free of React imports; `tests/film-timeline.spec.ts` gains cases for every row of that table (seven, one per strings-table row incl. both `follow` and held-equals-playing)
  - [ ] `film-tab.tsx` owns `const [pointFocus, setPointFocus] = useState<PointFocus>({ mode: "follow" })` beside `room`, two `useCallback`s `holdPoint(id)` / `followPlayback()` with stable identity, a derived `displayedPointId`, and one effect on `[filteredPoints, pointFocus]` that calls `followPlayback()` when held and no point in `filteredPoints` has the held id; no persistence (no `localStorage`, no URL) and no reset code on tab-leave (the view unmounts, L121–126); `pointFocus`, `displayedPointId`, `onHoldPoint`, `onFollow` are passed to the shell `<PointList>` (L608) and to `<FilmFullscreen>`, which forwards all four through `FilmRoomDrawerProps` to the drawer's `<PointList>`; `FilmThisPoint` and `film-this-point.tsx` are NOT in the diff (T20)
  - [ ] In `point-list.tsx`: `wellStops` filters `shotStops` by `displayedPointId` and the `<ShotWell>` mounts under the row whose `point.id === displayedPointId` (L480 no longer keys on `isActive`); `isActive` (L456) and `data-playing` (L608) keep reading `activePointId`; the keep-in-view effect early-returns while `pointFocus.mode === "held"` (both tones) and otherwise moves the same target by `list.scrollTo({ top, behavior: reducedMotionNow() ? "auto" : "smooth" })` (import from `./film-motion`, never `scrollIntoView`), setting `followScrollRef.current = true` before the call and clearing it on the scroller's `scrollend` or a 400 ms fallback timer; the comment at L302–304 is rewritten to describe the listeners below it (T17 follow-up 2) and the L306–310 rule paragraph stays
  - [ ] Intent listeners live on the `listRef` scroller only when `tone === "dark"`, added in an effect and removed on cleanup: `wheel` (passive), `touchmove` (passive), `pointerdown` where `e.target === list`, and `keydown` for `PageDown`, `PageUp`, `Home`, `End`, `" "`, `ArrowUp`, `ArrowDown` when `!e.defaultPrevented`; each calls `onHoldPoint(displayedPointId)` when `displayedPointId` is non-null and does nothing while already held (hand scroll is an enter only); there is no `scroll` listener that sets state; the row and well click paths go through one stable wrapper that calls `onFollow()` when `point.id === activePointId`, else `onHoldPoint(point.id)`, and THEN `onSelect(point)` / `onSelectShot(stop)` (the seek is unchanged); `step` in `film-fullscreen.tsx` (L798) calls `p.onFollow()` before `seek(stop.start)` so `←`/`→`/`↑`/`↓` and the transport chevrons re-follow; `selectMark` (L827) does not hold (default 6)
  - [ ] `tests/fixtures/film-playback-refresh-harness.tsx` gives points `a`/`b`/`c` two timed `shots` each (`videoTime` 1.70/1.74, 1.85/1.89, 1.95/1.99; the other `MatchShot` fields null/0) and honours a `pad=N` query param that appends N untimed points (`videoTime: null`, `pointNumber` 5+) so the drawer scrolls at 1280×720; `tests/film-playback-refresh.spec.ts` gains, after `openRoomWithDrawer(page, …, { pad: "12" })`: (a) click `DRAWER_ROW("a")`, `seekTo(page, ROOM, 0.4)` → `expect.poll(playingRow)` is `b` while `aside[aria-label="Points"] [data-point-id="a"] + [data-shot-well]` still exists and no well follows row `b`; (c) from follow, `page.mouse.move` over the drawer then `page.mouse.wheel(0, 200)` → the scroller's `scrollTop` is > 0, then `seekTo(page, ROOM, 0.5)` → `playingRow` is `c`, the well is still under `b`, and `scrollTop` is unchanged; (d) `page.keyboard.press("ArrowRight")` → the well mounts under the playing row without a click and an `Element.prototype.scrollTo` spy (installed by `page.evaluate` after the room opened, as `holdSeeks` is) recorded `{ behavior: "smooth" }`; (e) click `DRAWER_ROW("b")`, seek to `c`, choose `Saved only` from the drawer's `Point filters` menu → after `Clear all filters` the well is under `c` with no click (state is `follow`); every existing case in the file still passes, plus `tests/film-timeline.spec.ts`, `tests/film-shot-row-reveal.spec.ts`, `npm run lint`, `npm run typecheck`
- **notes:** Implements the Handoff of `docs/superpowers/specs/2026-09-22-film-follow-hold-design.md` items 1–8, 11a/c/d/e, 12 — read the doc in full first; the design is SETTLED, do not re-litigate. One state shared by both surfaces; the WELL holds, the lit row / board / court / counters / court title "This point" keep following; one open well stays the rule (no accordion); playback, loop and skip-dead untouched; not persisted. Author's answers to the six Open items that touch this task: (2) the shell column's `PointList` (`tone="light"`) holds on a row click and its keep-in-view effect stops while held, but it draws NO pill and gets NO intent listeners — the card's header line (T20) is the shell's return; (3) keyboard scrolling of the drawer IS a fourth hold source — the key set above; `ArrowUp`/`ArrowDown` are listed per the author, knowing the room's window handler (film-fullscreen.tsx L1136–1143, listener at L1212) steps on them next with `preventDefault`, so with focus on a drawer row the net result of an arrow is `follow` (both state updates land in one native event and batch) — say so in a comment; `Enter`/`Space` on a row are `defaultPrevented` by the row (L627–632) and never count; (6) a court-mark click does not hold. Enter/leave rules verbatim from the doc: click on a point row or a shot row → `held(thatPointId)` and the click still seeks; click on the row already playing → `follow` (the seek still happens, a restart); hand scroll → `held(displayedPointId)` — "Hand scroll is an enter only: scrolling while already held keeps the held point"; `←` `→` `↑` `↓`, transport chevrons → `follow`, then the step from the playing point; open/close the room or drawer → leaves the state alone (the room is `FilmRoom`'s child, film-tab.tsx L633–662); a cut change that removes the held point → `follow`; seeking, `J`/`L`, skip-dead, loop, play/pause → neither enter nor leave. Intent: "`scroll` cannot detect intent — a programmatic `scrollTo` fires `scroll` exactly as a wheel does, and a smooth one fires it on every frame"; "the three intent listeners do not consult the flag: a wheel arriving mid-follow-scroll is intent and holds" — the flag exists so nothing DERIVED from the scroller's motion (T19's chevron re-read on `scroll`) mistakes the effect's travel for the viewer's. Motion: follow-mode scroll = `scrollTo({ top, behavior: "smooth" })` on the list's own `scrollTop` (Chromium eases ~300 ms; no scroll-jacking), `"auto"` under `prefers-reduced-motion` via `reducedMotionNow` (film-motion.ts L68; film-fullscreen imports it as `prefersReducedMotion` L78); the well fold `film-shot-well-open` and row stagger `film-shot-row-in` unchanged; nothing new animates on hold ("holding is the absence of a scroll"). Seams verified: `wellStops` L294–297 and `isActive` L456 are the only two reads the state splits; the well mount at L480 is `isActive && wellOpen && onSelectShot` and must key on the displayed id; `PointRow`/`ShotWellRow` are memoized on stable callbacks (L557–563, L794–800) — keep the click wrapper stable (a ref for `activePointId` rather than a dep, or accept one re-render per point crossing, and say which); the room's `activePoint` is `playingStop?.point` (L478), null in dead time; the room's `step` walks `p.walkStops`, the same array as the shell's `position` (film-tab.tsx L255–258, L313–317). Harness: `openRoomWithDrawer` (spec L1103–1121) accepts no extra params today — add a third argument threaded into `open()`'s `extra`; the fixture has NO shots today (`buildShotStops` reads `stop.point.shots`, film-shots.ts L60), so the drawer has no well until the fixture gets them — `MatchShot` shape at match-points-server.ts L5–33, builder precedent tests/fixtures/film-shot-row-reveal-harness.tsx L100–112; the clip is 2.000 s and windows start at 0.2/0.35/0.45 (harness L38–45, spec `STOP_START` L1089); untimed rows render at `opacity-45` and are not seekable, so `pad` never touches `walkStops`; install spies with `page.evaluate` AFTER the room opens, never `addInitScript` (T16's lesson, L1104 sets only localStorage); Playwright's `page.mouse.wheel` fires a real `wheel` and really scrolls; run `tests/film-playback-refresh.spec.ts` alone first (it webpacks the harness in `beforeAll`; the `(live)` shared-DB specs that trip in a full run are unrelated). Repo rules: never `foo.tsx` beside `foo.ts`; if a hook is extracted it is `use-scroll-intent.ts` beside `use-corner-drag.ts` / `use-seek-settling.ts`; `useMatchSides()` stays the only you/opp source; keep `data-point-id`, `data-playing`, `data-shot-id`, `data-shot-well`, `film-room-video`, `film-player-video` untouched; the `widget-states` hook fires on this dashboard edit — run the skill before commit (no loading/empty state changes here; say so in the log). Load `.skills/advantage-analytics-design/SKILL.md` + `reference/foundations.md` (Duration Scale L299–316, Reduced Motion L349–351) and `docs/ui-revamp-guardrails.md`.

## T19 · The drawer's "Now playing" pill: mount, strings, chevron, motion

- **status:** done
- **model:** opus
- **needs:** T18
- **files:** (guess) src/components/dashboard/matches/match-detail/film/point-list.tsx (the `listRef` div L433–436 gets a wrapper; a new `nowPlaying` prop; the pill component beside `ShotWell`); film-fullscreen.tsx (`position` L659–662 → the drawer's `nowPlaying`); film-room-drawer.tsx (one pass-through prop); src/app/globals.css (a `film-follow-pill-in` keyframe after `film-shot-well-fade` L884–893); tests/film-playback-refresh.spec.ts (cases after the T18 block)
- **done when:**
  - [ ] `PointList` gains `nowPlaying: { id: string; index: number | null } | null` (the room passes `activePoint` + `position?.index ?? null`, film-fullscreen.tsx L659–662 / L1590; the shell passes the same from film-tab.tsx `position` L313–317 but draws nothing — default 2), the `listRef` div is wrapped in a `relative flex min-h-0 flex-1 flex-col` div inside the same `section`, and the pill renders as that wrapper's SECOND child (DOM order after the scroller), only when `tone === "dark"` and `followAffordance(pointFocus, nowPlaying)` (T18) is non-null; it is unmounted when hidden — never `opacity-0` in the tab order — so `aside[aria-label="Points"] button[aria-label^="Now playing:"]` has count 0 in `follow`, in dead time, and while the held point is the playing point
  - [ ] The pill is `<button type="button" data-film-chrome aria-label={affordance.ariaLabel}>` whose visible text is `affordance.label` (`Now playing · Point 14`, Inter not mono, middot U+00B7) followed by an `aria-hidden` Lucide `ChevronDown`/`ChevronUp` at `className="size-3" strokeWidth={1.6}` when `inCut`; direction = the lit `[data-playing="true"]` row's `getBoundingClientRect().top` against the scroller's box centre (`down` below, `up` above), re-read on the scroller's `scroll` event and on `nowPlaying` change; NO chevron when `inCut` is false (`Now playing · not in this cut`, default 4) and NO chevron when the lit row is fully inside the scroller's box (default 5); pressing it calls `onFollow()` and nothing else (the T18 effect does the scroll and the well)
  - [ ] Classes verbatim from the doc's Geometry: `inline-flex h-7 items-center gap-[7px] rounded-[var(--radius-button)] bg-[rgba(13,13,13,0.72)] px-2.5 text-[11px] font-medium text-white transition-[opacity,transform,background-color] duration-200 ease-[var(--ease-primary)] hover:bg-[rgba(13,13,13,0.9)] focus-visible:shadow-[var(--focus-ring)] focus-visible:outline-none` plus `shadow-[inset_0_0_0_1px_rgba(255,255,255,0.1)] active:scale-[0.97] absolute bottom-3 left-1/2 -translate-x-1/2 cursor-pointer`; no new hex or alpha (the two alphas are the "Points" trigger's L1518 and the drawer's L98); no tooltip; `film-room-drawer.tsx` L98 and the drawer's padding are unchanged
  - [ ] `globals.css` gains, beside `film-shot-row-in`, `@keyframes film-follow-pill-in { from { opacity: 0; transform: translateY(4px) } to { opacity: 1; transform: translateY(0) } }`, `.film-follow-pill-in { animation: film-follow-pill-in var(--duration-fast) var(--ease-primary) both }`, and a `@media (prefers-reduced-motion: reduce)` block that keeps the fade and drops the transform (an opacity-only keyframe, NOT `animation: none` — the pill still appears); the pill carries the class on mount, and leaves through a 100 ms opacity-only transition before unmount (a `leaving` flag cleared on the pill's own `transitionend`, the `onTransitionEnd` guard pattern of film-room-drawer.tsx L94–96, with a 150 ms timer fallback); the exit on room exit is the existing `[data-film-chrome]` fade (film-fullscreen.tsx L926–934), untouched
  - [ ] `tests/film-playback-refresh.spec.ts` (with `pad: "12"`): (b1) click `DRAWER_ROW("a")`, seek to `b` → the pill exists with text `Now playing · Point 2` and `aria-label` `Now playing: point 2 — follow playback`, `page.mouse.wheel(0, 600)` scrolls the lit row above the box → the pill's svg has class `lucide-chevron-up`; (b2) press the pill → the pill is gone, `[data-point-id="b"] + [data-shot-well]` exists, and `expect.poll` sees the scroller's `scrollTop` reach the value that puts row `b` inside the box; (b3) with `a` held and `b` playing and the lit row fully in view (no wheel), the pill has no `svg`; (b4) held on `a`, `b` playing, choose `Saved only` → text `Now playing · not in this cut`, no `svg`, `aria-label` `Now playing: a point outside this cut — follow playback`; (g) `page.emulateMedia({ reducedMotion: "reduce" })` before the room opens, an `Element.prototype.scrollTo` spy installed after → pressing the pill records `{ behavior: "auto" }`, and a `readFileSync` of `src/app/globals.css` (the film-shot-row-reveal.spec.ts L159–170 pattern) matches a `prefers-reduced-motion: reduce` block for `.film-follow-pill-in` that contains no `transform:`; `npm run lint`, `npm run typecheck`, `tests/film-playback-refresh.spec.ts`, `tests/film-shot-row-reveal.spec.ts`, `tests/design-drift.spec.ts` pass
- **notes:** Implements Handoff items 4, 10 (the pill) and 11b/g of `docs/superpowers/specs/2026-09-22-film-follow-hold-design.md`; frame section B1 of `…/2026-09-22-film-follow-hold/frames.html` — "Read the frame before writing any px, alpha or radius value". Author's answers: (4) `Now playing · not in this cut` with no chevron IS the string for a playing point the cut excludes ("re-follow still puts the list back on the film's next in-cut point"); (5) the chevron is HIDDEN when the lit row is already fully visible; the wording `Now playing · Point 14` stays as designed; the frame's in-row annotation text ("PLAYING · POINT 14 · LIT, NO WELL") is frame-only — no such label ships in a point row. The control is a BUTTON, so it takes `--radius-button` (a rounded rectangle) — the DS reserves `rounded-full` for filter pills, tabs, avatars and indicators; "pill" in the doc is loose wording for the same geometry as the room's "Points" trigger. Show rule: `held` AND a point is playing AND `playing.id ≠ held.pointId` — never in dead time ("Between points … gets no words", H2 R7), never in `follow`. The number is `position.index`, the playing point's index in `walkStops` plus one — the same field as the card's `14 / 87` and the transport's `Point 14 / 87` (film-transport.tsx L173), so all agree; `position === null` while a point plays is the "not in this cut" row. Why the wrapper and not the drawer: "the drawer has no padding and the Advanced branch (L337) replaces the scroller in the same `section`, so the pill must sit with the scroller it belongs to"; the pill "never covers the held well: the wrapper pins it to the list's bottom edge and the well sits wherever the viewer left it". Reading `scroll` for DIRECTION is fine — it is intent that `scroll` cannot carry — and T18's `followScrollRef` is there so the direction read during the effect's own travel is harmless. Tailwind v4 (`^4.2.2`): `-translate-x-1/2` sets the `translate` property, not `transform`, so the keyframe's `transform: translateY(4px)` composes with it — the frame's `pill-in` used `translate(-50%, 4px)` only because it is plain CSS; do not copy that. Motion (DS scale, no value outside the doc's table): enter 150 ms (`--duration-fast`) opacity 0→1 + 4 px rise on `--ease-primary` (the rise is `film-shot-row-in`'s, globals.css L827–836); exit 100 ms opacity only; hover `0.72 → 0.9` over the trigger's own 200 ms transition; press `scale 0.97`; reduced motion: "skip transforms, keep opacity" (foundations.md L349–351) — unlike `.film-shot-row-in`'s `animation: none` (L844–848), the pill keeps its fade because it carries state. Accessibility from the doc: a `<button>` inside `aside[aria-label="Points"]`, in DOM order after the scroller so Tab reaches it after the last row and before the transport; focus on it holds the room's chrome up via `focusHolds` (film-fullscreen.tsx L1062–1067) with no extra code; visible text + `aria-hidden` chevron, so no `ChromeTooltip` (chrome.md's icon-only rule does not apply). Lucide React (`v0.562.0`) stamps `lucide lucide-chevron-up` on the svg — the class the spec asserts. Keep `data-film-chrome` on the pill so the R2 chrome collapse (200 ms opacity) and the exit fade take it. Repo rules as in T18; `widget-states` hook fires — run the skill before commit; keep every existing test hook.

## T20 · The shell card holds: the header line, the card's click wrapper, `FilmPlayer.onStep`

- **status:** done
- **model:** opus
- **needs:** T18, T19
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-this-point.tsx (props L50–68, the head L81–106, `ShotRow` `onClick` L244); film-tab.tsx (the `<FilmThisPoint>` mount L592–601, `pointShots` L295–301, `handleSelectShot` L303–305, `handleStep` L308–310, `<FilmPlayer>` L568–591); film-player.tsx (`step` L466–474, props L160–185 / destructure L260–272, no other change); tests/film-playback-refresh.spec.ts (cases after the T19 block)
- **done when:**
  - [ ] `FilmPlayer` gains `onStep?: () => void`, called at the top of its internal `step` (L466) before `seekTo`, so the transport's `Previous point`/`Next point` glyphs (L720–735), the shell's `ArrowUp/Down/Left/Right` handler (film-tab.tsx L436–445 → `playerRef.current.step`) and the card's step buttons (→ `handleStep` → the same `step`) all re-follow through one path; `film-tab.tsx` passes `onStep={followPlayback}`; `handleStep` and the key handler are otherwise unchanged; the diff states in a comment why the notification was chosen over lifting `step` to the tab
  - [ ] `FilmThisPoint` receives the DISPLAYED point: `point` = `stops.find(s => s.point.id === displayedPointId)?.point ?? null` (the playing point in `follow`), `shots` = that point's shots (`pointShots` L295–301 re-keyed on the displayed id), plus `affordance: ReturnType<typeof followAffordance>` (computed once in `film-tab.tsx` from `pointFocus` and `{ id: activePoint.id, index: position?.index ?? null }`), `onFollow`, `onHoldPoint`; `position`, `activeShotId`, `onStep` keep reading the PLAYING point; the shot rows, the footer (`N shots · Ns · Result`) and the empty copy all come from `point`/`shots`, so while held they do not change when the film crosses into the next point, and `bg-[var(--surface-subtle)]` appears on a row only when the playing point is the held one (no code — `activeShotId` belongs to the playing point's shots)
  - [ ] In the head (L84–105): while `affordance` is non-null the counter `<span>` is REPLACED (default 1 — not kept beside it) by `<button type="button" aria-label={affordance.ariaLabel}>` with the doc's classes verbatim — `inline-flex h-7 shrink-0 cursor-pointer items-center gap-1 rounded-[var(--radius-element)] px-1.5 text-[11px] font-medium whitespace-nowrap transition-colors duration-200 hover:bg-[var(--surface-subtle)] focus-visible:shadow-[var(--focus-ring)] focus-visible:outline-none film-follow-pill-in` and `style={{ color: "var(--ink-600)" }}` — whose text is `affordance.label` followed by an `aria-hidden` Lucide `ChevronRight` `size-3` `strokeWidth={1.6}` in the same ink (shown for both the in-cut and the not-in-cut label), sitting between the two `StepButton`s in their tab order; pressing it calls `onFollow()`; otherwise the counter span renders exactly as today (its `min-w-[56px]` is not applied to the line); both `StepButton`s keep `disabled={!position}`; the head's height does not change (`h-7` line in a `size-7` row)
  - [ ] `ShotRow`'s click path (L244) goes through a stable wrapper in `film-tab.tsx` (`handleSelectShot` L303–305): `onFollow()` when `stop.point.id === activePoint?.id`, else `onHoldPoint(stop.point.id)`, then `playerRef.current?.seekTo(stop.start)`; `useMatchSides()` remains the row's only name source; no new hex, alpha, tooltip or DOM node beyond the one button
  - [ ] `tests/film-playback-refresh.spec.ts`, on the shell (no room): (f1) `seekTo(page, REPORT, 0.25)`, click the shell row `[data-point-id="a"][role="button"]:not(aside *)`, `seekTo(page, REPORT, 0.4)` → `playingRow` is `b`, the card `section[aria-label="This point"]` still lists `a`'s two `[data-shot-id]`s, and `button[aria-label="Now playing: point 2 — follow playback"]` inside it has text `Now playing · Point 2` while `.mono` counter text `2 / 3` is absent from the head; (f2) press it → the line is gone, the head shows `2 / 3`, the rows are `b`'s shots; (f3) hold `a` again, seek to `b`, click the transport's `Next point` glyph → the line is gone and the rows are `c`'s (follow through `onStep`); (f4) hold `a`, seek to `b`, click the card's `Next point` step button → same; (f5) the shell's `section[aria-label="Point list"]` never contains a `button[aria-label^="Now playing:"]` (default 2); `npm run lint`, `npm run typecheck`, `tests/film-playback-refresh.spec.ts`, `tests/film-shot-row-reveal.spec.ts` pass; `film-fullscreen.tsx`, `film-room-drawer.tsx`, `point-list.tsx` are not in the diff
- **notes:** Implements Handoff items 3 (card props), 7 (card wrapper), 8 (`FilmPlayer` notification), 9, 10 (the line) and 11f of `docs/superpowers/specs/2026-09-22-film-follow-hold-design.md`; frame section C1 — "the header line takes the counter's place between the two step buttons … the 56px `min-w` of the counter is released while the line is in its place; the head does not change height". Author's answers: (1) while held the card's `14 / 87` counter is REPLACED by the header line — the line carries "14" and the counter returns as-is on re-follow; (2) the card's header line is the shell's return affordance; there is no pill in the shell's own point list. Folds T17 follow-up 4 (the `onStep` gap: "`FilmPlayer`'s transport chevrons call an internal `step` the tab cannot observe"). Strings (from T18's `followAffordance`, do not re-spell them): `Now playing · Point 14` / `Now playing · not in this cut`; `aria-label` `Now playing: point 14 — follow playback` / `Now playing: a point outside this cut — follow playback`; the line's `→` in the plan IS the 12 px `chevron-right` glyph, shown in both variants (only the drawer pill drops its chevron for "not in this cut", because the pill's chevron points at a row and the line's arrow means "go"). Per-surface table for the card, verbatim: HOLDS "the shot rows (`shots` = the displayed point's), the footer (`N shots · Ns · Result`), the empty copy if the held point has no timed shots"; KEEPS FOLLOWING "the position counter (`14 / 87` — while held its number is carried by the header line and the counter returns as-is on re-follow); the step buttons; the active-row wash (only when the playing point is the held point)"; outside the card nothing holds (the player, the seek lane, the rail scoreboard via `usePublishFilmHead` L289–294, the shell list's lit row). Motion: the line enters with the same `film-follow-pill-in` class T19 added (150 ms opacity + 4 px rise on `--ease-primary`; reduced motion → fade only), exits with the counter's return (unmount; the 100 ms opacity exit is optional here — the doc's card column says "The counter returns"); the row swap on re-follow replays the T9 stagger because rows re-key (`ShotRow` L245–250); reduced-motion rows are `animation: none` as shipped. Colour: `var(--ink-600)` is the footer's ink (L161) — "the eyebrow's weight without its tracking"; hover `hover:bg-[var(--surface-subtle)]` is the step buttons' (L206). Trap: `position` is null in dead time and for a point outside the cut — the step buttons' `disabled={!position}` stays keyed to it; a held point with no timed shots shows "The shots on this point were never timed against the video." (L130–131), not the pre-serve copy. The fixture's shots and `pad` arrive with T18; the shell has no `pad` need. Repo rules as in T18; `widget-states` hook fires on this dashboard edit — run the skill before commit and say in the log that the card's two empty copies are unchanged in wording; keep `data-shot-id`, `film-player-video`; `useMatchSides()` only.

## T21 · Drawer pill pins to the edge the playing row is beyond, with hysteresis

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/film/point-list.tsx (`FollowPill`, ~L764–870: the `direction`/`inCut` effect and the `className`); src/app/globals.css (`film-follow-pill-in` / `film-follow-pill-fade`, if the rise needs a per-edge variant); docs/superpowers/specs/2026-09-22-film-follow-hold-design.md; tests/film-playback-refresh.spec.ts (the T19 block)
- **done when:**
  - [ ] `FollowPill` keeps an `edge: "top" | "bottom"` with memory: it becomes `"top"` only once the lit row is fully above the scroller's box (`row.bottom <= box.top`) and `"bottom"` only once fully below (`row.top >= box.bottom`); while the row overlaps the box, `edge` keeps its last value. The not-in-cut pill always resolves to `"bottom"` with no chevron
  - [ ] The pill is unmounted, not just chevron-less, while the lit row is fully inside the scroller's box, and mounted otherwise (including not-in-cut). The existing "no chevron while the lit row is fully in view" case is updated to assert the pill has count 0; every other T19 pill case (strings/aria, press-to-follow, not-in-cut, reduced motion) passes with no other assertion changed
  - [ ] The pill carries `top-3` xor `bottom-3` (`left-1/2 -translate-x-1/2` unchanged) matching `edge`, with `chevron-up` on top and `chevron-down` on bottom; the enter rise moves away from the edge it lands on, and `prefers-reduced-motion` keeps the opacity-only fade on both edges. No new hex, alpha or radius
  - [ ] `tests/film-playback-refresh.spec.ts` gains two cases on a `pad`-lengthened list: (a) held on `a`, `b` playing, list parked at `scrollTop = scrollHeight` via `page.evaluate` → pill has `top-3` and a `lucide-chevron-up` svg; (b) hysteresis: pill pinned bottom, scroll so the lit row is partly inside the box → still `bottom-3` + chevron-down, then scroll to fully above → flips to `top-3` + chevron-up
  - [ ] The design doc's strings table documents the two-edge pin, the hysteresis rule, the shared 12px inset, and "hidden while the lit row is fully in view"; the open item "Chevron direction threshold" is removed as resolved
- **notes:** Today `direction` is a stateless centre test and the pill is always `bottom-3`, with only the chevron hidden when the row is in view. Keep `data-film-chrome`, the `onTransitionEnd` exit-unmount plumbing, `data-point-id`/`data-playing`. The shell card's "Now playing" header line is out of scope. Park `scrollTop` with `page.evaluate` rather than a wheel (wheel travel is machine-dependent, see T18's step case). Dashboard edit: widget-states mark before commit; loading/empty/error states unchanged.

## T22 · Revert the shell card to following playback (undo T20's hold)

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-this-point.tsx (props L50–91, the head's ternary L119–142); film-tab.tsx (`displayedPoint` L337–343, `pointShots` L344–350, `activePointIdRef` L355–358, `handleSelectShot` L362–369, `affordance` L397–401, the `<FilmThisPoint>` mount L680–692); tests/film-playback-refresh.spec.ts (the T20 block, L1780–1887); docs/superpowers/specs/2026-09-22-film-follow-hold-design.md (the state table L88–95, Handoff items 3/7/9/10/11f L214+); docs/superpowers/specs/2026-09-22-film-follow-hold/frames.html (`#C1` L241–243)
- **done when:**
  - [ ] `FilmThisPoint`'s props type (film-this-point.tsx) no longer declares `affordance`, `onFollow`, or `onHoldPoint`, and the header-line branch (`{affordance ? <button aria-label={affordance.ariaLabel}>…Now playing…</button> : …}`) is gone — the head renders the position counter `<span className="mono tabular …">` unconditionally, exactly as before T20; the `ChevronRight` import stays (still used by `StepButton`)
  - [ ] film-tab.tsx: the `<FilmThisPoint>` mount passes `point={activePoint}`, not `displayedPoint`, and carries no `affordance`, `onFollow`, or `onHoldPoint` prop; `pointShots` is filtered by `activePoint.id` (not `displayedPointId`); `handleSelectShot` is `(stop) => playerRef.current?.seekTo(stop.start)` with no `holdPoint`/`followPlayback` call; the `displayedPoint` const, the `activePointIdRef` ref and its effect, and the `affordance` const are deleted (`grep -c "activePointIdRef\|const displayedPoint\|const affordance" film-tab.tsx` → 0); `onStep={followPlayback}` on `<FilmPlayer>` and `onStep={handleStep}` on `<FilmThisPoint>` are UNCHANGED (`handleStep` still calls `followPlayback()` before `playerRef.current?.step`); `pointFocus`, `displayedPointId`, `holdPoint`, `followPlayback`, `nowPlaying` and the shell `<PointList>`'s hold props are untouched
  - [ ] `tests/film-playback-refresh.spec.ts`'s three T20 tests assert the card follows: with the shell list held on `a` and the film playing `b` (list-hold setup unchanged), `cardShots(page)` reads `["b-shot-1","b-shot-2"]`, `${CARD_HEAD} .mono` shows `2 / 3`, and `${CARD} button[aria-label^="Now playing:"]` (`LINE`) has count 0 throughout every test; `SHELL_PILL` stays count 0 as before; the transport-glyph and card-step-button cases keep proving `onStep`/`handleStep` re-follow the LIST (drop the card-specific "line disappears" assertions, keep the `playingRow`/list assertions)
  - [ ] `docs/superpowers/specs/2026-09-22-film-follow-hold-design.md`'s "The state, per surface" table row for `Shell "This point" card` is rewritten: HOLDS becomes "Nothing"; KEEPS FOLLOWING lists the shot rows, the footer, the empty copy, the position counter, the step buttons and the active-row wash — the whole card, matching the room's own "(outside the drawer)" row; Handoff items 3, 7, 9, 10 and 11f each gain a one-line note that they are superseded by this revert (the doc stays a record, not a deletion)
  - [ ] `docs/superpowers/specs/2026-09-22-film-follow-hold/frames.html`'s `#C1` state block's `state-title` is prefixed `Superseded — ` (frame kept for history)
- **notes:** Author's decision (2026-09-23): "I also don't like how the this point widget stays on the point, should I revert it back to be consistent with the video player?" — the shell card returns to always showing the playing point (`activeStopAt`), like the scoreboard/court/transport counter beside it; only the shell `PointList` and the room drawer keep follow-or-hold. `pointFocus`, `holdPoint`/`followPlayback`, `followAffordance`, `displayedPointId`, `FilmPlayer.onStep` and the step-path re-follow all STAY — the list still needs them. Selective revert of T20's diff (`git show 0b053c14`); do not touch `film-fullscreen.tsx`, `film-room-drawer.tsx`, or `point-list.tsx` (T21 owns it). T21 edits a disjoint block of the same spec file; the queue drains serially. widget-states mark before commit; the card's two empty copies are unchanged in wording. `npm run lint`, `npm run typecheck`, `tests/film-playback-refresh.spec.ts`, `tests/film-shot-row-reveal.spec.ts` must pass.

## T23 · Give the shell's own point list the "Now playing" pill too

- **status:** done
- **model:** opus
- **needs:** T21, T22
- **files:** (guess) src/components/dashboard/matches/match-detail/film/point-list.tsx (the affordance gate, currently `tone === "dark" ? followAffordance(pointFocus, nowPlaying) : null` ~L512–513; `FollowPill`'s props and className, T21's edge/hysteresis version); docs/superpowers/specs/2026-09-22-film-follow-hold-design.md (the state table L88–95, the strings table's "Card header line" column L118–129); tests/film-playback-refresh.spec.ts (the `SHELL_PILL` assertions inside T22's rewritten card-following block)
- **done when:**
  - [ ] `point-list.tsx`'s affordance computation runs for both tones (drop the `tone === "dark" ? … : null` gate so `FollowPill` receives a non-null `affordance` in the light tone under the same `followAffordance(pointFocus, nowPlaying)` rule the drawer uses), while the intent-listener effect keeps its own `tone !== "dark"` early return unchanged — hand scroll, wheel, touchmove, pointerdown and the scroll-key set still do NOT hold in the light tone; only whether the pill may mount changes
  - [ ] `FollowPill` gains a `tone: FilmListTone` prop and its className adds `shadow-[var(--shadow-floating)]` only when `tone === "light"` (foundations.md: "Dark floating UI"); `tone === "dark"` keeps exactly its shipped `shadow-[inset_0_0_0_1px_rgba(255,255,255,0.1)]`; every other class (`bg-[rgba(13,13,13,0.72)]`, `text-white`, `rounded-[var(--radius-button)]`, `text-[11px]`, the `size-3 strokeWidth={1.6}` chevron, the enter/exit motion classes) is identical in both tones — no new hex or alpha literal; T21's edge-pin-with-hysteresis and hide-while-in-view behavior is reused as-is, not reimplemented
  - [ ] `tests/film-playback-refresh.spec.ts`: on a `pad`-lengthened shell list (no room), held on `a` via the shell row click with `b` playing and `b`'s row scrolled out of the shell list's view, `SHELL_PILL` has count 1, its text/`aria-label` match the drawer's strings exactly (`Now playing · Point 2` / `Now playing: point 2 — follow playback`), and its edge/chevron behaves like the drawer's post-T21 pill; pressing it re-follows (the held well closes, the well opens under the playing row); the "lit row fully in view → no pill" case is proven on the shell list too; no case asserts hand-scroll holds the shell list
  - [ ] `docs/superpowers/specs/2026-09-22-film-follow-hold-design.md`: the state table gets its own row for the shell's point list (mirroring "Room drawer"): HOLDS = "the open well; the list's scroll position (keep-in-view off while held)"; KEEPS FOLLOWING = "the lit row"; entry by click/Enter/Space only (no hand-scroll hold on the light tone); the strings table's "Card header line" column is renamed "List pill" since the header line is gone (T22) and the pill is the return affordance on every `PointList` surface
- **notes:** Author's follow-up (2026-09-23): "should the non-full screen points widget also have the 'Now playing <point>' rectangular chips as well?" — decision: yes. film-tab.tsx needs no change: the shell `<PointList>` already receives `pointFocus`, `nowPlaying`, `onFollow`; the only gate is point-list.tsx's `tone === "dark"` check. `needs: T21` (extends T21's edge-pin pill), `needs: T22` (builds on T22's rewritten spec block and doc table). Out of scope: any new hold source on the light tone. widget-states mark before commit; no loading/empty/error changes. `npm run lint`, `npm run typecheck`, `tests/film-playback-refresh.spec.ts`, `tests/design-drift.spec.ts` must pass.

## T24 · Hand scroll holds the shell list too, and the pill shows whenever the lit row is out of view

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-timeline.ts (`followAffordance` L313–332); point-list.tsx (the intent-listener effect L516–550 and its comment L496–512, the header comment L120–123); film-tab.tsx (stale comments L365–367 and L688–692 only — no logic); tests/film-timeline.spec.ts (L448–480); tests/film-playback-refresh.spec.ts (`wheelDrawer` L1447–1471, the shell helpers L1949–2020, `boundShellList` L2107, new cases after the T23 block L2299); docs/superpowers/specs/2026-09-22-film-follow-hold-design.md (state table L88–96, Enter table L101–114, strings table L119–130, Open item L336–352)
- **done when:**
  - [ ] `followAffordance` no longer returns `null` when `playing.id === focus.pointId`: held AND a point playing → the in-cut affordance when `index` is a number, the not-in-cut one when `index` is null, whatever the held id; `null` stays for `follow` and for `playing === null`. `tests/film-timeline.spec.ts`'s "held, the playing point is the held point" case is rewritten to expect `{ label: "Now playing · Point 9", ariaLabel: "Now playing: point 9 — follow playback", inCut: true }` for `{ id: "p9", index: 9 }` and the not-in-cut object for `{ id: "p9", index: null }`; no other row of that describe changes. `FollowPill`'s T21 in-view rule is what hides the pill now — no code change in `FollowPill`
  - [ ] `point-list.tsx`'s intent-listener effect drops the `tone !== "dark"` early return (keeps `!scrollerMounted`), so `wheel`, `touchmove`, `pointerdown` on the scroller itself and the `SCROLL_KEYS` `keydown` hold on the light tone with the same handlers and the same `heldRef` enter-only rule; the comment block above it (L496–512) and the header comment (L120–123: "on the dark tone a hand scroll") are rewritten to say both tones hold on hand scroll, and to record that on the shell an arrow on a focused row is a SCROLL, not a step — `film-tab.tsx`'s window handler returns early for `[role=button]` targets (L484), so the net result there is `held`, unlike the drawer where the room's handler steps and re-follows; `film-tab.tsx`'s two stale comments (L365–367 "draws no pill yet", L688–692 "no intent listeners") are corrected, no logic in that file changes
  - [ ] `tests/film-playback-refresh.spec.ts` gains, on the shell with `openShell(page, …, { pad: "12", ttl })` + `boundShellList(page, 240)`: (a) `seekTo(page, REPORT, 0.25)` → `playingRow` is `b`; a real wheel over the shell scroller (generalise `wheelDrawer` into `wheelScroller(page, selector, deltaY)` and keep `wheelDrawer` as a one-line wrapper) then `parkScroller(page, SHELL_SCROLLER, "end")` → `SHELL_PILL` count 1 with text `Now playing · Point 2` (held ≡ playing, row out of view — intent 2); `seekTo(page, REPORT, 0.5)` → `playingRow` is `c`, `shellScrollTop` unchanged after 400 ms, the pill reads `Now playing · Point 3`; pressing it → count 0 and `shellLitPlace` is `inside`; (b) `page.focus(SHELL_ROW("b"))` at 0.25, `page.keyboard.press("ArrowDown")`, park at end, seek to 0.5 → `shellScrollTop` unchanged and `SHELL_PILL` count 1 (the key held; the existing "a focused point row keeps the arrow keys" case still passes unchanged — the time stays 0.25); (c) on the drawer with `pad: "12"`: click `DRAWER_ROW("a")`, `drawerSettled`, `parkDrawer(page, "end")` with no seek → `PILL` count 1, text `Now playing · Point 1`, `pillEdge` `top`; press → count 0 and `litRowInView` true. Cases (a)–(c) fail on the current tree; every existing case in the file, `tests/film-timeline.spec.ts`, `tests/design-drift.spec.ts`, `npm run lint`, `npm run typecheck` pass
  - [ ] `docs/superpowers/specs/2026-09-22-film-follow-hold-design.md`: the state table's shell-list row (L93) drops "Entered by a row click / Enter / Space only — the light tone installs no hand-scroll hold (T23)" for "same hold sources as the drawer (T24, 2026-09-23)"; the Enter table's hand-scroll row (L108) reads Surface "Drawer, shell list", and rows L105/L106 replace "Held ≡ playing at that instant, so no pill" with "no pill until the lit row leaves the box"; the strings table's "Shown when" column drops `playing.id ≠ held.pointId` from L123/L124/L126 and row L129 ("Held; playing point is the held point") becomes "as the rows above — shown once the lit row is out of the box"; the Open item's "_Answered by T23_" note gains "_Superseded by T24: hand scroll holds on the light tone too_"; T23's "no case asserts hand-scroll holds the shell list" is noted as superseded in the same line
- **notes:** Author's words (2026-09-23): (1) "for the point list outside, if I am scrolling to future points, the pill doesn't appear at top and jumps back to the current point" — cause: the light tone installs no intent listeners, so a wheel on the shell list holds nothing and the next crossing's keep-in-view scrolls the list back; (2) "should the chip just be persistent?" — agreed: not always-on, but shown whenever held AND the lit row is not wholly inside the scroller's box, including when the held point IS the playing one (today `followAffordance` hides it then, so scrolling away from the playing point offers no way back). Wording, edge pin, hysteresis, motion, `--shadow-floating` on light: unchanged. The T23 design note "no hand-scroll hold on the light tone" is superseded by author decision. Trap for (c): a click on `a` at film zero holds `a` and seeks it, so held ≡ playing — that is the point of the case. Trap for the key case: Playwright's `keyboard.press` on a focused `[role=button]` row bubbles to the list's native listener first (hold), then the tab's window handler returns early — assert hold via the pill, never via the time. Existing `holdAThenPlayB` / `shellHoldAThenPlayBAbove` assert `PILL`/`SHELL_PILL` count 0 right after the click on `a` — that still holds because row `a` is in view at `scrollTop` 0; do not move those assertions. `parkScroller` assigns `scrollTop` and fires only `scroll`, so parking never holds — fine to park after the wheel to make "out of view" deterministic. `wheelDrawer` waits for the drawer's slide to settle before the wheel; the shell scroller does not move, but keep the post-wheel "stopped moving" read. Repo rules as T18–T23: keep `data-point-id`, `data-playing`, `data-shot-well`, `film-room-video`, `film-player-video`; no new hex/alpha; `widget-states` mark before commit (loading/empty/error states unchanged — say so in the log). The design doc's state and Enter tables and the T23 row must be updated as above. Run `tests/film-playback-refresh.spec.ts` alone first; two `(live)` specs flake only in the full suite.

## T25 · A hand scroll with no displayed point still holds: `held` with `pointId: null`

- **status:** done
- **model:** opus
- **needs:** T24
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-timeline.ts (`PointFocus` L284, `displayedPointId` L287–292); film-tab.tsx (`holdPoint` L183–186, the cut-change effect L279–284); point-list.tsx (`onHoldPoint` prop type L263, `hold()` L521–525 and the listener comment); film-fullscreen.tsx (`onHoldPoint` in the props L188); film-room-drawer.tsx (L56); tests/film-timeline.spec.ts (L433–445, L448–480); tests/film-playback-refresh.spec.ts (new cases after T24's); docs/superpowers/specs/2026-09-22-film-follow-hold-design.md (state table L88–96, Enter table L108, strings row L128)
- **done when:**
  - [ ] `film-timeline.ts` exports `type PointFocus = { mode: "follow" } | { mode: "held"; pointId: string | null }` — `pointId: null` is "held with no well": a hand scroll made while nothing was displayed (the drawer's R7 dead time, where `playingStop` is null; both surfaces before the first point, where `activeStopAt` is null until 0.1 s) keeps the list where the viewer left it and opens no well. `displayedPointId(focus, playing)` returns `focus.pointId` (so `null`) while held; `followAffordance` is unchanged from T24 (held + a playing point → the pill). `tests/film-timeline.spec.ts` gains `displayedPointId({ mode: "held", pointId: null }, "p14") === null` and `followAffordance({ mode: "held", pointId: null }, { id: "p14", index: 14 })` → the in-cut object
  - [ ] `holdPoint(pointId: string | null)` in `film-tab.tsx`, `onHoldPoint: (pointId: string | null) => void` in `PointListProps`, `FilmFullscreenProps` and `FilmRoomDrawerProps`; `point-list.tsx`'s `hold()` drops the `if (id)` guard and calls `onHoldPoint(displayedPointRef.current)` (still an enter only — `heldRef` unchanged); the row/shot click wrappers still hold a real id; `film-tab.tsx`'s cut-change effect re-follows only when `pointFocus.pointId !== null && !filteredPoints.some(...)` — a null hold survives a cut change (there is no row for the cut to drop), with the reason in a comment
  - [ ] `tests/film-playback-refresh.spec.ts`, drawer, `openRoomWithDrawer(page, …, { pad: "12" })` at film zero (nothing playing, no well): (a) `wheelDrawer(page, 200)`, `parkDrawer(page, "end")`, then `seekTo(page, ROOM, 0.4)` → `playingRow` is `b`, `drawerScrollTop` unchanged after 400 ms, `aside[aria-label="Points"] [data-shot-well]` count 0 (no well opened by the hold), `PILL` count 1 with text `Now playing · Point 2`; pressing it → count 0, `WELL_UNDER("b")` count 1, `litRowInView` true; (b) after the same wheel at film zero, choose `Saved only` from the drawer's `Point filters` menu, then `seekTo(page, ROOM, 0.4)` → `PILL` text `Now playing · not in this cut` (the null hold survived the cut change; `b` is unsaved); (c) after the wheel at film zero, click `DRAWER_ROW("a")` → `WELL_UNDER("a")` count 1 and `playingRow` `a` (a click replaces the null hold with a real one). Case (a) fails on the current tree (the hold is a no-op and the crossing scrolls the list to `b`)
  - [ ] The same on the shell, `openShell(page, …, { pad: "12", ttl })` + `boundShellList(page, 240)` at film zero: `wheelScroller` (T24) over the shell scroller, `parkScroller(page, SHELL_SCROLLER, "end")`, `seekTo(page, REPORT, 0.3)` → `playingRow` `b`, `shellScrollTop` unchanged after 400 ms, `SHELL_PILL` count 1; every existing case in the file, `tests/film-timeline.spec.ts`, `npm run lint`, `npm run typecheck` pass
  - [ ] `docs/superpowers/specs/2026-09-22-film-follow-hold-design.md`: the state table's drawer and shell-list rows say what a null hold holds ("no well; the scroll position"); the Enter table's hand-scroll row reads "`held(displayedPointId)` — null in dead time or before the first point: held with no well, the list stays put"; the strings row L128 ("Held; no point playing") keeps "never" and adds "a hold made in dead time shows the pill once the next point begins"; the cut-change row (L113) notes a null hold is not reset; `PointFocus` in Handoff item 1 is updated to the widened type
- **notes:** Author's words (2026-09-23): "sometimes when I am scrolling it still jumps to the playing point." Confirmed cause by code reading: `hold()` (point-list.tsx L521–525) early-returns when `displayedPointRef.current` is null, which is every hand scroll made in the drawer's dead time between points (`activePoint = playingStop?.point ?? null`, film-fullscreen.tsx L493) and every scroll before the first point on either surface — the wheel then holds nothing and the next point's keep-in-view scrolls the list to the lit row. The harness has no gap between `a`/`b`/`c` (`filmStops` clamps each window's end to the next start), so the only dead time it can reach is film 0 → 0.1 s: drive the cases at film zero, exactly where T18's click case already relies on "nothing is playing". Decision recorded here: the null hold holds NOTHING visible (no well pops open on a wheel — "holding is the absence of a scroll") rather than adopting the last reached point (`boardStop`), which would open a well on hold. Not fixed here, noted for the record: a wheel arriving during the effect's own 300 ms smooth `scrollTo` holds (the listeners ignore `followScrollRef`), whether Chromium lets that wheel interrupt the in-flight animation is browser behaviour, not ours; middle-click autoscroll and selection-drag autoscroll fire only `scroll` on a child target and so never hold, but neither is on a macOS trackpad path nor drivable from Playwright. Repo rules as T24; `widget-states` mark before commit (loading/empty/error unchanged — say so). The design doc's state and Enter tables must be updated as above, and the T23 row of the state table (rewritten by T24) must stay consistent with the null case. Run `tests/film-playback-refresh.spec.ts` alone first.

## T26 · A re-follow jump puts the playing row at the top of the box; continuous follow stays minimal

- **status:** done
- **model:** opus
- **needs:** T25
- **files:** (guess) src/components/dashboard/matches/match-detail/film/point-list.tsx (the keep-in-view effect L440–493 and its comment, the constants beside `FOLLOW_SCROLL_FALLBACK_MS` L150–155); tests/film-playback-refresh.spec.ts (new cases after T25's; the T19 press case L1737 and T23 press case L2280 keep their assertions); docs/superpowers/specs/2026-09-22-film-follow-hold-design.md (Enter table L109, motion table L179/L182, "Where the drawer pill sits" L145+)
- **done when:**
  - [ ] `point-list.tsx` exports (for the spec) `REFOLLOW_JUMP_INSET_PX = 8` and `REFOLLOW_JUMP_WINDOW_MS = 800`, with a comment saying 8px is the DS base spacing step (foundations.md's spacing scale) and clears the row's own `py-1.5` so the row reads as the first thing in the box; the keep-in-view effect keeps a `prevHeldRef` and, on a run where the previous `held` was true and the current is false, arms `jumpUntilRef.current = performance.now() + REFOLLOW_JUMP_WINDOW_MS`; the effect's deps stay `[activePointId, activeShotId, wellOpen, held]`, the transition is observed inside the effect, not signalled by a new prop — `film-tab.tsx`, `film-fullscreen.tsx`, `film-room-drawer.tsx` are not in the diff
  - [ ] While `performance.now() < jumpUntilRef.current` a run is a JUMP: the target is always the point row `[data-point-id="${activePointId}"]` (never the shot row, so the well sits below the row), `top = list.scrollTop + rowBox.top − listBox.top − REFOLLOW_JUMP_INSET_PX` clamped to `[0, list.scrollHeight − list.clientHeight]`, and it scrolls even when the row is already in view (skipping only when `|top − list.scrollTop| < 1`); otherwise the run is today's CONTINUOUS keep-in-view, unchanged (shot-or-row target, early return when in view, minimal delta). Both paths share the one `list.scrollTo({ top, behavior: reducedMotionNow() ? "auto" : "smooth" })` call, `followScrollRef` and the settle plumbing, so the T18/T19 reduced-motion spy cases keep passing; the comment above the effect explains the window: a step re-follows before its seek lands, so the crossing that follows inside the window is part of the same jump
  - [ ] `tests/film-playback-refresh.spec.ts` gains a `rowOffset(page, scroller, id)` helper returning `{ top: row.top − box.top, bottom: row.bottom − box.bottom }` and cases: (a) shell, `pad: "12"`, `boundShellList(page, 140)`, click `SHELL_ROW("a")`, park 0, `seekTo(page, REPORT, 0.3)` → `shellLitPlace` `below`, press `SHELL_PILL` → `expect.poll(() => rowOffset(…, "b").top)` settles within `[7, 9]` and the pill is gone; (b) shell, `boundShellList(page, 240)`, `shellHoldAThenPlayBAbove` (row above), press → `b`'s `top` within `[7, 9]`; (c) drawer, `holdAThenPlayB`, press `PILL` → `b`'s `top` in `DRAWER_SCROLLER` within `[7, 9]` and a `spyScrollTo` record has `behavior: "smooth"`; (d) drawer step: `holdAThenPlayB`, `page.keyboard.press("ArrowRight")` → `c` lights and `c`'s `top` settles within `[7, 9]` (the crossing landed inside the window); (e) continuous: shell `boundShellList(page, 140)` in follow, `seekTo(page, REPORT, 0.3)` then `seekTo(page, REPORT, 0.5)` → `c`'s `bottom` within `[−1, 1]` and `c`'s `top` > `REFOLLOW_JUMP_INSET_PX + 1` (minimal, not a jump); (f) clamp: shell with a `pad` chosen so `c`'s aligned `top` exceeds `scrollHeight − clientHeight` (`pad: "0"` at 140px is the guess — verify), hold `a` at park 0, seek to 0.5, press the pill → `scrollTop` equals `scrollHeight − clientHeight` (±1), row `c` is fully inside the box, and `c`'s `top` is NOT within `[7, 9]`. Cases (a)–(d) and (f) fail on the current tree; (e) passes before and after; every existing case, `npm run lint`, `npm run typecheck` pass
  - [ ] Design doc: the Enter table's pill row (L109) reads "`follow`. The list scrolls so the playing row sits at the TOP of the box (8px inset) with its well below it"; the step row (L110) and cut-change row (L113) note the same jump for a re-follow that has to travel; the motion table's "Re-follow" row (L182) says "jump: the playing row to the top of the box, 8px inset, clamped at the end of the list; smooth 300ms / instant under reduced motion" and the "Follow-mode keep-in-view" row (L179) adds "continuous crossings keep the minimal scroll — no lurch per point"; a one-paragraph "Jump vs. continuous" note under "Where the drawer pill sits" records the 800ms window rule and why (a step re-follows before its seek lands)
- **notes:** Author's words (2026-09-23): "if I click the pill at the bottom, should the point be bumped to the top of the scroll and not at the bottom of it at the end of the visible list?" — agreed: yes for a re-follow JUMP (pill, step/transport from held, clicking the playing row from held, re-follow after a cut change); ordinary continuous follow keeps today's minimal scroll. Why a time window and not a one-shot token: `step` in both surfaces calls `onFollow()` and then seeks, and the seek's landing (the `activePointId` change) arrives on a later tick — a token consumed by the first scroll would top-align the OLD point and leave the new one second; inside the window both runs jump and the second wins (two smooth scrolls ~100ms apart — acceptable, the end state is right). Clicking the playing row from follow is not a transition and scrolls nothing. Jump mode ignores the shot selector on purpose: after re-follow the well mounts under the playing row and the lit shot is inside it; aligning the shot would push the row off the top. Existing press cases (T19 L1737 "reaches the value that puts row b inside the box", T23 L2280 `inside` + `scrollTop < parked`) remain true under the jump — do not weaken them. `parkScroller`/`boundShellList` never hold. Both tones share the one effect, so one implementation covers the drawer and the shell. `widget-states` mark before commit (loading/empty/error unchanged — say so). The design doc's Enter and motion tables must be updated as above; the state table and T23 row are untouched by this task. Run `tests/film-playback-refresh.spec.ts` alone first.

## T27 · The room grows from the report frame: top-left origin, off-screen fade, entrance pinned in the harness

- **status:** done
- **model:** opus
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-motion.ts (`RoomKeyframe`, `collapsedRoomFrame`, `OPEN_ROOM_FRAME`, header doc); film-fullscreen.tsx (the entrance layout effect L1047–1088, the root `className` L1336, the "Motion" doc block L120–128); tests/film-motion.spec.ts; tests/fixtures/film-motion-box.ts (new — `screenBox` moved out of the unit spec so both specs share it); tests/film-playback-refresh.spec.ts (new `spyAnimate` / `openRoomSpied` helpers + three cases); docs/superpowers/specs/2026-09-21-video-fullscreen-h2-design.md (L31, one parenthetical)
- **done when:**
  - [ ] `RoomKeyframe` gains `transformOrigin: "0 0"` and both `collapsedRoomFrame()` and `OPEN_ROOM_FRAME` carry it; the room root's class list also gains `origin-top-left`. The module header states the precondition in one sentence — the default origin is `50% 50%`, under which `translate(x, y) scale(s)` puts the room's left edge at `rw/2 + x − s·rw/2`, not `x`, which is the offset that shipped. `tests/film-motion.spec.ts`: `screenBox` moves to `tests/fixtures/film-motion-box.ts` (exported, unchanged maths), a new case asserts `transformOrigin === "0 0"` on both keyframes, and a third geometry case maps a bottom-right frame (`{ left: 760, top: 400, width: 480, height: 270 }` in a 1280×720 room) back within 0.1px
  - [ ] `film-motion.ts` exports `roomMotionPath(frame: Rect | null, room: { width: number; height: number }): "frame" | "fade"` — `"fade"` for `null` or a frame whose intersection with `[0, room.width] × [0, room.height]` has no positive area, `"frame"` otherwise — with unit cases: null, wholly above (`top + height <= 0`), wholly right, partial overlap → `"frame"`, fully inside → `"frame"`. The entrance effect computes `path = reduced ? "fade" : roomMotionPath(p.originRect(), roomSize)` (the rect is never read under reduced motion): `"frame"` → today's collapsed→open animation on `ROOM_ENTER_MS` / `ROOM_EASE_ENTER`, `"fade"` → today's opacity 0→1 200ms; the `[data-film-chrome]` fade stays opacity-only, delayed only on the `"frame"` path. No keyframe in the file carries `width`, `height`, `top` or `left`; `ROOM_ENTER_MS`, `ROOM_EXIT_MS` and both easings are unchanged
  - [ ] `tests/film-playback-refresh.spec.ts` gains `spyAnimate(page)` — installed by `page.evaluate` after `open()` and BEFORE the maximize click, never `addInitScript` — which wraps `Element.prototype.animate` to push `{ room: el.getAttribute("aria-label") === "Film room", chrome: el.hasAttribute("data-film-chrome"), keyframes, options }` onto `window.__filmAnimations` and returns the real `Animation` (the room must still exit on `finished`); `readAnimations(page)` and `roomSize(page)` (the dialog root's `clientWidth`/`clientHeight`, not an assumed 1280×720); and `openRoomSpied(page, matchId, extra?)` = `open` → spy → record `page.locator(REPORT).boundingBox()` → click "Open the film room fullscreen" → wait `ROOM` at `readyState >= 1`, returning that box
  - [ ] Cases: (a) `openRoomSpied` → exactly one record with `room: true`, two keyframes, `keyframes[0].transformOrigin === "0 0"`, `options.duration === 460`, and `screenBox(keyframes[0], await roomSize(page))` equals the returned box within 1px on `left`/`top`/`width`/`height`; every `chrome: true` record has only `opacity` keys and `delay > 0`. (b) `page.emulateMedia({ reducedMotion: "reduce" })` before `openRoomSpied` → the `room: true` record's keyframes carry `opacity` and no `transform`/`clipPath`, `duration` 200, and chrome records have `delay` 0. (c) the ⇧-click door with the player off-screen: `open(page, …, { pad: "12" })`, spy, `window.scrollTo(0, playerBox.y + playerBox.height + 8)` (computed from the boxes), assert `REPORT`'s box has `y + height <= 0` AND `SHELL_ROW("c")` is inside the viewport, then `click({ modifiers: ["Shift"] })` on it → the `room: true` record is the opacity fade with no `transform`. Every existing case in the file, `tests/film-motion.spec.ts`, `npm run lint`, `npm run typecheck` pass
  - [ ] Docs: the "Motion" block in `film-fullscreen.tsx` (L120–128) records the top-left origin and the off-screen fade fallback; the H2 spec's L31 mention of `film-motion.ts` gets "(origin-top-left fix + off-screen fade, T27 2026-09-23)"
- **notes:** Author's words (2026-09-23): "Improve the animation when entering full screen mode because it doesn't come from where the video is, use /impeccable animate and /impeccable layout." Root cause by code reading: `collapsedRoomFrame` (film-motion.ts L39–56) and its unit spec both assume `transform-origin: 0 0`, but nothing in the film directory sets one (`film-clock.ts` L24 records the same trap on the progress bar), so the root scales about its centre and the collapsed frame lands ~`(rw − fw)/2, (rh − fh)/2` off the player. WAAPI keyframes accept `transformOrigin`; the class is the belt, the keyframe is what the unit test pins. The room is a body portal, not `requestFullscreen()` (film-fullscreen.tsx L107), so the root is freely transformable — do not reach for the Fullscreen API. Load `impeccable` (`reference/animate.md`: FLIP-style transforms for continuity, transform/opacity only, exit faster than entrance, every animation needs a reduced-motion path; `layout.md`: never animate layout-driving properties) — the durations already sit in its 300–500ms overlay band and on the DS `--ease-out-expo` (foundations.md "Animation & Motion"), so this task fixes geometry, not timing. Do not swap the room's own `<video>` for the shell's (header doc L114–118: re-parenting drops the buffer). Chrome fades in at `0.55 × ROOM_ENTER_MS` today (expo-out is ~93% there) — leave it unless, after the origin fix, it visibly scales in; if changed, name the constant in `film-motion.ts`. Case (c): if the page cannot be scrolled so that the player is off-screen while row `c` stays in view, say so in the run summary and pin the fade path through a `page.evaluate` seam on `originRect` instead — do not drop the case. Keep test ids `film-room-video`, `film-player-video`, `data-film-chrome`. `film-fullscreen.tsx` is under `src/components/dashboard` — run the widget-states mark before committing, in a separate call from `git commit`.

## T28 · Exit shrinks into the report frame only when it is on screen; the exit path pinned in the harness

- **status:** done
- **model:** opus
- **needs:** T27
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-fullscreen.tsx (`exit` L939–990, the "Motion" doc block); tests/film-playback-refresh.spec.ts (three cases after T27's, reusing `spyAnimate` / `openRoomSpied` / `roomSize`); tests/fixtures/film-motion-box.ts (read-only)
- **done when:**
  - [ ] `exit` computes `path = reduced ? "fade" : roomMotionPath(p.originRect(), roomSize)` (T27's predicate; the rect is not read under reduced motion): `"frame"` → today's open→collapsed animation on `ROOM_EXIT_MS` / `ROOM_EASE_EXIT` with `fill: "forwards"`, `"fade"` → today's 180ms opacity 1→0. The order is unchanged and stated in one comment: `pause` → `p.onHandoff(time)` → chrome 120ms fade-out → root animation → `p.onExit` on `finished`. No other line of `exit` changes; `film-tab.tsx` and `film-player.tsx` are not in the diff
  - [ ] Case (a), on-screen shrink: `openRoomSpied` → `seekTo(page, ROOM, 0.4)` → clear `window.__filmAnimations` → `page.keyboard.press("Escape")` → wait `ROOM` count 0 → the single `room: true` record has two keyframes, `keyframes[1].transformOrigin === "0 0"`, `duration` 320, `fill` `"forwards"`, and `screenBox(keyframes[1], room)` (room recorded before the exit) equals `REPORT`'s `boundingBox()` within 1px on all four sides; every `chrome: true` record (opacity 1→0, `duration` 120) has a lower array index than the root record; `state(page, REPORT).time` is within 0.1 of 0.4
  - [ ] Case (b), off-screen fade: T27's case (c) continued past Escape → the `room: true` record is opacity 1→0, `duration` 180, no `transform`/`clipPath`. Case (c), reduced motion: `page.emulateMedia({ reducedMotion: "reduce" })`, `openRoomSpied`, Escape → the same opacity-only record. Every existing case in the file, `tests/film-motion.spec.ts`, `npm run lint`, `npm run typecheck` pass
  - [ ] The "Motion" doc block in `film-fullscreen.tsx` says exit runs the entrance backwards in 320ms INTO the report frame when it is on screen, and fades when it is not (window resized under the room, or the ⇧-click door opened it from a scrolled-off player)
- **notes:** The exit already reads `p.originRect()` and hands the playhead back first (film-fullscreen.tsx L952 → film-tab.tsx L594–597), so the handoff order is pinned, not changed. The shrink target is the SAME rect T27 fixed the origin for — after T27 the exit inherits the correct origin through the shared keyframes; this task adds only the on-screen predicate and the observability. Impeccable `animate.md`: "exit faster than entrance" is already 320 vs 460 — keep. Widget-states mark before commit, separate call from `git commit`.
