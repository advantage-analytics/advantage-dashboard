# Tasks — claude/match-report-film-room-a05b7e

> Scope: the settled match report Statistics page (design canvas 04, frames F1–F8) — `MatchReport` compound frame, rail, title row, insight card and widget deltas, per docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md.

Run one with `/task-next`. To drain the file, loop a plain-text instruction —
**not** `/loop /task-next`, which a scheduled fire cannot invoke:

> `/loop Read .claude/skills/task-next/SKILL.md and follow it exactly — run one task from this branch's queue; do not add, edit, or reorder tasks; then stop.`

Append freely while it runs: the queue is re-read at the start of every
iteration, and the runner only ever rewrites a task's `status:` line.
Mark a task `next` to jump the queue.

Status values: `todo` (eligible to run), `next` (jump the queue), `doing` /
`done` / `blocked` (written by the runner around a dispatch), and `later`
(deferred — `/task-next`'s picker never selects it, so a loop drain skips
straight past it; promote a task to `todo` by hand once it's actually
ready).

## T1 · Pure report rules and their specs

- **status:** done
- **model:** sonnet
- **files:** src/components/dashboard/matches/match-detail/report-view.ts, src/components/dashboard/matches/match-detail/insight-text.ts, src/components/dashboard/matches/match-detail/report-scoreboard.ts, src/lib/data/match-stats-server.ts, tests/report-view.spec.ts, tests/insight-text.spec.ts, tests/report-scoreboard.spec.ts, tests/match-kpi-history.spec.ts (guess)
- **done when:**
  - [ ] `report-view.ts` exports `REPORT_VIEWS` = `[{value:"statistics",label:"Statistics"},{value:"shots",label:"Visualizations"},{value:"film",label:"Video"}]`, a `ReportView` type, `parseReportView()` (`"shots"`/`"film"` pass through; `null`, `""`, `"Shots"`, `"stats"` all read `"statistics"` — the `parseMatchTab` rule) and `reportViewQuery(params, view)` (deletes `tab` for `"statistics"`, sets it otherwise, carries every other param such as `set=2` through, never mutates the caller); `tests/report-view.spec.ts` covers each of those cases (spec › Architecture › `report-view.ts`; F1 rail labels)
  - [ ] `insight-text.ts` exports `splitInsight(summary) → { claim, evidence }`: the split is the first `[.!?]` followed by whitespace and a capital letter, so F2's text yields claim `"The serve, not the rally, decided the second set."` and the remaining two sentences as `evidence`; a one-sentence summary yields `evidence: null`; a decimal (`"1.5 shots"`) and a lowercase continuation (`"e.g. the"`) do not split; `tests/insight-text.spec.ts` asserts each (spec › `insight-text.ts`; F2)
  - [ ] `report-scoreboard.ts` exports a pure `setOutcome(set: ScoreLineSet) → "you" | "opp" | "level"` — the higher game count wins, equal games are `"level"`, a tiebreak set resolves by games alone; `tests/report-scoreboard.spec.ts` covers 6-4 → you, 3-6 → opp, 7-6(5) → you, 6-6 → level (spec › decisions 6; F8 draws 6-4 / 3-6 / 7-5)
  - [ ] `match-stats-server.ts` exports `hasComparisonBaseline(history: Pick<MatchKpiHistory, "baseline"> | null): boolean` = `Object.keys(history?.baseline ?? {}).length > 0`, and `tests/match-kpi-history.spec.ts` gains cases for `null` → false, one measured match (`buildKpiHistory` of a single row → empty baseline) → false, and two matches → true (spec › `match-stats-server.ts`)
  - [ ] `npm run typecheck` and `npx playwright test tests/report-view.spec.ts tests/insight-text.spec.ts tests/report-scoreboard.spec.ts tests/match-kpi-history.spec.ts tests/client-bundle-boundary.spec.ts` pass; no existing file other than `match-stats-server.ts` and the kpi-history spec changes
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md — read it first — › Architecture and › Sequence steps 0–1; frames F1, F2, F8 in docs/superpowers/specs/2026-09-15-match-report-settled-statistics/. Pure modules only: no React, no `"use client"`, no DOM; the three new files may `import type` from `@/lib/ui/score-format` and nothing from `*-server.ts`. `ScoreLineSet` carries no "unfinished" flag, so the spec's "level or unfinished sets keep both digits ink-900" resolves as equal games = `"level"` — say so in `setOutcome`'s doc comment. Baseline for T10: before editing, run `npm run lint` and `npm run build` once and state the warning count in your final report (spec › Sequence step 0). Tests use the Playwright `test`/`expect` import the sibling specs use; `tests/set-scope.spec.ts` is the model.

## T2 · MatchReport context, frame parts and `use()` providers

- **status:** done
- **model:** opus
- **needs:** T1
- **files:** src/components/dashboard/matches/match-detail/match-report-context.tsx, src/components/dashboard/matches/match-detail/match-report.tsx, src/components/dashboard/matches/match-data-provider.tsx (guess)
- **done when:**
  - [ ] `match-report-context.tsx` (`"use client"`) exports `MatchReportProvider` (props `matchId, summary, canCompare, isDerived, statsPublished, children`) and `useMatchReport()` returning `{ state: { view, insight }, actions, meta }` typed exactly as spec › `match-report-context.tsx`; `view` is `useSearchParams().get("tab")` through `parseReportView`; `insight` is `"dismissed"` when `useInsightDismissal(matchId).dismissed`, else `"collapsed"`/`"expanded"` from a `useState`; the value is `useMemo`d; `useMatchReport` reads with `use()` and throws when there is no provider
  - [ ] `actions.selectView(view)` is a no-op for the active view and otherwise calls `window.history.pushState(null, "", …)` with `reportViewQuery(…)` on `usePathname()` — the same push `match-tabs.tsx`'s `select` does today, one history entry per view; `collapseInsight`/`expandInsight` flip the local state and `dismissInsight` calls the hook's `dismiss` (key `advantage-ai-insight-dismissed:${matchId}` unchanged)
  - [ ] `match-report.tsx` (`"use client"`) exports a `MatchReport` namespace object with `Provider`, `Frame` (`flex min-h-0 flex-1 items-stretch`), `Rail` (`<aside aria-label="Match summary">`, `flex [flex:0_0_300px] min-h-0 flex-col overflow-y-auto border-r border-[var(--border-hairline)] bg-[var(--surface-card)]`), `Spacer` (`min-h-4 flex-1`), `RailFooter` (`p-3`, renders children), `Pane` (`flex min-h-0 min-w-0 flex-1 flex-col gap-4 overflow-y-auto` with inline `padding: "20px 56px 24px"` and `background: "var(--surface-page)"` plus a comment naming the DS principle 6 conflict — the one place that token appears) and `When` (`view` prop; renders children only when `state.view === view`, as `role="tabpanel"` `flex min-h-0 flex-1 flex-col gap-4`) (spec › Architecture › `match-report.tsx`; F1: `flex:0 0 300px`, `padding:20px 56px 24px`)
  - [ ] `match-data-provider.tsx` renders `<MatchDataContext value={…}>` (React 19 — no `.Provider`) and `useMatchData` reads with `use()`; `git diff` on that file touches nothing else (types, defaults and the thrown message stay)
  - [ ] `npm run typecheck`, `npm run lint` and `npx playwright test tests/client-bundle-boundary.spec.ts` pass with the old shell still rendering the page (nothing imports the new files yet)
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Architecture (the `MatchReport` compound component) and › Docs checked › React 19; frame F1. Copy the shape of `src/components/dashboard/matches/new-match-wizard/UploadWizardProvider.tsx` (`createContext` + `use()`, `{state, actions, meta}`). The rail/pane scroll only works inside `layout.tsx`'s `h-[calc(100vh-var(--header-h))] overflow-hidden` box — keep `min-h-0` on every flex link (`Frame` → `Rail`/`Pane` → `When`). No `useEffect`-to-set-state, no `forwardRef`, no `title` attributes, tokens not hex; only `import type` from `match-stats-server.ts`/`match-detail-server.ts`. T3 adds `Scoreboard`/`ViewSwitcher`, T4 the title-row parts and T5 `Insight` to the same namespace object — leave an obvious place for them. `page.tsx` is a Server Component that will dot-access this namespace (`<MatchReport.Frame>`); if the client-reference proxy refuses that at T6's `npm run build`, also export each part by name. Run the `widget-states` skill before handing back — the commit hook `.claude/hooks/widget-states-gate.sh` blocks the runner's commit on dashboard `.tsx` changes otherwise.

## T3 · Rail scoreboard, view switcher and composable Share

- **status:** done
- **model:** opus
- **needs:** T2
- **files:** src/components/dashboard/matches/match-detail/report-scoreboard.tsx, src/components/dashboard/matches/match-detail/report-view-switcher.tsx, src/components/dashboard/matches/match-detail/share-match-button.tsx, src/components/dashboard/matches/match-detail/match-report.tsx, src/components/dashboard/score-line.tsx (guess)
- **done when:**
  - [ ] `report-scoreboard.tsx` exports the F8 card, attached as `MatchReport.Scoreboard`: outer `p-3`; card `rounded-[var(--radius-card)] border border-[var(--border-hairline)]` with `padding: 15px 13px` and column `gap-[14px]`; header `flex items-baseline gap-2` = status `.text-micro` (sentence-cased `formatScoreboardStatus(match.matchContext)`: "Final" / "Unfinished" / "Retired" / "Withdrew" / "Defaulted") · spacer · duration `mono tabular text-[10px] text-[var(--ink-400)]` from `formatClock(match.durationSec, { alwaysShowHours: true })`, omitted unless `durationSec > 0` (spec › Geometry › Rail scoreboard; F8: `padding:15px 13px`, `gap:14px`, "Final 1:26:00")
  - [ ] The two score rows sit in a `gap-[11px]` column, each `flex items-center gap-[7px]`: viewer row = `sides.you.name` `text-[13px] font-medium whitespace-nowrap text-[var(--ink-900)]` + a 6px `rounded-[var(--radius-pill)] bg-[var(--blue)]` dot; opponent row = `sides.opp.name` `text-[13px] text-[var(--ink-600)]`; the right side is `mono tabular text-[13px]` with one `w-[11px]` right-aligned slot per set and `gap-2` between slots; per set the winner's digit is ink-900 and the loser's ink-400 by T1's `setOutcome(set)` (level → both ink-900); the digit from `tiebreakOf(set)` renders as the `ScoreLine` superscript (0.6em, `vertical-align: 1.05em`, 0.5px — export `TIEBREAK_STYLE` from `score-line.tsx` rather than copying it) inside that set's slot on the row of the side that lost the set; sets are `useMatchSides().sets`, never `match.score` (F8: `width:11px`, `gap:8px`, `gap:11px`)
  - [ ] `report-view-switcher.tsx` exports `MatchReport.ViewSwitcher`: container `flex flex-col gap-0.5 px-3 pt-0.5` (F1 `padding:2px 12px 0`), `role="tablist" aria-orientation="vertical"`; one row per `REPORT_VIEWS` entry, `role="tab" aria-selected`, `flex h-10 cursor-pointer items-center rounded-[var(--radius-element)] text-[13px]`, a `size-10` leading cell holding a 16px lucide icon at stroke 1.5 (`Table2` · `ScatterChart` · `Video`), label `flex-1 truncate`; active `bg-[var(--surface-subtle)] font-medium text-[var(--ink-900)]`, inactive `text-[var(--nav-fg)] hover:bg-[var(--surface-subtle)] hover:text-[var(--ink-900)]` with a 200ms `var(--ease-primary)` colour transition; click calls `actions.selectView`; no focus classes written (F1: `height:40px` rows)
  - [ ] `share-match-button.tsx`: `ShareMatchButton` no longer takes `match` (reads it from `useMatchData()`), takes `children` as the trigger rendered through `PopoverTrigger asChild` plus `side`/`align` props; a `ShareRailTrigger` in the same file draws the F1 footer button — `cn(advButton("primary", "md"), "w-full gap-[7px]")` from `src/lib/ui/adv-button.ts`, `Share2` 15px stroke 1.7, label "Share", `aria-keyshortcuts` — and `isMac` is a lazy `useState` initialiser guarded on `typeof navigator` (the `useEffect`+`setIsMac` pair is gone); `SharePopoverPanel` and the ⌘⇧L / Ctrl⇧L `keydown` listener are unchanged; `grep -rn "ShareMatchButton\|share-match-button" src` still shows no importer beyond the comment in `claim/referral-link.tsx`
  - [ ] `npm run typecheck`, `npm run lint` and `node scripts/check-design-drift.mjs` (every check still at its seed) pass; the old page still renders
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Geometry › Rail scoreboard, Rail view switcher, Rail footer Share; frames F8 and F1. Traps: `formatScoreboardStatus` returns UPPERCASE — sentence-case it here, do not change `match-utils.ts`; `.text-micro` is an unlayered DS class that pins colour, so any override is an inline `style={{ color: … }}` (memory `ds-class-layering-trap`); you/opp only through `useMatchSides()` (guardrails §4 — see `use-match-sides.ts`'s header); lucide 0.562 exports `ScatterChart` as an alias of `ChartScatter` — either name typechecks. `SharePopoverPanel` keeps its existing in-palette hex; only the new trigger must be tokens, and it starts from `advButton()` (memory: never hand-roll a near-miss primary). No `title` attributes, no `forwardRef` (React 19 `ref` prop if `asChild` needs one), no `useEffect`-to-set-state. Run the `widget-states` skill before handing back (commit hook).

## T4 · Title row, facts, Compare and the ⋯ menu

- **status:** done
- **model:** opus
- **needs:** T2
- **files:** src/components/dashboard/matches/match-detail/report-title-row.tsx, src/components/dashboard/matches/match-detail/report-facts.tsx, src/components/dashboard/matches/match-detail/report-compare-button.tsx, src/components/dashboard/matches/match-detail/report-more-menu.tsx, src/components/dashboard/matches/match-detail/match-report.tsx, src/components/ui/float-menu.tsx, src/components/dashboard/matches/match-actions/match-actions-menu.tsx (guess)
- **done when:**
  - [ ] `report-title-row.tsx` exports `TitleRow` (`flex items-end gap-2.5`), `Title` (`.text-display whitespace-nowrap`, text = the active view's `REPORT_VIEWS` label: "Statistics" / "Visualizations" / "Video") and `TitleActions` (a `flex-1` spacer then a `flex items-center gap-2` cluster), all attached to `MatchReport` (spec › Geometry › Title row; F1/F4/F5/F6: `font-size:30px;font-weight:300;letter-spacing:-0.6px;line-height:1.2`)
  - [ ] `report-facts.tsx` exports `MatchReport.Facts`: `mt-[9px] flex h-[18px] flex-nowrap items-center gap-3.5`, each fact `inline-flex items-center gap-1.5` = a 13px icon at stroke 1.5 in ink-700 + a `.text-micro` span with inline `style={{ color: "var(--ink-700)" }}`; in this order, each gated: `Swords` "{points.length} points · {games} games" `tabular` only when `points.length > 0` (games = Σ `set.player1 + set.player2` over `match.score.sets`) · `Calendar` `shortMonthDate(match.date)` `tabular` · `/icons/tournament-icon.svg` + `match.tournamentName` when set · `/icons/tennis-court-icon.svg` + `match.courtType` when set · `CircleCheck` "Verified result" when `match.verificationStatus`; no "Away"/`MapPin` and no `GraduationCap` (spec › decisions 7)
  - [ ] `report-compare-button.tsx` exports `MatchReport.CompareButton`: returns `null` unless `meta.canCompare`; otherwise `inline-flex h-8 items-center gap-[7px] rounded-[var(--radius-button)] px-3 text-[12px] font-medium text-[var(--ink-700)] hover:bg-[var(--surface-subtle)] hover:text-[var(--ink-900)]` with `SlidersHorizontal` 14px ink-500 and label "Compare", `aria-disabled="true"` at full opacity, wrapped in `ChromeTooltip label="Comparing matches isn't available yet"`, click prevented (spec › Compare; F5 absent, F6 present, `height:32px`)
  - [ ] `report-more-menu.tsx` exports `MatchReport.MoreMenu`: trigger `size-8 rounded-[var(--radius-element)] text-[var(--nav-fg)] hover:bg-[var(--surface-subtle)] hover:text-[var(--ink-900)]` with `MoreHorizontal` 15px stroke 1.5, `aria-label="More"`, `ChromeTooltip hidden={open}`; `FloatMenu width={212} align="end"`; rows in order, each a `FloatMenuItem` with an `icon` (so it renders `role="menuitem"`) styled `MENU_ROW_ICON`: "Export report" / "PDF with the charts" (`disabled`), "Re-run analysis" / "Uses your latest video" (`disabled`), "Review score" → `EditMatchDialog({ matchId, open, onOpenChange })`, `FloatMenuDivider`, "Delete match" with `className={DESTRUCTIVE_ROW}` and icon `cn(MENU_ROW_ICON, DESTRUCTIVE_ICON)` → `DeleteMatchDialog({ matchId, matchLabel: `${sides.you.name} vs ${sides.opp.name}`, open, onOpenChange })`; then `FloatMenuNote` "Export and re-run analysis aren't available yet."; `match-actions-menu.tsx` now `export`s `MENU_ROW_ICON`, `DESTRUCTIVE_ROW`, `DESTRUCTIVE_ICON` with their values unchanged (spec › More menu; F6: `width:212px`, row copy)
  - [ ] `float-menu.tsx`'s `FloatMenuItem` gains `disabled?: boolean` → `aria-disabled`, `opacity-45`, no hover wash, `onSelect` not called; `npx playwright test tests/float-menu-selected-option.spec.ts` still passes (it asserts the source strings `role={icon ? "menuitem" : "menuitemradio"}`, `aria-checked={icon ? undefined : chosen}`, `!chosen && "hover:bg-[var(--surface-subtle)]"`, `{icon ? (` and `export function ChosenCheck` verbatim); `npm run typecheck`, `npm run lint` and `node scripts/check-design-drift.mjs` pass
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Geometry › Title row, Compare, More menu and › decisions 2, 3, 7; frames F1, F4, F5, F6. Traps: `.text-display`/`.text-micro` are unlayered and pin colour — override inline only (memory `ds-class-layering-trap`); names through `useMatchSides()` only (guardrails §4); the destructive row rests grey and turns `--danger` on intent (the DS wins over F6's resting red — the spec says so); F6 draws no row glyphs, but a `FloatMenuItem` without `icon` becomes a `menuitemradio` with a check slot, so give every row a 13px glyph (`Pencil`/`Trash2` as `MatchActionsMenu` uses; something like `FileDown`/`RefreshCw` for the two disabled rows); precedent for the inert `aria-disabled` control is `InertGlyph` in `film/film-player.tsx`. `MoreHorizontal` is a lucide alias of `Ellipsis` in 0.562 — either typechecks. No `title` attributes (`forbid-dom-title-attribute`), no `forwardRef`, tokens not hex. Run the `widget-states` skill before handing back (commit hook).

## T5 · Insight card variants

- **status:** done
- **model:** opus
- **needs:** T2
- **files:** src/components/dashboard/matches/match-detail/report-insight-card.tsx, src/components/dashboard/matches/match-detail/match-report.tsx, scripts/check-design-drift.mjs (guess)
- **done when:**
  - [ ] `report-insight-card.tsx` exports `MatchReport.Insight`, which returns `null` when `meta.summary` is null or `state.insight === "dismissed"` and otherwise renders one of two explicit components, `InsightExpanded` or `InsightCollapsed` — no `collapsed` boolean prop on a single component; both read `useMatchReport()` and split the summary with T1's `splitInsight` (spec › Architecture › `Insight`; F2/F3/F4)
  - [ ] `InsightExpanded` is `flex flex-col gap-2 rounded-[var(--radius-card)] border border-[var(--border-hairline)] bg-[var(--surface-card)] p-[16px_18px_12px] shadow-[var(--shadow-card)]`; claim at 15px / weight 300 / line-height 1.35 / ink-900 `max-w-[62ch] [text-wrap:pretty]`; evidence `text-[12px] leading-[1.65] text-[var(--ink-600)] max-w-[86ch] [text-wrap:pretty]` rendered only when `splitInsight` returns one, ending with the inline "Why this" link (`text-[11px] font-medium whitespace-nowrap text-[var(--blue)]`, `ArrowUpRight` 12px `ml-[3px]`, `href="/dashboard/ask"`; it trails the claim instead when there is no evidence); nothing invented and no figure emphasised (F2: `padding:16px 18px 12px`, `font-size:15px;font-weight:300;line-height:1.35`, `max-width:62ch`/`86ch`)
  - [ ] Its footer is `flex items-center gap-[7px] border-t border-[var(--border-hairline)] pt-2`: a 16px `rounded-[3px] bg-[var(--ink-900)]` square holding `/logos/logo3.svg` at 9×6 with `brightness-0 invert` · "Advantage Intelligence" as `.text-micro` (ink-500) · spacer · "Dismiss" `text-[11px] font-medium text-[var(--ink-500)] hover:text-[var(--ink-900)]` calling `actions.dismissInsight` · "Collapse" `size-[22px] rounded-[var(--radius-element)] text-[var(--ink-400)] hover:bg-[var(--surface-subtle)] hover:text-[var(--ink-700)]` with `ChevronUp` 14px, `aria-label="Collapse"` + `ChromeTooltip`, calling `actions.collapseInsight`; both exits always visible, no hover reveal (F2: `width:16px;height:16px`, 22px button; the spec records the 16px/9×6 chip over `EngineChip`'s 20px/12×8)
  - [ ] `InsightCollapsed` has the same surface classes with `flex h-11 items-center gap-2.5 px-[18px]`: the logo chip · claim `min-w-0 flex-1 truncate text-[13px] text-[var(--ink-700)]` · "Show" `text-[11px] font-medium text-[var(--blue)] hover:text-[var(--blue-hover)]` calling `actions.expandInsight` · a `mx-0.5 h-3.5 w-px bg-[var(--border-hairline)]` divider · "Dismiss" as above; a comment on the component records that `useInsightDismissal`'s server snapshot reads "dismissed", so the card mounts after hydration (F3: `height:44px`)
  - [ ] `npm run typecheck`, `npm run lint` and `node scripts/check-design-drift.mjs` pass — 15px is not in the checker's `TYPE_SCALE`, so if the claim uses a `text-[15px]` utility the `size` seed in `scripts/check-design-drift.mjs` moves from 4 to 5 with a comment naming F2 as the recorded frame-over-DS conflict (not dodged with an inline `fontSize`)
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Geometry › Insight card, Collapsed, Dismissed and › decisions 4, 5; frames F2, F3, F4. `splitInsight` is T1's `insight-text.ts`; `useInsightDismissal` stays in `insight-dismissal.ts` (key unchanged) and is called only inside T2's provider — this file reads `state.insight` and calls `actions`. The retired `rail-insight-card.tsx` is the copy precedent for the logo chip (`/logos/logo3.svg`, `brightness-0 invert`) and the `/dashboard/ask` destination. `.text-micro` pins ink-500 and is unlayered — leave it for the credit line, override inline only where the spec names another ink. No `useEffect`-to-set-state, no `title` attributes, no `forwardRef`, tokens not hex. Run the `widget-states` skill before handing back (commit hook).

## T6 · Cut page.tsx over to MatchReport and retire the old shell

- **status:** done
- **model:** fable
- **needs:** T3, T4, T5
- **files:** src/app/dashboard/matches/(detail)/[matchId]/page.tsx, src/app/dashboard/matches/(detail)/[matchId]/layout.tsx, src/components/dashboard/matches/match-detail/statistics-view.tsx, src/components/dashboard/matches/match-detail/set-scope.tsx, src/lib/data/match-detail-server.ts, src/lib/data/single-match-server.ts, src/components/dashboard/matches/match-detail/match-detail-shell.tsx, src/components/dashboard/matches/match-detail/match-tabs.tsx, src/components/dashboard/matches/match-detail/match-rail.tsx, src/components/dashboard/matches/match-detail/rail-insight-card.tsx, src/components/dashboard/matches/match-detail/statistics-tab.tsx, src/components/dashboard/matches/match-detail/match-kpi-strip.tsx (guess)
- **done when:**
  - [ ] `statistics-view.tsx` exports `StatisticsView` (no props; reads `meta` from `useMatchReport()`): `{!statsPublished && <UnpublishedStatsNotice />}`, `<MatchReport.Insight />`, a widgets row `flex items-start gap-4` = `<HeadToHeadCard />` in a `flex-1` slot + a `flex w-[416px] shrink-0 flex-col gap-4` column of `PerformanceTrackerChart`, `RallyLengthCard`, `PointEndingsCard isDerived={meta.isDerived}` — the column becomes `flex-1` with no fixed width when `!statsPublished`, since the head-to-head returns null then — and `{meta.isDerived && meta.statsPublished && <MatchDataBlock />}` after the row (spec › Architecture › `statistics-view.tsx`; F1: 436px + 416px in the 868px pane)
  - [ ] `page.tsx` renders the two variants exactly as spec › Architecture › `page.tsx`: the awaiting-analysis branch (gate `isInFlight || isAnalysisFailed` on the `withStatsPublished` status, computed as today) = `MatchReport.Provider` with `summary={null} canCompare={false} statsPublished={false}` → `Frame` → `Rail` (`Scoreboard`, `Spacer`, `RailFooter` holding `<ShareMatchButton side="top" align="start"><ShareRailTrigger /></ShareMatchButton>`) + `Pane` holding `MatchAnalysisProgress` and no title row; the ready branch = `MarkReportSeen` + `Provider` with `summary` (still `sides.pick`ed), `canCompare={hasComparisonBaseline(kpiHistory)}`, `isDerived`, `statsPublished` → `Rail` (`Scoreboard`, `ViewSwitcher`, `Spacer`, `RailFooter`/Share) + `Pane` (`TitleRow` › a `min-w-0` div with `Title`+`Facts`, `TitleActions` › `CompareButton`+`MoreMenu`; then `When view="statistics"` `StatisticsView`, `When view="shots"` `ShotsTab`, `When view="film"` `FilmTab video={video}`); the `dynamic()` imports, the RLS-gated `reconcileBeforePageRead` block and `getMatchVideo` stay; the `film` note plumbing and `SetScopeChips` are gone (guardrails §3.3)
  - [ ] These six files are deleted in this same commit and `grep -rn "match-detail-shell\|match-detail/match-tabs\|match-detail/match-rail\"\|rail-insight-card\|statistics-tab\|match-kpi-strip" src tests` returns nothing: `match-detail-shell.tsx`, `match-tabs.tsx`, `match-rail.tsx`, `rail-insight-card.tsx`, `statistics-tab.tsx`, `match-kpi-strip.tsx`; `shared/kpi-tile.tsx`, `legend-swatch.tsx`, `chart-tooltip.tsx`, `insight-dismissal.ts`, `format-clock.ts`, `unpublished-stats-notice.tsx` and `mark-report-seen.tsx` are untouched; `layout.tsx` changes only its comment (the `match-detail-shell.tsx` reference → `MatchReport.Frame`)
  - [ ] `set-scope.tsx` drops `SetScopeChips`, the `useRouter`/`usePathname` imports and `SetScope.select`, and still exports `SET_PARAM`, `selectableSets`, `parseSetParam`, `setScopeQuery`, `scopePoints`, `scopeMeta`, `useSetScope` (returning `{ activeSet, selectable }`) so `tests/set-scope.spec.ts` and `head-to-head-card.tsx`'s read are unchanged; `match-detail-server.ts` returns `insights: dbRow.insights ?? null` with the `FILLER_INSIGHTS` constant deleted (`FILLER_KEY_MOMENTS` stays) and `single-match-server.ts`'s comment about that substitution is corrected (spec › decisions 4)
  - [ ] `npm run typecheck`, `npm run lint`, `npm run test` (incl. `set-scope`, `match-h2h-rows`, `match-kpi-history`, `client-bundle-boundary`, `design-drift`, `generate-map`) and `npm run build` pass — the build is the check that no `Suspense` boundary is needed around `useSearchParams` and that the Server Component can dot-access `MatchReport.*`; `npm run map` produces no diff
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Architecture › `statistics-view.tsx` and `page.tsx`, › Files, › decisions 4, 8, 9; frames F1 and F4. Do not touch the short-circuit gate's computation, `reconcileBeforePageRead`'s RLS existence check, or anything under `src/lib/services/splitstep/**`; `hasComparisonBaseline` is called in `page.tsx` only, and new client files only `import type` from the `*-server.ts` modules (`tests/client-bundle-boundary.spec.ts`). `getMatchSides` stays the single attribution point in `page.tsx` (guardrails §4). The pane background/token decision lives in T2's `Pane`, not here. If T2's namespace object cannot be dot-accessed from `page.tsx` at build, import the parts by their named exports rather than restructuring. `KpiTile`/`KpiTileStrip` keep their other importers (Home, season strip) — grep before deleting `match-kpi-strip.tsx`; `tests/match-h2h-rows.spec.ts` and `tests/match-kpi-history.spec.ts` import from files this task must not touch. The container query for the widgets row is T10's, not this task's. `rm -rf .next/dev` before `npm run build` if a dev server ran. Run the `widget-states` skill before handing back (commit hook).

## T7 · Head-to-head card deltas

- **status:** done
- **model:** sonnet
- **needs:** T6
- **files:** src/components/dashboard/matches/match-detail/head-to-head-card.tsx (guess)
- **done when:**
  - [ ] Card padding is `18px 20px 14px` (was `18px 24px`); the header row has `pb-[14px]` and its caption (`text-micro tabular`, inline ink-400) reads `"{meta.label} · {meta.points} points"` — the "· N games" segment is gone; the column-header row has `pb-[11px]` (spec › Head-to-head card deltas; F7: `padding:18px 20px 14px`)
  - [ ] `COLUMN` is `w-[64px]` (was 104) on both value columns and both name cells; the viewer's name cell is `text-[12px] font-medium text-[var(--ink-900)]` with the 11px `Check` in `--viz-good` at stroke 2 only when `match.verificationStatus`, the opponent's `text-[var(--ink-600)]`; section titles are `.eyebrow-sm` (ink-400) in a `pt-3 pb-0.5` row (F7: `width:64px` ×8)
  - [ ] Rows are `min-h-[30px] -mx-2 px-2 rounded-[var(--radius-element)] hover:bg-[var(--surface-muted)]` with a 200ms `var(--ease-primary)` transition; values stay `text-[13px] tabular` right-aligned in the 64px column, weight 500/ink-900 for the `rowLeader` side and 400/ink-500 otherwise (F7: `min-height:30px`)
  - [ ] The hover readout is `ChartTooltip align="center" bottomOffset={-4}` with `px-2.5 py-2 gap-0.5`: label `text-[12px] font-medium text-white`, detail `mono tabular text-[10px] text-white/[0.64]` reading `"{you} {detail} · {opp} {detail}"` (F7: `calc(100% - 4px)`)
  - [ ] Every export `tests/match-h2h-rows.spec.ts` imports (`H2H_GROUPS`, `POINT_ROWS`, `RETURN_ROWS`, `SERVE_ROWS`, `buildStatRows`, `rowLeader`, `H2HRow`, `H2HRowConfig`, `H2HStats`) is unchanged in name and behaviour; `useSetScope().activeSet` is still read; `npm run typecheck`, `npm run lint`, `npx playwright test tests/match-h2h-rows.spec.ts` and `node scripts/check-design-drift.mjs` pass
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Geometry › Head-to-head card deltas; frames F7 and F1. Rendering-only deltas — the pure rules above the `/* ── Rendering` marker in the file do not change. `.text-micro`/`.eyebrow-sm` are unlayered and pin colour: overrides are inline `style={{ color: "var(--ink-400)" }}`, as the file already does. You/opp only via `useMatchSides()` (guardrails §4) — the column order is `sides.you` first, never player order. `ChartTooltip`'s `bottomOffset` is a number inside `calc(100% + ${n}px)`, so `-4` is the frame's `calc(100% - 4px)`. Tokens not hex, no `title` attributes. Run the `widget-states` skill before handing back (commit hook).

## T8 · Performance tracker and rally length deltas

- **status:** done
- **model:** sonnet
- **needs:** T6
- **files:** src/components/dashboard/matches/match-detail/performance-tracker-chart.tsx, src/components/dashboard/matches/match-detail/rally-length-card.tsx (guess)
- **done when:**
  - [ ] `performance-tracker-chart.tsx`: the card column is `gap-2.5`; the inert "Expand" button and its `Tooltip` are removed (the header is eyebrow + spacer only); the "{you} above" label is `pointer-events-none absolute left-0 top-0 z-[2] bg-[var(--surface-card)] pr-1.5 text-[10px] whitespace-nowrap` — a plain background, not the `surface-card` class with its border and shadow (spec › Performance tracker deltas; F1)
  - [ ] The tracker's hover readout is `px-[11px] py-[9px] gap-[3px]` with three lines: `text-[12px] font-medium text-white` · `text-[11px] text-white/[0.64]` · `mono tabular text-[10px] text-white/[0.64] pt-px` (was `px-3 py-2 gap-0.5`, `/[0.72]` and `/[0.52]`); the `DARK_READOUT_*` surface and the left/top/bottom parking logic stay
  - [ ] `rally-length-card.tsx`: card padding `16px 20px 14px` and column `gap-3` (12px); the mosaic keeps `min-h-24`, its bands keep a 2px separation (`gap-0.5`, or the existing 2px `borderRight` seam if that is what draws it) and `--radius-cell` on the outer corners; the labels row stays 11px ink-700 + `mono tabular text-[10px]` ink-400; the legend keeps the 8px `LegendSwatch`es and "Width is how often" is inline ink-400 (was ink-500) (spec › Rally length deltas; F1: `padding:16px 20px 14px`)
  - [ ] The band readout is `px-[11px] py-[9px] gap-[3px]`: title `text-[12px] font-medium text-white` · `"{count} points · {share}% of the match"` `text-[11px] text-white/[0.64]` · `"{you} {pct}%"` `text-[11px] text-white pt-0.5` · `"{opp} {pct}%"` `text-[11px] text-white/[0.78]` — the raw won-counts are gone from the two player lines
  - [ ] `npm run typecheck`, `npm run lint`, `node scripts/check-design-drift.mjs` and `npx playwright test tests/set-scope.spec.ts` pass; the `useSetScope().activeSet`/`scopePoints` reads and the `useMatchSides()` orientation are unchanged in both files
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Geometry › Performance tracker deltas, Rally length deltas; frame F1 (the right-hand 416px column). The tracker's readout is hand-positioned, not `ChartTooltip` — keep its `left`/`top`/`bottom`/`transform` parking and change only padding, gap and the three lines. Removing "Expand" also removes its `Tooltip`/`TooltipTrigger`/`TooltipContent` imports if nothing else uses them; `docs/match-detail-v46-flags.md` #11 already calls it inert. `.text-micro` is unlayered — colour overrides inline only. You/opp via `useMatchSides()` (guardrails §4); tokens not hex; no `title` attributes; no `forwardRef`. Run the `widget-states` skill before handing back (commit hook).

## T9 · How-points-ended deltas and MatchDataBlock as a pane card

- **status:** done
- **model:** sonnet
- **needs:** T6
- **files:** src/components/dashboard/matches/match-detail/point-endings-card.tsx, src/components/dashboard/matches/match-detail/match-data-block.tsx (guess)
- **done when:**
  - [ ] `point-endings-card.tsx`: card padding `16px 20px 14px` and column `gap-3` (12px); each per-player block is `gap-1.5` (was `gap-[5px]`) with name `text-[11px] text-[var(--ink-600)]` + total `mono tabular text-[10px] text-[var(--ink-400)]`; bars stay `h-2.5 gap-0.5`; the "Own outcomes" caption is `.text-micro` with inline ink-400 (spec › How points ended deltas; F1: `padding:16px 20px 14px`)
  - [ ] The legend is `flex flex-wrap gap-x-3.5 gap-y-1.5 pt-0.5` (was `gap-x-4`) with 6px swatches and `.text-micro` labels forced to ink-400 inline; the segment readout stays `ChartTooltip` `px-2.5 py-2 gap-0.5` with `text-[12px] font-medium text-white` + `text-[11px] text-white/[0.64]`
  - [ ] `match-data-block.tsx`'s `<section aria-label="Match data">` is a `surface-card` block (`className="surface-card flex flex-col gap-2.5"`) with the `border-t border-[var(--border-hairline)] pt-5` rail styling removed; its eyebrow, "Coming soon" pill, the two caveat lines, the disabled "Review flags" button and the caption are unchanged; it spans the pane's full width under the widgets row where `statistics-view.tsx` already mounts it (spec › `MatchDataBlock`; decisions 8)
  - [ ] `npm run typecheck`, `npm run lint` and `node scripts/check-design-drift.mjs` pass; `isDerived` still drops the aces outcome and `useMatchSides()` still orders the two rows
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Geometry › How points ended deltas, `MatchDataBlock`; frame F1 (bottom of the 416px column). `MatchDataBlock` is not drawn in F1–F8 — it is a real derived-match state that stays on the Statistics view (decision 8); only its frame changes, never its copy. `.text-micro` is unlayered and pins ink-500 — every ink-400 here is an inline `style`. You/opp via `useMatchSides()` (guardrails §4); tokens not hex; no `title` attributes. Run the `widget-states` skill before handing back (commit hook).

## T10 · Container-query stacking, visual pass, guardrails review, full gate

- **status:** todo
- **model:** opus
- **needs:** T7, T8, T9
- **files:** src/components/dashboard/matches/match-detail/match-report.tsx, src/components/dashboard/matches/match-detail/statistics-view.tsx (guess)
- **done when:**
  - [ ] `MatchReport.Pane` carries Tailwind v4's `@container` and the widgets row in `statistics-view.tsx` is `flex-col @min-[720px]:flex-row` with the right column `w-full @min-[720px]:w-[416px]` (T6's `flex-1` case for `!statsPublished` preserved) — no raw `@container` block in `globals.css`; below 720px of pane the head-to-head sits above the three charts, at 868px they sit side by side (spec › `statistics-view.tsx`, › Docs checked › Tailwind v4)
  - [ ] A visual pass at 1512×950 against F1 is recorded in the final report as a table of computed values (`getComputedStyle`, not class inspection): rail width 300px; pane padding 20/56/24; title 30px/300/36px; switcher rows 40px; h2h value columns 64px and rows ≥30px; insight card padding 16/18/12; and the computed `color` on every `.text-micro` the spec forces to ink-700 (facts) or ink-400 (h2h caption, legends, "Own outcomes", "Width is how often") — each line names the match id it was measured on
  - [ ] The state walk is recorded with a pass/fail per step: insight expanded → Collapse → Show → Dismiss → reload stays dismissed; `?tab=shots` titles "Visualizations" and `?tab=film` "Video" with the matching rail row active; browser Back restores the previous view; ⋯ opens with the two disabled rows, the note, "Review score" → `EditMatchDialog`, "Delete match" → `DeleteMatchDialog`; Compare absent on a match with no other analysed match and present (inert, tooltip) on one with history; a derived match (`0db449ab…`) shows `MatchDataBlock` and the Video row; an analysing match still short-circuits to scoreboard + Share + `MatchAnalysisProgress` (spec › Verification)
  - [ ] The `pipeline-guardrails-reviewer` agent has reviewed the branch diff against `docs/ui-revamp-guardrails.md` and its findings are either none or fixed in this task; every you/opp decision in the new files goes through `useMatchSides()`/`getMatchSides()` (grep shows no `isUserPlayer1`/`player1Stats` reads in the `report-*`, `statistics-view` and `match-report*` files)
  - [ ] Full gate green after killing the dev server and `rm -rf .next/dev`: `npm run typecheck`, `npm run lint` (warning count not above the baseline T1 recorded), `npm run test`, `npm run build`, `npm run map` (no diff), `npm run format` then `npm run format:check`; `node scripts/check-design-drift.mjs` reports every check at its seed
- **notes:** Spec: docs/superpowers/specs/2026-09-15-match-report-settled-statistics-design.md › Verification and › Sequence steps 5–7; all frames, F1 is the reference at 1512×950. Method: `npx next dev -p <free port>` in the background from this worktree (the root `launch.json` is the main checkout's), then the in-app Browser pane, which is signed in for `localhost:<any port>` (memory `designsync-cap-and-present-view`); force the team workspace with `document.cookie='advantage_workspace=<program id>; path=/'`; matches per the spec's Verification list (`0db449ab…` derived; `5ebb25c1…`/`68a8b896…`/`9567a5c9…` SwingVision with a real `insights.summary`). Measure with `getComputedStyle` because the DS type classes pin colour and beat utilities (memory `ds-class-layering-trap`). Named containers (`@container/report`, `@min-[720px]/report:`) only if a nested container appears. The 44px header is out of scope (decision 10); nothing under `film/` or `src/lib/services/splitstep/**` changes. Run the `widget-states` skill before handing back (commit hook `.claude/hooks/widget-states-gate.sh`).
