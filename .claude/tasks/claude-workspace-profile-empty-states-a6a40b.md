# Tasks — claude/workspace-profile-empty-states-a6a40b

> Scope: the team player profile's zero-match state — one graded, inert day zero under the identity row (direction B2), and the serve placement card's ghost + band shell for partial states.

Run one with `/task-next`. To drain the file, loop a plain-text instruction —
**not** `/loop /task-next`, which a scheduled fire cannot invoke:

> `/loop Read .claude/skills/task-next/SKILL.md and follow it exactly — run one task from this branch's queue; do not add, edit, or reorder tasks; then stop.`

Append freely while it runs: the queue is re-read at the start of every
iteration, and the runner only ever rewrites a task's `status:` line.
Mark a task `next` to jump the queue.

Status values: `todo` (eligible to run), `next` (jump the queue), `doing` /
`done` / `blocked` (written by the runner around a dispatch), and `later`
(deferred — `/task-next`'s picker never selects it, so a loop drain skips
straight past it; promote a task to `todo` by hand once it's actually
ready).

## T1 · Shared DayZeroGrade + profile day-zero copy helper

- **status:** done
- **model:** opus
- **files:** src/components/dashboard/home/day-zero-shape.tsx, src/components/dashboard/home/day-zero-home.tsx, src/lib/ui/profile-day-zero-copy.ts (new), tests/profile-day-zero-copy.spec.ts (new) — guess
- **done when:**
  - [ ] `day-zero-shape.tsx` exports `DayZeroGrade({ description, className?, children })` rendering `<p className="sr-only">{description}</p>` followed by a `<div inert>` whose inline style sets BOTH `maskImage` and `WebkitMaskImage` to exactly `linear-gradient(180deg, rgba(0,0,0,0.62) 0%, rgba(0,0,0,0.46) 40%, rgba(0,0,0,0.32) 100%)`; the module's doc comment no longer says Home is "deliberately NOT built on this" (it now says the grade lives here and the flat 0.32 `DayZeroShape` is the card/table form).
  - [ ] `day-zero-home.tsx` renders `<DayZeroGrade description=… className="flex flex-1 flex-col gap-4">{kpiStrip}{children}</DayZeroGrade>` in place of its inline `<p className="sr-only">` + `<div inert style={{ WebkitMaskImage, maskImage }}>`; the file contains no `linear-gradient(` literal, its sr-only sentence text is unchanged, and its component doc comment (the "graded rather than drawn flat" / "The tail is decoration" paragraphs) is kept, with the inline "One continuous grade" comment moved onto `DayZeroGrade` or kept beside the call, not deleted.
  - [ ] New `src/lib/ui/profile-day-zero-copy.ts` (no React import) exports `profileDayZeroCopy({ isSelf, firstName, canUpload }: { isSelf: boolean; firstName: string; canUpload: boolean })` returning `{ lead: string; tail: string | null; link: { label: string } | null; description: string }`, and for a third party the strings use `firstName` and never "their"/"they".
  - [ ] New `tests/profile-day-zero-copy.spec.ts` asserts the four voices in the notes as exact strings (assembled as `lead + (link?.label ?? "") + (tail ?? "")`), that `link` is null exactly when `canUpload` is false, and the two `description` strings; `npx playwright test tests/profile-day-zero-copy.spec.ts` passes.
- **notes:** Mask string (verbatim, both properties): `linear-gradient(180deg, rgba(0,0,0,0.62) 0%, rgba(0,0,0,0.46) 40%, rgba(0,0,0,0.32) 100%)`. Home's `DayZeroHome` output must stay byte-identical: same sr-only text, same `inert` div classes `flex flex-1 flex-col gap-4`, same style keys. `team/team-day-zero-home.tsx` carries the same mask inline — out of scope, do not touch. Copy table (firstName "Maya" in the spec): · self, canUpload → lead `"Your first match fills this page. Send match video from New match, or "`, link `{ label: "import a SwingVision export" }`, tail `"."` → sentence "Your first match fills this page. Send match video from New match, or import a SwingVision export." · staff (isSelf false), canUpload → lead `"Maya's first match fills this page. Send match video from New match, or "`, same link, tail `"."` · self, cannot upload → lead `"Your first match fills this page, once your coaching staff send one."`, link null, tail null · teammate viewer (isSelf false, cannot upload) → lead `"Maya's first match fills this page, once Maya or the coaching staff send one."`, link null, tail null. `description` (the sr-only line, independent of canUpload): self → `"Once your first match is analysed this page fills with season numbers, every match, results by line, and where first serves land. Nothing below is real data yet."`; third party → `"Once Maya's first match is analysed this page fills with season numbers, every match, results by line, and where first serves land. Nothing below is real data yet."`. Spec pattern: plain `import { profileDayZeroCopy } from "@/lib/ui/profile-day-zero-copy"` under `@playwright/test`, as `tests/date-value.spec.ts` does. Gates before marking done: `npm run typecheck && npm run lint && npm run format:check`. Approved plan: ~/.claude/plans/system-reminder-you-are-operating-shimmering-cocke.md.

## T2 · Profile card ghosts + serve card CardEmpty shell

- **status:** done
- **model:** opus
- **files:** src/components/dashboard/team/player-profile/last-match-card.tsx, match-history-card.tsx, line-history-card.tsx, serve-placement-card.tsx, src/components/dashboard/home/serve-placement-quiet-strip.tsx, src/app/dashboard/team/roster/[playerId]/page.tsx (call sites only) — guess
- **done when:**
  - [ ] `last-match-card.tsx` exports `LastMatchGhost` (the `grid grid-cols-[32px_minmax(0,1fr)_15px_auto] items-center gap-3` block: `size-8` square + the four `GhostRule`s, moved verbatim), the `LastMatchEmpty` function and the `CardEmpty`/`CardSubject` imports are gone, and `LastMatchCard({ match })` has no `matchesPlayed`/`subject` props and returns `<PlayedMatch>` or `null`.
  - [ ] `match-history-card.tsx` exports `MatchHistoryGhost` (`<MatchHistoryHeader />` + the `GhostRows`/`GhostRule` block moved verbatim), contains no `CardEmpty` usage and no `subject`/`importHref` props, and `MatchHistoryCard` renders header + rows only; `line-history-card.tsx` exports `LineHistoryGhost` (`<LineHistoryHeader />` + its `GhostRows` block) and its `lines.length === 0` branch still renders `CardEmpty` with the "Lines fill in from the schedule" band and the `Open schedule` action, with the populated markup unchanged.
  - [ ] `serve-placement-quiet-strip.tsx` exports `ServePlacementGhost` containing exactly the claim-rule div, the `COURTS.map(EmptyCourtBar)` column and the `Legend muted` hairline row (moved lines, no class/attribute edits), and the strip's empty branch is `<><ServePlacementGhost />{the existing sentence span}</>` so its rendered output is unchanged.
  - [ ] `serve-placement-card.tsx` takes `{ serve, subject: CardSubject }` (no `isSelf`, no `matchesPlayed`, no `emptyCopy` passed): `serve.zoneStats !== null` renders `ServePlacementQuietStrip` as today; `serve.zoneStats === null` renders `<section aria-label="Serve placement" className="surface-card …" style={{ padding: "var(--pad-card)" }}>` with `<span className="eyebrow">Serve placement</span>` and `CardEmpty` wrapping `<ServePlacementGhost />` with the band below and `band.action` omitted.
  - [ ] `page.tsx` compiles against the new signatures: `<LastMatchCard match={profile.lastMatch} />`, `<MatchHistoryCard rows playerName />`, `<ServePlacementCard serve={profile.serve} subject={subject} />`; nothing else in the page changes (the zero-match branch is T3).
- **notes:** Band copy for the serve card: title `subject.isSelf ? "Your serve map lands here" : \`${subject.firstName}'s serve map lands here\``; body `"Where first serves go on each court, once the first report lands."`; `CardEmpty` `description`(sr-only):`"No report yet: this card shows where first serves land on each court, T, body and wide."`. No action — it would repeat the header's New match (commit d6fd4d16's rule; the same reason `LastMatchEmpty`never had one).`EmptyCourtBar`and`Legend`stay module-private; only the composed`ServePlacementGhost`is exported. Keep`MatchHistoryHeader`/`LineHistoryHeader`private and let the ghosts render them so T3 can drop each ghost under a bare eyebrow row. Intermediate state is acceptable: until T3 lands, a zero-match profile shows no Last match card and an empty Match history header — do not re-add bands to cover it. Home's`home-content.tsx`and`tests/home-empty-loading.spec.ts`must not need edits. Gates:`npm run typecheck && npm run lint && npm run format:check`. Approved plan: ~/.claude/plans/system-reminder-you-are-operating-shimmering-cocke.md.

## T3 · ProfileDayZero composition, page branch, docs, render spec

- **status:** todo
- **model:** fable
- **needs:** T1, T2
- **files:** src/components/dashboard/team/player-profile/profile-day-zero.tsx (new), src/app/dashboard/team/roster/[playerId]/page.tsx, .skills/advantage-analytics-design/reference/empty-and-loading.md, tests/profile-day-zero.spec.ts (new) — guess
- **done when:**
  - [ ] New `profile-day-zero.tsx` exports `ProfileDayZero({ subject, canUpload, importHref }: { subject: CardSubject; canUpload: boolean; importHref: string })` rendering (a) one `<p>` at `text-[13px]` / `text-[var(--ink-700)]` built from `profileDayZeroCopy({ isSelf: subject.isSelf, firstName: subject.firstName, canUpload })` — `lead`, then when `link` is non-null a `next/link` `<Link href={importHref} className="text-[var(--blue)] … hover:text-[var(--blue-hover)]">{link.label}</Link>`, then `tail` — and (b) `<DayZeroGrade description={copy.description} className="flex flex-col gap-4">` containing `<SeasonKpiStrip kpis={[]} hasStats={false} matchesPlayed={0} subject={subject} />` then a `div.grid.items-start.gap-4.lg:grid-cols-[1.9fr_1fr]` whose left column (`flex min-w-0 flex-col gap-4`) holds a Last match `section` (`surface-card`, padding `18px 20px`, `<span className="eyebrow">Last match</span>`, `<LastMatchGhost />`) and a Match history `section` (`surface-card`, padding 20, eyebrow, `<MatchHistoryGhost />`), and whose right column holds a Line history `section` (padding 20, eyebrow, `<LineHistoryGhost />`) and a Serve placement `section` (`surface-card`, padding `var(--pad-card)`, eyebrow, `<ServePlacementGhost />`); no `RowAction`, no `Link` other than the sentence's, no `CardEmpty`, no `advButton` inside the grade.
  - [ ] The component's doc comment names commit `d6fd4d16` and states that this is not the deleted `ProfileDayZero` because it draws no offer headline and no second primary — the identity row is the page's one heading and holds New match.
  - [ ] `page.tsx` renders, after the unchanged `<ProfileIdentity …/>`, `profile.matchesPlayed === 0 ? <ProfileDayZero subject={subject} canUpload={canUpload} importHref={\`${newMatchHref}&source=swing-vision\`} /> : <>SeasonKpiStrip + the 2:1 grid exactly as today</>`; the "same five blocks whether or not a match exists" comment is replaced by one saying the page keeps its frame and the zero-match tail is graded and inert; `ProfileActions` / header props are untouched.
  - [ ] `.skills/advantage-analytics-design/reference/empty-and-loading.md`, day-zero row of the "Three states" table, lists `team/player-profile/profile-day-zero.tsx` with a clause that it is the one day zero with no offer, because the identity row is the heading and holds the primary.
  - [ ] New `tests/profile-day-zero.spec.ts` renders via `createLoader()` from `tests/fixtures/vm-modules` (stubs for `next/link` → `<a href>`, `next/image` → `<img>`, `next/navigation`; `markUnknown: true` or an alias stub for `@/components/dashboard/shared/season-kpi-strip` if its client deps will not load) and asserts: exactly one element with the `inert` attribute and it carries the mask string on `mask-image`; inside it no `bg-[var(--blue)]` (advButton primary) and none of the band titles ("lands here", "newest first", "Lines fill in from the schedule"); with `canUpload: true` the sentence text and an `<a href="…&source=swing-vision">import a SwingVision export</a>` render, with `canUpload: false` no `<a>` renders before the grade; `ServePlacementCard` with `serve.zoneStats: null` renders "Your serve map lands here" / "Maya's serve map lands here" plus two `— serves` tracks; `DayZeroHome` (with `DayZeroOffer` stubbed) still emits the same mask string; `npx playwright test tests/profile-day-zero.spec.ts` passes.
- **notes:** Mask string to assert (verbatim): `linear-gradient(180deg, rgba(0,0,0,0.62) 0%, rgba(0,0,0,0.46) 40%, rgba(0,0,0,0.32) 100%)` — `renderToStaticMarkup` emits it as `mask-image:…;-webkit-mask-image:…`. Sentence strings are in T1's notes (`profileDayZeroCopy`); band strings in T2's. The zero-match KPI strip is `KpiStripEmpty` via `SeasonKpiStrip` with `hasStats={false}`; `kpis` is required by its signature, so pass `[]`. Eyebrows stay in the grade; "Full report", "All N matches", "Placement view" and every band do not. Grid classes come from `page.tsx`: `grid items-start gap-4 lg:grid-cols-[1.9fr_1fr]`, columns `flex min-w-0 flex-col gap-4`, page stack `gap-5`. Why-not-the-deleted-one rationale for the doc comment: d6fd4d16 removed a `ProfileDayZero` that added "a second 30px headline and a second New match" under the identity row over a flat-0.32 page; direction B2 restores the graded tail under Home's mask but keeps the identity row as the single heading and single primary, so this component renders no `DayZeroOffer`, no headline, no button — one 13px sentence with at most one inline link, then the inert grade. Spec loader reference: `tests/report-empty-states.spec.ts` (createLoader + markUnknown + alias stubs). Gates: `npm run typecheck && npm run lint && npm run format:check`, then `npm test` in full — two specs flake only in the full suite; re-run them before calling the suite failed. `git add -A` before the widget-states mark; mark and commit are separate calls. Commit message must note out of scope: the route's `loading.tsx` still flashes the populated skeleton before a zero-match profile resolves (needs a per-player presence read, cf. 651d613f); and Home's serve-strip sentence and `team-day-zero-home.tsx`'s inline mask are untouched. Approved plan: ~/.claude/plans/system-reminder-you-are-operating-shimmering-cocke.md.
