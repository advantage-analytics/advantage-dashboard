# Run log — claude/eyebrow-text-wrapping-c359ca

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add `programEyebrow()` helper to programs-server — done
- **gate:** lint clear · `tsc --noEmit` exit 0 · `npm test` 301 passed · `task-completion-reviewer` VERDICT: pass, all five criteria met individually · `rls-boundary-reviewer` ran (diff touches `src/lib/data/`) and reported the diff clear with no findings · `pipeline-guardrails-reviewer` skipped: the diff touches nothing under `src/app/dashboard/`, `src/components/dashboard/` or the upload wizard.
- **changed:** One purely additive hunk in `src/lib/data/programs-server.ts` — `programEyebrow(schoolName, team, division)` beside `programSubtitle`, composing `school · squad · division` through the existing `teamLabel`/`divisionLabel` and dropping a null division via `.filter(Boolean)`. Its doc comment records why conference is excluded: the four-field eyebrow reaches 136 characters and roughly 1,134px, wider than any column in the claim flow. No caller is wired — that is T3 and T4. `programSubtitle`, `teamLabel` and `divisionLabel` are untouched and all four `programSubtitle` call sites remain.

## T2 · Add a full-width `heading` slot to `ClaimShell` — done
- **gate:** lint clear · `tsc --noEmit` exit 0 · `npm test` 301 passed · `task-completion-reviewer` VERDICT: pass, having checked line by line that the diff's 11 deletions are re-indentation only and that no class string, prop or default changed under cover of it · both guardrails skipped, correctly: the diff touches only `src/components/claim/claim-shell.tsx`, which is neither a dashboard surface (`src/app/dashboard/`, `src/components/dashboard/`, upload wizard) nor a data/API surface (`src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`), and adds no table, view or query. No untracked files.
- **changed:** `ClaimShell` gained an optional `heading?: React.ReactNode` prop, rendered above the aside grid inside a new `flex flex-col` wrapper carrying the shell's existing `gap`. The doc comment records why the slot exists: the grid narrows the body column for the aside, and the longest program name alone renders wider than the 492px that leaves. Every existing prop, default, class string and `ClaimColumn` call is untouched. No page passes `heading` yet, so all seven claim screens still render identically — the reviewer confirmed the single-child flex wrapper stretches exactly as the previous block child did.

## T3 · Adopt eyebrow helper and heading slot on the unclaimed status screen — done
- **gate:** lint clear (0 errors; 37 pre-existing warnings in unrelated files) · `tsc --noEmit` exit 0 · `npm test` 301 passed · `task-completion-reviewer` VERDICT: pass, having verified directly that the `active` and `claim_pending` branches and the shared four-field `eyebrow` const are byte-identical and still fed by `programSubtitle` · both guardrails skipped, correctly: the diff touches only `src/app/claim/[programKey]/page.tsx`, which is neither a dashboard surface nor a data/API surface, and adds no table, view or query. No untracked files.
- **changed:** The F3.2 unclaimed state now composes its own eyebrow through `programEyebrow()` and passes its `ClaimHeading` through `ClaimShell`'s `heading=` slot, so the eyebrow renders at the shell's full 840px instead of the 492px the aside leaves. A comment records why the file carries two eyebrows: the other two states this route renders are out of scope and keep the shared four-field one. Body copy, actions, micro line, aside, width, gap and back are untouched.

## T4 · Adopt eyebrow helper and heading slot on the setup screen — done
- **gate:** lint clear (0 errors; 37 pre-existing warnings in unrelated files) · `tsc --noEmit` exit 0 · `npm test` 301 passed · `task-completion-reviewer` VERDICT: pass, having checked the string-identity claim against `programEyebrow`'s actual implementation rather than on trust — argument order, separator, null handling, and the squad the helper now derives internally all match the removed inline composition · both guardrails skipped, correctly: the diff touches only `src/app/claim/[programKey]/setup/page.tsx`, which is neither a dashboard surface nor a data/API surface, and adds no table, view or query. No untracked files.
- **changed:** The setup screen's `ClaimHeading` now goes through `ClaimShell`'s `heading=` slot, so its eyebrow renders at the full 1000px rather than the 612px the 340px aside leaves — its worst case, North Carolina A&T's women's program, measures 616px and missed that column by four pixels. The inline composition became a `programEyebrow()` call producing a byte-identical string, and the now-unreferenced `divisionLabel` import was dropped while `teamLabel` stays to feed the title. Title, width, gap, aside width, back, aside and the email provider wrapper are untouched.

## T5 · Add the eyebrow width-budget regression spec — done
- **gate:** lint clear · `tsc --noEmit` exit 0 · `npm test` 304 passed (301 before, plus this spec's three) · `task-completion-reviewer` VERDICT: pass, having independently run its own exact-count query to confirm 1,941 rows, checked that `program_key` is unique so the paging key partitions rather than overlaps, and confirmed the count assertion would fail hard rather than pass vacuously if paging broke · `rls-boundary-reviewer` ran (the spec adds a new query on the service-role client) and reported it clear: service-role use is confined to the test fixture that four sibling specs already share, never reaches `src/lib/supabase/admin.ts` or a client bundle, the spec is read-only, absent credentials skip rather than fall back to a degraded client, and the four columns read are the same anon-readable directory fields the public RPCs already return · `pipeline-guardrails-reviewer` skipped: no dashboard surface touched. The new file is untracked, so surfaces were determined from `git ls-files --others --exclude-standard` as well as `git diff HEAD --stat`.
- **changed:** New `tests/claim-eyebrow-width.spec.ts`, 175 lines. It pages the whole `programs` table by `program_key`, cross-checks the row count against an independent `count: 'exact'` head query so PostgREST's 1,000-row cap cannot hide half the table, then asserts every `programEyebrow()` result stays within 97 characters and contains no conference value. The budget carries its provenance: 840px of hoisted heading divided by 8.6px per character, measured. Across 1,941 rows the longest eyebrow is 74 characters, so there are 23 characters of headroom. The subagent verified the pagination guard by temporarily breaking it and watching the count assertion fail, and verified the skip path by running with the Supabase variables unset.
- **follow-ups:** 1. The 8.6px-per-character constant is the spec's one unguarded assumption — if `.eyebrow` ever changes size, weight or tracking, nothing notices. A pointer comment in the design tokens back to this spec would close that loop.
