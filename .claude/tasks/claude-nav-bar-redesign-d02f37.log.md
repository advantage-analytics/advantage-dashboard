# Run log — claude/nav-bar-redesign-d02f37

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Nav data: ChartLine, UsersRound, comingSoon flag + spec — done
- **gate:** mechanical — lint 0 errors (32 pre-existing warnings, untouched files), `tsc --noEmit` clean, `npm test` green. Completion review — `VERDICT: pass`, all five criteria met, no out-of-scope changes. Guardrails — `pipeline-guardrails-reviewer` run deliberately even though `src/lib/dashboard/` is outside its trigger list (the file drives `app-sidebar.tsx`): clear on every guardrailed surface (misattribution inputs, workspace scoping, role gating, `canSubmitVideo`, analysis short-circuit, provider naming). It raised one non-guardrail note — the new `comingSoon` doc comment says the flag surfaces in the rail tooltip and `aria-label`, which nothing reads yet — recorded rather than blocked: that wording is what this task's own criterion 2 required, and T2 (`needs: T1`) is the reader. `rls-boundary-reviewer` skipped — the diff touches no Supabase client, data loader, API route, migration, table, view or query.
- **changed:** `src/lib/dashboard/nav.ts` — `BarChart3 → ChartLine` on both Statistics entries, `Users → UsersRound` on Roster, old imports dropped; `NavLink` gains optional `comingSoon?: true` with a doc comment; the flag set on exactly four entries (personal + team Statistics and Ask), Opponents deliberately unflagged. New `tests/nav-icons.spec.ts` asserts both icon swaps and the exact `comingSoon` href set — 3 assertions, passing.

## T2 · Rail row renders the coming-soon marker — done
- **gate:** mechanical — lint 0 errors (32 pre-existing warnings), `npm test` green; `tsc --noEmit` first reported one error, `.next/dev/types/validator.ts` pointing at the deleted harness route `src/app/preview-t2-harness/page.js` — the documented stale-route-type false failure, every error path under `.next/`. Cleared `.next/types` + `.next/dev/types` and re-ran to green. Completion review — `VERDICT: pass`; it could not re-run the browser check itself and judged criterion 4 from the code, confirming `rail-tooltip.tsx` and `chrome-tooltip.tsx` are byte-identical and that the reported behaviour follows directly from the diff. Guardrails — `pipeline-guardrails-reviewer` run (diff is `src/components/dashboard/`): nothing found; no wizard input, workspace scoping, role gating, `canSubmitVideo`, analysis short-circuit or provider-naming impact. `rls-boundary-reviewer` skipped — no Supabase client, data loader, API route, migration or query in the diff.
- **changed:** `sidebar/rail-item.tsx` gains an optional `comingSoon?: boolean`, passed through to the existing `RailTooltip` `detail` slot as "Coming soon" and widening the row's `aria-label` to `<label>, coming soon` on both the `<Link>` and `<button>` branches. `app-sidebar.tsx` passes `comingSoon={link.comingSoon}` in the `mainLinks` map only. Verified in a browser through a throwaway preview route (since deleted): collapsed Statistics shows a two-line tooltip, Matches stays one line, the expanded label is unchanged, and `aria-label` reads "Statistics, coming soon" at both widths.

## T3 · Sign-out row on Settings → Account — blocked
- **gate:** mechanical — lint 0 errors; `tsc` green after clearing the documented stale route types (they pointed at the deleted harness route `src/app/zz-preview-t3`); `npm test` did NOT come back clean — 401 passed, 3 live-DB specs failed with `signIn(...): Request rate limit reached` (join-requests-staff-read, pending-invites, rls-workspace-isolation). That is Supabase auth throttling from repeated suite runs in this loop, not the diff, which touches no auth, RLS, data-loader or API code — but it is not a clean run and is recorded as such. Completion review — `VERDICT: pass` (all five criteria met; it judged the second row's added "Every device" title acceptable fallout of the required restructure). Guardrails — `pipeline-guardrails-reviewer` run (`src/app/dashboard/`): no findings, but it flagged one thing to sanity-check outside its scope, which is what blocks this task. `rls-boundary-reviewer` skipped — no Supabase client factory, data loader, API route, migration or query in the diff.
- **blocked because:** the task's own criteria are wrong, and the diff faithfully implements them. Criterion 1 gives the new "This device" row the copy "Ends this session only. Other devices stay signed in."; criterion 2 wires it to `useRequestLogout()`. But `logout-dialog.tsx:50` calls `supabase.auth.signOut()` with no options, and `@supabase/auth-js`'s `signOut(options = { scope: 'global' })` defaults to **global** — it revokes every refresh token on the account. So the new row would do exactly what "Sign out everywhere" does while telling the reader it does not, and the two rows would be the same action under two labels. Verified in `node_modules/@supabase/auth-js/dist/module/GoTrueClient.js`.
- **needs a decision:** either (a) the this-device row calls a local sign-out — `signOut({ scope: 'local' })` — which means changing `logout-dialog.tsx`, and criterion 2 forbids touching it; or (b) the copy changes to describe a global sign-out, which then makes the second row redundant and reopens design open question 3. Not the runner's call.
- **stash:** 7b9a3ec3b074e2f497057388ad410b2b6485d152 (recoverable with `git stash apply 7b9a3ec3b074e2f497057388ad410b2b6485d152`; the code is otherwise complete and reviewed)

## T5 · Workspace switcher opens focused on the current row — done
- **gate:** mechanical — lint 0 errors; `tsc` green after clearing the documented stale route types (pointing at the deleted harness route `src/app/t5-harness`); `npm test` fully green this run, which also retroactively confirms T3's three failures were Supabase auth rate limiting rather than code. Completion review — `VERDICT: pass`; it judged the `preventDefault()` at the clamped ends as satisfying rather than exceeding criterion 2, and accepted criterion 4 on the pre-existing two-press Escape (see follow-ups). Guardrails — `pipeline-guardrails-reviewer` run (`src/components/dashboard/`) with workspace scoping called out for particular attention: nothing found; `switchTo` and `setActiveWorkspace` are untouched, arrow keys move focus only and cannot activate a workspace, and the `pendingId` guard matches the existing `disabled` guard on each row. `rls-boundary-reviewer` skipped — no Supabase client factory, data loader, API route, migration or query in the diff.
- **changed:** `sidebar/workspace-row.tsx` — a `useRef<Map<string, HTMLButtonElement>>` of row buttons populated by ref callbacks; `onOpenAutoFocus` focuses the active workspace's row (falling through to Radix's default when that ref is absent); `onKeyDown` moves focus with ArrowUp/ArrowDown in `available` order, clamped at both ends, ignored entirely while a switch is pending. No focus classes written — `focus.css` supplies the ring. Browser-verified through a throwaway route (since deleted) with three fixture workspaces: keyboard open put the blue ring on the current row, arrows moved and stopped at the ends, and a mouse-opened menu left the same row focused with no ring drawn.
- **follow-ups:**
  1. Escape needs two presses to close the switcher — the first is swallowed by the focused row's `ChromeTooltip` dismissable layer. The subagent confirmed this is pre-existing by stashing the diff and reproducing it on the unmodified component. Likely affects any tooltip-wrapped row inside a popover, so it is a `ChromeTooltip` layering question rather than a switcher one.
  2. The header's `WorkspaceOptionList` (`workspace-switcher.tsx`) has none of this focus or arrow behaviour — design open question 2 is whether the two switchers converge.
