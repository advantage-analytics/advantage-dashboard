# Tasks — claude/video-progress-bar-nav-c8cb80

> Scope: Seek-lane hover frame preview on both match-detail film players (report player + room)

Run one with `/task-next`. To drain the file, loop a plain-text instruction —
**not** `/loop /task-next`, which a scheduled fire cannot invoke:

> `/loop Read .claude/skills/task-next/SKILL.md and follow it exactly — run one task from this branch's queue; do not add, edit, or reorder tasks; then stop.`

Append freely while it runs: the queue is re-read at the start of every
iteration, and the runner only ever rewrites a task's `status:` line.
Mark a task `next` to jump the queue.

Status values: `todo` (eligible to run), `next` (jump the queue), `doing` /
`done` / `blocked` (written by the runner around a dispatch), and `later`
(deferred — `/task-next`'s picker never selects it, so a loop drain skips
straight past it; promote a task to `todo` by hand once it's actually
ready).

## T1 · Seek-preview geometry, lane-lift gradient and seek coalescer as pure helpers

- **status:** done
- **model:** sonnet
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-seek-preview.ts (new); tests/film-seek-preview.spec.ts (new, node-only like tests/film-motion.spec.ts)
- **done when:**
  - [ ] New `film-seek-preview.ts` exports the design's numbers as named constants: `PREVIEW_FRAME = { report: { width: 160, height: 90 }, room: { width: 256, height: 144 } }`, `PREVIEW_OVERHANG_PX = { report: 8, room: 0 }` (the report lane sits 16px inside its frame and the box may reach 8px inside the frame; the room lane sits on the 24px gutter and the box stays inside it), `PREVIEW_PAD_PX = 4`, `PREVIEW_HANG_PX = 6`, `PREVIEW_REST_MS = 150`, `PREVIEW_FADE_MS = 200`, `PREVIEW_SEEK_INTERVAL_MS = 120`, `type PreviewSize = keyof typeof PREVIEW_FRAME`, and `previewBoxWidth(size)` = frame width + 2 × pad
  - [ ] `previewLeft({ pointerX, laneWidth, boxWidth, overhang })` returns the box's left edge in lane pixels: `pointerX − boxWidth / 2` clamped to `[−overhang, laneWidth − boxWidth + overhang]`, the lower bound winning when the lane is narrower than the box; `laneFraction(clientX, laneLeft, laneWidth)` returns the 0..1 fraction with the same arithmetic as `FilmTrack.seekFromPointer` (which is not edited here) — spec cases: centred, clamped at both ends for overhang 8 and for 0, lane narrower than the box, zero-width lane returns 0
  - [ ] `trackRunGradient({ start, end })` returns the run's `linear-gradient(to right, …)` with three stops: `var(--blue)` from 0 to the existing `--film-t` split expression, `rgba(255,255,255,0.34)` from that split to a second split built identically from `var(--film-hover, -1)`, and `rgba(255,255,255,0.22)` from there to 100% — spec asserts the exact string for `{ start: 10, end: 20 }`; the function's comment records that with the hover var unset the second split clamps to 0% and CSS's stop-ordering rule collapses the lift to zero width
  - [ ] `createSeekCoalescer({ seek, minIntervalMs, now, schedule })` returns `{ request(t), landed(), cancel() }`: a request with no seek in flight and ≥ `minIntervalMs` since the last issue calls `seek(t)` at once; a request inside the interval or while in flight is held as the single latest wanted time (never a queue) and issued by `landed()` or the interval timer, whichever comes first once both allow; `cancel()` drops the wanted time and the timer — spec with an injected clock and scheduler: 20 requests 10 ms apart (each seek `landed()` 5 ms later) produce ≤ `Math.ceil(200 / 120) + 1` seeks and the final seek carries the last request's time; a request during flight is issued exactly once on `landed()`; `cancel()` then `landed()` issues nothing
  - [ ] `npx playwright test tests/film-seek-preview.spec.ts` passes; `film-timeline.ts`, `film-track.tsx` and `tests/film-timeline.spec.ts` are not in the diff; `npm run lint` and `npm run typecheck` pass
- **notes:** Pure TypeScript only — no React, no DOM. The hook (T2) is `use-seek-preview.ts` and the markup lives in `film-track.tsx`, so no `foo.tsx` ever sits beside `foo.ts`. `PREVIEW_SEEK_INTERVAL_MS` is the author's number to tune later; 120 matches the seek-settling grace already used in this subtree. The lane's seek/offset conversion is untouched by design — these helpers only turn a pointer into a lane fraction and a box position. Design source: canvas https://claude.ai/artifact/9J6a1qz5v2JwoX6EKiXfhL (Spec artboard).

## T2 · Hover frame preview on the report player's lane

- **status:** todo
- **model:** opus
- **needs:** T1
- **files:** (guess) src/components/dashboard/matches/match-detail/film/use-seek-preview.ts (new); src/components/dashboard/matches/match-detail/film/film-track.tsx; src/components/dashboard/matches/match-detail/film/film-player.tsx (one prop on `<FilmTrack>`); tests/film-playback-refresh.spec.ts; docs/match-video-attachments.md (one sentence)
- **done when:**
  - [ ] New `use-seek-preview.ts`: `useSeekPreview({ url, generation })` owns a second `<video data-testid="film-seek-preview-video" muted playsInline preload="metadata">` with `src={url}` and `key={generation}`, and the string `crossOrigin`/`crossorigin` appears nowhere in the diff; it mounts on the lane's first non-touch pointer event or first scrub (never at page load, never while `url` is null) and stays mounted across open/close; every seek goes through T1's `createSeekCoalescer` with `PREVIEW_SEEK_INTERVAL_MS`, `landed()` wired to the element's `seeked`, and no seek is issued before the element's `loadedmetadata` (an earlier request is held as the wanted time); it reports `state: "empty" | "held" | "live"` — `empty` until the first frame of this generation has landed, `held` while a newer seek is in flight past a 120 ms grace (reusing `useSeekSettling` is fine), `live` otherwise
  - [ ] `FilmTrack` takes optional `preview?: { url: string | null; generation: number; size: PreviewSize }` and, when given, renders inside the slider an `aria-hidden` box `data-testid="film-seek-preview"` with `data-state="closed" | "empty" | "held" | "live"`: `absolute pointer-events-none z-10 p-1` (`z-10` so it clears the room's court readout `z-[3]` in T4), `bottom: calc(100% + PREVIEW_HANG_PX)`, `width: previewBoxWidth(size)`, `left` from `previewLeft` with `PREVIEW_OVERHANG_PX[size]`; inside it a frame `data-testid="film-seek-preview-frame"` `overflow-hidden` sized exactly `PREVIEW_FRAME[size]` holding the preview video (`h-full w-full object-cover`) and, below it, the time `data-testid="film-seek-preview-time"` from `formatClock`; the box's only transition is `opacity 200ms var(--ease-primary)` (`PREVIEW_FADE_MS`) — no `left`/`transform` transition, so reduced motion needs no extra rule — and `closed` is opacity 0 plus `visibility: hidden` once the fade ends. The slider sets `--film-hover` (seconds) and `--film-hover-x` (lane px) and `data-film-hover=""` on pointer move and while scrubbing, and removes all three on leave, with no React state per pointer move; the runs' `background` comes from `trackRunGradient`, and an `aria-hidden` 1×9 px marker span sits at `left: var(--film-hover-x)`, rendered only while `data-film-hover` is set
  - [ ] Open/close rules, as cases in `tests/film-playback-refresh.spec.ts` against the report lane (`[role="slider"][aria-label="Seek"]` inside the report frame): (a) mouse at 75% of the lane's width → the box leaves `closed`, `film-seek-preview-time` reads `0:01`, the slider's `--film-hover` is within 0.05 of 1.5, and `expect.poll` reaches `data-state="live"` with the preview video's `currentTime` within 0.1 of 1.5; moving the mouse 40 px below the lane → `data-state="closed"` and the box `toBeHidden()`; (b) `locator.focus()` on the slider with the mouse parked elsewhere → the box is absent or `closed`, and `ArrowRight` still moves `aria-valuenow` by 5 s (or to the end) as today; (c) `mouse.down` at 30%, `mouse.move` to 70%, then 40 px below the lane still down → the box is open, reads `0:01`, and the report video's `currentTime` is within 0.1 of 1.4; `mouse.up` there → `closed`; (d) `dispatchEvent("pointermove", { pointerType: "touch", … })` on the lane → no `film-seek-preview-video` mounts; `pointerdown` with `pointerType: "touch"` → open; `pointerup` → `closed`. `PREVIEW_REST_MS` is the `setTimeout` delay before the first open in the hook and is not timed by the spec
  - [ ] Throttle and refresh, same spec: (e) after a first hover has mounted the element and it reports `readyState >= 1`, wrap its `currentTime` setter (the `holdSeeks` idiom) to count sets and record the last value, then `mouse.move` across the whole lane with `steps: 30` while timing it → sets ≤ `Math.ceil(elapsedMs / PREVIEW_SEEK_INTERVAL_MS) + 2` and the last set is within 0.1 of the final pointer's lane time; (f) hover once, mark the preview element (`dataset.marker`), run the existing silent-refresh flow (`release`, `awaitCredential(REPORT, 2)`), hover again → the preview video's `src` contains `cred=2` and carries no marker (a remount, not a `src` swap)
  - [ ] `film-timeline.ts`, `tests/film-timeline.spec.ts`, `film-clock.ts`, `film-transport.tsx` and `film-fullscreen.tsx` are not in the diff (the `preview` prop is optional, so the room renders exactly as before and is T4's); `seekFromPointer`'s seek value for a given `clientX`, ←/→/Home/End and `data-film-own-keys` are unchanged; `docs/match-video-attachments.md`'s "Playback needs nothing from CORS" paragraph gains one sentence saying the seek-preview element sets no `crossOrigin` either; no new `#rrggbb` in `src/` (`tests/design-drift.spec.ts` passes); `npm run lint`, `npm run typecheck`, and `npx playwright test tests/film-playback-refresh.spec.ts tests/film-trace.spec.ts tests/film-motion.spec.ts` pass
- **notes:** Structure and behaviour only — paint (ink-900 box, shadow + inset ring, radii, well colour, dims, marker colour) is T3, so use the minimum classes needed for position, size, overflow and visibility here. Design intent (canvas https://claude.ai/artifact/9J6a1qz5v2JwoX6EKiXfhL, Spec artboard): 150 ms rest on first enter, then follows the pointer with no transition, 200 ms `--ease-primary` fade; first hover shows an empty well with the time, afterwards the last frame holds dimmed to 60% until the next lands; stays during a scrub anchored to the thumb; touch only while dragging; nothing on keyboard focus. `preload="metadata"` + a seek is what paints the first frame (the wizard's `paintFirstFrame` comment, `TrimStepContent.tsx:931`); the wizard's `seekLatest` coalescer (`TrimStepContent.tsx:~894-926`) is the precedent for "one in flight, remember the latest". The preview video's `src` must be the identical string to the main element's so Chrome can share its media cache for range requests. Do not mount the preview eagerly: on a moov-at-tail Azure file (T15's finding on the previous branch) each fresh element range-requests the tail before it can seek, so a lane nobody hovers should cost nothing. Playwright's `dispatchEvent` accepts `PointerEvent` init (`pointerType`); if the `pointerup` for (d) needs the window, dispatch it via `page.evaluate`. Author eyes-on after this lands (not a criterion): on the Revelli vs Stepanov match compare the 160×90 frame against 192×108 by eye and record the verdict in the run log — `PREVIEW_FRAME.report` is the only place to change. Widget-states mark before commit (separate call from the commit).

## T3 · Paint the seek preview to the Spec artboard

- **status:** todo
- **model:** sonnet
- **needs:** T2
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-track.tsx
- **done when:**
  - [ ] The `film-seek-preview` box carries `rounded-[12px] bg-[var(--ink-900)]` and `boxShadow: "var(--shadow-dropdown), inset 0 0 0 1px rgba(255,255,255,0.08)"`; the `film-seek-preview-frame` carries `rounded-[8px]`; the time carries `mono tabular text-[11px] text-white/90` in a 16 px row centred under the frame (Roboto Mono is what `.mono` resolves to via `--font-mono`) with a 4 px gap from the frame — no other size, padding or position class from T2 changes
  - [ ] States paint as designed: in `empty` the frame shows `bg-white/[0.06]` with the preview video at `opacity-0`; in `held` the video is `opacity-60`; in `live` it is `opacity-100`; the video's own transition is `transition-opacity duration-200 ease-[var(--ease-primary)]`, and the box gains no transition beyond T2's opacity one
  - [ ] The hover marker is `h-[9px] w-px bg-white/70`, vertically centred on the 3 px runs, and still renders only while the slider carries `data-film-hover`
  - [ ] `npx playwright test tests/film-playback-refresh.spec.ts` passes with the T2 cases unmodified; `tests/design-drift.spec.ts` passes (no new `#rrggbb`, no off-scale `text-[Npx]` — 11 px is already on the scale in this subtree); `npm run lint` and `npm run typecheck` pass; no file other than `film-track.tsx` is in the diff
- **notes:** Paint only, from the canvas Spec artboard: dark box `--ink-900`, radius 12, padding 4, `--shadow-dropdown` + 1 px inset 8% white, 16:9 frame radius 8 `object-fit: cover`, 11 px Roboto Mono time at 90% white, empty well 6% white, held frame 60%, marker 1×9 white/70. Precedents in the tree: `DARK_READOUT_STYLE` in `match-detail/chart-tooltip.tsx` (same background + shadow), the inset-ring idiom `shadow-[inset_0_0_0_1px_rgba(255,255,255,0.28)]` in `film-court-card.tsx:149`, the players' `opacity-60` dim (`film-player.tsx:~652`).

## T4 · The room's lane gets the 256×144 preview, drawn above the board and the court

- **status:** todo
- **model:** opus
- **needs:** T2
- **files:** (guess) src/components/dashboard/matches/match-detail/film/film-transport.tsx; src/components/dashboard/matches/match-detail/film/film-fullscreen.tsx (the `<FilmTransport>` call site at ~1567-1592 only); tests/film-playback-refresh.spec.ts
- **done when:**
  - [ ] `FilmTransportProps` gains `previewSource: { url: string | null; generation: number }` and `FilmTransport` passes `preview={{ ...p.previewSource, size: "room" }}` to `FilmTrack`; `film-fullscreen.tsx` supplies `{ url: p.url, generation: p.generation }` — asserted in the existing "both surfaces, one credential" flow: hover the room lane before and after the swap → the room's preview video `src` moves to the new `cred=` and is a different node
  - [ ] Room geometry, as a case on `openRoomWithDrawer`: hover the room lane at its far right → `film-seek-preview-frame`'s bounding box is 256×144 and the box's right edge ≤ the slider's right edge + 0.5 px (overhang 0, inside the drawer-shortened lane); at the far left the box's left edge ≥ the slider's left edge − 0.5 px
  - [ ] Paint order: drag the board to `bottom-left` (the T11 drag idiom, reading `[data-board-anchor]`), hover the lane's left end → the box's rect intersects the board's rect, and `document.elementFromPoint` at a point inside that intersection — with the box's `style.pointerEvents` set to `"auto"` inside the same `evaluate` and restored before it returns — returns a node the box `contains`; with the court on and overlapping, the same probe against `[data-court-anchor]` passes too
  - [ ] One element per hovered lane: with the room open and only the room lane hovered, `[data-testid="film-seek-preview-video"]` has count 1 (the report lane under the room mounted none); nothing under `[data-testid="film-seek-preview"]` in the room takes the pointer (`pointer-events-none` on the box, so the lane's own hover/scrub is uninterrupted)
  - [ ] `film-timeline.ts`, `board-position.ts`, `film-track.tsx`, `use-seek-preview.ts`, `film-scoreboard.tsx` and `film-court-card.tsx` are not in the diff; `seek`, `mark`, `land`, `settle` and the keydown switch in `film-fullscreen.tsx` are untouched; `npm run lint`, `npm run typecheck`, and `npx playwright test tests/film-playback-refresh.spec.ts tests/film-board-position.spec.ts` pass
- **notes:** Why no z-index work is expected here: the scoreboard (`film-fullscreen.tsx:~1468`) and `FilmCourtLayer` (`~1500`) precede the transport's positioning layer (`~1544`) in DOM order and set no z-index; the only z-index under the room's overlays is the court readout's `z-[3]` (`film-court-card.tsx:474`), and T2's box carries `z-10` inside a layer that creates no stacking context (`opacity-100` while the chrome is up, no `z-`), so the box already paints above both — the probe in criterion 3 is the proof, and if it fails, the fix is a z-index on the box or its layer, never on the board or court. `elementFromPoint` ignores `pointer-events: none`, which is why the probe flips it on for one call. The lane sits inside `right-[320px]` while the drawer is open, so the clamp is measured against the slider's own rect, not the room's. Keep `film-fullscreen.tsx` edits to the transport call site — the file is ~1600 lines and a sweep through it blows the subagent's context.
