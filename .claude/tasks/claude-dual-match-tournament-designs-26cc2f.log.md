# Run log — claude/dual-match-tournament-designs-26cc2f

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

> The T1–T25 log for this branch was deleted with the add-event-polish
> workspace; it is preserved in git history at `c4f896a`.

## T26 · Add the date libraries and the ISO conversion module — done
- **gate:** mechanical (lint, tsc, npm test) green — 668 specs, 7 of them new; `task-completion-reviewer` VERDICT: pass, all five criteria met and no scope creep. Guardrails skipped, both legitimately: the diff touches `package.json`, `package-lock.json`, `src/lib/ui/` and `tests/` only — no `src/app/dashboard/`, no `src/components/dashboard/`, no upload wizard, so `pipeline-guardrails-reviewer` had no surface; no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/` and no new table, view or query, so `rls-boundary-reviewer` had none either.
- **changed:** Added `react-aria-components ^1.21.1` and `@internationalized/date ^3.12.4`; `npm ci` verified from the updated lockfile. New `src/lib/ui/date-value.ts` holds the feature's only two string↔object conversions — `parseIsoDate` and `formatIsoDate` — React-free and with no `"use client"`, so a server-side caller can normalise a date without pulling react-aria into a Server Component. New `tests/date-value.spec.ts` covers round-trip, empty, malformed, the two impossible February dates and a real leap day.
- **follow-ups:**
  1. `src/lib/ui/date-format.ts` already does display-side formatting. Once the component lands, check the two are not growing overlapping parsers — a shared ISO regex may be the right consolidation.
  2. `npm audit` reports 3 moderate advisories. They pre-date this install; neither new package added one.

## T27 · Build the `DateField` primitive — done
- **gate:** mechanical (lint, tsc, 668 specs) green with the preview route gone; `task-completion-reviewer` VERDICT: pass, all six criteria met and scope exactly the one declared file. The reviewer independently confirmed the two token substitutions were grounded — `--radius-input` and `--ease-out` exist nowhere in `src/styles/design-system/`, so `rounded-[6px]` and `--ease-out-expo` are corrections, not inventions. Guardrails skipped, both legitimately: the diff is one new file under `src/components/ui/`, which is neither `src/app/dashboard/`, `src/components/dashboard/` nor the upload wizard, so `pipeline-guardrails-reviewer` had no surface — T32 carries the wizard and will face it — and nothing under `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/` changed, so `rls-boundary-reviewer` had none.
- **changed:** New `src/components/ui/date-field.tsx`: the product's date primitive on react-aria's `DatePicker`, taking and emitting `YYYY-MM-DD` strings only through T26's converters. Three chromes — `underline` (34px, the family's height), `bare` (the parent row owns the rule), `boxed` (30px) — plus `min`/`max`, `disabled`, `required` and a `handleRef` exposing `focus()` for the match-edit dialog's focus-first-invalid map. The only focus code is `data-focus-ring="none"` on each segment, earned by the blue fill being a real on-focus change; the calendar button and day cells inherit `focus.css` untouched. The calendar popover wears `FloatMenu`'s surface by value and says in the header why it cannot import it: `role="menu"` will not hold a grid. Verified on a dev server through a throwaway route that was deleted before the gate.
- **follow-ups:**
  1. `validationBehavior="aria"` was needed: react-aria's native default only reports a bound violation on form submit, so a typed out-of-range date never set `data-invalid` without it. Measured, not assumed.
  2. The design document names `--radius-input` and `--ease-out`, neither of which exists. Worth correcting there, or adding the aliases, so the next reader does not chase them.
  3. T31's `FieldCell` focus pairing and T32's `spinbutton` case in `isFormControl` are both still outstanding and both already written into their task blocks — verified here only against a stand-in wrapper.

## T28 · Migrate the statistics from/to dates — done
- **gate:** mechanical (lint, tsc, npm test) green. The subagent reported `tests/claim-eyebrow-width.spec.ts` failing and called it pre-existing drift; the dispatcher re-ran the suite rather than take that claim, and it passed — live-database row-count drift between two paged reads, not this diff. `task-completion-reviewer` VERDICT: pass, all four criteria met, scope exactly the one declared file. `pipeline-guardrails-reviewer` ran (the diff touches `src/components/dashboard/`) and returned an explicit all-clear: no wizard field, no workspace query, no role check, no `canSubmitVideo` path, no analysis short-circuit, no provider-naming string, and the swap preserves the `YYYY-MM-DD` / `""` contract the filter logic already assumed. `rls-boundary-reviewer` skipped legitimately — nothing under `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/`, and no new table, view or query.
- **changed:** Both filter dates in `match-selector.tsx` are now `DateField variant="boxed"`, with the old `aria-label`s carried through the primitive's `label` prop and the old chrome classes dropped — the variant draws them. The `→` and the "N matches in range" count are untouched, and so is the filter predicate. Mutual bounds added: `max` on *from* is the current *to*, `min` on *to* is the current *from*, each passed as `undefined` rather than `""` when its counterpart is empty, so an inverted range is now unpickable rather than silently empty.
- **follow-ups:**
  1. The mutual bounds are the behaviour addition the design flagged as an open question. They are in as instructed; striking them later is two props and nothing else.

## T29 · Migrate the profile birthdate — blocked
- **gate:** mechanical (lint, tsc, 668 specs) green. Failed at **completion review**: `task-completion-reviewer` returned VERDICT: needs-work on the fourth criterion. Guardrails not run — the gate stops at the first failure.
- **why:** The fourth criterion asks that Save be verified to persist "the same `YYYY-MM-DD` the field showed". `/dashboard/settings/profile` needs a real session, which the sandbox has none of, so the subagent verified through a throwaway unauthenticated preview route with fixture props: the empty state, the placeholder colour, the 2px blue emphasis rule, the Missing marker, and typing clearing both were all observed with real key events. Save could not be: it returned "Not signed in", so the persistence half of the criterion was never checked end to end. The reviewer judged the wire contract makes a mismatch unlikely but declined to call an unverified claim met, which is the correct reading of a criterion written as one verified statement.
- **what the reviewer passed:** the first three criteria, explicitly. It confirmed `DateField variant="underline"` replaces `SettingsUnderlineInput` with no `type="date"` left, that `max` is today and disables later days, and that the emphasis rule works — the call site's `[&_[role=group]]` selector at specificity (0,2,0) does beat the primitive's own `border-b`, and each segment keeps its independent blue fill, so the field never has zero focus indicators nor two stacked. It also judged the added `missing` prop to be the minimal way to make the third criterion true rather than scope creep: the page's "N fields left" strip already counted an empty birthdate, but the inline marker had never been wired for that field.
- **stash:** 30b5728e715b8571a2f49ad1a26b61cbf3a0c7f1 — `git stash apply 30b5728e715b8571a2f49ad1a26b61cbf3a0c7f1` restores the work. It is one file, `profile-form.tsx`, and nothing about it is known to be wrong.
- **to unblock, one of:** give the profile page a session in this environment and re-verify Save; or amend the fourth criterion to what an unauthenticated harness can observe, since the persistence half asserts behaviour this migration does not change — `saveProfile` and the draft shape are untouched by the diff.
