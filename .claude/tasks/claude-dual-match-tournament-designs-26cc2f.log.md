# Run log — claude/dual-match-tournament-designs-26cc2f

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

> The T1–T25 log for this branch was deleted with the add-event-polish
> workspace; it is preserved in git history at `c4f896a`.

## T26 · Add the date libraries and the ISO conversion module — done
- **gate:** mechanical (lint, tsc, npm test) green — 668 specs, 7 of them new; `task-completion-reviewer` VERDICT: pass, all five criteria met and no scope creep. Guardrails skipped, both legitimately: the diff touches `package.json`, `package-lock.json`, `src/lib/ui/` and `tests/` only — no `src/app/dashboard/`, no `src/components/dashboard/`, no upload wizard, so `pipeline-guardrails-reviewer` had no surface; no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/` and no new table, view or query, so `rls-boundary-reviewer` had none either.
- **changed:** Added `react-aria-components ^1.21.1` and `@internationalized/date ^3.12.4`; `npm ci` verified from the updated lockfile. New `src/lib/ui/date-value.ts` holds the feature's only two string↔object conversions — `parseIsoDate` and `formatIsoDate` — React-free and with no `"use client"`, so a server-side caller can normalise a date without pulling react-aria into a Server Component. New `tests/date-value.spec.ts` covers round-trip, empty, malformed, the two impossible February dates and a real leap day.
- **follow-ups:**
  1. `src/lib/ui/date-format.ts` already does display-side formatting. Once the component lands, check the two are not growing overlapping parsers — a shared ISO regex may be the right consolidation.
  2. `npm audit` reports 3 moderate advisories. They pre-date this install; neither new package added one.

## T27 · Build the `DateField` primitive — done
- **gate:** mechanical (lint, tsc, 668 specs) green with the preview route gone; `task-completion-reviewer` VERDICT: pass, all six criteria met and scope exactly the one declared file. The reviewer independently confirmed the two token substitutions were grounded — `--radius-input` and `--ease-out` exist nowhere in `src/styles/design-system/`, so `rounded-[6px]` and `--ease-out-expo` are corrections, not inventions. Guardrails skipped, both legitimately: the diff is one new file under `src/components/ui/`, which is neither `src/app/dashboard/`, `src/components/dashboard/` nor the upload wizard, so `pipeline-guardrails-reviewer` had no surface — T32 carries the wizard and will face it — and nothing under `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/` changed, so `rls-boundary-reviewer` had none.
- **changed:** New `src/components/ui/date-field.tsx`: the product's date primitive on react-aria's `DatePicker`, taking and emitting `YYYY-MM-DD` strings only through T26's converters. Three chromes — `underline` (34px, the family's height), `bare` (the parent row owns the rule), `boxed` (30px) — plus `min`/`max`, `disabled`, `required` and a `handleRef` exposing `focus()` for the match-edit dialog's focus-first-invalid map. The only focus code is `data-focus-ring="none"` on each segment, earned by the blue fill being a real on-focus change; the calendar button and day cells inherit `focus.css` untouched. The calendar popover wears `FloatMenu`'s surface by value and says in the header why it cannot import it: `role="menu"` will not hold a grid. Verified on a dev server through a throwaway route that was deleted before the gate.
- **follow-ups:**
  1. `validationBehavior="aria"` was needed: react-aria's native default only reports a bound violation on form submit, so a typed out-of-range date never set `data-invalid` without it. Measured, not assumed.
  2. The design document names `--radius-input` and `--ease-out`, neither of which exists. Worth correcting there, or adding the aliases, so the next reader does not chase them.
  3. T31's `FieldCell` focus pairing and T32's `spinbutton` case in `isFormControl` are both still outstanding and both already written into their task blocks — verified here only against a stand-in wrapper.

## T28 · Migrate the statistics from/to dates — done
- **gate:** mechanical (lint, tsc, npm test) green. The subagent reported `tests/claim-eyebrow-width.spec.ts` failing and called it pre-existing drift; the dispatcher re-ran the suite rather than take that claim, and it passed — live-database row-count drift between two paged reads, not this diff. `task-completion-reviewer` VERDICT: pass, all four criteria met, scope exactly the one declared file. `pipeline-guardrails-reviewer` ran (the diff touches `src/components/dashboard/`) and returned an explicit all-clear: no wizard field, no workspace query, no role check, no `canSubmitVideo` path, no analysis short-circuit, no provider-naming string, and the swap preserves the `YYYY-MM-DD` / `""` contract the filter logic already assumed. `rls-boundary-reviewer` skipped legitimately — nothing under `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/`, and no new table, view or query.
- **changed:** Both filter dates in `match-selector.tsx` are now `DateField variant="boxed"`, with the old `aria-label`s carried through the primitive's `label` prop and the old chrome classes dropped — the variant draws them. The `→` and the "N matches in range" count are untouched, and so is the filter predicate. Mutual bounds added: `max` on *from* is the current *to*, `min` on *to* is the current *from*, each passed as `undefined` rather than `""` when its counterpart is empty, so an inverted range is now unpickable rather than silently empty.
- **follow-ups:**
  1. The mutual bounds are the behaviour addition the design flagged as an open question. They are in as instructed; striking them later is two props and nothing else.

## T29 · Migrate the profile birthdate — blocked
- **gate:** mechanical (lint, tsc, 668 specs) green. Failed at **completion review**: `task-completion-reviewer` returned VERDICT: needs-work on the fourth criterion. Guardrails not run — the gate stops at the first failure.
- **why:** The fourth criterion asks that Save be verified to persist "the same `YYYY-MM-DD` the field showed". `/dashboard/settings/profile` needs a real session, which the sandbox has none of, so the subagent verified through a throwaway unauthenticated preview route with fixture props: the empty state, the placeholder colour, the 2px blue emphasis rule, the Missing marker, and typing clearing both were all observed with real key events. Save could not be: it returned "Not signed in", so the persistence half of the criterion was never checked end to end. The reviewer judged the wire contract makes a mismatch unlikely but declined to call an unverified claim met, which is the correct reading of a criterion written as one verified statement.
- **what the reviewer passed:** the first three criteria, explicitly. It confirmed `DateField variant="underline"` replaces `SettingsUnderlineInput` with no `type="date"` left, that `max` is today and disables later days, and that the emphasis rule works — the call site's `[&_[role=group]]` selector at specificity (0,2,0) does beat the primitive's own `border-b`, and each segment keeps its independent blue fill, so the field never has zero focus indicators nor two stacked. It also judged the added `missing` prop to be the minimal way to make the third criterion true rather than scope creep: the page's "N fields left" strip already counted an empty birthdate, but the inline marker had never been wired for that field.
- **stash:** 30b5728e715b8571a2f49ad1a26b61cbf3a0c7f1 — `git stash apply 30b5728e715b8571a2f49ad1a26b61cbf3a0c7f1` restores the work. It is one file, `profile-form.tsx`, and nothing about it is known to be wrong.
- **to unblock, one of:** give the profile page a session in this environment and re-verify Save; or amend the fourth criterion to what an unauthenticated harness can observe, since the persistence half asserts behaviour this migration does not change — `saveProfile` and the draft shape are untouched by the diff.

## T30 · Migrate the dual facts Date cell — blocked
- **gate:** mechanical (lint, tsc, 668 specs including `schedule-static-copy` and the `MAP.md` staleness spec) green. Failed at **completion review**: `task-completion-reviewer` returned VERDICT: needs-work on the fourth criterion. Guardrails not run — the gate stops at the first failure.
- **why:** Same shape as T29, and the same root cause. The fourth criterion is one conjunctive sentence ending "and Continue stays disabled until the date is set". The dual flow needs a session, so the subagent rendered `DualFactsStep` through a throwaway unauthenticated route and said plainly it never saw the real Continue button — it verified the gate's input instead, mirroring `new-dual-flow.tsx`'s exact expression beside the field (empty → `""` → disabled; a picked day → the matching `YYYY-MM-DD` → enabled). The reviewer accepted the observed halves and refused the unobserved one. Correct reading of the criterion as written; the criterion is what is wrong.
- **what the reviewer passed:** the other four criteria explicitly — `DateField variant="bare"` in place with every webkit hack string gone, `FieldCell`'s `glyph` prop and the `Calendar` import deleted, the doc comment rewritten, and the suite green.
- **the unnamed change, reviewed in full and endorsed:** the subagent also changed `FieldCell`'s `rule` branch from `<label>` to `<div>`, and this was a real bug it found by measurement, not a liberty. A `DateSegment` is a plain `[tabindex]` div and so is not a labelable element, while the primitive's calendar trigger is a real `<button>` — so the `<label>` forwarded every segment click to that button and the month segment could not be clicked into. The reviewer confirmed the mechanism against the HTML spec, confirmed the fix matches the precedent already set in `match-selector.tsx`, and confirmed nothing regresses: naming still flows through `DateField`'s `aria-label`, `:focus-within` applies to a div identically, and Site/Surface/Format were already `chrome="none"` divs, so Date is the only cell the swap touches.
- **stash:** 131aeba14147ce0e1b93f7e16aceddc3efb637c8 — `git stash apply 131aeba14147ce0e1b93f7e16aceddc3efb637c8` restores it. One file, and no part of it is known to be wrong.
- **to unblock, one of:** a session for the dual flow so the real Continue button can be observed; or split that criterion so the glyph, picker and focus halves stand separately from the Continue-button half, which asserts behaviour in `new-dual-flow.tsx` that this diff does not touch.

## T31 · Migrate the tournament builder's Starts and Ends — done
- **gate:** mechanical (lint, tsc, 668 specs including `schedule-static-copy` and the `MAP.md` staleness spec) green. `task-completion-reviewer` VERDICT: pass on all five criteria, and it checked the three things it was asked to scrutinise: the `focus.css` edit is comment-only (both hunks inside the header block, no selector or rule touched — the dispatcher confirmed this independently before dispatch), both selects genuinely carry an accessible name rather than an intended one, and the new `data-focus-ring="none"` on `FieldSelect` is earned now that the cell answers focus. `pipeline-guardrails-reviewer` ran (the diff touches `src/components/dashboard/`) and returned an explicit all-clear: it traced the format vocabulary end to end and confirmed `FORMATS.find(...)` → `onEdit({ format: chosen })` is byte-identical, `adScoring` untouched, and `min` constrains the picker without transforming the emitted string.
- **changed:** Both tournament dates are `DateField variant="bare"`, with `min` on Ends the Starts value (passed `undefined`, never `""`). This file's `FieldCell` now answers focus with the dual builder's rule verbatim — the pairing the task called non-optional, since its hairline never changed and the primitive's segments opt out of the ring.
- **the two traps, both handled:** The cell was a `<label>`, the same trap T30 measured — a date segment is not a labelable element, so the wrapper forwarded segment clicks to the calendar button. Unlike the dual builder's cell, this one was also the accessible name for two native selects that carry none of their own, so the fix could not be a bare element swap: `FieldSelect` gained an `aria-label`, verified present in the accessibility tree rather than assumed from the markup.
- **declared deviation:** `src/styles/design-system/focus.css`, comments only. Two passages there named the tournament builder's selects among controls that keep the standard ring *because their hairline never changes* — a premise this diff removes. The file's count of remaining ring-drawing native selects went five to three. The reviewer judged correcting it in the same diff right, since leaving a design-system document contradicted by the code it describes is worse than a same-diff fix that changes no behaviour.
- **follow-ups:**
  1. When a date cell's calendar button is focused, the button keeps its own ring while the row's rule is simultaneously blue — two marks on that one control. It is the primitive's own documented decision, identical in the `underline` family, and both reviewers agreed a single-file task should not silently edit a shared primitive to change it. Worth settling once in `date-field.tsx`.
  2. `FieldCell`'s rule does not answer `data-invalid`, so an out-of-range Ends reads as invalid only to assistive technology, not on screen. The dual builder's cell has the same gap, and `DateField`'s `underline` variant already has the red-rule treatment to copy.
  3. Nobody traced whether `new-tournament-flow.tsx`'s submit gate refuses an out-of-range `endsOn`. The primitive reports the value upward by design and the call site is the authority, so that gate is where the answer lives.

## T32 · Migrate the upload wizard's date cell — done
- **gate:** mechanical (lint, tsc, 669 specs) green. `task-completion-reviewer` VERDICT: pass on all five criteria, having checked the four things it was asked to: the walk-selector widening is a real fix (it read react-aria's `HiddenDateInput` source and confirmed the hidden input is `position: fixed`, so it kept a non-null `offsetParent` and was matched by the old clause), `formatDateRead` had no remaining callers before deletion, the time input's opt-out is earned by its own rule going 2px blue on `:focus`, and `Cell`'s new `tag` prop renders nothing for the cells that omit it. `pipeline-guardrails-reviewer` ran as its own stage — the fifth criterion names it — and returned an explicit all-clear: the five vendor-required fields untouched, §4's three corrupting inputs nowhere in the diff, and `job-request.ts` confirmed to have no dependency on the match date or time at all. It agreed the change sits inside §3.5's permission to redesign the wizard's step presentation.
- **changed:** The wizard's date cell is no longer a Radix popover holding two native inputs. The cell holds `DateField variant="underline"` and the still-native time input side by side under one label, which removes a click from the wizard's most-used step. Nesting react-aria's popover inside Radix's is what this avoids — two focus scopes and two dismiss layers on one control. `max` is today's **local** date now, via `useSyncExternalStore` with an undefined server snapshot: the old `toISOString().slice(0,10)` was UTC, which is the wrong day for anyone west of Greenwich in the evening. `formatDateRead` deleted with its last caller.
- **two keyboard fixes:** `isFormControl` now recognises `role="spinbutton"`, without which Enter typed inside a date segment advanced the whole step. And the ⌘/Ctrl+Enter focus walk now excludes `tabindex="-1"` on every element kind rather than only the bare `[tabindex]` clause — react-aria renders a visually-hidden `<input type="date" tabindex="-1">` for form submission, and the chord was landing on it and needing a second press. That second fix was found in verification, not planned, and it also repairs the same stall in T31's already-shipped tournament dates.
- **declared deviation:** `src/styles/design-system/focus.css`, doc comment only — the wizard's time input joins that file's registry of earned opt-outs.
- **follow-ups:**
  1. The date and time now draw two hairlines with a gap between them. If the design wants one continuous rule under the cell, the row could own it the way the tournament builder's `FieldCell` does.
  2. A segmented time primitive matching `DateField` is the natural next step now that a native time input sits beside a react-aria field. Deliberately out of scope here.
  3. The design skill's opt-out table could list the wizard's time input; the skill file was left alone, and T34 is where it would go.

## T33 · Migrate the match-edit dialog's date — done
- **gate:** mechanical (lint, tsc, 669 specs) green. `task-completion-reviewer` VERDICT: pass on all four criteria, having checked the three things it was asked to. `pipeline-guardrails-reviewer` ran (the diff touches `src/components/dashboard/`, and this dialog writes to a stored match) and returned an explicit all-clear: the PATCH payload's player and score fields are byte-for-byte unchanged in shape and ordering, only the mechanism setting `date` moved, and it confirmed no timezone-bearing `Date` crosses the boundary — `CalendarDate` is a calendar-day type, so the classic off-by-one that resaves a match on the previous day cannot occur here.
- **changed:** The dialog's Date is `DateField variant="bare"` inside `UnderlineField`, which keeps drawing the rule and the error colour. `dateRef` is a `DateFieldHandle`, and `fieldRefs` is a union of the two ref shapes with a `focusField` helper branching on `instanceof HTMLElement` — no `any`, and the reviewer confirmed the branch is exhaustive rather than merely type-checking, since a handle is a plain object and never an element.
- **a real bug found, fixed in the primitive (declared deviation from `files:`):** Radix's `Dialog` sets `pointer-events: none` on `<body>` while open, and the calendar popover portals to `body` — so inside this dialog every day cell inherited `none` and the calendar was completely mouse-dead while looking perfectly normal. Measured up the ancestor chain, with Playwright reporting `<html>` intercepting the click. The popover is not reachable from the call site, so the one-line `pointer-events-auto` belongs in `date-field.tsx`. The reviewer confirmed the three already-shipped call sites are none of them inside a Radix dialog, so restoring it is safe for all of them.
- **a behaviour addition, judged necessary:** `handleSubmit` gained a client-side empty-date guard. The native `required` input used to let the browser block the submit; the primitive reports validity through ARIA only, so without the guard a cleared date would post and depend on the server to reject it. Its message and field key match the API's own `badRequest("Date is required.", "date")` exactly, so it duplicates without conflicting, and the server stays the authority.
- **follow-ups:**
  1. The date's segments render at 13px while the rest of this dialog runs at 14px — visible beside "Round". Matching it needs a type-size escape hatch on the primitive, which is a primitive-wide decision rather than a call-site one.
  2. The old input carried `aria-invalid`, and the dialog's error text is not wired to the field with `aria-describedby`. `DateField` takes neither. Worth closing in the primitive across all four call sites.
  3. The three still-unmigrated native selects named in `focus.css` — two in `schedule/add-result-dialog.tsx`, one in `schedule/add-result-row.tsx` — remain outstanding.

## T29 · Migrate the profile birthdate — done (re-run after amendment)
- **gate:** mechanical (lint, tsc, 669 specs) green. `task-completion-reviewer` VERDICT: pass on all four criteria, judged against the amended fourth. `pipeline-guardrails-reviewer` ran and returned an explicit all-clear: contract-preserving on the value shape and on `SettingsField`'s API for every other consumer.
- **why it took two runs:** the first run was gated on the original criterion, which ended "Save persists the same `YYYY-MM-DD`" — a claim needing a logged-in session, asserting `saveProfile` and the draft shape, neither of which this migration touches. The author amended it on 2026-09-08 to ask for the value reaching the save call. The re-run captured the actual Next server-action POST body rather than stubbing: typing gave `"birthdate":"2000-06-15"`, picking September 4 2026 from the calendar gave `"birthdate":"2026-09-04"`, each the date the segments displayed.
- **a real defect the first run had dismissed:** it reported that clicking a month segment left focus on the calendar button, and called it a Playwright quirk. It was not. `SettingsField` wraps its children in a `<label>`, a `DateSegment` is not a labelable element, and the primitive's calendar trigger is a real button — so the label forwarded every segment click to it and a user could reach the month and day only by Tab. This is the same trap T30 and T31 hit in two other files, and its signature was in the report. The dispatcher caught it before committing and sent the work back.
- **the fix, and its blast radius:** `SettingsField` gained an opt-in `labelless` prop rendering a `<div>` wrapper, used only by the date branch. A bare element swap was not available here: the `<label>` is what names several other settings fields, exactly as it was in T31's cell. Both reviewers confirmed the call-site list is complete — `team-identity-card`, `edit-player-dialog`, `roster-invite-dialog`, `add-player-dialog`, `merge-profiles-dialog` — and that none passes the prop, so all keep the label and their names. After the fix, clicking each of the month, day and year segments focuses that segment and typed digits land in it.
- **declared deviation:** `src/components/dashboard/settings/settings-card.tsx`, authorized by the dispatcher mid-run once the defect was confirmed.
- **follow-ups:**
  1. The emphasis rule still reaches the primitive's internal `role="group"` wrapper with a descendant selector, because `DateField` has no `emphasis` prop. Reviewed twice as correct, but growing the prop would be cleaner and touches one file.
  2. Clicking rather than focusing a date segment behaved oddly under Playwright even after the fix. Worth knowing before someone writes an end-to-end spec that clicks into a date.

## T30 · Migrate the dual facts Date cell — done (re-run after amendment)
- **gate:** mechanical (lint, tsc, 669 specs including `schedule-static-copy`) green. `task-completion-reviewer` VERDICT: pass on all five criteria against the amended fourth, having verified rather than accepted the two claims it was asked to check: that `FieldCell` passes its label down to each child, so `DateField` and `MenuSelect` both set it as their own `aria-label` and naming does not regress, and that the Site, Surface and Format cells were already on the `chrome="none"` branch, which was a `<div>` before this diff and is untouched by it. `pipeline-guardrails-reviewer` returned an explicit all-clear: the format vocabulary and `adScoring` do not appear in the diff at all, and the date's shape and flow are unchanged.
- **why it took two runs:** the first run was gated on the original criterion ending "Continue stays disabled until the date is set" — a button in `new-dual-flow.tsx`, behind a session, in a file this task does not touch. The author amended it on 2026-09-08 to ask for the value that gate reads. No code changed between the runs.
- **the re-run:** verified against the current tree rather than the one the work was written on, since `pointer-events-auto` and the `useWizardKeys` changes had landed in between. Observed through a harness mirroring `new-dual-flow.tsx`'s gate expression verbatim: empty gave `""` and disabled, typing gave `"2026-09-26"` and enabled, clicking day 14 in the calendar set `"2026-09-14"` and closed the popover. One glyph in the cell. A focused segment left the row's rule at 2px blue with no outline and no shadow. And the `<label>` fix was confirmed by clicking the month segment and reading where focus landed — the same check a sibling task got wrong by reasoning about it instead.
- **follow-up worth a decision at the primitive:** clearing one segment of a complete date leaves the field reading `mm/14/2026` on screen but fires no `onChange`, so the draft keeps the old date and the Continue gate stays enabled. That is react-aria's contract — it reports complete dates or an explicit null — and it affects every call site equally, so both the subagent and the reviewer judged it out of scope here. A coach who backspaces one segment sees an unfinished date while the flow still holds the old one. Worth deciding in `date-field.tsx`.
