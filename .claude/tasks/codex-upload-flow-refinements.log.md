# Run log — codex/upload-flow-refinements

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Reproduce multi-set score loss — blocked

**gate:** Mechanical: lint and tsc passed; full keyless npm test passed (632 passed, 61 skipped). Initial Chromium launches were sandbox-denied; rerun with launch permission passed. Completion: VERDICT: needs-work. Tests called buildMatchData directly with manually seeded arrays, without mounting the real wizard, importing a fixture, entering score cells, or intercepting an actual wizard submission. Preset/video helper cases likewise did not exercise the required paths. Pipeline and RLS guardrails were not dispatched: completion failed first; changed production surfaces: none.

**changed:** Attempted utility-level regression test preserved in stash `7655ffd7af541b1284f2e0ea715702c82e3c25e0`; no task code committed. It demonstrates possible stale numberOfSets submission truncation, not the required UI reproduction. Task implementation and completion review used GPT-5.6 Terra because GPT-5.3 Codex Spark is unavailable in this session's subagent tool. Retry needs actual wizard coverage. No production writes, uploads, or vendor calls occurred.

## T5 · Define completion and import identity rules — blocked

**gate:** Mechanical: lint, tsc, and npm test passed after running the test step with empty Supabase credentials (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` set to empty, escalated launch to allow browser execution). Completion review: VERDICT: needs-work due one criterion. `pipeline-guardrails-reviewer` and `rls-boundary-reviewer` were not dispatched (completion failed first).

**changed:** Added shared identity/completion validation contracts in `src/components/dashboard/matches/new-match-wizard/types.ts`, new helpers in `src/components/dashboard/matches/new-match-wizard/validation.ts`, and coverage in `tests/upload-validation.spec.ts`; blocked by missing assertion that successful/failed identity matching and confirmation preserve attribution IDs, so changes were stashed to `6366f4a6717e7bc82ee168ebb294372ca4cf8c5e`.

## T1 · Reproduce multi-set score loss — blocked

**gate:** Mechanical: lint, tsc, and the focused regression spec passed; full keyless npm test passed after rerunning outside the macOS Chromium sandbox (622 passed, 61 skipped). Completion: VERDICT: needs-work. The test calls `buildMatchData` with manually seeded arrays instead of mounting the real wizard, loading a fixture, entering sets 2 and 3, checking focus, and intercepting the wizard submission boundary. Pipeline and RLS guardrails were skipped because the completion stage failed first.

**changed:** Preserved the utility-level regression test in stash `dd56d35fdbfb37cd201e339f020850989fe45fe9`; no task code was committed. The test documents score truncation at the submission builder but does not satisfy the required real-wizard reproduction. No upload, production database write, or vendor job occurred. GPT-5.3 Codex Spark was requested by the task but was unavailable to this session's subagent tooling, so the prior implementation/review used the available fallback.

## T8 · Align shared selected-option styling — done

**gate:** Mechanical: lint, tsc, and the full keyless npm test passed; Chromium was rerun outside the macOS sandbox after the sandbox-only launch denial. Completion: VERDICT: pass. Pipeline guardrails were skipped because the diff does not touch `src/app/dashboard/`, `src/components/dashboard/`, or the upload wizard. RLS guardrails were skipped because the diff does not touch Supabase, data, API, migration, table, view, or query surfaces.

**changed:** Updated `FloatMenuItem` so a chosen option is marked by its Signal Blue check without a persistent or pointer-hover grey fill, while unchosen pointer hover and keyboard focus feedback remain. Updated the canonical Dropdown / Menu documentation and added focused coverage for selected-option and action-menu semantics. Semantic success glyphs and white radio checks are unchanged. The implementation and completion review used GPT-5.6 Terra at medium because GPT-5.3 Codex Spark is unavailable in this session's collaboration tool.

## T9 · Order providers and align source selections — blocked

**gate:** Mechanical: lint, tsc, and the full keyless npm test passed; Chromium was rerun outside the macOS sandbox after the sandbox-only launch denial. Completion: VERDICT: needs-work because selected athlete/entity rows lost their persistent wash but still render no Signal Blue check, so the shared chosen-row treatment is incomplete. Pipeline and RLS guardrails were not dispatched because the completion stage failed first.

**changed:** Attempted provider ordering, selected source/entity styling, label wrapping protection, and focused source coverage are preserved in stash `515e1654de13f31db1580f7ee515dd8554a24827`; no task code was committed. Retry must add the shared blue-check indicator to chosen athlete/entity rows and prove it in coverage while retaining source-link, draft, preset, and action behavior. GPT-5.3 Codex Spark was requested but unavailable in this session's collaboration tool, so GPT-5.6 Terra medium was used.

## T1 · Reproduce multi-set score loss — done

**gate:** Mechanical: lint, tsc, and the full keyless npm test passed; Chromium was rerun outside the macOS sandbox after the sandbox-only launch denial. The focused real-wizard browser suite passed all 3 import, video, and preset cases. Completion: VERDICT: pass. Pipeline guardrails: CLEAR, no findings; the harness is production-inaccessible and changes no parser, attribution, trim, upload, or vendor behavior. RLS guardrails were skipped because no Supabase/data/API/migration/query boundary changed.

**changed:** Added a development-only wizard reproduction route that mounts the real `UploadMatchFlow`, regenerated `MAP.md`, and replaced the utility simulation with fixture-backed Playwright coverage. One-set import and preset paths show focus advancing while sets 2 and 3 are discarded and submissions contain `[6,0,0]` / `[4,0,0]`; the video control retains and submits `[6,6,6]` / `[4,3,2]`. The test runs only against localhost and intercepts auth and match creation while blocking all other upload, Azure, vendor, and external traffic. No runtime fix was included. GPT-5.3 Codex Spark was unavailable in this session's collaboration tool, so GPT-5.6 Terra medium was used.

## T5 · Define completion and import identity rules — blocked

**gate:** Mechanical: lint, tsc, the focused 9-test validation suite, and the full keyless npm test passed; Chromium was rerun outside the macOS sandbox after the sandbox-only launch denial. Completion: VERDICT: needs-work because `IdentityConfirmationScope` and `buildImportIdentityConfirmationKey()` omit `importedAthleteId`, allowing a changed imported attribution ID with the same normalized name to retain stale confirmation. Pipeline and RLS guardrails were not dispatched because the completion stage failed first.

**changed:** The restored validation contracts and new attribution-preservation assertions are preserved in stash `3bfb6bc212b46e8d9078478bd4b9f72fbbd67c4b`; no task code was committed. The retry now proves matching, mismatch, and confirmation paths preserve both attribution IDs, but the confirmation key must also bind `importedAthleteId` and invalidate when it changes.

## T5 · Define completion and import identity rules — done

**gate:** Mechanical: lint, tsc, the focused 9-test validation suite, and the full keyless npm test passed; Chromium was rerun outside the macOS sandbox after the sandbox-only launch denial. Completion: VERDICT: pass. Pipeline guardrails: CLEAR, no findings. RLS guardrails were skipped because no Supabase/data/API/migration/query boundary changed.

**changed:** Added pure completion requirements for both players' required hand/backhand values; normalized import identity comparison; and confirmation keys bound to file generation, workspace, athlete, imported athlete ID, and normalized imported identity. Tests cover case/whitespace matching, missing/initial/nickname/punctuation/different-name confirmation, every key invalidation, empty-name behavior, and preservation of both attribution IDs. No UI, parser, API, database, or runtime wiring changed.

## T2 · Preserve newly entered score sets — done

**gate:** Mechanical: lint and tsc passed; the full keyless npm test passed after rerunning outside the macOS sandbox, with live-database tests skipped. Focused score-state and validation coverage passed 44 tests, and the real-wizard import, video, and preset regression suite passed all 3 cases. Completion: VERDICT: pass. Pipeline guardrails: CLEAR, no findings. RLS guardrails were skipped because no Supabase/data/API/migration/query boundary changed.

**changed:** Added an atomic score-state transition that null-pads short arrays, preserves other cells and meaningful zeroes, retains scoring bounds, and advances the active set count with the first entered digit. Wired the wizard to that transition and added focused coverage through set five, clearing, tiebreaks, bounds, submitted arrays, and the T1 real-wizard paths. Preset/import provenance and parsers are unchanged.

**follow-ups:** 1. T3 can remove the redundant ghost-cell `onSetsChange` call and use the atomic transition while repairing focus behavior.

## T3 · Repair game-score focus order — blocked

**gate:** Mechanical: lint and tsc passed; the full keyless npm test passed after rerunning outside the macOS sandbox, with live-database and localhost-harness tests skipped. Completion: VERDICT: needs-work because the focused browser run timed out in the existing source-selection helper before any new focus assertions executed, so the browser-proof criterion remains unmet. Pipeline and RLS guardrails were not dispatched because completion failed first.

**changed:** The attempted ScoreBlock focus-order fix and browser assertions for correction selection, invalid/cleared no-advance behavior, ghost-set mounting, tiebreak entry, and final-cell retention are preserved in stash `d07abfbca6e152ff20d440f420a7631f5e9f8424`; no task code was committed. Retry must repair the harness/source-selection setup and run the focused browser case through its score assertions. GPT-5.3 Codex Spark was unavailable in this session's collaboration tool, so GPT-5.6 Terra medium was used.

## T3 · Repair game-score focus order — done

**gate:** Mechanical: lint and tsc passed; the full keyless npm test passed after rerunning outside the macOS sandbox, with live-database and localhost-harness tests skipped there. The focused real-wizard browser suite ran separately against `localhost` and passed all 4 cases. Completion: VERDICT: pass. Pipeline guardrails: CLEAR, no findings. RLS guardrails were skipped because no Supabase/data/API/migration/query boundary changed.

**changed:** Restored the saved T3 implementation, corrected the focused harness URL so Next hydrates the interactive flow, and verified game-score focus advances player 1 to player 2 to the next mounted set. Existing values select for correction, clearing and invalid values stay focused, tiebreaks accept multi-digit entry without advancing, and the final game cell neither submits nor targets a nonexistent input. GPT-5.3 Codex Spark was unavailable in this session's collaboration tool, so GPT-5.6 Terra medium was used.

## T4 · Protect scores when reducing the format — done

**gate:** Mechanical: lint and tsc passed; the full keyless npm test passed after rerunning outside the macOS sandbox, with live-database and localhost-harness tests skipped there. The focused real-wizard browser suite ran separately and passed all 6 cases. Completion: VERDICT: pass. Pipeline guardrails: CLEAR, no findings. RLS guardrails were skipped because no Supabase/data/API/migration/query boundary changed.

**changed:** Added an explicit confirmation before a format reduction discards populated game or tiebreak sets. Cancelling preserves the original format and scores; confirming trims only excluded sets; empty reductions proceed directly; and event-owned format/scoring remain read-only. Score edits stay in local wizard state until the existing submission boundary. GPT-5.3 Codex Spark was unavailable in this session's collaboration tool, so GPT-5.6 Terra medium was used.

## T6 · Wire identity confirmation into wizard state — blocked

**gate:** Mechanical: lint and tsc passed; the focused hook/score suite passed 29 tests; and the full keyless npm test passed after rerunning outside the macOS sandbox, with live-database tests skipped. Completion: VERDICT: pass. Pipeline guardrails blocked on one scoring-provenance finding: a preset with no event-owned `adScoring` can preserve the previous import's value when a replacement file supplies a different value. RLS guardrails were skipped because no Supabase/data/API/migration/query boundary changed.

**changed:** The identity-state wiring, stale-generation guards, reset behavior, required-style enforcement, and deterministic hook coverage are preserved in stash `5c280ba5dcb6bb12dba7499f220480c0b1620204`; no task code was committed. Retry must preserve scoring only when the attached event actually supplies it and cover replacement files when event scoring is unspecified.

## T6 · Wire identity confirmation into wizard state — done

**gate:** Mechanical: lint and tsc passed; the focused hook/score suite passed 30 tests; and the full keyless npm test passed after rerunning outside the macOS sandbox, with live-database tests skipped. Completion: VERDICT: pass. Pipeline guardrails: CLEAR, no findings. RLS guardrails were skipped because no Supabase/data/API/migration/query boundary changed.

**changed:** Restored and completed identity confirmation state with original parsed names kept separate, stale async generations rejected, personal/team mismatch gates applied to progression and submission, context changes invalidating confirmation, parser perspective preserved, and required styles enforced at the final handler. The provenance repair now preserves scoring only when the event explicitly owns it; replacement imports can update unspecified scoring, including when an attached line changes during parsing.

## T7 · Show import identity confirmation — blocked

**gate:** Mechanical: lint and tsc passed; the focused validation suite passed 30 tests; the implementation run reported 2 passing real-wizard identity cases; and the full keyless npm test passed after rerunning outside the macOS sandbox, with live-database and localhost-harness tests skipped. Completion: VERDICT: needs-work because browser coverage exercises only the personal harness and does not prove team-workspace copy, missing-name wording, or athlete-ID preservation; the reviewer also could not reproduce the localhost-only run without its server. Pipeline and RLS guardrails were not dispatched because completion failed first.

**changed:** The identity notice, file-step integration, accessible announcement, confirmation/rejection actions, keyboard/button blocking, and personal-flow browser coverage are preserved in stash `7f7ba6ca5ab5a519dd174c620551fd20b676fdc2`; no task code was committed. Retry must add executable real-wizard coverage for team wording, missing imported names, and unchanged athlete IDs across confirmation/name editing. GPT-5.3 Codex Spark was unavailable in this session's collaboration tool, so GPT-5.6 Terra medium was used.

## T11 · Define reusable upload eligibility — blocked

**gate:** Mechanical: lint, `tsc --noEmit`, and the full keyless `npm test` all passed (770 passed, 6 skipped); no stale `.next/` retry was needed. Completion: VERDICT: pass, with all five `done when:` lines matched to specific tests and the five out-of-list call-site edits judged compiler-forced rather than drift. Pipeline guardrails: CLEAR, no findings — the three vendor attribution inputs and `job-request.ts` are absent from the diff, and nothing outside the new module's own test imports it. RLS guardrails: BLOCKED on one finding — the new module's header docstring describes `uploadEligibility()` in present tense as already consulted by "the wizard before it lets someone continue, the wizard again before it writes, and the two video routes," while the function has zero callers outside its test; read on its own, the file claims an enforcement integration that T12–T14 have not built yet. The same reviewer cleared every other boundary: no `admin.ts` import, no migration, no new table or view, the `programStatus` column sourced from the same already-RLS-readable `programs` row as the pre-existing `canSubmitVideo`, and webhook/cron auth untouched.

**changed:** The eligibility contract is preserved in stash `02c3a59856799f42a37947b183ed9abd314c36fe`; no task code was committed. It adds `src/lib/workspace/upload-eligibility.ts` (a pure `uploadEligibility()` reasoner returning nine distinguished refusal reasons, with `approval-unknown`/`roster-unknown` marked retryable, plus `ownRosterIdentity()`), a non-optional `Workspace.programStatus: ProgramStatus | null` in `src/lib/workspace/types.ts` to separate the approval question from `canSubmitVideo`'s video-spend question, 41 fixture-driven keyless tests in `tests/upload-eligibility.spec.ts`, and one compiler-forced field at each of five `Workspace`-literal call sites. Retry needs only the header paragraph rewritten to future tense naming T12–T14 as the planned consumers, then a re-gate.

**follow-ups:**

1. For T12: `program_roster_full`'s first arm excludes a `program_players` row claimed by a staff member, and `myPlayerId` is null for staff, so `getRosterPlayerOptions()` will never surface an owner's own claimed profile — T12 needs an extra read mirroring `claimedProfilesByProgram()` to offer that row, which the contract already accepts once present.
2. For T15/T16: an older row whose `player1_id` is a claimed player's login id resolves to the profile id in the eligibility result — compare people, not strings, and do not write the resolved id back.
3. `explainVideoRefusal()` still says "still being confirmed" for a suspended program; pre-existing wording, now distinguishable via the new `programStatus` field.

## T17 · Present video requirements before selection — done

**gate:** Mechanical: lint, `tsc --noEmit`, and the full keyless `npm test` all passed; no stale `.next/` retry needed. Completion: VERDICT: pass — every figure traced to `02_design/output/design.md` and `01_brief/output/brief.md` rather than invented, and the "does not obscure errors/progress" line verified structurally in the JSX rather than taken on the implementer's word. Pipeline guardrails: CLEAR, no findings — the three vendor inputs (`initialTopPlayerIsPlayer1`, set-score ordering, `fixedCamera`) have zero diff, `TrimStepContent.tsx` remains their sole owner, and the new camera-placement copy describes physical setup for image quality without implying an answer to the step-4 end-of-court question. RLS guardrails were skipped because no Supabase client, data loader, API route, migration or query changed.

**changed:** Added `src/components/dashboard/matches/new-match-wizard/VideoRequirements.tsx` — a presentational panel carrying the spec line (1080p minimum, 30 fps minimum with 29.97 accepted, 60 fps preferred), an inline-SVG court-framing schematic showing both baselines, the far service line and unlabelled space beyond the court, and four requirement rows covering camera position, framing, singles plus complete-game trim, and file size/format (under 8 GB, MP4/H.264 preferred without narrowing accepted formats). The schematic states no measured margin and carries the caption "A guide, not a check — nothing here confirms the framing," so it makes no automatic-validation claim. `FileStepContent.tsx` renders it on the video branch in place of the removed inline `VIDEO_REQUIREMENTS` array, after every error and progress strip in render order; the SwingVision/export branch and `EXPORT_REQUIREMENTS` are untouched. Inspected at 1440px and 390px via the existing `src/app/wizard-reproduction/` harness, including a forced probe-error path to confirm the panel sits below the error notice; no throwaway route was added and the inspection screenshots were removed before hand-off.

**follow-ups:**

1. T18 should compare the probe's thresholds against this copy. Note `src/lib/video/probe.ts` already lists 29.97 in `STANDARD_FPS` and snaps within a 2% tolerance, so that criterion may already be satisfied by shipped code — a verify-not-rewrite.
2. T18's criterion "checks do not infer frame rate from playback timing" may conflict with the shipped probe, which estimates fps by observing 20 frames over a 2-second sample window. Whether it conflicts depends on whether the sampler reads per-frame `mediaTime` or wall-clock; if it is genuinely wall-clock, T18 should report the contradiction rather than invent a threshold.
3. The `wizard-reproduction` harness 500s on a second direct navigation once its fixture auth cookie is set — `getClaims()` in `src/lib/supabase/middleware.ts` rejects a JWT payload with no `exp` claim. The real Playwright spec is unaffected because it controls the cookie lifecycle via `addInitScript`, but a manual inspection trips on it; adding `exp` to the harness fixture payload would fix it.

## T11 · Define reusable upload eligibility — done

**gate:** Retry of the run blocked above; the stashed work (`02c3a598…`) was restored onto a clean tree after T17 landed and applied without conflict. Mechanical: lint, `tsc --noEmit`, and the full keyless `npm test` all passed (770 passed, 6 skipped). Completion: VERDICT: pass — all five `done when:` lines re-verified against unchanged code and tests, and the documentation fix checked in both directions, confirming it did not overcorrect into denying true properties. Pipeline guardrails: CLEAR, no findings — attribution re-verified rather than inherited (`self` refused in a team workspace, the resolved athlete always echoing the matched roster row's `playerId`, a staff login failing at `athlete-not-on-roster`), and the three vendor inputs plus `job-request.ts` absent from the diff. RLS guardrails: CLEAR — the prior run's sole finding is resolved, with zero production callers independently confirmed by grep, no `admin.ts` import, no migration, no new table or view, `programStatus` still sourced from the same RLS-scoped `.eq("user_id", userId)` query that already read `programs.status`, and `claim-state.ts` confirmed dependency-free so the new type-only import cannot drag server code into a client bundle.

**changed:** Only comments changed from the blocked run — no code, tests or types moved. Seven passages across `upload-eligibility.ts` and `types.ts` were rewritten from present into future tense: the header now opens "NOT YET WIRED IN," names T12–T16 as the planned consumers, and states that attribution remains enforced solely by the `matches_block_client_regraft` trigger and RLS until those land. The retry's own sweep found four overclaims beyond the three fixed by hand — the §4 LINE comment, the `ProgramApprovalReading` doc, the `retryable` field doc, the `PENDING_APPROVAL_NOTICE` doc, and the `explainVideoRefusal()` addendum in `types.ts`. The contract itself is unchanged: a pure `uploadEligibility()` returning nine distinguished reasons with only `approval-unknown` and `roster-unknown` retryable, `ownRosterIdentity()`, a non-optional `Workspace.programStatus`, 41 fixture-driven keyless tests, and one compiler-forced field at each of five `Workspace`-literal call sites.

**follow-ups:**

1. `LINE_REQUIRES_STAFF_REFUSAL` (`upload-eligibility.ts:207`) is documented as reading "exactly as" what `explainWriteFailure()` renders for the 42501, but it carries a trailing period the rendered string does not. Verified against the **live** database this run, not the migration file: `matches_block_client_regraft` raises `only a program's staff can attach a match to a scheduled line` with no full stop, and `capitalize()` (`src/lib/utils.ts:12`) adds no punctuation. Either drop the period from the constant so the two genuinely match, or soften the comment to say a full stop is added for UI copy. Nothing renders the constant yet, so this is free to change until T13 wires it in.
2. For T12: `program_roster_full`'s first arm excludes a `program_players` row claimed by a staff member, and `myPlayerId` is null for staff, so `getRosterPlayerOptions()` will never surface an owner's own claimed profile — T12 needs an extra read mirroring `claimedProfilesByProgram()` to offer that row, which the contract already accepts once present.
3. For T15/T16: an older row whose `player1_id` is a claimed player's login id resolves to the profile id in the eligibility result — compare people, not strings, and do not write the resolved id back.
4. Once T12–T16 land, the "NOT YET WIRED IN" header and the T-number references in the rewritten comments should be collapsed back to present tense in one pass, ideally by the last of those tasks.

## T12 · Enforce roster subjects in wizard state — done

**gate:** Mechanical: lint, `tsc --noEmit`, and the full keyless `npm test` all passed (801 passed, 6 skipped — the `HAVE_ENV`-guarded live-database specs). Completion: VERDICT: pass — criterion 3 audited in code rather than tests, confirming no `?? userId`-style fallback survives on any team path and `rosterSubjectOrNull()` carries no self/uploader branch; both out-of-list files judged required rather than creep. Pipeline guardrails: CLEAR, no findings — the three vendor inputs untouched, the removed "Myself" row proven not to strand an owner who holds a real profile, and the doubles carve-out cleared because `attribution: null` names nobody rather than misattributing. RLS guardrails: CLEAR, no findings — the new `program_players` read scoped to the viewer's own live row on the active program, archived and merged rows excluded at both the query and the fold, the widened effect reading nothing the viewer could not already read, and no `admin.ts` import in a client hook.

**changed:** Wired T11's contract into wizard state — its first caller. Added `src/components/dashboard/matches/new-match-wizard/subject-eligibility.ts`, a pure seam holding `eligibleRosterOptions()`, `wizardAthleteChoice()`, `wizardUploadEligibility()`, `rosterSubjectOrNull()`, `identityAthleteFor()` and `draftResumeStep()`, with `MatchSubject`/`RosterSubject` moved there and re-exported so importers do not move. `useUploadMatchWizard.ts` now runs its roster effect for every team workspace including presets, adds a `program_players` read that folds in the viewer's own claimed profile (which `program_roster_full`'s first arm drops for staff), exposes an `eligibility` memo, and gates `handleProviderContinue`, `handleFileContinue` and `handleCreateMatch` on it. `SourceStepContent.tsx` lost the "Myself" row, gained a "You" pill and a roster-load-failure message. 32 new tests across pure fixtures and the real hook.

**The hole this closed.** The replaced line read `preset.playerUserId ?? (matchSubject.kind === "roster" ? matchSubject.playerId : userId)`. That `?? userId` filed a team upload under the uploader whenever no subject was chosen — a live silent-misattribution path. `player1_id` now comes only from `eligibility.attribution`; `userId` survives for `created_by` alone.

**follow-ups:**

1. **Correction to the RLS reviewer's account, for anyone reading this log.** That reviewer had no Supabase MCP access and read `supabase/migrations/`, concluding `matches_block_client_regraft` "does not reference `player1_id` at all." Verified against the **live** database this run: it does. An insert is rejected unless `player1_id` matches a `program_players` row or a `program_members` row on the same program (`that player is not on this program's roster, so the match cannot be filed under it`, errcode 42501). What it does not check is **role** — a coach's own `program_members.user_id` passes. So the gap T14 must close is role-blindness, not the absence of any check. Do not plan T14 off the migration file.
2. The import-identity blocking check still uses the raw `askWhoPlayed && !matchSubject` rather than `eligibility`. Not a gap — the submit-time gate catches it — but inconsistent with the rest of the diff; `/pr-check`'s whole-branch review is where that belongs.
3. A doubles preset (`playerUserId: null`, `supportsVideo: false`) passes with `attribution: null`, preserving prior behaviour, while a singles line with nobody assigned is now refused with `athlete-required`. The coach's fix is assigning the line on the event; T13 should surface that sentence on preset entry.
4. `src/app/wizard-reproduction/` mounts a personal workspace with a preset carrying `playerUserId`, which the contract refuses as `athlete-not-personal` at Save match. Harmless for its score-reproduction purpose, but it will bite anyone who submits from it.
5. T13 can consume `eligibility.retryable` plus `whoPlayed.loadFailed`/`reload()` directly; Continue is not yet disabled by eligibility in `UploadMatchFlow`, which is T13's button-state line. Rosters are re-read only on open, workspace change or reload — the pre-submit status re-read is also T13's.

## T14 · Enforce upload eligibility on direct writes — done

**gate:** Mechanical: lint and `tsc --noEmit` clean; `npm test` 801 passed / 37 skipped (the 31 new database specs skip without `LOCAL_SUPABASE_*`, per the repo's `HAVE_ENV` convention). `npm run format:check` initially failed on `tests/upload-write-eligibility.spec.ts` and was fixed with `scripts/format-file.sh` — the same fix the pre-commit hook applies, and CI runs `format:check`, so this would otherwise have gone red there. **The 31 database specs were then run by the runner directly against the local Docker stack: 31 passed.** Completion: VERDICT: pass, with the regraft body confirmed byte-identical to `20260824211820` above the new block and `recordScore`'s `analysis_method: "manual"` confirmed to fall outside `match_is_upload_shaped`. RLS guardrails: CLEAR, no findings — `search_path = ''` pinned on all four functions with every reference schema-qualified, the service-role check the repo's established non-spoofable `request.jwt.claims` idiom rather than a new one, `execute` revoked from `public`/`anon`/`authenticated` on both SECURITY DEFINER helpers so `upload_eligibility_refusal` cannot become a roster oracle, and no new table, view or policy. Pipeline guardrails were skipped because nothing under `src/app/dashboard/`, `src/components/dashboard/` or the upload wizard changed.

**changed:** Added `supabase/migrations/20260911000000_upload_eligibility.sql` (new, **not applied** anywhere), `tests/upload-write-eligibility.spec.ts` (31 specs), `tests/fixtures/local-supabase/live-baseline.sql`, and `supabase/config.toml` plus `supabase/.gitignore` from `supabase init` — there was no local-stack config in this repo before. The migration adds `match_is_upload_shaped()` and `upload_eligibility_refusal()` (the SQL reading of T11's five questions, returning T11's own sentences), re-creates `matches_block_client_regraft()` with the live body plus the new rule and widens its trigger column list with `source_provider, analysis_method`, and adds `match_upload_transition_guard()` as a BEFORE INSERT trigger on `processing_jobs` and `match_files`. All three tables funnel through the one refusal function, satisfying "one database surface". Null `player1_id` still passes, so doubles and hand-typed names remain valid answers.

**Live-drift check, performed by the runner.** The migration's correctness depends on its transcription of the live trigger being current, and both reviewers on this branch lost Supabase MCP access and could only read migration files. Verified directly: live `matches_block_client_regraft` has `md5(prosrc) = 958732b561243c7d22262b312efda811`, length 2768 — **byte-identical** to `20260824211820`'s body, so there has been no live drift. All 75 non-blank lines of that live body appear verbatim in the new migration (zero missing); it is not one contiguous substring only because the new rule is woven in before the return. The migration's own pre-apply instruction to re-diff `pg_get_functiondef` is therefore satisfied as of 2026-09-12, but should be re-run at application time if that is not same-day.

**Why the earlier reviewer was wrong, resolved.** T12's log recorded a correction to an RLS reviewer who claimed the trigger "does not reference `player1_id` at all." The cause is now known: that reviewer read `20260821144058`, the original migration, which predates the `player1_id` arm. `20260824211820` is the current one and matches live. The real gap this task closes is **role-blindness** — a coach's own `program_members.user_id` satisfies the live roster check — plus `claim_pending`, which the live trigger never consults.

**follow-ups:**

1. **Not applied to production.** Applying is a separate deployment action. Before applying, re-diff `pg_get_functiondef('public.matches_block_client_regraft')` against the migration's copy; the header carries the application steps, a post-apply check query and a commented rollback block.
2. Local green proves the file applies over the live definitions **as transcribed on 2026-09-12** and behaves as written. It is not proof of production behaviour: the local stack was seeded from `live-baseline.sql`, a hand-written reconstruction, because `supabase/migrations/` cannot bootstrap (its first file is a `create or replace function` over tables it never creates) and runs ~100 migrations behind. All three file headers state this; do not let T20 represent it as enforcement proof.
3. An unrecognised or absent JWT role makes `v_is_client` false and **skips** the guard — it fails open. That is inherited from the pre-existing trigger, not introduced here, but it is worth a deliberate decision rather than inheritance.
4. `processing_jobs` INSERT still lets a member attach a job to a teammate's match; with `processing_jobs_one_live_per_match` that can block the real owner's job. A separate ownership rule, deliberately not added here.
5. T15/T16 should consult `upload_eligibility_refusal`'s semantics before minting a SAS credential or spending quota, so the refusal arrives before the bytes.
6. `AGENTS.md` says nothing about `supabase/config.toml` or the local-stack workflow, which now exists and is the only way to test a migration in this repo.

## T15 · Verify transfer authorization for the athlete — done

**gate:** Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (832 passed, 37 skipped). The runner additionally confirmed `route.ts` exports only `runtime`, `dynamic` and `POST` — valid Next route exports — because `npm run build` is deliberately absent from CI, so an export violation in a restructured route would surface only at Vercel. Completion: VERDICT: pass, reached by a branch-by-branch diff of the old `route.ts` against the new `handler.ts` confirming every path survived (401, 400×2, 500, 404, 403 no-workspace, 403 video refusal, 400 bad extension, 503 mint failure, the `processing_jobs` write), and by confirming from `git show HEAD` that the old select genuinely lacked `player1_id`. RLS guardrails: CLEAR, no findings — no ownership bypass, no cross-account exposure, no credential leakage, no fail-open path. Pipeline guardrails were skipped because nothing under `src/app/dashboard/`, `src/components/dashboard/` or the upload wizard changed.

**changed:** The verification found a real gap: `src/app/api/splitstep/upload-url/route.ts` selected only `id, created_by, program_id`, so the row's `player1_id` was invisible to the route. With T14's migration unapplied and the live trigger blind to role, a match whose athlete was a staff-only login could reach a signed Azure SAS credential. The gap was demonstrated before it was fixed — short-circuiting the new block gives 14 failed / 17 passed. The decision logic moved to a new sibling `handler.ts` taking injected `UploadUrlDeps`, leaving `route.ts` as wiring; Next reserves a route file's exports and Playwright has no module mocking, so dependency injection was the only way to satisfy criterion 4's "stub credential minting" at the full-request level. Order is now sign-in → body → match/ownership → `billingWorkspaceFor()` → **`uploadEligibility()` on the row** → `explainVideoRefusal()` → object key → mint. `athleteOnRow()` maps the row to T11's `AthleteChoice` and never rewrites a team row to the uploader. A retryable refusal answers 503, a decided one 403. No third copy of the rules was written — the route calls T11's contract. 31 new tests in `tests/upload-url-authorization.spec.ts`; the task's `files:` guess of "existing upload-url authorization tests" was wrong, as no test file for this route existed.

**Live checks performed by the runner.** Three reviewers in a row on this branch have now lost Supabase MCP access and left live-verification items open, so these were closed directly. `program_roster_full`, `user_program_ids`, `is_program_staff` and `my_player_ids` are all SECURITY DEFINER with `search_path=""` pinned. `program_roster_full` bypasses RLS by definition, but its body references `user_program_ids`, so it scopes to the caller's own memberships; T15 additionally only ever passes it a program `billingWorkspaceFor()` has already established the caller holds. Two independent constraints, neither relied on alone.

**follow-ups:**

1. **For T16:** call the same `uploadEligibility()` on the job's match row before `reserveQuota()`, using the same `athleteOnRow()` mapping. Consider lifting `athleteOnRow` and the `loadRoster` wiring into `src/lib/services/splitstep/` so both routes share one roster read — T15 kept them local to respect its own surface. T16 must also decide what a retryable refusal does to a job already sitting at `uploaded`; this route answers 503, which the wizard surfaces as a failed transfer.
2. User-visible wording shifted: a role refusal now carries T11's sentence ("…a match can't be recorded…") rather than `explainVideoRefusal`'s ("…can't be sent for analysis…"), because the contract is consulted first per its own header. `explainVideoRefusal` still runs behind it. Deliberate, but worth a copy review.
3. One live team match has `player1_id` null (`79de1d34…`, a `splitstep` match with two jobs). It will be refused `athlete-required` if its owner re-runs an upload; assigning the athlete on the row fixes it. Worth checking once T14's migration is applied.
4. The new roster wiring in `route.ts` was typechecked and reviewed against `active-workspace-server.ts` but not exercised against a real session — this worktree has no browser credentials. The logic is covered by fixtures.

## T16 · Verify final job eligibility before quota spend — done

**gate:** Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (869 passed, 37 skipped). The runner also confirmed `job-request.ts` is untouched, that `jobs/route.ts` exports `runtime`, `maxDuration`, `POST` exactly as at HEAD (it never carried `dynamic`), and that `upload-url/route.ts` kept its `force-dynamic` through the refactor. Completion: VERDICT: pass, by a branch-by-branch diff of HEAD's monolithic route against the new `handler.ts` — every status code, the submittability checks, the release→retire→`failed` path and the `after()` adoption block all preserved, with exactly one insertion. Pipeline guardrails: CLEAR, no findings — all three vendor bindings traced through the restructure intact. RLS guardrails: CLEAR, no findings requiring remediation.

**changed:** The route loaded the match selecting `program_id` but not `player1_id`, so it asked whether the workspace's budget was open to the caller and never whether the athlete on the row was a live roster player on an approved program. One `uploadEligibility()` call now sits between `billingWorkspaceFor()` and `reserveQuota()`, using the same `athleteOnRow()` mapping and roster read as `upload-url`. Quota policy is untouched — `reserveQuota()` still owns the spend question and still runs exactly once after eligibility passes. Following T15, the decision moved to `src/app/api/splitstep/jobs/handler.ts` taking injected `SubmitJobDeps`, with `route.ts` as wiring; that shape is what lets criterion 4's vendor and quota boundaries be stubbed. `athleteOnRow` was lifted to `src/lib/services/splitstep/match-athlete.ts` and the roster loader to `eligible-roster.ts`, both now shared by the two routes rather than duplicated — the reviewers confirmed the move was verbatim and T15's 31 tests still pass unchanged. 39 new tests.

**Criterion 4's decision.** The eligibility check returns _before_ the `try`, so a refusal writes nothing to the job row — no `submitting`, no `failed`, no `attempt_count` bump — and the job stays at `uploaded`, where `isSubmitStalled()` still offers "Try again". Only failures past the reservation (vendor error, empty job id, minter throw) run the pre-existing release→retire→`failed` path. Retryable refusals answer 503, decided ones 403. Marking an already-transferred job failed because a separate authorization step refused would have destroyed expensive work; it does not.

**The database was not merely blind here — it was absent.** Verified directly against live by the runner: `matches_block_client_regraft` opens with `if not v_is_client then return new; end if;`, where `v_is_client` is true only for the `authenticated`/`anon` JWT roles. This route loads the match through a **service-role** client, so every guard in that trigger is skipped on this path — not weakened, skipped. With T14's migration unapplied, there was no database-side protection whatsoever before this change.

**An architectural consequence worth carrying forward.** T14's migration copies the same `v_is_client` idiom into its new `match_upload_transition_guard` on `processing_jobs` and `match_files`. So once T14 is applied, those guards will **also** be skipped for service-role connections — deliberately, since service-role processing must keep working. The database gate therefore never covers service-role paths, which means the route-level checks T15 and T16 added are the permanent enforcement for them, not a stopgap until the migration ships. The chain reads the other way round at first glance; it is not defence-in-depth on this path, it is the only defence.

**follow-ups:**

1. `src/lib/services/splitstep/resubmit-job.ts` mirrors this route's mint-and-submit steps for `/jobs/[jobId]/resubmit` and does **not** ask `uploadEligibility()`. It operates on an already-vendor-accepted job, so T16 left it alone as out of surface, but it is the one remaining seam that spends against a row nobody re-checked. Worth a task.
2. `docs/ui-revamp-guardrails.md` §2 names `jobs/route.ts` as "the integration"; the decision now lives in `jobs/handler.ts` beside it, and the doc should name both. The same is true of `upload-url/handler.ts` after T15.
3. Neither route has been exercised against a live browser session — the Playwright config has no web server, so both are covered through handler fixtures only.
4. Four consecutive reviewers on this branch lost Supabase MCP access and said so rather than substituting migration-file reasoning. The live checks in this entry and T15's were run by the runner instead. If that access is intermittent, T20 should plan for it rather than assume live verification is available.

## T18 · Align existing video validation with requirements — done

**gate:** Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (884 passed, 37 skipped). Completion: VERDICT: pass — the GB arithmetic independently recomputed, `snapToStandardFps` confirmed idempotent so no boundary shifts, and no threshold constant altered. Pipeline guardrails: CLEAR, no findings — the three vendor inputs absent from the diff, provider naming correct everywhere user-visible and now test-enforced, thresholds unmoved. RLS guardrails were skipped because the diff touches only the video probe and the upload validator — no Supabase client, data loader, API route, migration or query.

**changed:** A comparison task that changed only what it demonstrated. The traced chain is `useUploadMatchWizard.ts:1486` → `getProviderStrategy()` → `SplitStepUploadStrategy.validateFile` → `validateSplitStepVideo` (`src/lib/services/upload/validators/splitstep-validator.ts`) → `probeVideo` (`src/lib/video/probe.ts`) — one validator, one probe, no second implementation. Four changes: (1) `formatGigabytes` divided by 1024³ while `MAX_VIDEO_SIZE_BYTES` is decimal `8e9−1`, so an oversized file read "Video is 7.5 GB. The maximum is 7.5 GB." — the same figure twice, and contradicting the "Under 8 GB" T17 had just put on screen beside it; now decimal, threshold untouched. (2) `snapToStandardFps` exported from the probe and applied in the validator, so a documented-accepted 29.97 is accepted on the validator's own terms rather than incidentally because the probe snapped first. (3) The unknown-fps warning now says the requirement still applies and Advantage Intelligence may still reject after upload. (4) The validator split into `checkVideoFileBasics` and `evaluateVideoProbe` with `validateSplitStepVideo` as a thin orchestrator, which is what lets boundaries be driven from constructed `VideoProbe` fixtures — no behaviour change. 15 new tests, every literal read from `config.ts`. Deliberately unchanged: the fps floor, resolution floor, size limit, container allowlist, and the probe's sampling strategy.

**Criterion 3's frame-rate question, settled.** The criterion forbids inferring frame rate from playback timing, and the probe samples 20 frames over a 2-second window — which looked like a contradiction when this task was queued. It is not one. `measureFps` (`probe.ts:92`) computes `frameSpan / timeSpan` from `last.presentedFrames − firstSample.presentedFrames` over `last.mediaTime − firstSample.mediaTime`, both `VideoFrameMetadata` fields; the wall-clock `now` argument is explicitly discarded. Playback is only the mechanism that makes frames present — throttling or slow decode delays samples but cannot skew the ratio. Verified in the source by both the implementer and the runner. No contradiction to report, and the probe's sampling was correctly left alone.

**Contradictory provider boundaries, reported not invented.** The vendor's guide accepts 29.97 while its error table describes rejection below 29.9; both purport to describe the same boundary. The existing, more permissive behaviour was kept and documented in the module header rather than a third number being invented. Effective fatal boundary today is ≈29.4 (the 2% snap window below 30), leaving a narrow 29.4–29.9 band this repo accepts and the vendor may refuse — a post-upload failure, not a false rejection.

**follow-ups:**

1. **A live contradiction between T17's shipped copy and this validator.** `VideoRequirements.tsx:55` tells users Advantage Intelligence "accepts most formats a camera or phone can produce"; the validator accepts `.mp4` and `.mov` only. Someone with an `.avi` reads that it is accepted and is refused at pick time. Reported rather than fixed, and both reviewers agreed that was right: the vendor's own boundary ("formats ffmpeg can decode") is unenumerable, and `videoExtensionFor()` (`src/lib/services/splitstep/object-keys.ts:30`) _throws_ outside the allowlist, so widening the picker alone would trade a legible pick-time refusal for a failure after a long upload. Resolving it needs a product decision in one direction: either obtain an enumerable accepted-container list and widen `ACCEPTED_VIDEO_EXTENSIONS` **and** `object-keys.ts` together, or narrow T17's copy to what is actually accepted. This is user-facing today.
2. Portrait 1080×1920 is refused by `width < 1920`. The guide says "1080p minimum" without addressing orientation. Probably right for court framing, but it is an assumption, not a sourced constraint.
3. Real-file probing remains out of reach — no `ffmpeg` on this machine and no video fixtures under `tests/`. Nothing exercises `probeVideo()` against actual bytes; the specs drive the accept/reject decision from constructed `VideoProbe` values. The spec header says so rather than implying coverage it lacks.
4. The requirement chips in `providers/splitstep.ts:45` render `MP4 / MOV` from the same constant, so they move automatically if the allowlist is ever widened.

## T19 · Document Save draft behavior — done

**gate:** Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (884 passed, 37 skipped), including `tests/generate-map.spec.ts`, so `MAP.md` is undisturbed. Completion: VERDICT: pass — the shipped-vs-recommendation convention tested adversarially with no proposal found disguised as a present-tense fact, and the citations spot-checked against source and confirmed accurate to the line. Both guardrail reviewers were skipped because the diff touches no `.ts`, `.tsx` or `.sql` file at all — there is no surface for either to audit.

**changed:** Added `docs/upload-draft-behavior.md` and registered it in `docs/README.md` following that file's own convention. Documentation only; no source, migration or test file touched. Every claim outside a blockquote is shipped behaviour carrying a file/line citation; every proposal is a `> **Recommendation:**` blockquote with its own "Acceptance:" clause, and the convention is stated in the document's opening before it is used.

**Three defects found while documenting, none fixed here.**

1. **A failed draft save is indistinguishable from a successful one.** `saveDraft()` returns `false` on failure, but `handleSaveDraft` (`UploadMatchFlow.tsx:685-693`) discards the boolean and calls `router.push(exitHref)` regardless — the user is navigated away believing their work is saved. Verified independently by the runner in the source, not just reported.
2. **A draft can be resumed into the wrong workspace.** `loadMatchDraft` (`actions.ts:488-498`) does not select `program_id`, and nothing compares the draft's program against the active workspace on resume. The completion reviewer confirmed the omission in the select. This is attribution-adjacent, immediately beside what T12–T16 hardened.
3. **Retention is indefinite** — no expiry job exists anywhere under `src/app/api/` or `supabase/functions/`, which matches the design's "no new expiry job" intent but means nothing ever reclaims an abandoned draft.

**Verified, and worth keeping:** a draft and a started transfer cannot coexist — the draft is deleted synchronously before `createProcessingJob`/`uploadAndSubmitVideo` run — so the transfer-start question has a simpler answer than the criterion anticipated. `match_drafts` carries RLS `(select auth.uid()) = user_id` (migration `20260903120000`, applied). `upload_eligibility` (`20260911000000`, T14) is confirmed **absent** from `list_migrations` — still not live.

**follow-ups:**

1. The Matches-table's own delete-draft call site was not located within the searched subtree and is marked "presumed" in the document rather than asserted. Worth confirming.
2. A failed `deleteMatchDraft` at completion leaves a stale Resume row with no guard against a subsequent duplicate-match submission.
3. `onVideoPick` resets trim bounds on file replacement but does not reset `initialTopPlayerIsPlayer1`/`fixedCamera` — a carryover across a file swap, documented as uncleared. These are two of the three vendor inputs, so this is worth a deliberate decision rather than inheritance.
4. Renaming the autosave's localStorage keys so they do not read as durable persistence.

## T7 · Show import identity confirmation — done

**gate:** Retry of the run blocked above; stash `7f7ba6ca…` was restored onto a clean tree after five intervening commits and applied without conflict. Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (896 passed, 37 skipped). **The skip count held at 37 while passing rose 884 → 896**, which is the runner's independent confirmation that all 12 new tests actually execute in the standard suite rather than being quietly skipped — the second half of what blocked this task. Completion: VERDICT: pass, with the three previously-unproven clauses each traced to a specific executable test and the dead-confirm-button claim verified against the unmodified hook. Pipeline guardrails: CLEAR, no violations. RLS guardrails were skipped because no Supabase client, data loader, API route, migration or query changed.

**changed:** The retry did not merely add the missing coverage — it corrected the restored implementation in three ways, each verified at the gate. (1) Four copy branches instead of two: the old copy collapsed both missing-name cases, so a team upload with no athlete chosen rendered "The export is missing a player name" _and a confirm button that did nothing_, because `confirmImportIdentity` returns early while `askWhoPlayed && !matchSubject` (`useUploadMatchWizard.ts:765-775`). That state now reads "No player is selected yet", offers only Change player / Choose another file, and no confirm. (2) The Continue gate was narrowed from every `blocked` reason to `identityNoticeVisible && importIdentity.blocked`; the broad version disabled the button for states with no notice on screen, silently swallowing `handleFileContinue`'s explanatory sentence. (3) The notice was wrapped in a `flex flex-col gap-9` column, as `WizardShell`'s content slot has no gap and the stash left it flush against the step content. The unreproducible localhost-only spec was **deleted**, not skipped, and replaced by 12 keyless tests that render the real component through `ts.transpileModule` + `vm` + `renderToStaticMarkup`.

**Why narrowing the gate does not weaken the block.** This was the change most worth checking, and the completion reviewer confirmed it in code: `handleFileContinue` (`useUploadMatchWizard.ts:1432-1473`, unmodified) still checks the **full, unnarrowed** `importIdentityBlocked` on every invocation, and `continueHandler` is the single function passed to both the footer button and `useWizardKeys`' `onContinue`. So narrowing changes only whether a case gets a disabled button or an enabled one that then shows its message — neither click nor Enter can bypass the real gate. Criterion 3's "one blocking result" holds.

**Attribution, the clause the first run left unproven.** `tests/upload-identity.spec.ts:418-457` calls the real `wizardUploadEligibility()` across three different imported/typed name pairs and asserts an identical `attribution`, demonstrating that names are not inputs to the value that becomes `matches.player1_id`. The guardrails reviewer traced the same path independently: `confirmImportIdentity`/`rejectImportIdentity` only set local `identityAnswer` state keyed by file generation and workspace, and reach nothing that writes attribution. A negative answer does not flip parser perspective — it tells the user to supply a correctly oriented export, per T6's constraint.

**follow-ups:**

1. `FileStepContent`'s parse-error strip still promises "You can still enter the details by hand on the next step", which `importIdentityBlocked` has made untrue since T6. That copy belongs to the file step's owner and should be corrected on its own branch.
2. The `identityNoticeVisible` scoping means a blocked-but-noticeless state leaves Continue enabled until clicked, at which point the message appears. A quieter inline hint would make that refusal visible without notice-weight UI.
3. No browser or dev-server run: the rendered spacing and colour were verified as markup and tokens, not visually on the real page. The click→keyboard round trip is proven at the level of the shared blocking value and the handler's re-check, not by driving a live wizard. Worth revisiting once the repo has a reproducible browser harness that does not need a hand-started localhost server — `src/app/wizard-reproduction/` plus its dev-time "Missing exp claim" 500 is not that yet.

## T9 · Order providers and align source selections — done

**gate:** Retry of the run blocked above; stash `515e1654…` was restored onto a clean tree after seven intervening commits. The runner dry-ran it with `git apply --check` before restoring, because T12 had rewritten 53 lines of the same `SourceStepContent.tsx`; it applied cleanly. Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (908 passed, 37 skipped) — **skips held at 37 while passing rose 896 → 908**, confirming all 12 new tests execute in the standard suite. Completion: VERDICT: pass, and the reviewer independently reproduced the mutation tests rather than trusting them — deleting the `Check` from `RosterRow` fails exactly 2 tests, reverting `providers.ts` fails exactly 4, which is what proves the assertions are not vacuous despite a stubbed icon library. Pipeline guardrails: CLEAR, no findings. RLS guardrails were skipped because no Supabase client, data loader, API route, migration or query changed.

**changed:** The block was one criterion: the first attempt removed the grey selection wash but never added the Signal Blue check, leaving chosen rows with neither indicator. The retry found the fix was narrower than expected — the workspace and source rows **already** carried T8's check, and only `RosterRow` was missing it. `RosterRow` now emits a `flex-1` spacer, then `trailing`, then a reserved 13px slot holding a `Check` (`text-[var(--blue)]`, `strokeWidth 1.5`, `aria-hidden`, with `aria-selected` carrying the semantics) when chosen and a `w-[13px]` spacer when not, so nothing reflows on selection. `src/lib/providers.ts` puts Advantage Intelligence ahead of SwingVision. `src/components/ui/float-menu.tsx` was **not** touched — the shared primitive is used well beyond this wizard and needed no change. 12 keyless tests render the real `SourceStepContent` through `ts.transpileModule` → `vm` → `renderToStaticMarkup`, with a shared `expectSharedTreatment()` helper applied to the source menu, the workspace menu in both workspace kinds, and the roster menu.

**Two stale prose claims corrected.** The file's own header docstring and the **EntitySelect (v3)** paragraph in `.skills/advantage-analytics-design/SKILL.md` both still described the superseded grey wash as marking the pick — T8 fixed the neighbouring FloatMenu paragraph but left this one behind. Editing the repository's authoritative design reference was judged in scope by both reviewers: the edit is confined to that one paragraph, accurately describes the shipped treatment, and does not restate or contradict what T8 documented beside it.

**Coexistence with T12's "You" pill.** The check and the pill occupy separate slots in reading order — avatar → name → meta → `flex-1` → pill → check — and a dedicated test renders the viewer's own roster row as the chosen one, asserting the pill reads "You", the check is present, and the check's DOM index falls after the pill's. Who the person is, and what is picked, stay two facts rather than one glyph doing both.

**`EntitySelect` resolved.** There is no `EntitySelect.tsx`. It is a design-system **grammar name**, not a component file: the implementation for this step is inline in `SourceStepContent.tsx` (local `MENU_CLS` / `MENU_LABEL_CLS` / `ROW_TRIGGER_CLS`), specified at `.skills/advantage-analytics-design/SKILL.md:1124`, with a shared class variant in `new-match-wizard/styles.ts` and the canonical primitive in `src/components/ui/float-menu.tsx`. The task called this "a bounded import lookup, not a repository sweep" — a wrong answer here would have sent the run sweeping.

**follow-ups:**

1. `new-match-wizard/styles.ts`'s `floatMenuRowCls` still hard-codes unconditional `hover:bg-[var(--surface-subtle)]`, and its doc comment still claims "the current row keeps the wash and a Signal Blue check". It is consumed by `PinnedLineBar.tsx` and `DetailsStepContent.tsx` — other wizard steps, outside T9's surface. Aligning those to T8's treatment touches three files this task does not own, so it wants its own branch.
2. `.skills/advantage-analytics-design/SKILL.md:1121` notes the header's account menu predates the primitives and carries its own classes; it likely still draws the superseded wash.
3. Verification is static-markup rendering, not a real paint — hover and focus-visible are asserted as classes, not observed pixel states. No browser screenshot was taken.

## T10 · Make player details editable and required — done

**gate:** Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (920 passed, 37 skipped) — all 12 new tests run keyless. Completion: VERDICT: pass, with the shared-primitive change verified non-breaking against every other consumer rather than assumed. Pipeline guardrails: CLEAR, no findings — the three vendor inputs confirmed absent from the diff, T4's format-reduction guard untouched, schedule-owned locks intact. RLS guardrails were skipped because no Supabase client, data loader, API route, migration or query changed (`src/lib/wizard/actions.ts` was read, not modified).

**changed:** Step 4's player fields are now four always-visible, required, labeled underline `MenuSelect` controls — Player and Opponent × Hand and Backhand — replacing an ad-hoc Add/Change/Done toggle. No Unknown option and no guessed default: an unset field renders as genuinely unset. Backhand fields carry `width={220}` so "Two-handed backhand" stays on one line beside the reserved check slot, and both player rows use `flex flex-col gap-3 sm:flex-row` so narrow layouts stack rather than wrapping option text. The opponent's editable name gained a `Pencil` affordance; the subject's own name, settled on step 1, stays plain text. The dead `HAND_LABEL`/`BACKHAND_LABEL` maps were removed — `formData.playerHand`/`backhand` remain raw lowercase (`"right"`, `"two-handed"`) and `HAND_OPTIONS`/`BACKHAND_OPTIONS` carry the normalised display labels, keeping stored and displayed values separate.

**Two changes beyond the `files:` list, both justified at the gate.**

1. **`src/components/ui/menu-select.tsx`** — a shared primitive consumed by settings (preferences, teams role/identity/policies), both schedule builders and `date-field.tsx`. `value` widened to `T | undefined` with an optional `placeholder`, which criterion 1 genuinely requires: a control with no guessed default needs an unset state to exist. Verified non-breaking in both reviews — every other consumer always passes a `value` matching an existing option, so the new unset-styling and the `placeholder ?? value` fallback branch never execute for them.
2. **`UploadMatchFlow.tsx`** — the footer's missing-answers counter hand-rolled its own field list that **never checked hand/backhand**, and used a different camera-position label than the write-time check. It now calls the same `collectMatchCompletionRequirements` (T5's `validation.ts`, itself unchanged) that `useUploadMatchWizard.ts` already used at the write. That closes a real pre-existing gap where a missing style surfaced only as a submit-time error instead of proactively disabling Continue — which is what criterion 5's "missing styles block completion" asks for.

**Criterion 5, the data-integrity clause, verified in source.** `saveMyStyle` (`src/lib/wizard/actions.ts:416-430`) is the only profile-writing path: it takes no target id, scopes its write with `.eq("id", user.id)` off `auth.getUser()`, and never touches `program_players`. The "Save to your profile" button renders only when `subject.isSelf` and is absent from the opponent block. So a coach recording an athlete's hand and backhand records them on the **match row** only — nothing is written back to the coach's own profile or to a roster name.

**follow-ups:**

1. No browser run: narrow-layout stacking and check-slot spacing are proven at the class and static-render level, not as observed pixels. A visual pass at ~400px would be worth doing before merge if pixel confirmation is wanted.
2. `.skills/advantage-analytics-design/SKILL.md` has now been edited by both T9 and T10 in the same session — T9 corrected the EntitySelect paragraph, T10 documented `MenuSelect`'s undefined/placeholder behaviour. Both reviews confirmed the two edits are consistent and neither contradicts T8, but the file is the design authority and is worth a human read before merge.

## T13 · Show approval restrictions at every entry — blocked

**gate:** Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (931 passed, 37 skipped; skips unchanged while passing rose 920 → 931, so all 11 new tests run keyless). Completion: VERDICT: pass — all four criteria matched to tests, the workspace pin confirmed to engage on `existingMatchId` and unpin correctly, and the `+1` line in T7's `upload-identity.spec.ts` confirmed a mechanical fixture field that weakens no assertion. Pipeline guardrails: CLEAR, no findings. RLS guardrails: **BLOCKED on one finding** — a widened double-submit window, described below. Everything else that reviewer checked was clean: `refreshApproval()` uses the session-scoped browser client with no `admin.ts` anywhere in the diff, selects only `status` filtered by a `programId` the caller already holds active (nothing client-supplied can substitute an arbitrary program), maps any Supabase error or missing row to `"unknown"` which the contract treats as `retryable` and refused — so the fresh read genuinely fails closed — and `programs.status` is in any case already exposed to anonymous visitors via the `program_public_status()` SECURITY DEFINER function, so the read discloses nothing new.

**The finding, verified by the runner in source.** `handleCreateMatch` now opens with `await refreshApproval()` — a network round trip — at `useUploadMatchWizard.ts:~2199`, while `setIsCreating(true)` is not reached until **line 2239**. The Continue button's disabled state is driven by `isCreating`/`isUploading`/`isProbing`, none of which are true during that await, and there is no ref-based in-flight guard inside `handleCreateMatch` (grep finds only the `useState` at 589, the set at 2239 and the reset at 2613). At HEAD every check preceding `setIsCreating(true)` was synchronous, so the re-entrancy window was a single JS tick; this diff stretches it across a real network request, during which the button gives no feedback. A double-click — or a second click after a pause — re-enters `handleCreateMatch`, and if both eligibility rechecks pass, **two `matches` rows are written for the same file and athlete**. Not an auth or RLS breach (both writes are correctly attributed and RLS-scoped), but a submission-integrity regression this diff introduces, and no test covers it.

**changed:** The approval-restriction work is preserved in stash `7571136643a2d5a1a014758860dc81177667d257`; no task code was committed. It adds `EligibilityNotice.tsx`, `pinnedMatchWorkspace` + `activeWorkspaceRef` pinning so a live workspace switch cannot re-decide an existing match against a newly selected program, `approvalReading` + `refreshApproval()` with a submit-time fresh re-read feeding `freshEligibility.attribution` into the `player1_id` write, `retryEligibility()` scoped to the two retryable reasons, `eligibilityBlocked` threaded through `wizardContinueBlocked()`, and 11 keyless tests. Retry must add a ref-based in-flight guard checked and set **synchronously at the top of `handleCreateMatch`, before the first `await`** — `isCreating` state alone cannot close this window — and cover the double-click case with a test.

**follow-ups:**

1. The tab-focus/visibility "returning to the page" trigger is implemented (`useUploadMatchWizard.ts:1237-1257`) but guarded to a no-op in the DOM-less harness, so it is reasoned through rather than executably proven. The submit-time recheck — the half that actually guards the write — is proven.
2. A fifth consecutive reviewer on this branch lacked Supabase MCP access and said so rather than substituting migration-file reasoning. Its conclusions about the `programs_status_check` value space rest on `20260817073914_programs.sql`, which runs ~100 migrations behind live; worth a live confirmation that the constraint and the `programs` SELECT policy have not diverged.

## T13 · Show approval restrictions at every entry — done

**gate:** Retry of the run blocked above. Stash `7571136…` restored onto a clean tree and the blocking finding fixed **by the runner directly**, at the user's explicit choice — the fix was three lines plus a test, and spending a fresh subagent on it would have cost a full run for a mechanical change. Mechanical: lint, `tsc --noEmit`, `format:check` and the full keyless `npm test` all passed (932 passed, 37 skipped). Completion: VERDICT: pass. Pipeline guardrails: CLEAR. RLS guardrails: CLEAR — the reviewer that raised the finding confirmed it resolved.

**The fix.** `handleCreateMatch` now opens with `if (creatingRef.current) return; creatingRef.current = true;` before the first `await`, and its entire body is wrapped in `try { … } finally { creatingRef.current = false; }`. A ref rather than state because `isCreating` cannot flip within the tick a click arrives in; the whole body wrapped rather than clearing at each exit because the handler has **eight** early returns before the write — missing form data or file, missing provider, a refused fresh eligibility, the upload/probe/identity guard, missing completion answers, and two inside the video-processing branch — and a ref left stuck `true` on any one of them would permanently disable submission for the component's lifetime, silently. That would have been a worse bug than the one being fixed. All three reviewers independently traced the brace structure and confirmed the outer `finally` is the outermost frame, reached on every return, on a thrown exception caught by the inner `catch`, and on normal completion.

**Mutation-checked, three times over.** With the guard, the file's 12 tests pass; deleting **only** the `if (creatingRef.current) return;` line makes exactly the new double-click test fail while the other 11 still pass. The runner verified this, and the completion reviewer reproduced it independently. The new test fires two `handleCreateMatch()` calls without awaiting the first — the shape of a double-click, not two sequential submissions — and asserts exactly one write.

**changed:** Adds `EligibilityNotice.tsx`, shown on the provider and file steps for any refusal except `athlete-required` (which step 1's roster picker already explains, so no two sentences compete). `pinnedMatchWorkspace` + `activeWorkspaceRef` pin an existing match's workspace so a live switch cannot re-decide it against a newly selected program — the client mirror of T15/T16's server-side `billingWorkspaceFor(match.program_id)` rule. `approvalReading` + `refreshApproval()` re-read `programs.status` on tab focus and again inside `handleCreateMatch`, with `freshEligibility.attribution` — not the stale memo — supplying `player1_id`. `retryEligibility()` re-reads only whichever retryable reason failed and no-ops on a decided refusal. `eligibilityBlocked` threads through `wizardContinueBlocked()`, so the button and `useWizardKeys` still share one value. 12 keyless tests.

**Why the fresh read is safe.** Any Supabase error or missing row maps to `"unknown"`, which T11's contract treats as `retryable` and refuses — the re-read fails closed. It uses the session-scoped browser client with no `admin.ts` reachable anywhere in `new-match-wizard/`, selects only `status` filtered by a `programId` the caller already holds active, and `programs.status` is in any case already exposed to anonymous visitors through the `program_public_status()` SECURITY DEFINER function, so it discloses nothing new.

**follow-ups:**

1. The tab-focus/visibility "returning to the page" trigger (`useUploadMatchWizard.ts:1237-1257`) is guarded to a no-op in the DOM-less harness, so it is implemented and reasoned through but not executably proven. The submit-time recheck — the half that actually guards the write — is proven.
2. Six consecutive reviewers on this branch have now lacked or not used Supabase MCP access, each saying so rather than substituting migration-file reasoning. The live checks in this branch's log were run by the runner instead. T20 should not assume that access is available.

## T20 · Verify the integrated upload refinements — blocked

**gate:** Mechanical: lint (0 errors, 42 warnings), `tsc --noEmit`, and `format:check` all clean. `npm test` failed on its first run with three `Request rate limit reached` errors on Supabase auth sign-in (`claim-eyebrow-width`, `rls-workspace-isolation`, `teams-management`). The runner established this was environmental, not a regression: all three specs are untouched since merge-base `99397cb`, all three are `HAVE_ENV`-gated so they skip in a keyless CI checkout, re-running them in isolation gave 24 passed, and a clean full rerun gave **932 passed / 37 skipped / 0 failed**. The cause was three full-suite runs in quick succession against live auth. Completion: **VERDICT: needs-work**. Guardrail reviewers were not dispatched — the only source change is a test file, so neither has a surface to audit, and completion failed first regardless.

**The finding.** T20's deliverable is a verification record whose entire value is accuracy, and its central correction is wrong. It reported that in a keyless CI checkout the skip count "would be 43". The completion reviewer measured it — moved `.env.local` aside so `HAVE_ENV` is false, matching CI, and ran the suite — getting **98 skipped / 871 passed**. The runner cross-checked arithmetically without touching secrets: the eight `HAVE_ENV`-gated specs (`account-deletion-retention`, `claim-eyebrow-width`, `join-requests-staff-read`, `program-owner-name-live`, `pending-invites`, `personal-home-scope`, `rls-workspace-isolation`, `teams-management`) carry roughly 64 tests by a rough `test(` count, predicting ~101 skips. The measured 98 is authoritative; the estimate is approximate. Either way the claim is off by ~55–58 tests, not a rounding slip — and this was the one correction the task was explicitly asked to verify.

**changed:** Preserved in stash `28b5bb68ae570d7610f7f1302979f3f81a30d39b`; nothing committed. **The code change in it is good and should be kept on retry** — see below.

**What T20 got right, and what the retry should preserve.** It found that `tests/upload-score-regression.spec.ts`, the branch's only real-browser proof of T1–T4, had **silently gone red**. T10 added required hand/backhand fields; because the spec is env-gated behind `WIZARD_REPRODUCTION_BASE_URL` it never re-ran, so `[data-wizard-continue]` sat disabled on step 3 with the footer reading "4 to go". It started `next dev -p 3456`, reproduced it (2 passed / 4 failed), and added `answerPlayerStyles()` at four submit sites — asserting Save is disabled, driving all four `MenuSelect` triggers by pointer to their `menuitemradio` rows, asserting Save becomes enabled. After the fix: 5 passed / 1 failed. The completion reviewer confirmed the helper is genuinely pointer-driven rather than a shortcut. That is a real gap closed, and it is the precise failure mode of gating a test behind manual setup: it does not fail loudly, it quietly stops being evidence.

**The one remaining red browser case is correctly diagnosed and correctly left alone.** "one-set schedule preset" is refused with `line-requires-staff`, because `src/app/wizard-reproduction/wizard-reproduction-harness.tsx` mounts a **personal** workspace with a preset carrying a scheduled line — T12's follow-up 4 predicted exactly this. The completion reviewer verified it is a harness artefact, not a wizard defect, and agreed that leaving the fix out of scope was right, since the harness is a `src/` file outside this task's remit.

**Also verified by the completion reviewer, and worth carrying forward:** the regression claim holds — `git diff 99397cb` is empty for `job-request.ts`, `api/webhooks/**`, `api/cron/**`, `upload/parsers/**`, `providers/**` and `quota.ts`. The migration's five rollout prerequisites match its own header at `20260911000000_upload_eligibility.sql:61-76`. Draft persistence is confirmed plan-only against `02_design/output/design.md:202,277`. The direct-request specs genuinely import the `handler.ts` modules rather than driving the wizard, satisfying criterion 3's second half.

**The retry is narrow:** restore the stash, replace the keyless skip figure with the measured **98 skipped / 871 passed**, state that the eight gated specs carry the difference, and re-gate. Nothing else in the record was found wanting.
