# Run log — claude/task-list-workspace-condensing-370e4c

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Even out the workspace switcher rows — done
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed file; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass, all 5 criteria met with file:line evidence. Guardrails: `pipeline-guardrails-reviewer` ran (diff touches `src/components/dashboard/`) and found nothing — no wizard binding, workspace scoping, role gate, `canSubmitVideo`, analysis-state branch or provider string affected. `rls-boundary-reviewer` skipped: the diff touches no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/` path and introduces no table, view or query.
- **changed:** `src/components/dashboard/sidebar/workspace-row.tsx` only, +17/−11, confined to the `PopoverContent` body. Team rows used to stack a capitalised `role` line under `name · squad` while personal rows stayed single-line, so "ZZ Test Program · Men's" over "Coach" sat visibly taller than "Personal". The role line is dropped and the remaining label flattened to one `min-w-0 flex-1 truncate` span, making every row the same height. The squad stays inline — it is the only thing separating two rows of the same program name, whereas the role repeats across a coach's rows and disambiguates nothing; a third `·` segment would not fit the ~149px text budget without truncating the squad to pay for it. A comment above `available.map` records the choice and the measurement behind it. The `h-[42px]` two-line trigger row, the blue-soft active background, and the check/spinner slot are untouched.

## T2 · Load the new workspace's page on switch — done
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed files; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass, all 4 criteria met, and it confirmed the third file was required rather than creep. Guardrails: `pipeline-guardrails-reviewer` ran (diff touches `src/components/dashboard/`) and returned an explicit clean — no wizard binding, workspace scoping, role gate, `canSubmitVideo`, analysis-state branch or provider string affected; it also stress-tested the untrusted `fromPath` parse against protocol-relative, absolute-URL and `javascript:` payloads and found no open-redirect vector. `rls-boundary-reviewer` skipped: the diff touches no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/` path and introduces no table, view or query — `landingPath()` is pure routing with no data fetch.
- **changed:** `src/lib/workspace/actions.ts` (+120/−9), plus both switcher call sites. `setActiveWorkspace` now takes a second `fromPath` argument and ends in `redirect(landingPath(target, fromPath), RedirectType.replace)` after the cookie write and `revalidatePath`. Previously it only revalidated, which re-renders whatever path the switcher was clicked on — the wrong page whenever that route belongs to the workspace being left. The redirect makes Next stream the destination's Flight payload as the action response, so sidebar and body arrive from one post-cookie render and cannot disagree. New module-private `landingPath()` keeps the current path unless the new workspace cannot render it: team-only trees (`/dashboard/team`, `/dashboard/opponents`) fall back to `/dashboard`; staff-only pages fall back per `STAFF_ONLY_PAGES`; program-scoped record routes truncate to their list, since those loaders 404 on another program's id. `/dashboard/matches/[matchId]` is deliberately exempt — that loader reads by id under RLS, not by workspace. `fromPath` is treated as untrusted and reduced to a pathname via `new URL(from, 'http://workspace.invalid')`, then required to sit under `/dashboard`. The membership re-resolve that makes the client-writable cookie safe is byte-identical and still precedes every write.
- **follow-up:** both reviewers independently flagged, as a non-blocking nitpick, that `STAFF_ONLY_PAGES['/dashboard/team/upload']` maps to `/dashboard/team` while `team/upload/page.tsx` itself redirects a non-staff viewer to `/dashboard/team/schedule` — so the comment's claim of using "the destinations those pages redirect to themselves" is inaccurate for that one entry. Not a gate failure (both destinations are valid, and `/dashboard/team` gates its own staff controls), but the comment states something untrue and wants a follow-up task.

## T3 · Correct the STAFF_ONLY_PAGES fallback for team upload — done
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in `actions.ts`; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass — it was asked to be strict about criterion 2 and checked every factual claim the new comment makes against the source it describes, including the `TEAM_ONLY_TREES`-matches-first claim, and also confirmed neither constant has a call site outside this file. Guardrails: **both skipped**. The diff touches only `src/lib/workspace/actions.ts` — not `src/app/dashboard/`, `src/components/dashboard/` or the upload wizard (so no `pipeline-guardrails-reviewer`), and not `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or `supabase/migrations/`, with no table, view or query introduced (so no `rls-boundary-reviewer`).
- **changed:** `src/lib/workspace/actions.ts` only, one hunk, +6/−1. `STAFF_ONLY_PAGES['/dashboard/team/upload']` was `/dashboard/team`; it is now `/dashboard/team/schedule`, which is what `team/upload/page.tsx:47` redirects a non-staff viewer to. The doc comment's blanket claim that the values are "the destinations those pages redirect to themselves" was true of the settings entry and false of the upload one; it now names each destination against its page. A second paragraph records that the upload page's other guard — personal workspace to `/dashboard/matches/new` — deliberately has no entry, because `TEAM_ONLY_TREES` matches that path first and returns home before the map is read. The `/dashboard/settings/team` entry, `landingPath`'s body, the membership re-resolve, the cookie write and the `redirect` call are untouched.
- **note:** neither guardrail agent's trigger list covers `src/lib/workspace/`, even though `landingPath` is where the dashboard's post-switch role fallbacks now live. T2's version of this logic was reviewed only because that diff happened to also touch two files under `src/components/dashboard/`. Worth a task if that gap should be closed.

## T4 · Let players upload when their program allows it — blocked
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed files; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass — all 5 criteria met, and it re-verified the live schema itself rather than trusting the migration file. **Failed at 5c**, with findings from both guardrail reviewers; any finding blocks, and there is no severity triage at this gate.
- **stash:** 2c5f85b7fdb99e488a95fedb6ff639d6c79ef278 — recover with `git stash apply 2c5f85b7`
- **findings:**
  - `pipeline-guardrails-reviewer` (role gating): `canUploadForProgram()` is `isProgramStaff || playersCanUpload`, a program-wide policy only. The new code's own doc comment — and the migration's SQL comment — both assert a player needs a third thing, `program_members.upload_enabled`, "the per-person grant the database keeps". Nothing reachable checks it. That column defaults to false on every invite and is presented to coaches in the roster as a live "Can send video" switch. After this change a coach who flips that switch off for one player sees it flip, gets no error, and that player can still open the wizard and spend the program's shared monthly video budget — a silent false negative on a capability the guardrails single out because the spend cannot be taken back.
  - `rls-boundary-reviewer` (finding 2, introduced by this diff): the page's *default* path is broken for the people it was meant to admit. With no query params the page renders `LinePicker` over the upload queue; every row links to `?entry=<id>`, which makes the wizard write `event_entry_id`. The live trigger `matches_block_client_regraft` requires staff to set that column, with no `players_can_upload` exception, so a player admitted by the new gate picks a line and gets a raw `42501` surfaced as "Database error: …". `?match=`, `?player=` and the no-preset path still work. The trigger's own comment ("`/dashboard/team/upload` redirects non-staff") is now false and was not revisited.
  - `rls-boundary-reviewer` (finding 1, pre-existing but newly consequential): no policy, trigger or route handler reads `players_can_upload` at all. `matches` INSERT is `auth.uid() = created_by`; the trigger requires bare membership; `/api/splitstep/{upload-url,jobs}` authorize on ownership only. So "coaches only" is a UI-level control with nothing behind it — a player can drive the same calls directly. This diff is what first gives the flag real meaning in the UI, which is what makes the gap worth naming now.
- **changed:** nothing committed. The stash holds `src/lib/workspace/types.ts` (new `Workspace.playersCanUpload` and `canUploadForProgram()`), `src/lib/workspace/active-workspace-server.ts` (selects the column), `src/app/dashboard/team/upload/page.tsx` (gate swapped), `src/lib/workspace/actions.ts` (`STAFF_ONLY_PAGES` renamed `RESTRICTED_PAGES`, each entry carrying its page's predicate) and the untracked migration file.
- **THE DATABASE WAS ALREADY CHANGED, AND THE STASH DOES NOT UNDO IT.** Criterion 4 required applying the migration, so the subagent applied it before the gate ran. Verified live after stashing: `programs.players_can_upload` default is `true`, 1941 of 1941 rows are `true`, 0 are `false`, and version `20260824182016` is recorded in `supabase_migrations.schema_migrations`. The migration *file* is in the stash, so the repo no longer records a change the database has. With the code stashed the old `isProgramStaff` gate is back, so no player gains access — but Team Settings now reads "anyone" for every program, and the roster's per-member "Can send video" switch is still unbacked. Re-running T4 must not re-apply this migration; reverting it, if that is wanted, is a separate deliberate act.

## T5 · Re-land the player-upload gate without the line-picker dead end — blocked
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed files; tsc clean; 66/66 tests). **Failed at 5b**: `task-completion-reviewer` returned `VERDICT: needs-work` on criterion 1. 5c never ran — the gate stops at the first failure.
- **stash:** 914a8671df81c2eaa914022a6bfd40e903af554a — recover with `git stash apply 914a8671`. Note T4's stash is still present and is now `stash@{1}`; its SHA is unchanged at 2c5f85b7fdb99e488a95fedb6ff639d6c79ef278.
- **finding:** criterion 1 asked for T4's stashed work to be *carried*. Four of the five artifacts were restored faithfully — `actions.ts` and `active-workspace-server.ts` byte-identical to the stash, `types.ts` differing only by the legitimate criterion-4 comment rewrite. But the migration file was restored **and then edited**: 16 commentary lines inserted, and the `comment on column public.programs.players_can_upload` DDL string itself rewritten. That migration is already applied, so the file no longer matches what ran — the live column comment still holds the old sentence, and a fresh environment replaying these files would diverge from production. It is the same failure mode criterion 5 forbids, committed against the other applied migration in the same task. `.claude/skills/create-migration/SKILL.md` states it unconditionally: never edit an already-applied migration.
- **the other four criteria were met**, and the reviewer verified each by tracing code rather than trusting a report: the `?entry=` dead end is closed at both ends (`page.tsx:80-81` redirects a hand-typed `?entry=` for non-staff before the branch that sets `event_entry_id`; `page.tsx:220` returns the presetless wizard before the `LinePicker` is ever rendered to a non-staff viewer); `?match=`, `?player=` and the no-preset paths still work for an admitted player and staff still get the full picker; the false `upload_enabled` claims were removed rather than implemented, leaving T6 its work; and the stale trigger comment was corrected in `upload/page.tsx:72-79` — the file that trigger's comment names — with `20260821232306_...sql` itself untouched.
- **changed:** nothing committed. Re-running this task should restore `20260824182016_enable_players_can_upload_by_default.sql` byte-for-byte from stash 2c5f85b7 and put the commentary somewhere that is not an applied migration; everything else in stash 914a8671 is already gate-worthy.
- **note:** the implementing subagent terminated without a report — the machine slept mid-response, which also killed a guardrails agent it had spawned for itself. The work it left behind was judged on the diff alone. No database write occurred: `schema_migrations` still tops out at `20260824182016`, verified before the gate ran.

## T5 · Re-land the player-upload gate without the line-picker dead end — blocked (attempt 2)
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed files; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass — all 5 criteria met, and it recomputed the migration checksum itself rather than trusting the report. **Failed at 5c**: `pipeline-guardrails-reviewer` returned clean, but `rls-boundary-reviewer` returned a blocking finding. Any finding blocks, and there is no severity triage at this gate.
- **stash:** f21f594c62d16f8680c6691e58f690f8f9bfeac5 — recover with `git stash apply f21f594c`. Three stashes now stand: f21f594c (this attempt, migration file correct), 914a8671 (attempt 1, migration file wrong), 2c5f85b7 (T4, source of the correct migration file).
- **attempt 1's defect is fixed.** The migration file now matches stash 2c5f85b7 byte-for-byte — sha256 `fa24c387…52c7d1`, verified by me before the gate and recomputed independently by the completion reviewer. The displaced commentary moved into `Workspace.playersCanUpload`'s doc comment in `types.ts`, which the migration's own surviving comment already points a reader at. Delta from attempt 1 is that one comment plus the file swap; the other three source files are byte-identical.
- **finding (`rls-boundary-reviewer`, blocking):** the widened gate closes the `event_entry_id` door but not the `player1_id` door. Loosening the top-level check from `isProgramStaff` to `canUploadForProgram` made all three preset branches player-reachable, and only `?entry=` got a staff guard. In the `?player=` branch, `picked = roster.find(...)` resolves against `getLadder(active.id)` — the whole roster — and the preset carries `playerUserId: picked.userId` with `entryId: null`. `useUploadMatchWizard` then writes that teammate's id as `player1_id` while `created_by` stays the uploader's, so the `matches` INSERT policy (`auth.uid() = created_by`) is satisfied trivially and `matches_block_client_regraft` never looks at `player1_id` — it only validates `program_id` and `event_entry_id`. Net: any player admitted by `players_can_upload`, which the applied migration made the default for ~1,940 of 1,941 programs, can attribute a fabricated match, its video and its processing spend to any other named roster member. The victim sees it under their own account via the `player1_id IN (my_player_ids())` select policy. The `playerId` is not secret — it sits in the URL of any teammate's roster page, which players can open. Secondary and non-blocking: `?match=` is also newly player-reachable and leaks a teammate's match metadata into the preset, but the write fails closed under the UPDATE policy's `auth.uid() = created_by`.
- **the two reviewers disagreed.** `pipeline-guardrails-reviewer` traced the same `?player=` branch and judged it scoped-in product behaviour, citing T5's own criterion 3 ("`?player=` … still work[s] for an admitted player") and the pre-existing roster copy "Anyone on the team can upload for a teammate". `rls-boundary-reviewer` judged it a new authorization gap. The gate does not triage, so the finding blocks either way — but the disagreement is real and worth a human decision: if uploading for a teammate is intended product behaviour, criterion 3 and the roster copy say so, and the fix is to constrain *attribution* rather than to close the branch. A third agent, spawned by the implementing subagent for its own check, independently reached the same conclusion as `rls-boundary-reviewer` and called it critical.
- **changed:** nothing committed. No database write: `schema_migrations` still tops out at `20260824182016`, verified before the gate ran, and neither reviewer wrote.

## T7 · Bound match attribution to the program's own roster — done
- **gate:** mechanical green (lint 0 errors / 38 warnings — the same baseline as every prior run, so nothing new; the two hits inside changed files are pre-existing `setState`-in-effect warnings in `useUploadMatchWizard.ts`. tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass — it read the deployed function from `pg_proc` rather than inferring behaviour from the migration file. Guardrails: **both ran**. `pipeline-guardrails-reviewer` (diff touches `src/components/dashboard/`) — explicit clean, and it traced the id shapes end to end plus the `merge_program_players` carve-out. `rls-boundary-reviewer` (new file under `supabase/migrations/`) — "no blocking finding in this diff"; it diffed the deployed function against the prior trigger programmatically to confirm the change is strictly additive, and confirmed `recordResult`, `merge_program_players`, the wizard's self-upload, `?player=` and reuse-UPDATE paths all still satisfy the new predicate.
- **changed:** new migration `20260824211820_matches_bound_program_attribution.sql`, applied — `create or replace` on `matches_block_client_regraft`, now also firing `before insert or update of program_id, event_entry_id, player1_id`. When `program_id is not null`, `player1_id` must name a `program_players` row of that program or a `program_members` user of it; NULL still passes, for doubles. Both new blocks are gated on `new.program_id is not null`, so the personal path is provably untouched. Plus `explainWriteFailure()` in `useUploadMatchWizard.ts` so a refusal reads as the trigger's own sentence instead of `Database error: …`, and an "Uploaded by <name>" line on match detail resolved through the existing `program_roster_full` RPC and rendered only when the uploader differs from the attributed player.
- **this answers T5's blocker.** The `rls-boundary-reviewer` finding that blocked T5 was "the widened gate closes the `event_entry_id` door but not the `player1_id` door". That door is now shut in the database, which is the layer the finding asked for. T5's stash f21f594c should be re-queued next.
- **follow-up worth filing:** both 5c reviewers independently flagged, as pre-existing and explicitly out of scope, that a match with `program_id is null` still has no constraint on `player1_id` at all — a crafted PostgREST insert naming a stranger's `auth.uid()` makes the row readable by that stranger through the `player1_id in (select my_player_ids())` select policy. T7's criterion 5 required that path unchanged, so it is not a defect in this diff; it is the same primitive left open on the other side of the `program_id is null` branch.
- **housekeeping:** `supabase/.temp/` (Supabase CLI cache — project ref, org id, a password-less pooler URL, version strings) was created in the worktree by the gate's own reviewers and removed before committing so it could not be swept in by `git add -A`. `.gitignore` has no rule covering it, which is why it keeps reappearing; worth adding one.

## T5 · Re-land the player-upload gate without the line-picker dead end — done (attempt 3)
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed files; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass — it recomputed the migration checksum and, because criterion 5 had become a moving target, re-checked every factual claim the diff's comments make against the *currently deployed* `matches_block_client_regraft` read from `pg_proc`, not against the migration files. Guardrails: **both ran, both explicitly clear.** `pipeline-guardrails-reviewer` (diff touches `src/app/dashboard/`) confirmed the three wizard inputs untouched and that this composes with T7 rather than fighting it. `rls-boundary-reviewer` — the agent that blocked attempt 2 — verified T7's trigger live (`pg_proc`, `pg_trigger`, `pg_policy`), traced all four paths a newly admitted player can reach, and returned "no findings. This diff is safe to land."
- **changed:** `src/lib/workspace/{types,active-workspace-server,actions}.ts` and `src/app/dashboard/team/upload/page.tsx`, plus the restored untracked migration `20260824182016_enable_players_can_upload_by_default.sql` (sha256 `fa24c387…52c7d1`, byte-identical to stash 2c5f85b7, not re-applied). `/dashboard/team/upload` now gates on `canUploadForProgram()` (`isProgramStaff || playersCanUpload`) instead of `isProgramStaff` alone; `Workspace` carries `playersCanUpload`, resolved server-side beside `canSubmitVideo`; `STAFF_ONLY_PAGES` became `RESTRICTED_PAGES`, each entry holding the page's own predicate so the switcher's landing prediction and the page's gate share one spelling. Non-staff cannot reach `?entry=` by rendered link or typed URL, so the line-picker `42501` dead end is closed.
- **delta from attempt 2 (stash f21f594c):** three comment corrections in `upload/page.tsx`, no logic change. T7 landed between the two attempts and falsified some of what attempt 2's comments asserted: the "Database error:" wording (T7's `explainWriteFailure()` now drops that prefix for `42501`), the referent of the stale-comment NOTE (the trigger was replaced, so the stale sentence now survives only in `20260821232306`'s file header), and a two-check description of a trigger that now has three. Criterion 5 is about a comment telling the truth, and a comment true against the old trigger would have failed it against the new one.
- **the three-attempt arc, for the record:** attempt 1 blocked at 5b for editing an already-applied migration; attempt 2 blocked at 5c because nothing bounded `player1_id`; T7 fixed that at the database layer; attempt 3 passes. The blocking finding each time was real and none of them was visible to the task's own `done when:` list — which is the argument for the guardrail stage existing at all.
- **note:** stash f21f594c, 914a8671 and 2c5f85b7 are now spent. T4 remains `blocked` as bookkeeping, but its work has landed via this task. The stashes can be dropped once the branch is merged.

## T6 · Make the roster's "Can send video" switch real — done
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed files; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass — it verified both column defaults and the row counts against the live database rather than the migration file, and independently confirmed the missing-RPC claim by locating the definition in `20260818041025` and the drop in `20260818043926`. Guardrails: **both ran, both explicitly clear.** `pipeline-guardrails-reviewer` confirmed the three wizard inputs untouched and that narrowing the roster switch hid nothing anyone needs. `rls-boundary-reviewer` audited the new `SECURITY DEFINER` function against the live catalog — staff-gated on the caller's role for the passed `p_program_id`, `search_path` pinned, `prosecdef` true and necessary (no UPDATE policy exists on `program_members`), `has_function_privilege` false for `anon` and true for `authenticated`, one signature only, no cross-program or self-grant escalation path.
- **changed:** new migration `20260824223337_enforce_member_upload_enabled.sql`, applied. `Workspace` gains `memberUploadEnabled`, resolved in the same `program_members → programs` join that already yields `playersCanUpload`, coerced with `Boolean(...)` so a broken read fails closed. `canUploadForProgram()` becomes staff → true by early return, else `playersCanUpload && memberUploadEnabled` — so the per-member grant can only ever narrow a player, never bounce staff. Live state after: both defaults true, 3/3 member rows enabled.
- **two things the subagent found that the task did not anticipate, both accepted by 5b as required rather than creep:**
  1. `program_invites.upload_enabled` had to flip too. `accept_program_invite` copies the invite's value into the new `program_members` row rather than letting the member table's default apply, so flipping only the column criterion 3 names would have left every newly invited player disabled — criterion 3 would have been satisfied on paper and false in practice.
  2. **`set_member_upload_enabled` did not exist.** It was defined in `20260818041025` and dropped three migrations later by `20260818043926` ("It comes back with the control that needs it"), and never restored — while the roster feature, which landed later, has been calling it. The roster's "Can send video" switch has therefore been writing to nothing since it shipped. The migration restores it, and the RLS reviewer confirmed the restoration reproduces the *cumulative* pre-drop state, including a `revoke ... from anon` that a separate later migration had added — a verbatim copy of `20260818041025` alone would have left `anon` holding execute.
- **comment maintenance across tasks:** T5 deliberately landed a `types.ts` comment stating that nothing consults `upload_enabled`. T6 is what made that false, and criterion 5 existed to catch it. Rewritten, along with a now-false claim in `roster-actions.ts` that the RPC "has existed since the membership migration" — which is precisely why nobody noticed the switch was inert. This is the third time on this branch that a comment true when written was falsified by a later task; the pattern is worth remembering, not just fixing.
- **known gap, unchanged and out of scope:** `/dashboard/matches/new` is still ungated, so a player with a team workspace active can file against the program from the personal wizard route regardless of either flag. Pre-existing, applies equally to `players_can_upload`, and confirmed by both reviewers as neither introduced nor worsened here. The page gate is admission, not authorization — `matches_block_client_regraft` remains the enforcer.

## T8 · Enforce the upload permission where the budget is spent — blocked
- **gate:** mechanical green (lint 0 errors / 38 warnings, none in the changed files; tsc clean; 66/66 tests). `task-completion-reviewer` VERDICT: pass — all 5 criteria met, and it verified the highest-risk one (personal uploads unaffected) by reading the `kind` check's ordering rather than trusting the report. **Failed at 5c**: `rls-boundary-reviewer` returned an explicit no-findings, but `pipeline-guardrails-reviewer` returned two. Any finding blocks; there is no severity triage at this gate.
- **stash:** 8100b829313dfb4fa25c4cda17ba61a5143b47f6 — recover with `git stash apply 8100b829`
- **findings:**
  1. **The new pre-transfer seam checks one gate and not the other.** `upload-url/route.ts` calls `explainVideoRefusal(billingWorkspace)` but never `billingWorkspace.canSubmitVideo`. So a coach, or a fully authorised player, of a program still in `pending_review` mints the SAS, transfers gigabytes over tens of minutes, and is refused only at `/api/splitstep/jobs` by the untouched `canSubmitVideo` check — on a wasted blob. No quota is spent, so the "spend cannot be taken back" guardrail holds in the letter; but it defeats the rationale the diff writes for itself two lines above ("refusing before the credential is minted costs one cached lookup and spends nothing"). Not a regression — upload-url checked nothing before — but the diff builds exactly the scaffolding needed and stops one boolean short. One line closes it.
  2. **A comment the diff adds is false.** `upload-url/route.ts` says the sentence goes back as `error` "and the upload card renders it". Two reviewers traced it independently: the refusal now fires *before* `submit-match-video.ts` emits its `"started"` event, so `UploadMatchFlow`'s reducer finds no map entry for the matchId and silently drops the `"failed"` event. `UploadMatchSuccess` then falls through to **"Sent for analysis. Results are added as soon as they're ready."** — false, on the success screen, for an upload that was refused and whose job is already `status: 'failed'`. The sentence does reach the person, but via the global `UploadFailureListener` toast, not the card. The dropped-event behaviour is pre-existing (any pre-`started` failure hits it); what this diff changes is frequency — it was reachable only by session expiry or a bad extension, and now every barred player meets it.
- **changed:** nothing committed. The stash holds `src/lib/workspace/types.ts` (new `explainVideoRefusal()` and `billingWorkspaceFor()`), `src/lib/services/splitstep/quota.ts` (the authoritative check inside `reserveQuota`, beside the untouched `canSubmitVideo` block), and both `src/app/api/splitstep/{jobs,upload-url}/route.ts`.
- **the approach is right and worth re-landing.** Putting the authoritative check inside `reserveQuota` — the sole caller of `reserve_processing_quota`, itself `revoke`d from `anon` and `authenticated` — is what makes criterion 5 structural rather than a page guard. The RLS reviewer confirmed a barred player cannot reach the RPC by any route, that `billingWorkspaceFor` cannot resolve a workspace the caller is not a member of and refuses on `undefined` rather than skipping, and that no refusal path leaves a partial job, a minted SAS or an unreleased reservation. Re-running should keep all of that and fix only the two findings above.
- **no database write.** This task needed no migration; both flags already existed and were already resolved onto `Workspace`.
