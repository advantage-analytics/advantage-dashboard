# Run log — claude/admin-claims-redesign-c793cc

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Security migration: block `users.is_admin` self-promotion — done

- **gate:** mechanical — GATE PASS (lint, typecheck, tests). completion — VERDICT: pass, every `done when:` line confirmed independently against the live database (own MCP queries, not just the implementer's report).
- **changed:** Added `supabase/migrations/20260914100000_users_block_admin_self_update.sql` and applied it to the LIVE database via the Supabase MCP. New trigger `users_block_admin_self_update` (mirrors `users_block_plan_self_update`) blocks any `authenticated`/`anon` session from changing `is_admin`, raising `42501`. Table-level `ALL` grants to `anon`/`authenticated` on `public.users` (which predated this task and covered every column, `is_admin` and `plan` included) were revoked and replaced with `authenticated`: SELECT on the table, UPDATE on all columns except `is_admin` and `plan`; `anon` now has no grants on `public.users` at all. No INSERT grant added for `authenticated` — sign-up still runs through `handle_new_user()` (SECURITY DEFINER, owned by `postgres`), which is unaffected. Added partial index `users_admins_idx on users (id) where is_admin` for the upcoming admin-notification fan-out. The existing admin user is unchanged and `is_admin()` still resolves true for them; a regular authenticated profile update still succeeds.
- **follow-ups:**
  1. The migration landed on the live DB timestamped `20260914231500` rather than the repo filename's `20260914100000` (the repo migrations folder is already ~100 behind live). Harmless — every statement in the file is idempotent (`create or replace`, `drop trigger if exists`, `create index if not exists`, and re-running the revoke/grant is a no-op) — but worth a decision at `/pr-check` about whether to rename the repo file to match the live timestamp for future-reader clarity.

## T2 · Test: a signed-in user cannot self-promote to admin — done

- **gate:** mechanical — GATE PASS (lint, typecheck, tests). completion — VERDICT: pass; the reviewer independently re-ran `npx playwright test admin-self-promotion` live (3 passed) and again with the Supabase env vars blanked (3 skipped, stated reason), rather than trusting the implementer's report.
- **changed:** Added `tests/admin-self-promotion.spec.ts`, following `tests/teams-management.spec.ts`'s structure and the shared `tests/fixtures/live-db.ts` fixtures. Proves T1's fix end to end against the live DB: a session client's self-promotion `update` fails with `42501` and leaves `is_admin` false; `rpc('is_admin')` still returns false afterward; a service-role client can still set it (proving the guard is role-scoped, not absolute). Cleans up unconditionally in `afterAll` (resets `is_admin`, deletes the test users) — confirmed zero stray rows for the run-marker prefix in both `auth.users` and `public.users` after the run.

## T3 · Schema: claim verification columns + admin query indexes — blocked

- **gate:** mechanical — GATE PASS (lint, typecheck, tests). completion — VERDICT: needs-work.
- **reason:** Six of seven index/constraint/column criteria were applied correctly and confirmed live. The seventh, `processing_usage_account_month_idx on processing_usage (account_id, account_type, billing_month)`, collided with a pre-existing index of the SAME NAME but a DIFFERENT (narrower) definition — a partial index `... WHERE released = false`, added earlier for the quota-reservation release path. `create index if not exists` matches by name only, so the migration's statement for that index was a silent no-op: the unconditional index the criterion describes does not exist live, only the older partial one under that name. The reviewer independently tested (via `EXPLAIN` with `enable_seqscan` forced off) that the partial index is planner-provably sufficient for T18's stated usage-rollup convention (`... and not released`, which Postgres can prove implies `released = false`), but is unusable for any query that needs released/historical rows too — a shape T21/T22 (not yet written) may well need, and there is currently no index path for it at all. The migration's own comment also states the pre-existing index "already satisfies this name," which reads as the requirement being met when it silently isn't — a trap for the next reader of `supabase/migrations/`.
- **fix needed:** create the unconditional 3-column index under a distinct name (e.g. `processing_usage_account_month_all_idx`), or explicitly amend this task's criterion to accept the partial index on the reviewer's proof and update the migration's comment to say so plainly rather than "already satisfies."
- **stash:** 49a48482cfef7d1805e71bef702f62a51b461d43 (`supabase/migrations/20260915003708_claim_verification_and_admin_indexes.sql`, all six correct changes included — only the one index statement needs revision).
- **note:** the six correct schema changes (columns, constraint, five of six indexes) ARE already live on the database — this migration was applied via the Supabase MCP before the review found the index gap. Re-running the corrected file only needs to add the missing index; everything else is idempotent and safe to re-apply.

## T3 · Schema: claim verification columns + admin query indexes — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently re-verified all eight criteria against the live database, especially re-checking the fixed index's exact definition (no WHERE clause) rather than trusting the report.
- **changed:** Retry of the previously blocked T3. Recovered the six already-correct changes from the T3 stash (49a48482, now dropped) and fixed the one gap: added a new unconditional index `processing_usage_account_month_all_idx on processing_usage (account_id, account_type, billing_month)` under a distinct name, since the originally-specified name collided with a pre-existing, semantically different partial index (`WHERE released = false`) that a `create index if not exists` had silently left unchanged. That pre-existing index was left untouched. The migration's comment was rewritten to describe the name collision accurately instead of claiming false equivalence. All seven columns/constraint/indexes on `program_claims`, `program_requests`, `programs`, and `processing_usage` are now live and confirmed valid (`pg_index.indisvalid`/`indisready` both true).
- **follow-ups:**
  1. The fix applied as a second migration version (`20260915011946`) alongside the original (`20260915003708`), so the live migration ledger has two near-identically-named entries where the repo holds one file. The file is fully idempotent and reproduces live state exactly, but the ledger naming is a small hygiene item worth reconciling at `/pr-check`.
  2. `processing_usage` now carries two similar indexes over the same three columns (one partial on `released = false`, one unconditional) — whether the partial one still earns its keep, or could be dropped once nothing depends on its specific shape, is a branch-level question for `/pr-check`.

## T4 · RPCs: admin gates on existing functions + two new admin-only RPCs — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently pulled all six live function definitions via `pg_get_functiondef`, re-ran the `42501`/success spot-checks under its own simulated sessions rather than trusting the implementer's report, and specifically probed for SQL injection in `admin_create_program` with a hostile school-name payload (handled correctly as a bound value, not parsed).
- **changed:** `set_program_member_role`, `set_program_crest`, `create_program_invite` (6-arg), and `revoke_program_invite` were recreated from their LIVE bodies (read fresh via `pg_get_functiondef`, not the ~100-migrations-behind repo folder) with minimal admin-gate additions — `is_admin()` now resolves an admin's role as `owner` in `set_program_member_role`, and the other three widen their staff-only gate to `is_program_staff(...) or is_admin()`. Two new functions: `admin_transfer_program_ownership` (locks the program row, demotes any current owner to coach, requires the target already be coach/staff, promotes, updates `programs.owner_user_id`, writes an `ownership.transferred` audit row tagged `by_admin: true`) and `admin_create_program` (validates org_type/name/team/division against the live CHECK constraints in both directions, inserts `status='unclaimed'`). `create_custom_program` and `transfer_program_ownership` were left untouched — neither fits an admin acting on a program they don't belong to, which is why the two new functions exist instead of widening those. All six are `security definer`, `set search_path=''`, with no `public`/`anon` execute grants. Applied and verified live: non-admin/non-member gets `42501` from both new functions, an admin succeeds on a foreign program, `programs_one_owner` still holds, and every throwaway row created during verification was cleaned up.
- **follow-ups:**
  1. `admin_transfer_program_ownership` requires the target to already be `coach`/`staff` on the program — an admin cannot yet rescue a program whose only member was a departed owner (no staff left to promote). Matches the written spec; worth a follow-on task if that scenario needs handling.
  2. `public.is_admin()` (pre-existing, not introduced here) carries `EXECUTE` to `anon` in its ACL — harmless as written since it resolves through `auth.uid()`, but every new admin RPC now depends on this gate, so it's worth a look at `/pr-check`.

## T5 · Test: admin RPC gates — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently re-ran the live-DB spec (7 passed) and the env-blanked skip path (7 skipped), confirmed the UPDATE-based `programs_one_owner` collision test against the live index definition rather than assuming equivalence to a literal INSERT, and ran its own post-run stray-row query across five columns/tables (not just the marker prefix on one column).
- **changed:** Added `tests/admin-program-rpcs.spec.ts`, covering all five cases from T4's RPCs against the live database: an admin changes a role on a program they don't belong to; a non-member non-admin gets `42501` on the same call; `admin_transfer_program_ownership` moves both the member role and `programs.owner_user_id` and a second owner row for the same program still fails `23505` (`programs_one_owner` still holds); the same function succeeds when the program has no owner row at all; `admin_create_program` refuses a non-admin, refuses a college program missing key/group/team, and creates a club program with `program_key` null and `status='unclaimed'`. Follows `tests/teams-management.spec.ts`'s and `tests/admin-self-promotion.spec.ts`'s conventions (fixture module, serial mode, guarded `afterAll` cleanup, run-marker prefix `admin-rpc-`). Verified zero stray rows post-run.

## T6 · Guard module, action revalidation, pilot constants — blocked

- **gate:** mechanical — GATE PASS. completion — VERDICT: needs-work.
- **reason:** Everything else was correct and independently re-verified (requireAdmin/requireAdminOrNotFound match admin/layout.tsx's three-branch semantics exactly, all three revalidatePath calls updated, config.ts's three exports are genuinely thin derivations with no duplicated numbers, formatPilotEnd() confirmed UTC-stable across two extreme timezones, tsc clean). The one gap: `grep -rn "/admin/claims" src` still found `src/app/admin/layout.tsx:41 href="/admin/claims"`. The orchestrator's own dispatch instructions had told the implementer not to touch admin/layout.tsx ("a later task owns the shell"), and the implementer followed that literally and flagged the resulting conflict rather than guessing. The reviewer sided with the criterion: the criterion's own parenthetical ("no code should reference the old path by then") anticipates exactly a stale nav link, and swapping one string in an existing `<Link>` doesn't touch the shell's structure the way a later redesign would.
- **fix:** change `src/app/admin/layout.tsx:41`'s `href="/admin/claims"` to `href="/admin"` (the destination the new `revalidatePath("/admin", "layout")` calls already assume); link text unchanged.
- **stash:** f02470b79f27f0530dbbc0713eeb2b98b7c4ab5b (admin-guard.ts, admin-actions.ts, splitstep/config.ts, and the two doc-comment fixes in email/index.ts and templates/claim.ts — all six criteria besides the grep are already correct in the stash).

**Correction:** the T6 stash above was briefly applied and re-stashed while checking whether the reviewer's fix could be applied in the same invocation (it should not have been, without a fresh /task-next call — retrying belongs to a separate invocation, as with T3). No content changed; the stash is identical, just under a new SHA since `git stash push` always creates a fresh commit: `a66bf5b41182e44654df2ebec4bad608be3be6be`. Use this SHA to recover it, not the one recorded above.

## T6 · Guard module, action revalidation, pilot constants — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently re-ran `tsc` and the grep criterion, and confirmed the fix stayed a genuine single-line change to `admin/layout.tsx` rather than touching its structure.
- **changed:** Retry of the previously blocked T6. Recovered the five already-correct files from the T6 stash and applied the reviewer's exact fix: `src/app/admin/layout.tsx`'s stale nav link `href="/admin/claims"` → `href="/admin"` (visible text unchanged, no other edit to that file). New `src/lib/services/programs/admin-guard.ts` exports `requireAdmin()` (moved out of `admin-actions.ts` verbatim) and `requireAdminOrNotFound()` matching the layout's own guard semantics. `admin-actions.ts` now imports the shared guard and revalidates `/admin` (layout-wide) instead of the soon-to-be-deleted `/admin/claims`. `splitstep/config.ts` gains `PILOT_ENDS_AT`, `formatPilotEnd()` (UTC-stable), and `getMonthlyCapHours()` (a thin derivation, no duplicated 75/2 numbers). Two stray doc-comment mentions of the old path in the email service were reworded. `grep -rn "/admin/claims" src` is now empty.

## T7 · Shared primitives: ViewPills, extracted vertical stepper, plan-pill tokens — blocked

- **gate:** mechanical — GATE PASS. completion — VERDICT: needs-work.
- **reason:** Five of six criteria met and independently re-verified in detail (ViewPills matches LifecycleChips geometry line-for-line; the Step/StepMark/LABEL_INK/StepState extraction is character-identical modulo the deliberate, correct prop-shape change from a wizard-specific StepView object to explicit label/state/value/last/children props, with the reviewer confirming there is exactly one call site and no orphaned references; colors.css tokens added via sed with no reformatting; plan-pills.tsx matches spec; design-drift seeds unchanged). The one gap: the criterion's render-check ("`npm run dev` render check (or existing wizard Playwright spec) confirms the stepper is visually unchanged") never actually happened — no spec reaches this screen, and no dev-server browser check was done. The implementer substituted a careful manual line-by-line diff review, which the reviewer called strong evidence but not the check the criterion names, on a live screen with zero test coverage.
- **stash:** 0c2bf818fcd27980b0f097f1803222d4f01b5e27 (view-pills.tsx, plan-pills.tsx, vertical-steps.tsx, the UploadMatchSuccess.tsx extraction, the colors.css tokens — all five other criteria already correct in the stash, only the render verification is missing).
- **follow-ups:**
  1. The reviewer flagged the pilot token colour choice (violet, `#6D28D9`) as a design-system judgment call worth a look at `/pr-check`: the design system's stated precedent for "needs its own colour but blue is taken" is grey (`YouPill`), not a fourth semantic hue with no v3 provenance. Not a gate failure — the criterion only constrains token shape, which is met — but worth a second look before this ships.
  2. `--pilot-bg`/`--pilot-text` are declared on `:root` only, correctly per the criterion's "light-mode values," but will fall through to light values in the `.dark` scope if dark mode ever reaches this UI.

## T7 · Shared primitives: ViewPills, extracted vertical stepper, plan-pill tokens — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently reproduced the dev-server build blocker described below (real, pre-existing), confirmed byte-identity of the Step/StepMark extraction against the diff, and ran tsc/design-drift/a full production build itself rather than trusting the report.
- **changed:** Retry of the previously blocked T7. Recovered the five already-correct files from the T7 stash and closed the one gap: an actual rendered check of `UploadMatchSuccess`'s stepper via a throwaway route (deleted before finishing, confirmed by `git status`) rendering the component with three fixture match/upload states, exercising all four `StepMark` visual states (done/now/later/fail) plus right-aligned progress/file-size values. All rendered correctly, zero console errors. `view-pills.tsx` (generic `ViewPills`), `plan-pills.tsx` (`PilotPill`, `ApproveChip`), and `vertical-steps.tsx` (`VerticalStep`, `StepMark`, `StepState`, extracted from `UploadMatchSuccess.tsx`) are now available for the admin console to reuse; `colors.css` gained `--pilot-bg`/`--pilot-text`.
- **follow-ups:**
  1. **Pre-existing, unrelated to this task's diff, but real and worth a separate fix:** `npm run dev` (not `npm run build`) currently fails on every route in this branch, because Tailwind v4's automatic content-scanning has no `@source`/`@config` restriction in `globals.css` and picks up the literal string `` `rounded-[var(--radius-*)]` `` from the tracked task-queue file `.claude/tasks/claude-admin-claims-redesign-c793cc.md` (introduced in commit `8a6c9e01`, well before T7), trying to parse it as a utility class and crashing the dev build. Production builds are unaffected (`npm run build` succeeds). Confirmed independently by the completion reviewer. Worth a `/task-add` on its own: either scope Tailwind's content glob with `@source` to exclude `.claude/`, or stop writing raw utility-class-shaped strings into queue files.
  2. The design-system judgment on the pilot token colour (violet vs. grey) flagged in the previous blocked attempt still stands as a non-blocking note for `/pr-check`.
  3. `VerticalStep`'s `children` prop became optional where the original `Step` required it — harmless at the sole call site, and the right signature for a shared primitive, but the one non-identical byte in an otherwise verbatim extraction.

## T8 · Admin shell: layout, header, account menu — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently re-verified the live Requests count against the database (claims=5, requests=1 → 6, matching the report), confirmed the guard/radius/focus criteria mechanically, and checked the flagged FloatMenu icon-colour deviation against chrome.md and every other FloatMenu consumer in the codebase.
- **changed:** `admin/layout.tsx` now guards via T6's `requireAdminOrNotFound()` and renders new `AdminHeader` + a `--surface-page` `<main>` capped at 1200px, replacing the old inline chrome. `AdminHeader`: wordmark + divider + "Admin" label, a centred four-tab nav (Teams/Requests/Conferences/Uploads) with a live Requests count as plain text (never a badge), and a disabled 28px Search placeholder for T12. `AdminAccountMenu`: avatar + FloatMenu with "Back to the dashboard" and "Sign out" behind a 440px ConfirmDialog, calling the same `signOut({ scope: "local" })` as the dashboard's own logout. Verified live with a throwaway admin user (created and cleaned up during the run): admin session gets the shell and a 200 on the one route that still exists (`/admin/claims`); the same user demoted to non-admin gets 404.
- **follow-ups:**
  1. `FloatMenuItem`'s shared icon slot hard-codes `text-[var(--blue)]`, which chrome.md says menu-row glyphs should never be. This task's two menu items correctly override it inline (matching how every other FloatMenu consumer in the codebase already does), so nothing new shipped — but the primitive's blue default is now confirmed dead code no consumer relies on, and a shared `MENU_ROW_ICON` constant (several files each inline the same override) would be a one-line cleanup worth its own task.
  2. `layout.tsx` calls `auth.getUser()` a second time after `requireAdminOrNotFound()` already resolved it — two session round-trips per admin page render. Not incorrect, just worth a look at `/pr-check`.
  3. The Requests count query uses the service-role client inside a layout (necessary — `program_requests` has no RLS policies at all, so it's the only way to read it), gated by the guard running first. Worth a specific look from `rls-boundary-reviewer` at `/pr-check` given the pattern (service role in a layout, not a server action) is new to this admin console.
  4. Full route-by-route coverage of "all four tabs render inside the shell" is still pending T9, which creates the three placeholder pages that don't exist yet — expected ordering, not a gap in this task.

## T9 · Placeholders, redirects, deletions, MAP + drift seed — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently re-ran the full test suite (1195 passed), the two named specs directly, and the live redirect check on its own production server.
- **changed:** `/admin` now redirects to `/admin/teams` (server-side, not yet a real page — lands in T12); `/admin/conferences` and `/admin/uploads` render `ComingSoonPage` pointing back to `/admin/teams`; `next.config.ts` temporarily redirects `/admin/claims` → `/admin/requests` (307, confirmed live); the old `admin/claims/page.tsx` and `review-rows.tsx` are deleted; the design-drift `hex` seed dropped to 0 now that its one tracked violation is gone with the file; the email service's stale doc-comment reference to the old admin route was repointed; `MAP.md` regenerated.
- **follow-ups:**
  1. Both `/admin` and the two Coming Soon pages link to `/admin/teams`, which doesn't exist until T12 — following them today 404s. Explicitly accepted ordering, flagged only so it isn't forgotten if T12 slips.

## T10 · Teams list loader — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently re-ran the loader's actual PostgREST queries against the live DB for all four views, stress-tested the cursor's comma/quote-escaping with adversarial school names, and confirmed the owner FK constraint name directly against `pg_constraint`.
- **changed:** `src/lib/data/admin-teams-server.ts` exports `listAdminTeams()` with keyset pagination on `(school_name, id)`, all four views (On Advantage / In pilot / Needs review / Whole directory) verified against the live 1,942-program table — including a Needs-review row that qualifies only through an open `program_requests` row with zero claims. `tests/admin-teams-cursor.spec.ts` covers the pure cursor encode/decode pair.
- **follow-ups:**
  1. The code comment justifying the "embed all claims, reduce in JS" approach asserts PostgREST can't do a true latest-only embed. The reviewer found this is actually supported (`.order(...,{referencedTable}).limit(1,{referencedTable})`) and confirmed it live. Not a criterion miss — the one-query requirement is still met, and claim volume per program is tiny (max 5) — but the comment is factually wrong and the simpler embed would avoid the JS reduce entirely. Worth a small follow-up.
  2. `crestUrl()` constructs a fresh cookie-bound Supabase client per row (50/page) — no network I/O per call, but worth a look if this page ever feels slow.
  3. The two `needs_review` precomputation queries (`reviewClaimProgramIds`, `openRequestProgramIds`) read their tables unpaginated — fine at today's volume (10 claims, 1 open request) but subject to PostgREST's default row cap if those tables grow substantially.

## T11 · Teams table component — blocked

- **gate:** mechanical — GATE PASS. completion — VERDICT: needs-work.
- **reason:** Six of eight criteria met (layout module pattern, columns, 52px rows with header-only hairline, ProgramCrest 26px, TableEmptyBody empty state, no scope creep). Two real problems:
  1. **A genuine navigation bug.** The Plan cell's `ApproveChip` click was guarded with `<span onClick={(e) => e.stopPropagation()}>` wrapping the button, on the reasoning that `ApproveChip`'s prop type is `() => void` not an event handler. The reviewer traced the actual event flow: React dispatches the click through button → span → `stopPropagation()` halts further dispatch, but that means `next/link`'s own anchor `onClick` (where `preventDefault()` lives) never runs, so the browser's default anchor action fires and the row still navigates — as a hard page load, worse than the click doing nothing. `stopPropagation` alone doesn't prevent navigation; `preventDefault` does, and nothing calls it.
  2. **No render check was performed at all**, despite the criterion naming "fixture/mock rows" as the thing to render against. The implementer built fixtures and a throwaway preview route but never viewed it (`npm run dev` is broken for the known, unrelated Tailwind/task-queue reason) and deleted the route without falling back to `npm run build && npm run start`, which worked for every prior task in this chain that hit the same blocker.
     Also flagged, not necessarily blocking on their own but worth checking in the retry: `teams-table.tsx` has no `"use client"` directive despite using `onClick` handlers and function props — could crash if rendered from a Server Component; and the State column (`w-10`, 40px) may be too narrow for its own "State" header at `eyebrow-sm` — a real render check would have caught both.
- **fix:** (a) widen `ApproveChip`'s `onClick` prop in `src/components/admin/plan-pills.tsx` (T7's file, already committed — this is an explicit, disclosed amendment) to accept the click event and call `event.preventDefault()` itself, then pass it straight to the underlying `<button>`; remove the ineffective span wrapper in `teams-table.tsx`. (b) Perform an actual render check via `npm run build && npm run start` against the fixture rows (pilot/approve/none/null-conference-state/null-crest), confirming: the approve chip no longer navigates when clicked, `"use client"` is added if the component genuinely needs it, and the State column visually fits its header.
- **stash:** e5ddb7053eb977d624b5c1d3dbab429525f3acfe (teams-table-layout.ts, teams-table.tsx, the program-crest.tsx size-union edit — the layout module and crest edit are already correct; teams-table.tsx needs the click-guard fix and a real render check).

## T11 · Teams table component — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently reproduced the click-does-not-navigate fix with its own Playwright harness against `npm run build && npm run start` (zero `framenavigated` events after the click, `onApprove` fired once), rather than trusting the report, and confirmed the event-dispatch reasoning (preventDefault suppresses the anchor's hard navigation, stopPropagation then blocks next/link's soft-navigation handler).
- **changed:** Retry of the previously blocked T11. Recovered the correct layout module and ProgramCrest edit from the stash, then fixed the real bug: `ApproveChip`'s `onClick` prop (T7's `plan-pills.tsx`) widened from `() => void` to accept the click event, so its own click handler — not an ineffective wrapping span — calls `preventDefault()`/`stopPropagation()` before the row's `Link` ever sees the click. Added `"use client"` to `teams-table.tsx`, which was missing it despite owning real event handlers. Performed the actual render check the previous attempt skipped: a throwaway fixture route via `npm run build && npm run start`, confirming all five plan/edge-case fixture rows render correctly and the State column header isn't cramped at 40px (measured, not clipped).
- **follow-ups:**
  1. The Members column header slightly overflows its `w-12` track (stops 2px short of touching Plan) at 9px eyebrow size — not clipped, not a stated criterion, but worth a look during visual polish.

## T12 · Teams page: content, approve-pilot popover, admin search — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: needs-work, resolved without a code retry. The reviewer independently confirmed via the Supabase MCP that the real, pre-existing UCLA `pending_review` claim (`7377fd6e…`) was never touched (a prior dispatch attempt for this task had been blocked by the safety classifier specifically because it proposed transitioning that live claim; the retry correctly used only its own disposable fixtures instead) and that every code criterion was met — but found two inert throwaway users (`t12check-mu25y942-admin@example.com`, `t12check-mu25y942-claimant@example.com`) the verification run had created and not cleaned up. Deleted both directly (confirmed by id before deleting, re-confirmed empty afterward); no code change was needed, so no retry dispatch was required.
- **changed:** `/admin/teams` (force-dynamic) renders `TeamsPageContent`: `ViewPills` wired to the URL, `FilterTrigger`/`SortTrigger` opening `FloatMenu` panels (division/conference/state filters — a small additive extension to T10's loader; sort is honestly limited to the two orders T10's keyset actually supports, name A–Z/Z–A), a one-sentence grey summary strip (never chips, per tables.md), and "Load more" via a new `teams-actions.ts` server-action wrapper (keeps the service-role loader out of the client bundle, still re-checks admin on every call). `ApprovePilotPopover` shows the pilot terms and calls the existing `approveClaim`/`rejectClaim` actions, anchored to the clicked chip's captured bounding rect since T11's `onApprove` hands over the row, not the click event. `AdminSearch`/`adminSearchTeams` fills T8's Search placeholder. "Create team" is a stub pending T23.
- **follow-ups:**
  1. This is the second task in this chain (after several DB-writing verification runs) to leave orphaned test-fixture rows behind — the reviewer noted `jr-req-*`/`pend-inv-*` users from earlier tasks are also still on the live database. Worth a `/task-add` for a one-off cleanup pass, and worth tightening future dispatch instructions to require an explicit post-verification existence check (not just a cleanup step that can silently fail partway).
  2. Approving a claim from the `needs_review` view makes the row disappear from that view entirely (correctly — it no longer needs review) rather than visibly flip to `PilotPill` in place; the flip is only observable in `on_advantage`/`all`. Likely the right behavior, but worth a human eye at `/pr-check`.
  3. `admin-search-server.ts` lives under `src/lib/data/` per the task's own `files:` field, slightly inconsistent with `teams-actions.ts`'s placement under `src/components/admin/` — a naming/placement inconsistency, not a defect.

## T13 · Requests list loader — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass. The implementer had reported being "blocked from .env.local secrets" and verified only via raw SQL mirroring the loader's intended filters; the reviewer found this claim inaccurate, confirmed .env.local is readable, and actually ran the real TypeScript loader against live data — stress-testing the cross-table keyset cursor at limits 1–5 with zero duplicates or gaps, and confirming all four view counts exactly (waiting 6, verifying 0, live 2, closed 14).
- **changed:** `src/lib/data/admin-requests-server.ts` exports `listAdminRequests()`, merging `program_claims` and `program_requests` into one `AdminRequestRow` feed across four views (Waiting on you / Verifying / Live / Closed), with `objected` claims resolving to Waiting-on-you only per spec. Cross-table keyset pagination via a `{date, source, id}` cursor with asymmetric `lt`/`lte` boundaries per source table, verified correct at every page size. `tests/admin-requests-merge.spec.ts` covers the pure `mergeRequestRows` helper (interleaving, ties, cutoff, exhaustion).
- **follow-ups:**
  1. `compareRowsDesc` compares dates via `Date.parse`, which truncates Postgres microseconds — a claim and a request sharing the same millisecond (but differing in microseconds) at a page boundary could theoretically disagree with the DB's exact-string comparison. Unreachable on current live data (no such tie exists), but worth a string-compare fix if the app ever sees claims and requests created in the same request cycle.
  2. Query errors are logged and swallowed to an empty result rather than surfaced — a broken claims query would render as "no requests" instead of an error state. Worth a `/pr-check` look.
  3. Report accuracy: the implementer's claim about `.env.local` access was wrong and should not be treated as a real environment limitation in future task dispatches on this branch.

## T14 · Requests table + page shell — done

- **gate:** mechanical — GATE PASS. completion — reviewer's literal verdict was VERDICT: needs-work on one criterion; **overridden to pass by the orchestrator's explicit judgment**, reasoning below. Every other criterion was independently re-verified via an actual render check the reviewer performed itself (the implementer had skipped visual verification entirely, incorrectly treating the known-broken `npm run dev` as blocking when `npm run build && npm run start` — the workaround T7/T8/T9/T11/T12 all used successfully on this same branch — was available and untried).
- **why the one "not met" item was overridden:** T14's own criterion requires the page to render `RequestsTable` "inside `RequestsPageContent` (built in T15)". But T15's own `needs:` line requires T14. This is a genuine circular dependency in how the queue was originally written — not a shortcoming in this task's implementation. It is architecturally impossible to satisfy literally, the same shape as T8's forward-referenced tab routes and T9's forward-referenced redirect targets, both of which were correctly accepted earlier in this same chain as known, disclosed sequencing gaps rather than failures. The implementer built the page to render `RequestsTable` directly with a `TODO(T15)` comment, exactly the scope the orchestrator's own dispatch instructions asked for (explicitly forbidding building a placeholder `RequestsPageContent`, since that is T15's file to design). T15 will complete the wrapping when it lands.
- **changed:** `requests-table-layout.ts`/`requests-table.tsx` render Date/Team/For/From/Email check as 52px container rows (no Link, no chevron — click/Enter/Space call `onSelect`), matching `roster-table.tsx`'s container-row pattern exactly (confirmed live: click, Enter, and Space all fire `onSelect`; selection wash persists). Email check's three registers were confirmed to contain no green anywhere in the DOM. `/admin/requests` reads `view`/`after`, calls `listAdminRequests`, and renders the table with `ViewPills` via a new thin client wrapper (`requests-view-pills.tsx`, needed because a Server Component can't hand `ViewPills` a client `onChange`). T13's `AdminRequestRow` gained a batched, additive `crestUrl` field (re-verified: 11/11 of T13's existing tests still pass, no N+1 — `crestUrl()` is pure string construction, no query).
- **follow-ups:**
  1. **Process gap to correct going forward:** this task's implementer treated the pre-existing, already-logged `npm run dev` Tailwind bug as blocking all visual verification, when every prior task since T7 had already established `npm run build && npm run start` as the working alternative. Future dispatch instructions on this branch should state that workaround explicitly rather than relying on the implementer to rediscover it each time.
  2. The row uses `aria-pressed` for "open in the drawer" where `roster-table.tsx` uses `aria-current` for the same concept — worth reconciling at `/pr-check` so the two container-row tables announce selection the same way.
  3. The circular-dependency shape (T14 → T15 → T14) is a planning defect worth remembering for future task breakdowns: a task's own acceptance criteria should never name an artifact a _later_ task in its own dependency chain builds.

## T16 · Claimant identity verification email + `/claim/verify-identity` — done

- **gate:** mechanical — GATE PASS. completion — first review round: VERDICT: needs-work, one criterion "unverifiable" (the original end-to-end proof used a script that no longer existed, so it couldn't be independently confirmed after the fact — a verification-process gap, not a code defect; every code criterion and the email-safety claim were already confirmed). Resolved by dispatching a second, verification-only pass that redid the end-to-end proof and deliberately left its fixture rows in place; the orchestrator independently confirmed each row by id via the Supabase MCP before deleting them.
- **safety note, carried forward:** `RESEND_API_KEY` is live in this environment (an earlier task's finding of "mock mode" was wrong/stale). No task in this chain should call `sendEmail()` against a real address; every email-touching verification from here on must explicitly empty the key in its own process environment and assert on the rendered `EmailMessage`/DB effects rather than a real send. Confirmed zero real sends occurred in either T16 pass.
- **changed:** `claim-verification.ts` (pure — mint/hash/match/expiry, no stored expiry column, usability = `verification_sent_at + 7 days`); `admin-actions.ts` gains `sendClaimVerification` (mints, stamps `verification_sent_at`, clears `verification_opened_at`, preserves `verified_at` if already set, sends the email, returns the link for "Copy link" even if the send fails) and `saveClaimNote` (writes `review_notes` only); `claimVerifyIdentityEmail` added to the email service with a doc-table row; `/claim/verify-identity` (GET, stamps `verification_opened_at` once, reuses the claim flow's frame, renders the failure register for a dead/unknown token) and its `confirmClaimIdentity` POST action (stamps `verified_at` + a length-capped `voucher_note`).
- **independently confirmed live** (second pass, fixtures verified by id then deleted): the DB write `sendClaimVerification` performs; a real GET of the emailed link stamping `verification_opened_at`, unchanged on a second visit; a real form submission stamping `verified_at` and the note; the admin-gate logic (`requireAdmin`) against real signed-in sessions for both an admin and a non-admin stranger.
- **follow-ups:**
  1. `sendClaimVerification` is currently unreachable from the compiled app — Next.js only registers a server action reference when something in the module graph calls it, and nothing does yet (T15's "Resend" button, not yet built, is meant to). This is the same forward-reference shape already logged for T14/T15; it resolves automatically once T15 lands and imports the function. Not a defect in T16.
  2. The email-sending branch of `sendClaimVerification` itself (the actual `sendEmail()` call inside it, as opposed to the pure template and the DB write) remains unexercised end-to-end pending T15's wiring — confirmed correct by code reading (`send.ts`'s `!apiKey → console print` branch) but not by a live run through the real function, since doing so safely requires either T15's button or a sanctioned harness route (attempting one was blocked by the permission system as "Create RCE Surface" — correctly not worked around).
  3. `sendClaimVerification` returns a raw working verification URL to its (already-authenticated) admin caller for "Copy link" — a deliberate, documented design choice, but worth a security-shaped second look from `rls-boundary-reviewer` at `/pr-check` with the whole branch in view.

## T15 · Requests drawer + selection state machine — blocked

- **gate:** mechanical — GATE PASS. completion — VERDICT: needs-work.
- **reason:** Every other criterion was independently verified against the actual code (the selection state machine's exact logic, not just naming, including the subtle render-time-correction-when-the-refreshed-list-drops-the-open-row case; PeekDrawerFrame's real props; the drawer body against T13's actual detail shape; the ⋯ menu's status gating against nextClaimStatus; the requests-view-pills.tsx deletion confirmed unreferenced). The gap: the footer's "Approve/Approve anyway only when nextClaimStatus would accept it" gating is correct for objection_window/approved/rejected/objected claims, but it also silently swallows `pending_email` — which is every row in the entire "Verifying" view (that's literally the view's definition: verification_sent_at set, verified_at null). Those rows fall through to a generic "Nothing to decide here" footer sentence directly below an Email check section actively offering Resend/Copy link — an in-flight claim telling the admin there's nothing to do while the panel above it disagrees. Related: the "Approve anyway" ghost variant (for an unverified personal-address claim) is effectively dead in the normal flow, since a claim can only reach pending_review by clicking the verification link, which sets verified_at as a side effect.
- **fix:** give `pending_email` its own footer line — something like "Waiting to hear back — resend or copy the link above" — rather than falling into the generic "nothing to decide" default, so the Verifying view's footer doesn't contradict its own Email check section.
- **stash:** e10d749693c435a89a056729c722e6bc0025b19d (requests-page-content.tsx, request-drawer.tsx, the requests-table.tsx requestRowId addition, the requests-view-pills.tsx deletion, the page.tsx wiring — everything except this one footer-copy gap is already correct).

## T15 · Requests drawer + selection state machine — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently walked every `ClaimStatus` against `nextClaimStatus` and the footer function itself (not the retry's summary table), confirmed `globals.css` was fully reverted, and confirmed the fix is the only delta in the file.
- **changed:** `requests-page-content.tsx` implements the Roster's three-state selection machine verbatim (including the subtle render-time correction when a refreshed list drops the open row — load-bearing here, since Approve moves a row out of the Waiting view); `request-drawer.tsx` uses `PeekDrawerFrame`, renders the claimant/checks/Email-check/note sections, and wires every action (Approve, Reject, Hand back, Reopen, Resend, Copy link, Save note, Done, Dismiss) through `revalidatePath`-backed server actions. `requests-view-pills.tsx` (T14's placeholder) folded into the new component and was deleted. Fixed in retry: the footer previously fell through to a generic "Nothing to decide here" for `pending_email` claims — every row in the entire Verifying view — directly contradicting the Email check section's own Resend/Copy link buttons above it; now has its own line.
- **follow-ups:**
  1. The new `pending_email` footer line says "Resend it", but the Email check section's button reads "Send" rather than "Resend" until a link has been sent — a combination that shouldn't occur in practice (a claim only reaches `pending_email` once a link is sent) but is worth a copy pass if it ever does.
  2. `settledLine`'s `default` branch is now dead code for claims (every real `ClaimStatus` has its own case), kept only because the row's `status` field is typed as a bare `string` at the loader boundary rather than the narrower `ClaimStatus` union — a small type-tightening opportunity for `/pr-check`.
  3. Live end-to-end round-tripping (Approve/Reject/etc. against a running app + real database) was proven in the first attempt's interaction check before the footer bug was found; the retry's fix was verified with fixture-only, in-memory rendering (no DB writes needed for a footer-copy change) rather than re-running the full live check, which is appropriate given the surgical scope of the fix.

## T17 · Admin "needs a decision" notification email — blocked

- **gate:** mechanical — GATE PASS. completion — VERDICT: needs-work, but the substance of the task is now proven correct.
- **reason:** The review round strengthened verification significantly beyond the implementer's own report: rather than accepting the implementer's hand-simulated SQL stand-in for `claimSend()`, the reviewer imported and called the REAL `notifyAdminsReviewNeeded()` and `claimSend()` functions (with `RESEND_API_KEY=` explicitly emptied and a guard that would abort if the key were set) against a throwaway claim, and confirmed: exactly one print-mode notification for the one real admin, correct subject/CTA, and zero output on an immediate re-trigger — idempotency holds through the real code, not a simulation. Every other criterion (template shape, doc-table export, import discipline, no retry-wrapping) is confirmed correct by direct code reading. The one gap: none of the four `claim-actions.ts` wiring points (`completeClaim`, `completeClaimWithToken`, `raiseObjection`, `requestInvite`/`submitUnlistedProgram`) was exercised as code actually running inside a real request, because the dev server currently 500s on every route in this worktree — a pre-existing, unrelated bug (Tailwind's content scanner choking on a literal string in a `.claude/tasks/` markdown file) that blocks ALL route-level verification on this branch right now, not something specific to this task. The wiring itself was read and confirmed structurally correct: each call sits after its durable write, inside `after()`, gated on exactly the condition the criterion specifies.
- **not stashed as a full revert — held pending an environment fix already in flight.** The user has a separate local session running background task `task_8dc2cefb`, "Fix Tailwind class-scan crash from .claude/tasks markdown" — the exact bug blocking this task's last verification step. Code is stashed per the skill's own rule (a `needs-work` verdict routes to 6b regardless of cause), but the intent is to retry the interaction check specifically, not the whole task, once that fix lands — the code itself does not need to change.
- **stash:** b22935e1ff6f3bf00816f40595d95e48c8e664bc (templates/admin.ts, admin-review-mail.ts, the email/index.ts export/doc-row, all four claim-actions.ts wiring points — all confirmed correct by direct code review and the real-function-call test; only the request-triggered wiring proof is outstanding).
- **cleanup:** two `notification_sends` rows the reviewer's real-function-call test left behind (`zz-probe-claimsend-...`, `admin_review:claim:...`) were confirmed by exact dedupe_key match and deleted by the orchestrator via the Supabase MCP.

## T18 · Team page loader — done

- **gate:** mechanical — GATE PASS. completion — VERDICT: pass; the reviewer independently read the live bodies of `program_usage_total`, `program_usage_by_member`, and `program_seat_usage` via `pg_get_functiondef` and confirmed the loader's filtering (`sum(coalesce(actual_seconds, reserved_seconds)) where not released`, the seat-count source column) is mirrored exactly rather than approximated; confirmed `getAdminTeam()` is exported, wrapped in `React.cache()`, calls `requireAdminOrNotFound()` then `createAdminClient()` for every read (service role — the live RPCs gate on `user_program_ids()`, which is null for a non-member admin and would silently return zeros); confirmed the return shape matches the task spec exactly and every listed type (`TeamMember`/`TeamInvite`/`MemberRole` from `team-settings-server.ts`, `SeatUsage` from `teams-server.ts`, `getMemberAvatarUrls` from `member-avatars-server.ts`, `ProgramUsage` from `usage-server.ts`) is reused rather than reinvented; independently spot-checked the numbers against real live data (Dartmouth College Women's, `63e4377e-4e21-4a65-bb24-2b028a03585d`) and found the service-role loader path matches the member RPC path exactly; confirmed no scope creep (only `admin-team-server.ts` plus the queue's `status:` line changed).
- **changed:** `src/lib/data/admin-team-server.ts` (new) — `getAdminTeam(programId)`.
- **follow-ups:**
  1. A code comment in the new file incorrectly claims `processing_usage.job_id` is nullable; the live schema has it `NOT NULL`. The resulting conditional branches for a null `job_id` are dead code, harmless, but the comment is factually wrong and worth a one-line fix at `/pr-check`.
