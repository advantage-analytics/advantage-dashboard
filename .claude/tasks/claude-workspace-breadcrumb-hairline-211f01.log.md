# Run log — claude/workspace-breadcrumb-hairline-211f01

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add the destination predicate to nav.ts, with its spec — done
- **gate:** mechanical — `npm run lint` 0 errors (32 pre-existing warnings, none in touched files), `npx tsc --noEmit` clean, `npm test` 442 passed / 0 failed including 18 new `header-slot` tests. completion review — `VERDICT: pass`, all five criteria met, scope clean, inert landing confirmed (nothing outside the new spec references `isDestination`). guardrails — **both skipped, correctly**: surfaces determined from `git diff HEAD --name-only` *and* `git ls-files --others --exclude-standard`, which together give only `src/lib/dashboard/nav.ts` and `tests/header-slot.spec.ts`. `pipeline-guardrails-reviewer` needs `src/app/dashboard/`, `src/components/dashboard/` or the upload wizard; `rls-boundary-reviewer` needs `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or a migration. Neither surface is touched — `src/lib/dashboard/` is a route-label register, not the data layer.
- **changed:** `nav.ts` gains a module-level `DESTINATIONS` const (spreading `PERSONAL_NAV`, `TEAM_NAV`, `PERSONAL_BOTTOM`, `TEAM_BOTTOM` — deliberately not `ALL_LINKS`) and an exported `isDestination(pathname)` doing an exact href match, placed with `activeHref` / `navLabel`. Both carry the comments the task's notes required: why `UNLISTED` (`team/upload`) is excluded, and why the duplicate hrefs across the four arrays are left alone. New `tests/header-slot.spec.ts` (18 tests) follows `safe-next.spec.ts`'s pure-function shape: every `PERSONAL_NAV`/`TEAM_NAV` href, the `UNLISTED` guard, four flow paths, exact-vs-prefix including the trailing slash, and an unknown path. The subagent deduped hrefs with a `Set` when generating test titles, because `/dashboard/matches` appears in both rail arrays and Playwright rejects duplicate titles — it fixed that in the spec rather than by touching `nav.ts`'s deliberate duplication, which was the right call. Nothing consumes `isDestination` yet; that is T2.

## T2 · Switch the header's leading slot to the destination rule — done
- **gate:** mechanical — `npm run lint` 0 errors (32 pre-existing warnings; the 3 in `header.tsx` are `react-hooks/set-state-in-effect` on the `isMac`, match-crumb and profile-dismiss effects, all untouched here), `npx tsc --noEmit` clean, `npm run build` pass, `npm test` 442 passed / 0 failed. completion review — `VERDICT: pass`, all five criteria met; it ruled explicitly on the one edit the task did not name (see **changed:**) and found it legitimate, and confirmed T3's surface is absent from the diff. guardrails — `pipeline-guardrails-reviewer` **ran** (the diff touches `src/app/dashboard/`) and returned an explicit no-findings: the wizard's five vendor fields, the trim window, the three analysis predicates, the match-detail short-circuit, deletion cleanup and vendor naming are all untouched, and it separately verified that `isDestination` cannot match `/dashboard/matches/[matchId]` so the match-detail trail survives. `rls-boundary-reviewer` **skipped** — surfaces from `git diff HEAD --name-only` plus `git ls-files --others --exclude-standard` give only `src/app/dashboard/header.tsx`; no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or migration.
- **changed:** `header.tsx` drops the `WORKSPACE_TITLE_PATHS` three-path `Set` and computes `title` from `isDestination(pathname)` instead, with `!showGreeting` still evaluated first so the personal-Home greeting is byte-for-byte unchanged. The ~35-line provenance comment is replaced by a 23-line one that states the rule (a destination lights its own rail row and names itself in its body, so the slot's one useful fact is the workspace; a position inside a flow has a path worth tracing and nothing else tracing it) and keeps the 9g / 1a–1g / Pa2 pointers that explain why the greeting exception exists. One edit beyond the task's letter: a comment inside `getStaticBreadcrumbs` referred to "`WORKSPACE_TITLE_PATHS` above" — a symbol this task deletes — and was repointed to "the destination rule below". The reviewer ruled it load-bearing fallout of the mandated deletion rather than scope creep. Ten paths now carry the workspace title where three did; every flow path keeps its trail.
- **follow-ups:** 1. `/dashboard/settings/subscription` is a real route absent from `SETTINGS_SECTIONS`, so `settingsSection()` misses it and its crumb falls back to a bare "Settings" with no leaf. Pre-existing and unrelated to this change; per the branch-scope rule it wants its own branch. (Idea only — nothing here makes it eligible to run.)

## T3 · Make the header's bottom edge permanent — done
- **gate:** mechanical — `npm run lint` 0 errors (32 pre-existing warnings, none new), `npx tsc --noEmit` clean, `npm run build` pass, `npm test` 442 passed / 0 failed. completion review — `VERDICT: pass`, all five criteria met; it verified the four points it was asked to rule on directly: `useRef`/`useCallback` genuinely have no remaining consumer while `useState`/`useEffect` still do, `cn` keeps four other call sites so its import is not dead, the `shrink-0` / `px-6` / 44px explanation survived with only the scroll-state paragraph rewritten, and neither T2's leading-slot logic nor `colors.css` is touched. guardrails — `pipeline-guardrails-reviewer` **ran** (the diff touches `src/app/dashboard/`) and returned an explicit no-findings. It also answered the specific question put to it: grepping `src/` for `scrollTop`, `addEventListener("scroll")` and `onScroll` leaves only Tailwind `scroll-mt-*` utilities in `help/page.tsx`, so nothing else in the tree observed the shell's scrolling column and removing the listener has no side effect. `rls-boundary-reviewer` **skipped** — surfaces from `git diff HEAD --name-only` plus `git ls-files --others --exclude-standard` give only `src/app/dashboard/header.tsx`.
- **changed:** The header's bottom border is now unconditional — `border-b border-[var(--border-medium)]` — where it previously drew `--border-hairline` (`#F3F3F3`, ~1.11:1 on white, effectively invisible) at rest and swapped to `#EBEBEB` on scroll. `transition-colors duration-200` went with the state it animated, and the `cn()` wrapper on the header element became a plain string (the import stays; four other call sites). Deleted with it: `scrolled` / `setScrolled`, `handleScroll`, the `useEffect` binding and unbinding a scroll listener on the parent element, `headerRef` and its `ref` attribute; the React import narrowed to `useState, useEffect`. A comment at the border records the override — that DS v3 `Navigation Patterns → Header (v3)` specifies `border-transparent` at rest with `#EBEBEB` as a scroll indicator, that the indicator is dropped knowingly, and why `--border-medium` was chosen over a bare hex (already this bar's own gray on the activity/account divider and the profile popover, and it carries a dark-mode value). `h-11`, `shrink-0`, `px-6` and `sticky top-0 z-30` unchanged.
