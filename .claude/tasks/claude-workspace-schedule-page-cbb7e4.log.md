# Run log — claude/workspace-schedule-page-cbb7e4

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Build the New event chooser page (design 3b) — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit` and `npm test` all clear (lint's 37 warnings are pre-existing, 0 errors, none in the new files). Completion review — `VERDICT: pass`; the reviewer re-ran `npm run map` and `tests/generate-map.spec.ts` itself rather than taking the implementer's word, and traced the two extra MAP.md rows to commit `18a26df`. Guardrails — `pipeline-guardrails-reviewer` ran (diff touches `src/app/dashboard/` and `src/components/dashboard/`) and returned no findings, confirming the chooser hands off by static URL only and cannot reach the three misattribution-prone wizard inputs. `rls-boundary-reviewer` skipped: the diff touches none of `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, and adds no table, view or query — the new page reads only the existing `getWorkspaceContext()` helper.
- **changed:** New route `src/app/dashboard/team/schedule/new/page.tsx` (server guard: `/login` unauthenticated, `/dashboard` non-team, `/dashboard/team/schedule` non-staff — copied from the sibling `new/dual` page) rendering a new client component `src/components/dashboard/schedule/new-event-chooser.tsx` — the design-3b chooser: `EventShell` frame, eyebrow/h1/intro, a two-card `role="radiogroup"` (Dual match with lucide `Swords`; Tournament with the design's own inline bracket SVG, since lucide has no draw glyph), check badge on the selected card, the single-match escape line targeting the existing `new/single` flow, and a sticky footer of ghost Cancel + selection caption + primary Continue, both buttons from `advButton()`. `MAP.md` regenerated. `new-event-menu.tsx` deliberately left in place and unmodified — T3 retires its usage, so the new page is not yet reachable from the UI.
- **follow-ups:** 1. The card radiogroup has no arrow-key roving tabindex — but neither do the four existing `role="radiogroup"` implementations (`role-choice.tsx`, `team-pills.tsx`, `period-toggle.tsx`, `matches-filter-panel.tsx`), so this is worth fixing across all five at once or not at all. 2. `MAP.md` drifted between commits (`/dashboard/team/ask` and `/dashboard/team/statistics` were added in `18a26df` without a regen); `npm run map` may belong in a pre-commit hook rather than being a thing to remember, since `npm test` only catches it after the commit exists. 3. Three token judgment calls in the new component deviate from the mock with in-file rationale — `--blue-tint-12` for the card footer rule (no 15% tint token exists), `tabular` without `mono` on the "9" (the DS reserves Roboto Mono for machine values), and `--blue-text` over `--blue` on the 11px escape link (AA contrast) — flagged for `/pr-check` to weigh on exact fidelity.

## T2 · Dual detail-pane widget for the schedule page — blocked
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clear. Completion review — **`VERDICT: needs-work`**, so the gate stopped there: guardrails (5c) never ran, and `pipeline-guardrails-reviewer` is still owed on this work when it is re-run.
- **failed stage:** 5b, completion review, on criterion 4. The criterion states the footer unconditionally — `{played} of {total} matches · {singles} singles, {doubles} doubles` — but `event-detail-pane.tsx:160-170` renders that string only when `scope === "program"` and substitutes a different, un-requested format when results are withheld: `{n} lines · {s} singles, {d} doubles`, dropping the played count entirely. The implementer's reasoning was that "1 of 9" is itself a partial disclosure, which is arguable on the merits but is a fourth withholding surface that neither criterion 4 nor criterion 5 names — criterion 5 enumerates the team score, the dot strips and per-line results, and stops there. The reviewer cleared everything else: criteria 1, 2, 3 and 5 all met, the row-level withholding is squarely inside criterion 5's "per-line results" rather than an overshoot, and the criterion-2-vs-3 overlap resolves correctly (`Trailing()` checks `LINE_STATUS` before the singles-only report branch, so a doubles line that is still analyzing shows its chip rather than being swallowed by "Coming soon").
- **stash:** `c86720531cc06b5c71a2793e920cf5a388598c9a` — one new file, `src/components/dashboard/schedule/event-detail-pane.tsx`. Recoverable with `git stash apply c8672053`. Four of five criteria are met in that work, so the re-run should start from it rather than from scratch: the fix is to decide criterion 4's footer question and make the code and the criterion agree.
- **needs a decision before re-run:** either (a) the footer is genuinely unconditional and the withheld branch must print the same `{played} of {total} matches` string — the played count is not a result, so this is defensible; or (b) the author amends criterion 4 to carve out the withheld case, which makes the current code correct as written. `/task-next` cannot make that call: inventing the carve-out is exactly the fabricated-criterion failure the queue's rules exist to prevent, and the task file is the author's.
- **note:** T3 (`needs: T1, T2`) and T9 (`needs: T2, T6`) are both waiting on this task, so neither is eligible while T2 sits `blocked`. T4 and T8 remain independently runnable, so the loop can keep draining.

## T4 · Opponent dual-history helper — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit` and `npm test` all clear. Completion review — `VERDICT: pass`; the reviewer verified each claim against the code rather than the implementer's narration, including confirming by codepoint that the dash in `you lead 3–1` is U+2013 and that `dualScore` actually lives in `entry-state.ts` (not `schedule-server.ts`, where the task's own hint pointed) and is the same function `scheduleRowsFrom` gates on. Both guardrails skipped: the diff touches only `src/lib/schedule/`, so no `src/app/dashboard/`, `src/components/dashboard/` or upload-wizard surface for `pipeline-guardrails-reviewer`, and none of `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/` — and no new table, view or query — for `rls-boundary-reviewer`.
- **changed:** New pure module `src/lib/schedule/opponent-history.ts`. `opponentDualHistory(schedule)` maps normalized opponent school name → `{ played, us, them, lastPlayedOn }` over decided duals only, reusing `dualScore(entries).decided` rather than re-deriving the gate; a tied dual increments `played` but neither side. `formatOpponentRecord` yields the design's vocabulary (`never played`, `you lead 3–1`, `they lead 1–3`, `split 1–1`) and `formatLastPlayed` a short date or `—`. `ProgramSchedule` comes in as a type-only import, so nothing server-side or Supabase-shaped reaches a client bundle — the criterion with the real failure mode, verified rather than asserted.
- **follow-ups:** 1. `normalizedOpponentName` is a local trim/collapse/lowercase copy rather than an import of `normalizedPersonName`, which the reviewer confirmed is byte-identical in behaviour today. The reasoning is sound — `normalizedPersonName` is declared "the one definition of 'the same name'" for *people* and is wired to a SQL twin and the roster-merge RPC, so borrowing it would couple school matching to person-merge semantics — but the two can now drift silently. Extracting a shared `normalizeCasefold` primitive both build on would remove the risk without the coupling; it would touch `person-name.ts`, so it wants its own task. 2. `opponentHistoryFor(histories, name)` is a convenience lookup added beyond the four criteria; the reviewer judged it reasonable API surface for T5/T6 rather than creep, and the implementer flagged it rather than smuggling it in. 3. `opponents-server.ts` has an unrelated `HeadToHeadMatch`/`headToHeadRows` concept — per-*player* head-to-head over the pooled dataset — that reads confusingly close to this per-*program* dual history. Worth keeping straight when T5/T6/T7 wire the opponent lists.

## T5 · Find-the-school step for a new dual (design 2c) — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit` and `npm test` all clear. Completion review — `VERDICT: pass`. Both guardrails ran this time and both returned explicitly clear: `pipeline-guardrails-reviewer` (diff touches `src/app/dashboard/` and `src/components/dashboard/`) traced `programKey` end to end and confirmed no path attaches a dual to the wrong program; `rls-boundary-reviewer` (diff touches `src/lib/data/opponents-server.ts`) confirmed the widened select is not a new exposure.
- **changed:** New `src/components/dashboard/schedule/school-search.tsx` renders design 2c — eyebrow, h1, search field, `Your conference` / `All programs` sections, conference and division filter pills with Clear, the free-text escape row, and a footer whose Continue is disabled until something is chosen. `dual-form.tsx` gained a `step` state defaulting to `"school"` and early-returns the new component; step 2's body is otherwise unchanged, as T6 owns rebuilding it. `new/dual/page.tsx` additionally loads `getConferenceTable` and `getProgramSchedule` and hands down conference rows plus a T4-derived history map. Two files outside `files:`, both judged required rather than creep: `opponents-server.ts` gained `programKey`/`teamKey`/`status` on `ConferenceProgram` (without the raw `teamKey`, a conference pick could not build a type-correct `ProgramSearchResult` and the squad-mismatch warning would have had a label on one side and a raw value on the other), and `opponent-picker.tsx` gained an optional `searchSeed` so step 2 reopens on the bare school name rather than the squad-qualified string its own comment warns against.
- **verified rather than asserted:** the `ProgramSearchResult | null` contract survives both exits — a directory pick carries its real `programKey` into `createDual`, free text yields `null`, and `takeOpponent` is the single setter for name + row + seed so a stale key cannot outlive a changed pick. `createDual` independently re-resolves the key server-side against `programs.program_key` and nulls it if it resolves to the caller's own program, so there is a second check behind the UI one. Both reviewers confirmed the widened `programs` select is safe: `program_key` and `status` are already served anonymously by the public `/api/programs/search` route, and `programs` carries `using (true)` for `anon`.
- **honesty constraints held:** no `5 of 1,940` total (the search API caps its page and returns no count — the screen prints `{n} listed` or nothing), no Region chip (no region column exists), no opponent season record (`18–4` has no table behind it; the subline's history half is T4's own-program record).
- **follow-ups:** 1. A real key mismatch surfaced and was papered over knowingly rather than silently: `opponentDualHistory` keys on `event.name`, but `createDual` stores a directory pick as `programDisplayName(school, team)`, so a bare-school-name lookup would print "never played" over every dual ever entered through the picker. `historyFor()` tries the squad-qualified name first and falls back to the bare one. The durable fix is to key T4's module on `opponent_program_id` where entries carry one — a stronger key that also survives a school being renamed — but that changes T4's module, so it wants its own task. 2. `opponent-history.ts` still has no test coverage despite being pure and trivially testable. 3. Step 2's eyebrow still reads "New dual · opponent" while step 1 says "step 1 of 2"; T6 owns that body. 4. `passesChips` compares division by display label rather than raw code.

## T6 · Dual builder master-detail (design 2b) — blocked
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clear. Completion review — `VERDICT: pass`, all five criteria met including the negative one (no Forfeit, grepped: zero hits). Guardrails — `pipeline-guardrails-reviewer` ran and **returned a finding**, which blocks under the gate's fail-closed rule; there is no severity triage at this stage. `rls-boundary-reviewer` skipped: diff touches only `src/components/dashboard/`, no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or migration, and no new table, view or query.
- **failed stage:** 5c, `pipeline-guardrails-reviewer`. Re-targeting the opponent through the new persistent rail does not clear `theirLabels` — the opponent's own typed lineup names. A coach who types School A's players, then one-clicks School B in the rail, submits School A's names stamped with School B's `opponent_program_id`: `dual-form.tsx` sends `opponentLabels: row.theirs` while `actions.ts:165` stamps every line with the newly resolved id, and `actions.ts:185-204` then best-effort writes School A's players into School B's pooled roster via `contribute_opponent_player`. Nothing on screen signals it — `mismatchedSquad` compares men's/women's only, never player identity. This is the confidently-wrong-with-nothing-broken shape the guardrails doc exists to catch. The reviewer noted the old `OpponentPicker`'s "Change" link had the same gap, so the diff did not introduce the bug — but a persistent one-click rail measurably raises how often a coach re-targets mid-edit, so it ships the risk live rather than leaving it in a dead corner.
- **stash:** `acdb7e283de235ec7d2abae5c4ad4d6fe85f5eff` — `dual-form.tsx`, `event-shell.tsx`, `lineup-editor.tsx`, `school-search.tsx` modified plus new `opponent-rail.tsx`. Recoverable with `git stash apply acdb7e28`. Every criterion is met in that work and both other gate stages passed, so the re-run is narrow: decide what a re-target does to opponent-authored fields, then apply that one change.
- **needs a decision before re-run:** the fix is a product call, not a mechanical one. Either (a) clear `theirLabels` on re-target — safe, but silently discards typing the coach may have meant to keep; (b) prompt before clearing; or (c) keep them and make the stale binding visible. Whichever, it is arguably its own task rather than a T6 criterion, since the same gap exists on the old picker path and predates this work.
- **also noted, not blocking:** `opponent-picker.tsx` now has zero consumers — both its former mounts are replaced by `SchoolSearch` (T5) and `OpponentRail` (T6), and T5's freshly added `searchSeed` prop is dead with it. The guardrails doc names a dead near-duplicate beside working code as its own hazard ("how the wrong one gets edited later"), so this wants deleting once T7 confirms it is not the popover's basis.
- **note:** T7 (`needs: T6`) and T9 (`needs: T2, T6`) are both waiting on this task. With T2 and T6 now blocked, only T8 remains independently runnable.

## T8 · Tournament creation master-detail (design 3c) — blocked
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clear. Completion review — **`VERDICT: needs-work`**, so the gate stopped there: guardrails (5c) never ran and `pipeline-guardrails-reviewer` is still owed on this work when it is re-run.
- **failed stage:** 5b, completion review — not on a criterion, but on the review's other half: changing behaviour the task never asked to change. All five criteria are met, including the negative one (the "3 Big Ten programs are in this field" callout is absent; the phrase survives only in a doc comment explaining why). The work also removed three existing capabilities, and the reviewer judged them separately rather than as one lump: (a) **doubles entries** — HEAD's form rendered two editors, "Who's going · singles" and "Who's going · doubles"; `toggle()` now hardcodes `discipline: "singles"`, so no UI path can create a doubles entry, while `DraftEntry.discipline` and `createTournament` still accept one. Criterion 3 describes what the *"Entries · singles" section* contains; that is not a licence to delete the sibling section. **Not justified.** (b) **free-text / non-roster entries** — HEAD allowed typing any name with roster-id resolution via `rosterIdsForLabels`, so a walk-on or guest could be entered; entries are now roster-derived only and `toggle()` accepts a `LadderPlayer` alone. Criterion 1 describes the new rail; it never says the typed-name path must go. **Not justified.** (c) **draw options** narrowed from `["Main draw","Qualifying","Consolation","Flight A","Flight B"]` to the first two — criterion 3 literally spells out "(Main draw / Qualifying)". **Justified**, and should stay narrowed on the re-run.
- **stash:** `391f3a026f1fdc4cab17884b569a02f53e589f72` — `entry-editor.tsx`, `tournament-form.tsx`, `event-shell.tsx`, `new/tournament/page.tsx`. Recoverable with `git stash apply 391f3a02`. Every criterion is met in that work, so the re-run is additive: restore a doubles path and a non-roster entry path alongside the new rail, keep the narrowed draws.
- **worth noting:** the `flush` prop this work added to `event-shell.tsx` was verified byte-identical to the one in T6's stash (`acdb7e28`), which was the point of telling both tasks to implement it the same way — the two stashes will not fight over that file.
- **note:** with T2, T6 and T8 all blocked, nothing in the queue is eligible: T3 waits on T2, T7 on T6, T9 on T2 and T6. The loop has no further progress to make until the author rules on the three open decisions.

## T2 · Dual detail-pane widget for the schedule page — blocked (2nd)
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` all clear. Completion review — `VERDICT: pass`: the author's footer ruling landed, and a byte-diff against the previous stash confirmed the footer block was the ONLY hunk that changed, so nothing regressed. Guardrails — `pipeline-guardrails-reviewer` ran for the first time on this work (it never got to last round) and **returned a finding**, which blocks. `rls-boundary-reviewer` skipped: only `src/components/dashboard/` touched, no data/API/migration surface and no new query.
- **the footer fix is good and should survive:** one unconditional `{played} of {total} matches · {s} singles, {d} doubles`; `grep "lines ·"` finds nothing. The reviewer also checked the ruling's safety rather than taking it on faith — under a narrowed read `matches` is already RLS-filtered before the component sees it, so `entryPlayed()` can only be true for the viewer's OWN line, and the count therefore cannot reveal how many of the other eight are decided. The exception holds.
- **failed stage:** 5c, `pipeline-guardrails-reviewer` — the pane over-withholds. It derives one `withheld` flag from `scope` alone, so a player's own decided line is blacked out here while both sibling surfaces show it: `dual-sheet.tsx` gates per line on `line.readable` and its doc comment says outright *"Their own line still shows its score and its report link: it is theirs"*, and `dual-detail.tsx` passes no scope flag at all, rendering whatever RLS returned. Three renderings of one fact, and this one tells the reader a different story — the failure `line-status.ts` and `roster-vocabulary.tsx` exist to prevent, inverted. It errs safe (nothing false is shown) which is why the reviewer called it non-blocking for an unmounted file, but the gate does no severity triage and, more to the point, **T3 mounts this component** — passing it now ships the inconsistency one task later inside a diff that would not contain this file for a reviewer to catch.
- **stash:** `6ecdaa8ee6264b3664e04c4b84a8fd9ccac014e7` — `event-detail-pane.tsx`, with the footer ruling applied. `git stash apply 6ecdaa8e`. Everything except the withholding granularity is settled; the fix is to derive withholding per entry rather than from `scope` alone.
- **needs a decision before re-run:** criterion 5 says "per-line results are withheld together", which the current code satisfies literally — so this is the criterion's wording against the repo's established behaviour, not a bug against the criterion. Either (a) match the siblings: withhold per entry, so a player still sees their own line, and amend criterion 5 to say so; or (b) keep the stricter global withhold and accept that this pane deliberately shows a player less than Team Home does. The author's earlier ruling on the footer went the same shape — narrow the withholding to what is genuinely a result.
- **follow-up surfaced, not this task's to fix:** `schedule-server.ts` selects `source_provider` in `MATCH_COLUMNS` but never reads it when building `EntryMatch.status`, always falling back to `analysis?.status ?? "manual"`. A genuinely SwingVision-imported doubles match — the path doubles teams depend on — would therefore report as manual/no-video rather than imported/ready. Dormant today because `recordResult` always writes `analysis_method: "manual"`, but it means the pane's "Coming soon" comment attributes the behaviour to missing doubles aggregation when the nearer cause is that unread column.

## T6 · Dual builder master-detail (design 2b) — done
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` all clear. Completion review — `VERDICT: pass`, with every `sameTarget` case traced individually rather than deferred to the earlier verdict. Guardrails — `pipeline-guardrails-reviewer` (the stage that blocked this last round) returned "clear to ship": no misattribution path survives, no new desync introduced. `rls-boundary-reviewer` skipped: only `src/components/dashboard/` touched, no data/API/migration surface and no new query.
- **changed:** Applied stash `acdb7e28` cleanly and added the author's ruling — `takeOpponent` now clears every line's `theirLabels` when the target actually changes, plus two doc-comment corrections that had claimed everything survives a re-target. The clear is guarded by `sameTarget` so re-clicking the already-checked row is a no-op rather than wiping typed names. Otherwise the stashed work is unchanged: two-pane step 2 with the persistent `opponent-rail.tsx`, the design-2b right pane, `flush` on `event-shell.tsx`, and `school-search.tsx`'s helper extraction so the rail and step 1 share one history lookup.
- **the hole is closed, traced not asserted:** both reviewers walked `sameTarget`'s cases independently and agreed — directory→different directory clears; directory↔free text clears (one side null, the other not, so the same-target branch cannot be reached); free text→different free text clears; only a genuine re-click of the current pick is a no-op. The clear rewrites `theirLabels: []` through a shallow spread, so `ourIds`, `ourLabels`, `slot`, `key` and `discipline` survive on every line and array order is untouched — it cannot desync `ourIds` from `ourLabels` or disturb `lineup-editor`'s `move`/`setLabels`. The guardrails reviewer also established that the "go back to step 1 and forward again" path is structurally unreachable: `setStep("details")` is called once from `SchoolSearch.onChosen` and nothing calls `setStep("school")` again, so re-targeting only ever goes through the guarded rail.
- **follow-ups:** 1. The clear is silent by design — the author chose "clear silently" over "prompt before clearing", and both reviewers noted the cost is a coach who misclicks the rail loses typed opposing names with nothing on screen explaining why. A small inline note ("opposing names cleared — they belonged to {old school}") would make it legible; worth weighing alongside T7's popover, which owns that column. 2. The same carried-names hazard exists in principle for any future edit-opponent affordance on an EXISTING dual (`dual-detail.tsx` territory) — whoever builds re-targeting there should adopt the same rule rather than rediscover it. 3. `opponent-picker.tsx` still has zero consumers now that both its mounts are gone; T7 should either make it the popover's basis or delete it, since the guardrails doc names a dead near-duplicate beside working code as its own hazard.

## T7 · Add-opponent popover with saved-name dedupe (design 2d/2e) — done
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` (224 passed) all clear. Completion review — `VERDICT: pass`. Both guardrails ran and both returned explicitly clear: `pipeline-guardrails-reviewer` (dashboard surface) traced opponent identity end to end and found no path where a loosely-matched name determines what is written; `rls-boundary-reviewer` (new queries in `actions.ts`) found no authorization gap.
- **changed:** New `opponent-name-cell.tsx` replaces the bare opponent text input with a "+ Add name" / "Add pair" trigger and inline popover. Two new server actions in `actions.ts`: `opponentRosterForDual` (pooled roster + `priorMeetings` via `headToHeadRows`) and `saveOpponentPlayer` (best-effort `contribute_opponent_player`, returns `{saved}` and never throws). `dual-form.tsx` fetches the roster on opponent change behind a stale guard and builds `opponentTarget`; `lineup-editor.tsx` mounts the cell keyed on `${line.key}:${opponentTarget.key}`. `opponent-picker.tsx` **deleted** — it had zero consumers after T6 and was the wrong basis anyway (it picks schools from the directory; this dedupes person names against a pooled roster), so the guardrails doc's "dead near-duplicate beside working code" hazard applied. Two doc comments in `opponent-rail.tsx` / `school-search.tsx` re-pointed at `SchoolSearch.onChosen`'s contract.
- **the exact/fuzzy line, verified in code:** ranking is loose — `===` / `startsWith` / `includes` over `normalizedPersonName` — but every write commits a literal: a pick writes `candidate.name` (the roster's own spelling), a decline writes the typed text verbatim, and `candidate.playerId` is read only for a React `key`, never entering `commit()`. No `theirIds` field was added to `LineupLine`, so no client-side id can bind a label to the wrong person; `contribute_opponent_player`'s own `lower(btrim())` match remains the only thing that converges a label onto a row.
- **re-target invalidation holds under trace:** all three claimed mechanisms are real, and the guardrails reviewer hand-traced an A→B→A race — each effect instance closes over its own `stale` flag, so a late response for School A cannot land after the target moved to School B; `opponentTarget.candidates` is read only while `forKey === opponentKey`, so suggestions empty in the same render rather than an effect later; and the remount key drops draft, highlight and any pending "saved" state. `setOpponent`/`setOpponentProgram` are still called from exactly one place (`takeOpponent`), so nothing bypasses T6's clear.
- **honesty held:** "Saved to {school} roster" is gated on the server's `{saved: true}` alone. All four degradation paths commit a plain label and claim nothing — free-text opponent (no target program), claimed program (the card even reads "Keep as a different player" rather than promising a save), any other RPC refusal, and a single-token name (checked client- AND server-side).
- **checked against the live database, not just the migration folder:** the RLS reviewer had no live MCP access and verified RPC bodies against `supabase/migrations/`, which runs ~100 behind. Its load-bearing claim was spot-checked here: `pooled_roster` really is `SECURITY DEFINER`, really does filter on `roster_public`, and is granted to `authenticated`/`service_role` only — never `anon`. Also confirmed this run wrote nothing to production: the only `contributed_by_program_id` row dates to 2026-08-28.
- **follow-ups:** 1. `recordResult` never writes `matches.opponent_player_id`, so a dual line relies on name-time aggregation while a wizard upload carries the id — two paths to one identity, worth unifying. 2. The old shared `NameField` accepted bench-pill drops on BOTH columns, so one of OUR roster players could be dropped in as the opponent's name; `OpponentNameCell` has no drop handler, which removes that. Both reviewers judged it a latent bug closed rather than a regression, and the ours-side drop still works — but it was an incidental change, so it is recorded here rather than buried. 3. A junk row `clajersongimena / —` sits on Michigan State University (men's), created 2026-08-21 with a null `contributed_by_program_id` — pre-existing, unrelated to this work, worth deleting by hand.

## T8 · Tournament creation master-detail (design 3c) — done
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` all clear. Completion review — `VERDICT: pass`; the reviewer re-checked the previously-passing criteria with fresh eyes for further unrequested removals and found none. Guardrails — `pipeline-guardrails-reviewer` returned "No findings". `rls-boundary-reviewer` skipped: no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/` or migration touched, and `actions.ts` is byte-unchanged so no new query.
- **changed:** Applied stash `391f3a02` and added back the two capabilities the author ruled must return. `entry-editor.tsx` is now discipline-parameterised, so "Entries · singles" and "Entries · doubles" render as sibling sections and a tournament can again be created with a doubles pair; each section gained an "Add a name" / "Add a pair" typed field so a walk-on or guest can be entered without first being added to the roster. Draw options stay narrowed to Main draw / Qualifying, which criterion 3 names. The conference-programs callout is still absent.
- **the collision resolved itself:** the stash also carried a `flush` prop for `event-shell.tsx`, but T6 (`ceacb28`) had since committed that prop in exactly the shape the stash held, so `git stash apply` was silent and `git diff HEAD -- event-shell.tsx` came back empty. Telling both tasks to implement it identically is what turned a guaranteed conflict into a no-op.
- **attribution verified, not assumed:** the typed string reaches `rosterIdsForLabels(raw, roster)` unmodified — no trim or transform sits between the keystroke and `roster-match.ts`'s exact `normalizedPersonName` comparison — and the `<datalist>` only pre-fills the input, so a suggestion still routes through the same resolution rather than short-circuiting it. A name matching nobody contributes no id while keeping its typed label, which is exactly what makes a walk-on enterable. Rail-created entries skip name matching entirely, taking `userIds: [player.userId]` off the clicked row. The rail writes only `singles`, so a rail click cannot corrupt a doubles pair, and `position` is assigned over the concatenated list so the two sections can never claim the same index.
- **an improvement the reviewer noticed:** committed entries are now read-only text with remove-only correction, where the old form bound an `<input>` to an existing entry's labels and re-resolved `userIds` on every keystroke. That closes a re-attribution vector rather than opening one.
- **follow-ups:** 1. An entry's name is not editable in place — correcting a walk-on's typo means remove and re-add. Deliberate (see above) but worth a design decision. 2. For a doubles pair the `<datalist>` only helps with the first name, since browser matching runs against the whole input value and goes quiet after the " / ". A two-field pair picker would fix it but would fight the single-label convention. 3. `stateLine()` prints "Unranked" for a null ladder position, a case the mock never draws.

## T2 · Dual detail-pane widget for the schedule page — blocked (3rd)
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` all clear. Completion review — `VERDICT: pass`: the per-entry ruling landed, aggregates stayed wholesale, criteria 1–4 held. Guardrails — `pipeline-guardrails-reviewer` confirmed the round-2 finding is fixed, then **found a different one**, which blocks.
- **the ruling landed correctly:** `PaneRow` takes `scope` instead of a boolean and computes `const readable = scope === "program" || match !== null` — byte-identical to `team-home-server.ts:694` and used the same way, so a player's own line now shows its glyph, score and report link exactly as Team Home does. The team score and both dot strips stayed wholesale-gated. An unreadable line renders `RESULTS_WITHHELD_LABEL` ("Coaches only") rather than an empty cell, so it cannot be misread as "not played".
- **failed stage:** 5c, on the FOOTER — the same line round 1 was blocked on, for a different reason, and the two reviewers reached opposite conclusions from the same fact. Round 2 asked *does it leak?* and correctly answered no: under a narrowed read `entryPlayed()` can only be true for the viewer's own line, so the count reveals nothing about the other eight. Round 3 asked *is it true?* and answered no: that same fact means the count silently UNDERCOUNTS, printing "1 of 9 matches" when six have finished, with no caveat and no visual difference from a trustworthy read. Not a disclosure bug — an accuracy one.
- **and the repo already ruled on this exact figure:** `team-home-server.ts:837` computes `playedLines: entries.filter(entryPlayed).length` by the identical mechanism; it lives inside `dualTally`, gated at `:801` as `tally: scope === "program" ? dualTally(entries) : null`; and the doc comment at `:329` names "N of 9 in" as one of the values that "are the same claim stated three more" ways — explicitly grouping it with the team score as something that must fall together, not be exempted. `dual-sheet.tsx:240` renders it only when the tally exists.
- **stash:** `42fa64c045980159230305a8af54383478376e55` — `event-detail-pane.tsx` with BOTH rulings applied. `git stash apply 42fa64c0`. Only the footer's scope treatment is unresolved.
- **needs a decision, and the earlier one was made on incomplete information:** the author ruled the footer unconditional after round 1, on the argument that a played count is progress rather than a result. That argument holds for disclosure and fails for accuracy, which was not put to them. Either (a) gate the count like `dualTally` does — withholding it under a narrowed read, matching the repo's own documented precedent for the same number, and requiring no ruling that contradicts anything; or (b) keep it unconditional and accept an undercount until T10 ships.
- **note:** T10 (now approved, and being rewritten to make results visible to every program member always) dissolves this question entirely — with `scope` always `"program"` for a member, the narrowed branch is unreachable and T11 deletes it. The decision only governs the window between T2 landing and T10 landing.

## T10 · Team match results are always visible to team members — done
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` (223 passed) all clear. Completion review — `VERDICT: pass`, but only on the second pass; see below. Guardrails — `rls-boundary-reviewer` ran (migration plus `src/lib/data/`) and returned clean on all five checks. `pipeline-guardrails-reviewer` skipped: no `src/app/dashboard/`, `src/components/dashboard/` or upload-wizard file touched.
- **changed:** Migration `20260830120000_matches_visible_to_members.sql`, applied to the live database via `apply_migration` and committed. The `matches` SELECT policy's program clause is now `program_id is not null and public.user_program_role(program_id) is not null` — any member reads that program's matches, staff and player alike. The three non-program clauses are byte-identical. `public.visible_match_ids()` took the same predicate in the same migration, because it backs the SELECT policies on `match_stats` and `points`, and `shots` transitively via `visible_point_ids()`; `20260822090400` documents that these must move together, having once produced "a staff member who could read 2 match_stats rows for a match the list said did not exist". `resultsScope()` now returns `"program"` unconditionally with its parameter kept so callers compile until T11; nothing T11 owns was deleted. `tests/results-visibility.spec.ts` followed the contract — only the two `resultsScope` assertions changed, and the reviewer confirmed no `OWN`-scope refusal test elsewhere was weakened.
- **the gate earned its keep on criterion 4.** The first completion review returned `needs-work`, not because the work was wrong but because the criterion says "verified against the live database **rather than by reading the policy**" and nothing substantiated that. I supplied a hand-evaluated predicate — inlining the old and new conditions against real `program_members`/`programs` rows — and the reviewer refused it, correctly: that reasons ABOUT the predicate instead of letting Postgres's policy engine decide, and cannot catch another SELECT policy OR-ing in, RLS enabled-but-bypassed for the role, or `auth.uid()` resolving differently inside a real check. It cited this repo's own history of exactly that divergence. My stated reason for not impersonating was also simply wrong — I had conflated "cannot pass a uid to `user_program_role()`" with "cannot set the session identity". The real check: `set_config('role','authenticated', true)` plus `set_config('request.jwt.claims','{"sub":…}', true)`, then a genuine `select` against `matches`. Subject — a `player` on Dartmouth College (`roster_visible = false`) — read the teammate match (`program_matches_visible = 1`, `teammate_match_visible = 1`) with `auth.uid()` resolving to the subject INSIDE the same check; a uid with no `program_members` row read 0 through the identical mechanism; and the connection was confirmed clean afterwards (`role = none`, claims unset). The before-state remains a reconstruction from the verbatim pre-change qual recorded in the migration's rollback comment, since the migration has shipped and that state can no longer be executed.
- **follow-up — T11's `files:` list is incomplete.** The RLS reviewer found stale prose describing the retired rule in more places than T11 names: `src/app/dashboard/matches/page.tsx:35`, `src/app/dashboard/team/page.tsx:235,278`, `src/lib/data/team-roster-server.ts:32`, `src/lib/data/team-home-server.ts:324,1461,1584,1670`, `src/components/dashboard/schedule/dual-detail.tsx:29`, `dual-sheet.tsx:41`, `roster-table.tsx:52`, `src/lib/schedule/entry-state.ts:97`, `src/lib/schedule/actions.ts:365`, `roster-vocabulary.tsx:132,159`. All are comments above code that queries by `program_id` and trusts RLS — doc drift, not an authorization gap, which is why it did not block. Worth adding to T11's files by hand before it runs.
- **also confirmed by the reviewer:** `program_members` has no soft-delete column, so no stale membership can grant a read; `role` is CHECK-constrained to four values, so `is not null` cannot admit a degenerate one; `program_roster()` was already membership-only since `20260821070000`, so no other SQL gate is left inconsistent; and `public_results` gates on `roster_public`, a different column, untouched here.

## T2 · Dual detail-pane widget for the schedule page — done
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` all clear. Completion review — `VERDICT: pass`; all five criteria met, the played-count gating accepted per the author's third ruling. Guardrails — `pipeline-guardrails-reviewer` returned no findings: every scope/readability rule is a verified duplicate of the rule already governing `dual-sheet.tsx` and `team-home-server.ts`. `rls-boundary-reviewer` skipped: only `src/components/dashboard/` touched, no data/API/migration surface.
- **changed:** New `src/components/dashboard/schedule/event-detail-pane.tsx` — design 4c's right-hand pane widget. `DualPane`: eyebrow-sm facts line, `text-title-lg` "vs {opponent}", `text-score` team score from `dualScore`, S/D dot strips (one dot per line, `--success`/`--danger`/`--ink-200`). `PaneRow` per line: slot, `ResultMark` glyph, our/their labels, `ScoreLine` with `text-scoreboard-sm tabular`, state-driven trailing affordance via `LINE_STATUS`/`RowAction`/"Coming soon", chevron. Footer played count gated under `scope === "program"` matching `dualTally`. Per-entry withholding via `readable = scope === "program" || match !== null` (byte-identical to `team-home-server.ts:694`). `TournamentSummary` for non-dual events: name, dates, entry count, link. Three rounds of blocking resolved by author rulings: (1) unconditional footer format, (2) per-entry not per-scope withholding, (3) gated played count matching `dualTally`.

## T3 · Schedule page master-detail layout (design 4c) — done
- **gate:** mechanical — lint, `tsc --noEmit`, `npm test` all clear. Completion review — `VERDICT: pass` on second pass (first found missing link from `DualPane` to `[eventId]` page; fixed by adding a "View event" `RowAction` matching `TournamentSummary`'s pattern). Guardrails — `pipeline-guardrails-reviewer` returned no findings: workspace scoping preserved, role gating intact via `isProgramStaff`, scope gate on `teamScore` and `EventDetailPane` both derive from one `getProgramResultsScope` call. `rls-boundary-reviewer` skipped: no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/` touched and no new query.
- **changed:** `page.tsx` switched from `getScheduleRows` to `getProgramSchedule` + `getProgramResultsScope` in a single `Promise.all`; derives `scheduleRowsFrom(schedule, scope)` for list rows and builds a `Record<string, EventDetail>` from `eventDetailFrom()` for every event. `schedule-list.tsx` rewritten from filtered list to two-pane master-detail: header with `eyebrow-sm` school/squad label, `Schedule` h1 (300/26px/1/-0.5px), `text-body-sm` count line, `advButton("primary","sm")` Link replacing `NewEventMenu`; rows grouped under Upcoming/Completed `eyebrow-sm` headers; selection via `role="listbox"`/`role="option"`/`aria-selected` (carrying the removed filter pills' a11y intent); right pane renders `EventDetailPane`; responsive `flex-col`/`lg:flex-row` stacking. Default selection: most recent completed event, else next upcoming. `event-detail-pane.tsx` gained a "View event" `RowAction` link in `DualPane`'s footer (fixing the completion review's first-pass finding).
- **follow-ups:** 1. `DualPane`'s row grid has fixed-width columns (~575px) that could cramp at narrow widths in the stacked pane — a polish concern, not a functional failure. 2. `NewEventMenu` is now unused (zero consumers outside its own file) — safe to delete. 3. Roving `tabIndex` on the listbox options is simplified (all `tabIndex={0}`) rather than true roving focus — an a11y refinement.

## T9 · Forfeited lines — schema, scoring and the builder action — done
- **not run by `/task-next`.** Both T9 and T11 had been dispatched as single subagents, both exhausted their context window, and both left partial work in one shared tree. Finished by hand in the main session at the author's direction, after establishing that the tree was already mechanically green and that the two remaining gaps were small.
- **gate:** mechanical — `npx tsc --noEmit` clean, `npm run lint` 0 errors (37 pre-existing warnings), `npm test` 224 passed, `npm run build` compiles every route. No completion-review or guardrails subagent was dispatched: the finish was hand-driven rather than a runner dispatch, so the gate here is mechanical plus the live-schema verification below. Worth a `/pr-check` before merge.
- **live schema verified, not assumed:** `program_event_entries.forfeit` exists with `CHECK (forfeit is null or forfeit in ('ours','theirs'))`; `matches.event_entry_id` and `program_event_entries.updated_at` both exist (`setForfeit` writes them); the `Staff update entries` UPDATE policy is `is_program_staff(program_id)` on both USING and WITH CHECK, which is what makes `setForfeit`'s direct table update legal for staff and refused for a player.
- **what was already in the tree:** the migration (applied live), the `forfeit` field on `EventEntry`, `forfeitWon`, the `entryPlayed`/`entryWon`/`entryState`/`dualScore` changes, `supportsVideo`'s refusal, `recordResult`'s guard, the `setForfeit` action, `LINE_STATUS.forfeited`, and `line-row.tsx`'s event-page UI with its two-sided picker.
- **what was missing — criterion 3.** `lineup-editor.tsx` and `dual-form.tsx` were untouched: `LineupLineInput.forfeit` was plumbed through `createDual` but nothing set it, so design 2b's per-line Forfeit — which T6 explicitly deferred to this task — did not exist. Built it from the design's own markup (hover-revealed blue `Forfeit` per row; a forfeited row of slot / "— no available player" / "Forfeited"), plus the note line's forfeit clause T6 left off.
- **the side question, and why the builder only sets `ours`:** the design draws one Forfeit action with no side picker, and its forfeited row replaces OUR name column — that is an our-side forfeit, which is the only side knowable while writing your own lineup ("we can't field a sixth"). The opponent forfeiting is discovered on match day, and `line-row.tsx` already carries the two-sided picker for it. Recorded rather than left implicit because a silent default on this field is exactly the misattribution class the task warns about.
- **two defects found and fixed while building it:** (1) `filled` in `dual-form` filters on `ours.length > 0`, so a forfeited line would have been dropped and the dual written with eight lines instead of nine — `dualScore` would then read a decided 4–3 as 4–3 of eight. (2) A forfeited row still took part in drag reordering, so a dragged player could land in a row that renders "— no available player" and then be submitted as empty. `move()` now runs over the playable rows only and steps arrow-key travel past a forfeit.
- **coverage added:** `tests/forfeited-lines.spec.ts`, 10 tests. Nothing pinned criterion 2 before — the highest-stakes claim in the task had no test at all. The load-bearing one scores the same card twice differing only in who forfeited S6 and asserts 5–2 against 4–3, so the two can never agree; the rest cover the nine-point total, `decided` (with an unplayed-line control so the assertion is about the forfeit), `entryState`, the `LINE_STATUS` entry every surface renders through, and the upload refusal.
- **follow-ups:** 1. `line-row.tsx`'s forfeit handlers swallow `setForfeit`'s error — a refusal ("this line already has a match") leaves the row unchanged with nothing said. 2. The builder's Forfeit is hover-revealed with no confirm step, and taking it back leaves the row empty rather than restoring the name. 3. `move()`'s forfeit-skipping has no test — it is component-internal, so pinning it would mean extracting the function.

## T11 · Remove the results-visibility machinery now that nothing gates — done
- **not run by `/task-next`** — same as T9 above, same session, same commit.
- **gate:** mechanical, as above. Supabase security advisors re-run after the migration: `update_program_settings` no longer appears under `anon_security_definer_function_executable`, and the 10 entries that remain are the pre-existing ones `20260818042938` deliberately left alone.
- **the migration was committed but never applied, and the code already assumed it.** `programs.roster_visible` was still live and `update_program_settings` still had 9 parameters, while `team-actions.ts` had already been rewritten to call it with 8 NAMED arguments. PostgREST resolves by name, so `saveTeamSettings` and `setPlayersCanUpload` were both broken against the live database — the exact failure criterion 1 predicted, and invisible to `tsc`. Applied with the author's explicit approval for the destructive DDL.
- **pre-drop state recorded in the migration, because the drop is irreversible:** 1941 programs, exactly one with `roster_visible = true` — `ZZ Test Program` (`edaf1aa0…`), the fake team. Dartmouth and UCLA, the two real member-carrying programs, were both false, which since `20260830120000` made no difference to what their members read.
- **a regression the drop-and-recreate introduced, caught by checking the ACL rather than trusting the migration.** `create function` re-granted EXECUTE to `anon` via Supabase's `alter default privileges`, and the migration's `revoke all ... from public` does not touch an explicit grant to a named role — so the recreate silently undid `20260818042938_revoke_anon_execute_round4`, whose own header documents this exact trap. Revoked live and added to the migration with the reasoning. The function's ACL now matches `contribute_opponent_player` and `program_roster` byte for byte. Nothing leaked in between: the body raises on a null `auth.uid()`.
- **`getTeamHomeData`'s `viewerRole` was dead** — declared, documented, passed by the page, read by nothing once the scope branch was deleted. `tsc` does not flag an unused parameter, so criterion 3's "no dead-branch remnants" would have passed a stale signature. Removed, with its `ProgramRole` import and the caller's argument.
- **the prose sweep the T10 log asked for, which never made it into the task's `files:` list.** ~14 comments across `matches/page.tsx`, `team/page.tsx`, `matches-page-content.tsx`, `roster-table.tsx`, `team-roster-server.ts`, `team-home-server.ts`, `entry-state.ts`, `opponent-history.ts`, `actions.ts` and `new/dual/page.tsx` described a rule that no longer exists or pointed at a deleted module. The `entry-state.ts` and `dualScore` hazard comments were rewritten rather than deleted — the structural warning is still true, it just can no longer arise from `roster_visible` — with a line saying not to widen `matches` past `program_event_entries` again.
- **one of them was a garbled splice**, not drift: `team-home-server.ts` had half of the deleted scope comment fused onto the head of the `timeZone` comment, left by the crashed run.
- **follow-up:** neither task got a `task-completion-reviewer` or `pipeline-guardrails-reviewer` pass, and this diff touches `src/app/dashboard/`, `src/components/dashboard/`, `src/lib/data/` and two migrations — the trigger conditions for both reviewers plus `rls-boundary-reviewer`. Run `/pr-check` before merging.
