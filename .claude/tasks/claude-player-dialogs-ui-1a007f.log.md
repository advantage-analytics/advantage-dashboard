# Run log — claude/player-dialogs-ui-1a007f

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add `playedSets()` display-only trim helper — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clean (no stale `.next/` re-run needed). Completion review — `VERDICT: pass`, all five criteria met, no scope creep. Guardrails — both skipped legitimately: the diff touches only `src/lib/ui/` and `tests/`, so `pipeline-guardrails-reviewer` (no `src/app/dashboard/`, `src/components/dashboard/` or upload wizard) and `rls-boundary-reviewer` (no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query) had no surface to review. Confirmed against both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` — no untracked files.
- **changed:** `src/lib/ui/score-format.ts` gains one exported pure function, `playedSets(sets)`, trimming trailing sets where both sides are `0` and stopping at the first set that is not — interior and leading `0-0` survive on purpose. Its doc comment records why it is deliberately separate from `scoreSetsFrom`: the trim is display-only and must never reach a write path or change what other readers of the shared adapter see. `scoreSetsFrom`, `tiebreakOf` and `formatScoreText` are byte-unchanged. `tests/score-format.spec.ts` was extended (not replaced) with a `playedSets` describe block covering all five required cases. Nothing imports the helper yet — T2 wires it into the roster drawer.

## T2 · Drawer recent-match score: trim and unclip — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` (431 passed, including the 2 new) all clean, no stale `.next/` re-run needed. Completion review — `VERDICT: pass`, all five criteria met; the reviewer examined the third/fourth criteria closely and accepted the reproduction-style spec under the task's own `notes:` escape hatch, noting the disclosed desync risk. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "No guardrail violations found": none of the three vendor-attribution inputs, the wizard, `match-analysis.ts`, the match-detail short-circuit or provider naming are touched, and the trim is display-only with no write path. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` (which is how the untracked new spec was caught).
- **changed:** `src/components/dashboard/team/player-drawer.tsx` — the recent-match row now renders `sets={playedSets(match.sets)}`, and its grid score track goes from a fixed `72px` to `minmax(72px,max-content)` with `overflow-hidden` dropped from the score cell, so a genuine three-setter pushes the truncating opponent/event cell instead of being silently clipped. A comment records why the trim lives at the call site rather than in the loader. `src/lib/data/team-roster-server.ts` and every other `ScoreLine` caller are untouched; stored `matches.score` JSON is unchanged. New `tests/player-drawer-score.spec.ts` measures in a real browser at the drawer's 340px width: no `0-0` in the rendered score for a stored `[6,6,0]/[4,3,0]`, and `scrollWidth <= clientWidth` for a three-set all-tiebreak score. The implementer verified it is not vacuous (reverting the track to `72px` fails the width test).
- **follow-ups:**
  1. The spec reproduces the row's grid template and score markup rather than importing `player-drawer.tsx` and `<ScoreLine>` — Playwright's JSX transpilation blocked server-rendering the real component. A future edit to the row's className could silently desync the test. The spec's header flags this; a real component-render harness would close it.
  2. `match-rows.tsx` and the other `ScoreLine` call sites (home rail, schedule, search palette) share both defects — untrimmed sets and a fixed, clipping score track. Deliberately out of scope here; worth a sweep branch, possibly with a lint- or test-level guard so a new caller cannot reintroduce it.
  3. `playedSets`' both-zero heuristic cannot distinguish the known storage artifact from a hypothetical retirement at `0-0` in a genuinely started third set. Not introduced by this diff — it is inherited ambiguity in `matches.score` — and the guardrails reviewer judged it non-blocking, but worth a decision if it ever becomes material.

## T3 · Bench divider: symmetric dash gap — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clean, no stale `.next/` re-run needed. Completion review — `VERDICT: pass`, all five criteria met; the reviewer independently confirmed the `gap-2.5` count went 7→6 with the other six occurrences (lines 227, 243, 259, 529, 770, 787) untouched, and that JSX whitespace around the `sr-only` span reintroduces no stray space. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "No findings", confirming specifically that `value={BENCH}`, `key`, `dragListener={false}` and the `sequence` array are untouched, so drag behaviour, the bench/lineup partition and the position written on save are all unaffected. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` (no untracked files this run).
- **changed:** `src/components/dashboard/team/roster-table.tsx` — the bench divider's em dash is now its own `aria-hidden` span instead of living inside the caption string, and the container's `gap-2.5` becomes a single `gap-1.5` (6px) that produces both sides of the dash by construction. Measured: left 6px, right 6px — previously 10px left from the flex gap and ~4px right from a literal space character, which was the whole bug. An `sr-only` ": " was added so the sentence still reads as "Not in the lineup: drag a row below this line to bench them" rather than running the clauses together. A code comment records the gap value and that it was picked by eye at 11px against the eyebrow (the criterion allowed either a commit body or a comment; the subagent cannot commit, so the comment carries it).

## T4 · Dialog shell default width 440 → 520 — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clean; the implementer additionally ran `npm run build` green. Completion review — `VERDICT: pass`; the reviewer independently opened all five `RosterDialog` callers and confirmed the resulting widths rather than taking them on trust: Add (`add-player-dialog.tsx:287`), Invite (`roster-invite-dialog.tsx:371`) and Edit (`edit-player-dialog.tsx:196`) pass no `width` and so inherit 520; Merge passes an explicit 520; review-requests (`join-requests-card.tsx:196`) passes an explicit 480. No caller file is in the diff. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "no guardrail violations": none of the three vendor-attribution inputs live in these dialogs (they are wizard-only), no workspace scoping, role gating, `canSubmitVideo`, analysis short-circuit or provider naming is touched, and a CSS-only width change cannot alter field order, labels or defaults. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` (no untracked files this run).
- **changed:** `src/components/dashboard/team/dialog-shell.tsx` only — three edits: the `width` union becomes `440 | 480 | 520 | 560`, `RosterDialog`'s default goes `440 → 520`, and a header comment records why. The rationale is deliberately on the record: the DS's Dialog (v3) spec gives form dialogs `w-[440px]` and reserves `w-[520px]` for compare dialogs, and this shell was already fully v3-conformant, so 520 borrows a width the DS already sanctions rather than inventing a number — the roster's forms carry more rows than the v3 form dialog anticipates. Padding, the 18px rhythm, the title and contract sentence, the hand-rolled 28px close button, the footer and `maxWidth: calc(100vw - 32px)` are all byte-unchanged.
- **follow-ups:**
  1. `DialogContent`'s stock 16px close glyph still differs from the DS's 28px Chrome Icon Button. The shell's own comment already flags this as a cross-cutting change touching every dialog in the product — correctly out of scope here, but still worth doing.
  2. Once T5–T7 land, a visual pass over Add/Invite/Edit to confirm nothing inside them was tuned assuming 440px (a child sized off the old width rather than the container).
  3. Minor doc nit raised by the guardrails reviewer: 440 and 560 are now in the union with no caller using either. Deliberate — the criterion specified that union — but worth a decision if the shell is ever tidied.

## T5 · Add Player: occupied-spot acknowledgement gate — blocked
- **gate:** mechanical PASSED — `npm run lint`, `npx tsc --noEmit` and the full `npm test` suite all clean. (The subagent's own run showed 6 failures in the live-DB specs — `account-deletion-retention`, `join-requests-staff-read`, `pending-invites`, `program-owner-name-live`, `rls-workspace-isolation` — all failing at `signIn(...): Request rate limit reached` in `tests/fixtures/live-db.ts`. A clean re-run from the runner was fully green, so that was transient Supabase auth rate limiting, not this diff.) Completion review FAILED — `VERDICT: needs-work` on criterion 4 alone. Criteria 1, 2, 3 and 5 were all judged met, including independent confirmation that `player-fields.tsx`, `edit-player-dialog.tsx`, `roster-actions.ts` and `supabase/` are absent from the diff, that `DialogProblem` appears only as `message={error}`, and that no displacement or swap write was introduced. Guardrails NOT RUN — the completion stage failed first, and the gate stops at the first failure in cost order.
- **why it failed:** criterion 4 asks for a test covering the enable/disable sequence on the shipped gate. The component could not be driven directly: `AddPlayerDialog` is a `"use client"` component pulling in Radix and two server actions, and `@playwright/test` rewrites JSX in repo `.tsx` files — the same wall `tests/player-drawer-score.spec.ts` documents. The subagent instead wrote two tests and disclosed the gap plainly: a source-pinning test asserting the literal `ready` formula, the unchanged `disabled` expression, both acknowledgement resets and the `DialogProblem` usage; plus a behavioural test driving a hand-written replica of the state machine through the full sequence. The reviewer accepted the source-pinning half as a legitimate regression guard for those specific lines, but judged the gap material: nothing exercises the real `checked={spotAcknowledged}` binding, the real `onChange`, or the real `spotTakenBy.length > 0` JSX guard — so a checkbox wired to the wrong state, rendered outside the guard, or shipped with wrong copy or classes would pass both tests. That is precisely what criterion 2 specifies.
- **stash:** 74528e56ff5c5bdb6433762452f031e1e9ff62f2 (`blocked: T5`) — carries the full `add-player-dialog.tsx` change and the new `tests/add-player-spot-gate.spec.ts`. Recovered with `git stash apply 74528e56ff5c5bdb6433762452f031e1e9ff62f2`, NOT by index: the stash stack is shared across this repo's worktrees and other entries sit above and below this one.
- **what would unblock it:** a decision by the human, not a retry. The implementation itself was judged correct on every other count; the blocker is that this repo has no way to render a real client component in a test. Either (a) stand up a component-render harness — a real gap the branch has now hit twice, in T2 and T5 — and re-run T5 against it, or (b) amend criterion 4 to accept the source-pinning plus replica combination, as T2's `notes:` field did with its explicit escape hatch, and re-run. Retrying T5 unchanged will fail the same way.
- **follow-ups:**
  1. `sharedWith()` in `add-player-dialog.tsx` and `nameList()` in `player-fields.tsx` are now the same 1/2/N-others grammar in two places. Kept separate deliberately — the shared helper serves Edit player, which raises no confirm — but a third caller would justify extracting a name-list formatter with no opinion about the surrounding sentence.
  2. The acknowledgement is not announced to assistive tech. The spot note's live region already says the spot is held; whether the appearance of a newly-required checkbox should also be announced is an open a11y question.
  3. The recurring blocker: two tasks on this branch have now been unable to test a real component. A `@playwright/experimental-ct-react` setup, or a jsdom-based unit test runner alongside Playwright, would close it for the whole repo.

## T5 · Add Player: occupied-spot acknowledgement gate — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit` and the full `npm test` suite all clean. Completion review — `VERDICT: pass` on the AMENDED criteria (see the queue's T5 notes and commit 2e16a81; the author amended criterion 4 after this task's first run was blocked on it alone). Two review passes were needed: the first re-run still found criterion 4 unmet because the amended text requires the spec to "state plainly what it cannot catch" and the docstring did not, so a "What this cannot catch" section was added to `tests/add-player-spot-gate.spec.ts` — no test logic, assertion or source line changed — and the second pass judged the disclosure genuinely specific rather than token. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "No findings", confirming no write to any other player's row, `addProgramPlayer` and its SQL untouched, `spotHeldNote` reused rather than replaced, the reshuffle workflow still working (one extra click, no constraint, no server round trip), and nothing touching the wizard, `job-request.ts`, `match-analysis.ts`, workspace scoping, role gating or provider naming. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard`.
- **changed:** `src/components/dashboard/team/add-player-dialog.tsx` — a new local `spotAcknowledged` state gates submission when the chosen lineup spot is already held. `ready` gains exactly one term, `(spotTakenBy.length === 0 || spotAcknowledged)`; the primary's `disabled={!ready || pending}` is byte-unchanged. A checkbox — "Yes — share #N with {names} for now.", with an 11px sub-line "Nobody is moved off the line" — renders directly under the existing spot note, only while that note does, borrowing the "Also send an invite" checkbox's grammar wholesale rather than inventing a second one. A new `changeLineupSpot` wrapper is now the select's `onChange`, so the acknowledgement cannot outlive the spot it was given for: acknowledging #3 says nothing about #5. `reset()` clears it too. A local `sharedWith()` builds the name list, deliberately NOT bending the shared `spotHeldNote`, which Edit player uses and which raises no confirm. Nothing displaces anybody: sharing a spot stays legal, which is the whole point — what the gate refuses is the accidental share, not the deliberate one. Edit player is untouched, because editing an existing row's spot IS the reshuffle case the sharing decision exists for.
- **follow-ups:**
  1. Accepted residual risk, now recorded in the spec's own docstring: because the behavioural test drives a replica rather than the real component, a checkbox bound to the wrong state, rendered outside the `spotTakenBy.length > 0` guard, or shipped with wrong copy or classes would pass both tests. Two reviewers verified the actual wiring by hand this run; future edits to this checkbox rest on review.
  2. The recurring blocker behind that: this repo cannot render a real client component in a test — T2 and T5 both hit it. A `@playwright/experimental-ct-react` setup or a jsdom runner alongside Playwright would close it repo-wide. Infrastructure work for its own branch, which is why it was not done here.
  3. `sharedWith()` here and `nameList()` in `player-fields.tsx` are now the same 1/2/N-others grammar twice, kept separate deliberately. A third caller would justify extracting a formatter with no opinion about the surrounding sentence.
  4. The acknowledgement is not announced to assistive tech. The spot note's live region already says the spot is held; whether a newly-required checkbox appearing should also be announced is an open a11y question.

## T6 · Add Player: `initial` prefill prop + header wiring — done
- **gate:** mechanical — `npx tsc --noEmit` and `npm run build` clean; `npm run lint` exits 0. `npm test` failed once with 2 live-DB specs (`join-requests-staff-read`, `rls-workspace-isolation`) erroring at `signIn(...): Request rate limit reached` — the same transient Supabase auth rate limiting T5 hit. A targeted re-run of exactly those two specs passed, and a full re-run was green, so the failure was environmental and not this diff. Completion review — `VERDICT: pass`. Two points were put to the reviewer explicitly rather than decided here: (a) the diff adds a NEW lint warning (32→33), `react-hooks/set-state-in-effect` at `add-player-dialog.tsx:235` — judged acceptable-as-specified, because criterion 1 MANDATES the `useEffect` and forbids the `useState` initializer that would dodge the rule, so a setter inside an effect is the unavoidable mechanism, and the repo already carries the same class un-suppressed elsewhere; (b) whether the prefill can weaken the pre-existing `created`/`formKey` duplicate-suppression — the reviewer verified from SOURCE, not from the test's claim, that it cannot. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "No findings", independently confirming the suppression intact, no write path, server action or loader touched, no reachability to `job-request.ts` or any attribution column, and no workspace-scoping, role-gating or provider-naming surface. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard`.
- **changed:** `add-player-dialog.tsx` exports an `AddPlayerInitial` type and takes an optional `initial` prop, applied in a `useEffect` keyed on `[open]` behind a `wasOpen` ref so it fires only on the closed→open edge — never on first mount alone (the component stays mounted across opens, which is the whole reason a `useState` initializer would be wrong) and never on a re-render while open, which would stomp on what the coach is typing. Only non-empty values overwrite, so a prefill cannot blank a field. `reset()` is untouched and still clears to empty rather than to `initial`: Cancel leaves no residue, and the next open re-applies the prefill from the prop that still says it. `roster-header-buttons.tsx` holds `addInitial` beside `inviting`/`addingPlayer` and passes it down; nothing sets it yet, which is correct — T7 is what populates it. New `tests/add-player-prefill.spec.ts` follows T5's three-part pattern: source-pinning (including a negative assertion against the forbidden `useState(initial…)`), a behavioural replica driving prefill → unchanged-form exclusion → edit drops it → Cancel empties → reopen re-applies, and a docstring naming what it cannot catch.
- **follow-ups:**
  1. Raised by the implementer and deliberately deferred to T7: should `addInitial` be CLEARED when Add player closes? Otherwise a later plain "Add player" click could re-apply a stale hand-off email. That is a real behaviour decision and belongs with the wiring that creates the situation.
  2. The `react-hooks/set-state-in-effect` warning class now has four-plus instances repo-wide. If it is ever promoted to an error, this prefill would need a `key`-based remount of the dialog instead of an effect.

## T7 · Invite → Add Player email hand-off — done
- **gate:** mechanical — `npm run lint` (0 errors, no NEW warnings this time), `npx tsc --noEmit`, `npm run build` and the full `npm test` suite (443 passed) all clean; no rate-limit flake this run. Completion review — `VERDICT: pass` on all five. Three points were put to the reviewer explicitly: (a) the implementer added a THIRD gate, `!listed`, beyond the criterion's two — judged a legitimate narrowing rather than scope creep, because a pasted multi-address list is an input the criterion never contemplated and handing one draft address to a form that creates one row would silently drop the rest; (b) the stale-prefill hole deferred from T6 — verified in code, not just claimed: `AddPlayerDialog` applies `initial` only on the `open && !wasOpen.current` edge, and clearing on close fires strictly before any subsequent open, while the hand-off sets `addInitial` and opens in the same handler with no close event intervening, so the fix closes the hole without breaking the prefill; (c) criterion 4 — no existing spec covers the dialog's Send path (`tests/pending-invites.spec.ts` is a live-DB RLS/token spec, not the dialog), so both a source-pin of the unchanged `inviteMember` call site and a behavioural send assertion were added, judged adequate. Guardrails — `pipeline-guardrails-reviewer` RAN and returned no violations, confirming the invite path byte-untouched (`submit`, `inviteMember`, `ready` are not in the diff), no seat-accounting corruption (the hand-off never calls `inviteMember` and never spends a seat), correct cooperation with the existing duplicate tripwire (`add_program_player`'s partial unique index and two checks still enforce server-side, and only an address crosses so no false-negative on the name-collision note), and nothing touching attribution, the wizard, workspace scoping, role gating or provider naming. `rls-boundary-reviewer` skipped legitimately — no `src/lib/data/`, `src/app/api/`, `src/lib/supabase/`, `supabase/migrations/`, no new table or query, which criterion 5 independently required and the runner confirmed.
- **changed:** `roster-invite-dialog.tsx` takes an optional `onHandOffToAddPlayer` and renders one quiet blue text action — "Add a coach-managed profile instead →", DS footer-left register, not a button chrome — directly under the email field, gated on `!linked && !listed && draft !== ""`. Each gate is the same rule from a different side: the offer appears only when this invitation would MINT a profile rather than bind one. It passes the trimmed address and nothing else; this dialog collects an address, not a name, so no name is invented. `addresses`, `ready` and `submit` are untouched — "Someone new" plus Send still sends the invitation, which was the human's explicit requirement after an earlier hard-redirect proposal was rejected for deleting a working capability. `roster-header-buttons.tsx` gains `handOffToAddPlayer(email)`: no-op if Add player is already open (stealing the form out from under a coach mid-typing is worse than doing nothing), else set `addInitial`, close Invite, open Add player. `addInitial` is now cleared in Add player's `onOpenChange` on close — chosen over clearing on the header button because closing is the one event both openings pass through, and the button is no longer the only way in.
- **follow-ups:**
  1. The offer is deliberately invisible while a pasted list is present. A coach pasting a dozen addresses for players with no roster rows still has no bulk path to coach-managed profiles. If that turns out to be a real workflow it wants its own design, not a widened gate.
  2. `RosterInviteDialog`'s `initialEmail` is still read once on mount, per its own docstring. The reverse hand-off (Add player → Invite) would hit exactly the bug T6 avoided and would need a `key` remount or an open-edge effect like Add player's.
  3. Flagged by the guardrails reviewer, worth keeping visible: the tests here are source-pin plus replica and explicitly disclaim `inviteMember`, seat accounting and mail. "The tests pass" must not be read as "the seat and invite behaviour was exercised" — T8's sweep and manual QA carry that.
  4. `CopyInviteLink` in this footer remains deliberately disabled — unrelated, noted only because it is the one dead control there.

## T8 · Branch verification sweep + score persistence check — blocked
- **gate:** mechanical PASSED — `npm run lint` (0 errors), `npx tsc --noEmit`, `npm run build` and the full `npm test` suite all green, including every spec added by T1, T2, T5, T6 and T7. Completion review FAILED — `VERDICT: needs-work` on criterion 3 alone. Criteria 1, 2 and 4 were judged met, with the reviewer independently re-running lint and re-verifying the diff scope. Guardrails RAN before the completion review this time (T8's own criterion 4 requires it): `pipeline-guardrails-reviewer` swept the WHOLE feature diff and reported no blocking finding. `rls-boundary-reviewer` correctly skipped — criterion 2's grep found no data-path change, which is exactly the condition T8 names for skipping it.
- **why it failed:** criterion 3 asks for one match's stored `score` JSON read before and after LOADING THE ROSTER DRAWER. The drawer was never loaded — it needs an authenticated dashboard session this environment does not have. What was done instead: the values recorded in `work/player-dialogs-ui/02_design/output/design.md` at stage 02, before any code existed, were compared against a fresh read after all seven tasks. The reviewer judged that a genuinely weaker claim than the criterion asks for, and it is right — structural proof that no write path exists is not the same as observing that rendering the drawer changed nothing. `02_design/output/design.md` had specified the operational check in those terms too.
- **what the persistence evidence DOES establish, since it is worth keeping:** (1) No branch code can write to `matches` at all. Under the corrected diff scope (see below), nothing under `src/lib/data/`, `src/app/api/`, `src/lib/supabase/` or `supabase/` changed, and grepping every added line for `.update(`, `.upsert(`, `.insert(` or a score assignment returned only test-file reads and renders. (2) The one stage-02 row that still exists, `81458e8b-f838-4783-8b5d-75dabdb39ed5`, is byte-identical to its recorded value: `{"player1":[0,0,0],"player2":[0,0,0],"player1_tiebreaks":[null,null,null],"player2_tiebreaks":[null,null,null]}`. (3) Six rows still store all-zero trailing sets, so the phenomenon the branch fixes remains present in live data.
- **an anomaly investigated rather than assumed:** two of the three rows recorded at stage 02 — `d83c8b0b…` holding `[6,0,0]/[4,0,0]`, the exact row the drawer bug came from, and `2dda4482…` holding `[6,7,0]/[4,6,0]` — no longer exist; `select count(*)` returns 0 and the table is down to 28 matches. This is NOT a score rewrite and not this branch's doing. `tests/rls-workspace-isolation.spec.ts:171` runs `admin.from('matches').delete().in('created_by', authUserIds)` in its `afterAll`, and `tests/account-deletion-retention.spec.ts:185` deletes matches by id; both use the service-role client, and `npm test` ran many times across this drain. Those were fixture rows removed whole by the suite's own teardown.
- **a deviation recorded so it is not mistaken for an oversight:** criterion 2 names `git diff main...HEAD`. On this branch that command is wrong — `main` is far behind, the merge-base is `f24975e`, and the three-dot diff sweeps in roughly 100 unrelated files of prior work. The feature's true scope is `8e8d017..HEAD`, `8e8d017` being the parent of this pipeline's first commit (`git rev-parse --short 85c12e3^`). Every check above used the corrected scope, and the reviewer verified the correction itself before accepting criterion 2.
- **stash:** no stash — the task produced no changes. `git stash push -u -m "blocked: T8" -- ':(exclude).claude/tasks/'` printed `No local changes to save` and created no entry, which is correct for a verification-only task. Deliberately NOT recording a SHA here: `git rev-parse stash@{0}` would have returned another branch's stash, and false provenance in this log is worse than none.
- **what would unblock it:** a decision, not a retry. Either (a) stand up the authenticated Playwright harness — a throwaway Supabase user created via the service role plus a login against a worktree port — load the roster drawer for a player with a stored score, and read the row either side of it; or (b) amend criterion 3 to accept the structural proof plus the surviving-row comparison, recording that the operational check was traded away and why. Re-running T8 unchanged fails identically.
- **follow-ups:**
  1. Raised by the whole-branch guardrails sweep, informational: an all-zero score now renders as a BLANK score cell. The reviewer traced this through `matchOutcome`, which already returns `null` for such a row so the drawer already shows the neutral dash marker rather than a W/L mark, and judged a blank cell beside that marker not worse than the prior `0-0 0-0 0-0`, which actively asserted a tied, fully-played match. Worth a human eye at some point, since it is a real rendering change for six live rows.
  2. Also from that sweep: `dialog-shell.tsx`'s `width` union carries `560`, which no caller uses and the doc comment does not mention. Deliberate at T4 (the criterion specified that union) but the kind of thing a later reader trusts wrongly. A one-line comment fix, not made here because T8 lands no code.
  3. The recurring theme across this branch: T2, T5, T6 and T7 all had to approximate their tests, and T8 could not perform its operational check, all for the same reason — this repo cannot render a real client component or drive an authenticated dashboard session in a test. One harness branch would close all five gaps.

## T8 · Branch verification sweep + score persistence check — done
- **gate:** mechanical — `npm run lint` (0 errors, 33 warnings, none new), `npx tsc --noEmit`, `npm run build` and the full `npm test` suite all green, including every spec added by T1, T2, T5, T6 and T7; re-confirmed on an unchanged tree immediately before the review. Completion review — `VERDICT: pass` on the AMENDED criteria 2 and 3 (see commit c529d04 and the queue's T8 notes). Guardrails — `pipeline-guardrails-reviewer` swept the WHOLE feature diff (`git diff 8e8d017..HEAD -- src tests`, six changes read together rather than one at a time) and reported no blocking finding. `rls-boundary-reviewer` correctly skipped: criterion 2's grep found no data-path change, which is the exact condition the criterion names for skipping it.
- **two review passes, and an error of mine the gate caught.** The first pass on the amended criteria still failed, and rightly: I had told the reviewer the "before" values came from `02_design/output/design.md`, and they do not. That file records value PATTERNS in prose at lines 132-135 and no row ids; the UUIDs came from a live query run during stage 02 that lives only in the session transcript. The reviewer checked the cited source instead of taking my word and found the provenance missing. I corrected the claim rather than amending the criterion a second time — amending a criterion every time it fails is how a gate stops meaning anything — and resubmitted accurate facts once. It passed on those, with the reviewer verifying both test line references against the spec files directly.
- **what was verified.** Scope: the feature is `8e8d017..HEAD` (parent of this pipeline's first commit), 8 source files and 5 specs. Structural proof: `git diff 8e8d017..HEAD --name-only | grep -E "^(src/lib/data/|src/app/api/|supabase/|src/lib/supabase/)"` returns EMPTY — no loader, no route handler, no edge function, no migration — and grepping every added line for `.update(`, `.upsert(`, `.insert(` or a score assignment returns only test-file reads and renders. Empirical corroboration: `design.md` attested at stage 02, before any code, that live rows stored `[6,0,0]/[4,0,0]`, `[6,7,0]/[4,6,0]` and `[0,0,0]/[0,0,0]`; today six rows still store `[0,0,0]/[0,0,0]`, and `81458e8b-f838-4783-8b5d-75dabdb39ed5` is byte-identical at `{"player1":[0,0,0],"player2":[0,0,0],"player1_tiebreaks":[null,null,null],"player2_tiebreaks":[null,null,null]}`.
- **an anomaly investigated rather than waved past.** The rows holding the other two patterns are gone — `select count(*)` on their ids returns 0, and `matches` is down to 28 rows. This is not a score rewrite: `tests/rls-workspace-isolation.spec.ts:171` runs `admin.from('matches').delete().in('created_by', authUserIds)` in its `afterAll` and `tests/account-deletion-retention.spec.ts:185` deletes matches by id, both via the service-role client, and `npm test` ran many times across this drain. Fixture rows removed WHOLE by the suite's own teardown. The distinction matters and is the reason this is not a finding: a score rewrite leaves the row present with different contents, which is not what happened.
- **what this task does NOT prove, stated plainly as the amended criterion requires.** The operational check — reading a row's score either side of an actual roster-drawer load — was traded away, because this environment cannot drive an authenticated dashboard session. Nothing here observes the application running. So a write introduced by code NOT in this diff, or by a future edit to the drawer's loader, would not be caught by this task. The claim established is narrower than "rendering the drawer does not rewrite scores": it is "no code on this branch can write to `matches` at all, and the stored values this branch's design was derived from are unchanged."
- **follow-ups:**
  1. Informational, from the whole-branch sweep: an all-zero score now renders as a BLANK score cell. Traced through `matchOutcome`, which already returns `null` for such a row so the drawer already shows a neutral dash rather than a W/L mark; judged not worse than the prior `0-0 0-0 0-0`, which actively asserted a tied, fully-played match. Six live rows are affected — worth a human eye.
  2. `dialog-shell.tsx`'s `width` union carries an unused, undocumented `560`. Deliberate at T4 (the criterion specified that union) but the kind of thing a later reader trusts wrongly. A one-line comment fix; not made here because T8 lands no code.
  3. The theme of this whole branch: T2, T5, T6 and T7 all had to approximate their tests, and T8 could not perform its operational check, all for one reason — this repo cannot render a real client component or drive an authenticated dashboard session in a test. One harness branch (`@playwright/experimental-ct-react` or a jsdom runner, plus the throwaway-user login already sketched in the project's memory notes) would close all five gaps at once. This is the single highest-value piece of infrastructure this branch surfaced.
