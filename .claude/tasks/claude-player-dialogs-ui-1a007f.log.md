# Run log — claude/player-dialogs-ui-1a007f

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add `playedSets()` display-only trim helper — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clean (no stale `.next/` re-run needed). Completion review — `VERDICT: pass`, all five criteria met, no scope creep. Guardrails — both skipped legitimately: the diff touches only `src/lib/ui/` and `tests/`, so `pipeline-guardrails-reviewer` (no `src/app/dashboard/`, `src/components/dashboard/` or upload wizard) and `rls-boundary-reviewer` (no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query) had no surface to review. Confirmed against both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` — no untracked files.
- **changed:** `src/lib/ui/score-format.ts` gains one exported pure function, `playedSets(sets)`, trimming trailing sets where both sides are `0` and stopping at the first set that is not — interior and leading `0-0` survive on purpose. Its doc comment records why it is deliberately separate from `scoreSetsFrom`: the trim is display-only and must never reach a write path or change what other readers of the shared adapter see. `scoreSetsFrom`, `tiebreakOf` and `formatScoreText` are byte-unchanged. `tests/score-format.spec.ts` was extended (not replaced) with a `playedSets` describe block covering all five required cases. Nothing imports the helper yet — T2 wires it into the roster drawer.

## T2 · Drawer recent-match score: trim and unclip — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` (431 passed, including the 2 new) all clean, no stale `.next/` re-run needed. Completion review — `VERDICT: pass`, all five criteria met; the reviewer examined the third/fourth criteria closely and accepted the reproduction-style spec under the task's own `notes:` escape hatch, noting the disclosed desync risk. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "No guardrail violations found": none of the three vendor-attribution inputs, the wizard, `match-analysis.ts`, the match-detail short-circuit or provider naming are touched, and the trim is display-only with no write path. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` (which is how the untracked new spec was caught).
- **changed:** `src/components/dashboard/team/player-drawer.tsx` — the recent-match row now renders `sets={playedSets(match.sets)}`, and its grid score track goes from a fixed `72px` to `minmax(72px,max-content)` with `overflow-hidden` dropped from the score cell, so a genuine three-setter pushes the truncating opponent/event cell instead of being silently clipped. A comment records why the trim lives at the call site rather than in the loader. `src/lib/data/team-roster-server.ts` and every other `ScoreLine` caller are untouched; stored `matches.score` JSON is unchanged. New `tests/player-drawer-score.spec.ts` measures in a real browser at the drawer's 340px width: no `0-0` in the rendered score for a stored `[6,6,0]/[4,3,0]`, and `scrollWidth <= clientWidth` for a three-set all-tiebreak score. The implementer verified it is not vacuous (reverting the track to `72px` fails the width test).
- **follow-ups:**
  1. The spec reproduces the row's grid template and score markup rather than importing `player-drawer.tsx` and `<ScoreLine>` — Playwright's JSX transpilation blocked server-rendering the real component. A future edit to the row's className could silently desync the test. The spec's header flags this; a real component-render harness would close it.
  2. `match-rows.tsx` and the other `ScoreLine` call sites (home rail, schedule, search palette) share both defects — untrimmed sets and a fixed, clipping score track. Deliberately out of scope here; worth a sweep branch, possibly with a lint- or test-level guard so a new caller cannot reintroduce it.
  3. `playedSets`' both-zero heuristic cannot distinguish the known storage artifact from a hypothetical retirement at `0-0` in a genuinely started third set. Not introduced by this diff — it is inherited ambiguity in `matches.score` — and the guardrails reviewer judged it non-blocking, but worth a decision if it ever becomes material.

## T3 · Bench divider: symmetric dash gap — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clean, no stale `.next/` re-run needed. Completion review — `VERDICT: pass`, all five criteria met; the reviewer independently confirmed the `gap-2.5` count went 7→6 with the other six occurrences (lines 227, 243, 259, 529, 770, 787) untouched, and that JSX whitespace around the `sr-only` span reintroduces no stray space. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "No findings", confirming specifically that `value={BENCH}`, `key`, `dragListener={false}` and the `sequence` array are untouched, so drag behaviour, the bench/lineup partition and the position written on save are all unaffected. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` (no untracked files this run).
- **changed:** `src/components/dashboard/team/roster-table.tsx` — the bench divider's em dash is now its own `aria-hidden` span instead of living inside the caption string, and the container's `gap-2.5` becomes a single `gap-1.5` (6px) that produces both sides of the dash by construction. Measured: left 6px, right 6px — previously 10px left from the flex gap and ~4px right from a literal space character, which was the whole bug. An `sr-only` ": " was added so the sentence still reads as "Not in the lineup: drag a row below this line to bench them" rather than running the clauses together. A code comment records the gap value and that it was picked by eye at 11px against the eyebrow (the criterion allowed either a commit body or a comment; the subagent cannot commit, so the comment carries it).

## T4 · Dialog shell default width 440 → 520 — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit`, `npm test` all clean; the implementer additionally ran `npm run build` green. Completion review — `VERDICT: pass`; the reviewer independently opened all five `RosterDialog` callers and confirmed the resulting widths rather than taking them on trust: Add (`add-player-dialog.tsx:287`), Invite (`roster-invite-dialog.tsx:371`) and Edit (`edit-player-dialog.tsx:196`) pass no `width` and so inherit 520; Merge passes an explicit 520; review-requests (`join-requests-card.tsx:196`) passes an explicit 480. No caller file is in the diff. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "no guardrail violations": none of the three vendor-attribution inputs live in these dialogs (they are wizard-only), no workspace scoping, role gating, `canSubmitVideo`, analysis short-circuit or provider naming is touched, and a CSS-only width change cannot alter field order, labels or defaults. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard` (no untracked files this run).
- **changed:** `src/components/dashboard/team/dialog-shell.tsx` only — three edits: the `width` union becomes `440 | 480 | 520 | 560`, `RosterDialog`'s default goes `440 → 520`, and a header comment records why. The rationale is deliberately on the record: the DS's Dialog (v3) spec gives form dialogs `w-[440px]` and reserves `w-[520px]` for compare dialogs, and this shell was already fully v3-conformant, so 520 borrows a width the DS already sanctions rather than inventing a number — the roster's forms carry more rows than the v3 form dialog anticipates. Padding, the 18px rhythm, the title and contract sentence, the hand-rolled 28px close button, the footer and `maxWidth: calc(100vw - 32px)` are all byte-unchanged.
- **follow-ups:**
  1. `DialogContent`'s stock 16px close glyph still differs from the DS's 28px Chrome Icon Button. The shell's own comment already flags this as a cross-cutting change touching every dialog in the product — correctly out of scope here, but still worth doing.
  2. Once T5–T7 land, a visual pass over Add/Invite/Edit to confirm nothing inside them was tuned assuming 440px (a child sized off the old width rather than the container).
  3. Minor doc nit raised by the guardrails reviewer: 440 and 560 are now in the union with no caller using either. Deliberate — the criterion specified that union — but worth a decision if the shell is ever tidied.

## T5 · Add Player: occupied-spot acknowledgement gate — blocked
- **gate:** mechanical PASSED — `npm run lint`, `npx tsc --noEmit` and the full `npm test` suite all clean. (The subagent's own run showed 6 failures in the live-DB specs — `account-deletion-retention`, `join-requests-staff-read`, `pending-invites`, `program-owner-name-live`, `rls-workspace-isolation` — all failing at `signIn(...): Request rate limit reached` in `tests/fixtures/live-db.ts`. A clean re-run from the runner was fully green, so that was transient Supabase auth rate limiting, not this diff.) Completion review FAILED — `VERDICT: needs-work` on criterion 4 alone. Criteria 1, 2, 3 and 5 were all judged met, including independent confirmation that `player-fields.tsx`, `edit-player-dialog.tsx`, `roster-actions.ts` and `supabase/` are absent from the diff, that `DialogProblem` appears only as `message={error}`, and that no displacement or swap write was introduced. Guardrails NOT RUN — the completion stage failed first, and the gate stops at the first failure in cost order.
- **why it failed:** criterion 4 asks for a test covering the enable/disable sequence on the shipped gate. The component could not be driven directly: `AddPlayerDialog` is a `"use client"` component pulling in Radix and two server actions, and `@playwright/test` rewrites JSX in repo `.tsx` files — the same wall `tests/player-drawer-score.spec.ts` documents. The subagent instead wrote two tests and disclosed the gap plainly: a source-pinning test asserting the literal `ready` formula, the unchanged `disabled` expression, both acknowledgement resets and the `DialogProblem` usage; plus a behavioural test driving a hand-written replica of the state machine through the full sequence. The reviewer accepted the source-pinning half as a legitimate regression guard for those specific lines, but judged the gap material: nothing exercises the real `checked={spotAcknowledged}` binding, the real `onChange`, or the real `spotTakenBy.length > 0` JSX guard — so a checkbox wired to the wrong state, rendered outside the guard, or shipped with wrong copy or classes would pass both tests. That is precisely what criterion 2 specifies.
- **stash:** 74528e56ff5c5bdb6433762452f031e1e9ff62f2 (`blocked: T5`) — carries the full `add-player-dialog.tsx` change and the new `tests/add-player-spot-gate.spec.ts`. Recovered with `git stash apply 74528e56ff5c5bdb6433762452f031e1e9ff62f2`, NOT by index: the stash stack is shared across this repo's worktrees and other entries sit above and below this one.
- **what would unblock it:** a decision by the human, not a retry. The implementation itself was judged correct on every other count; the blocker is that this repo has no way to render a real client component in a test. Either (a) stand up a component-render harness — a real gap the branch has now hit twice, in T2 and T5 — and re-run T5 against it, or (b) amend criterion 4 to accept the source-pinning plus replica combination, as T2's `notes:` field did with its explicit escape hatch, and re-run. Retrying T5 unchanged will fail the same way.
- **follow-ups:**
  1. `sharedWith()` in `add-player-dialog.tsx` and `nameList()` in `player-fields.tsx` are now the same 1/2/N-others grammar in two places. Kept separate deliberately — the shared helper serves Edit player, which raises no confirm — but a third caller would justify extracting a name-list formatter with no opinion about the surrounding sentence.
  2. The acknowledgement is not announced to assistive tech. The spot note's live region already says the spot is held; whether the appearance of a newly-required checkbox should also be announced is an open a11y question.
  3. The recurring blocker: two tasks on this branch have now been unable to test a real component. A `@playwright/experimental-ct-react` setup, or a jsdom-based unit test runner alongside Playwright, would close it for the whole repo.

## T5 · Add Player: occupied-spot acknowledgement gate — done
- **gate:** mechanical — `npm run lint`, `npx tsc --noEmit` and the full `npm test` suite all clean. Completion review — `VERDICT: pass` on the AMENDED criteria (see the queue's T5 notes and commit 2e16a81; the author amended criterion 4 after this task's first run was blocked on it alone). Two review passes were needed: the first re-run still found criterion 4 unmet because the amended text requires the spec to "state plainly what it cannot catch" and the docstring did not, so a "What this cannot catch" section was added to `tests/add-player-spot-gate.spec.ts` — no test logic, assertion or source line changed — and the second pass judged the disclosure genuinely specific rather than token. Guardrails — `pipeline-guardrails-reviewer` RAN (the diff touches `src/components/dashboard/`) and returned "No findings", confirming no write to any other player's row, `addProgramPlayer` and its SQL untouched, `spotHeldNote` reused rather than replaced, the reshuffle workflow still working (one extra click, no constraint, no server round trip), and nothing touching the wizard, `job-request.ts`, `match-analysis.ts`, workspace scoping, role gating or provider naming. `rls-boundary-reviewer` skipped legitimately — no `src/lib/supabase/`, `src/lib/data/`, `src/app/api/`, `supabase/migrations/`, no new table or query. Surfaces determined from both `git diff HEAD --stat` and `git ls-files --others --exclude-standard`.
- **changed:** `src/components/dashboard/team/add-player-dialog.tsx` — a new local `spotAcknowledged` state gates submission when the chosen lineup spot is already held. `ready` gains exactly one term, `(spotTakenBy.length === 0 || spotAcknowledged)`; the primary's `disabled={!ready || pending}` is byte-unchanged. A checkbox — "Yes — share #N with {names} for now.", with an 11px sub-line "Nobody is moved off the line" — renders directly under the existing spot note, only while that note does, borrowing the "Also send an invite" checkbox's grammar wholesale rather than inventing a second one. A new `changeLineupSpot` wrapper is now the select's `onChange`, so the acknowledgement cannot outlive the spot it was given for: acknowledging #3 says nothing about #5. `reset()` clears it too. A local `sharedWith()` builds the name list, deliberately NOT bending the shared `spotHeldNote`, which Edit player uses and which raises no confirm. Nothing displaces anybody: sharing a spot stays legal, which is the whole point — what the gate refuses is the accidental share, not the deliberate one. Edit player is untouched, because editing an existing row's spot IS the reshuffle case the sharing decision exists for.
- **follow-ups:**
  1. Accepted residual risk, now recorded in the spec's own docstring: because the behavioural test drives a replica rather than the real component, a checkbox bound to the wrong state, rendered outside the `spotTakenBy.length > 0` guard, or shipped with wrong copy or classes would pass both tests. Two reviewers verified the actual wiring by hand this run; future edits to this checkbox rest on review.
  2. The recurring blocker behind that: this repo cannot render a real client component in a test — T2 and T5 both hit it. A `@playwright/experimental-ct-react` setup or a jsdom runner alongside Playwright would close it repo-wide. Infrastructure work for its own branch, which is why it was not done here.
  3. `sharedWith()` here and `nameList()` in `player-fields.tsx` are now the same 1/2/N-others grammar twice, kept separate deliberately. A third caller would justify extracting a formatter with no opinion about the surrounding sentence.
  4. The acknowledgement is not announced to assistive tech. The spot note's live region already says the spot is held; whether a newly-required checkbox appearing should also be announced is an open a11y question.
