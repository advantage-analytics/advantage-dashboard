# Run log — claude/audit-onboarding-claim-flow-59c114

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Gate first login into two-question onboarding — blocked
**gate:** mechanical (lint/tsc/test) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer clear (no findings) · rls-boundary-reviewer **FAILED** — 1 finding. Both guardrails ran (diff touched src/app/dashboard/ and supabase/migrations/ + programs services).
**failed stage & reason:** 5c, rls-boundary-reviewer: the migration's replacement `handle_new_user()` stamps `users.onboarded_at` from `raw_user_meta_data.onboarding_complete` — GoTrue `user_metadata` is settable by any caller of `signUp()` with the public anon key, so a crafted direct signup can pre-stamp its own account and bypass the onboarding gate. No cross-account or privilege impact today (persona `role` is decorative; `is_admin`/`plan` separately protected), but it is a security-definer trigger branching on attacker-controlled input for state the code asserts comes only from two trusted server paths. Fix path for a re-run: carry the flag in server-controlled `app_metadata` (both creation call sites are server-only — `join-actions.ts` admin.createUser, `claim-actions.ts` signInWithOtp — the latter cannot set app_metadata, so alternatively stamp `onboarded_at` directly from the trusted server path post-creation instead of via trigger metadata).
**stash:** b8c3bbc2cf1f6ef2a6b4ed6a6ffb584e952fb1df (`blocked: T1`; restore with `git stash apply <sha>`, not pop)
**live-DB note:** the migration WAS applied to the live database before the gate ran and is not reverted by the stash: `users.onboarded_at` exists (backfilled 15/15 from created_at) and `handle_new_user()` now honors the metadata flag. Harmless while no app code reads the column (that code is stashed), but the trigger's metadata branch is the flagged surface — the re-run should replace it, not add to it.
**follow-ups:** 1. Once onboarding is the only front door, decide whether /claim's public RoleChoice step is still needed (implementer note). 2. Consider a quiet back link on onboarding step 2 (implementer note; design has none). 3. T3/T5 re-point coach/junior routes via the routing table in src/app/onboarding/actions.ts (stashed).

## T2 · Non-college team workspace: schema + creation action — blocked
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (diff touches nothing under src/app/dashboard/, src/components/dashboard/, or the upload wizard — verified via git diff HEAD --stat + untracked list) · rls-boundary-reviewer **FAILED** — 2 HIGH findings.
**failed stage & reason:** 5c, rls-boundary-reviewer. (1) HIGH: `programs`' pre-existing `using (true)` public SELECT policy + anon/authenticated grant makes every custom-org row world-readable via PostgREST (`org_type=neq.college`), exposing school_name, status, flags and raw owner_user_id — the migration filtered search_programs and nulled program_key but never tightened the base-table policy its own comments call "private workspaces". Fix path: scope the SELECT policy (e.g. `org_type='college' OR owner_user_id=auth.uid() OR is_program_staff(id)`). (2) HIGH: `create_custom_program` has no per-user cap/rate limit; every custom org resolves as kind 'team' → 'program' tier → flat 75 processing-hours/month (splitstep quota.ts/config.ts), so one authenticated account could loop the RPC and mint unbounded instantly-active vendor-quota allowances. Fix path: per-user cap (e.g. max N owned custom orgs) enforced in the RPC, and/or an org-type-aware quota tier. Also noted (benign): the search_programs redefinition omits the file's conventional revoke/grant restatement — ACLs survive CREATE OR REPLACE, but restate them in the re-run.
**stash:** 293c38c229446ce94d24e5ac3666552feb20c6fe (`blocked: T2`; restore with `git stash apply <sha>`, not pop)
**live-DB note:** migration `20260830000931_custom_org_programs` WAS applied to the live database before the gate ran and is not reverted by the stash: org_type column, relaxed/added CHECKs, filtered search_programs, and the create_custom_program RPC all exist. Runner mitigation applied immediately after blocking: `revoke execute on function public.create_custom_program(text, text) from authenticated;` — the RPC is now uncallable, and `select count(*) ... where org_type <> 'college'` confirmed 0 custom rows exist, so neither finding has live exposure today. The re-run should fix both findings in a follow-up migration and re-grant execute.
**follow-ups:** 1. /dashboard/team/settings coerces null team to "mens" and offers a squad selector — hide it and pass null for custom orgs (natural T3 scope). 2. Make update_program_settings refuse writing a squad onto non-college rows. 3. Plumb Workspace.orgType if T3 wants "Club workspace"/"Academy workspace" switcher subtitles.

## T4 · Bind program claims to the signed-in account — blocked
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` (all five criteria, incl. e2e evidence for both paths) · pipeline-guardrails-reviewer skipped (nothing under src/app/dashboard/, src/components/dashboard/, or the upload wizard — verified via git diff HEAD --stat + untracked list) · rls-boundary-reviewer **FAILED** — 1 blocking finding.
**failed stage & reason:** 5c, rls-boundary-reviewer: cross-path row hijack on `pending_claims`. The table is keyed by email alone and BOTH start paths upsert onConflict:"email" without checking whether the row is already bound to someone else. (a) Denial: an authenticated attacker starts a signed-in claim with a victim's school address, setting claimant_user_id=attacker; the victim's later signed-out upsert doesn't include that column, so it survives, and the new completeClaim guard permanently rejects the victim with `no-pending` (their own retries extend expires_at, prolonging the lockout). (b) Redirection: after a coach starts a signed-in claim (token emailed, claimant bound), a signed-out attacker upserts the same email with a different program_key; the coach's genuine token then completes ownership of the attacker-chosen program. Fix path for a re-run: refuse (or fully reset token_hash/claimant_user_id on) an upsert that would change binding fields written by another identity — e.g. key rows by (email, claimant scope), or make each start path always write ALL binding columns and require the completion RPC to re-check program_key against what the token was issued for (bind program_key into the token hash input, or store and verify it alongside).
**stash:** e51eb77cedca0df11acb3768655291f48cea1dde (`blocked: T4`; restore with `git stash apply <sha>`, not pop; contains the 5 code files + the untracked migration mirror)
**live-DB note:** migration `20260830003627_pending_claims_signed_in_binding` WAS applied to the live DB and is not reverted by the stash: pending_claims gained nullable claimant_user_id + token_hash + partial unique index, and complete_program_claim_with_token exists (SECURITY INVOKER, EXECUTE service_role-only — verified). No live exposure: the RPC is unreachable by clients, the columns sit behind pending_claims' zero-policy RLS + revoked grants, and no app code writes them while the T4 code is stashed. No mitigation needed.
**environment note:** untracked `.codex/` directory + `AGENTS.md` (a Codex-format mirror of this repo's Claude config) appeared in the worktree at 17:40 during T4's run; not created by the task, excluded from its stash, left untracked pending the author's triage.
**follow-ups:** 1. Pre-existing: `complete_program_claim` RPC is directly callable by any authenticated session with client-supplied advisory flags (p_domain_matched, p_skips_manual_review, p_match_reason) and no pending-row requirement — reviewer-facing evidence is spoofable; consider revoking direct execute and routing through the server like the new token RPC. 2. Console-print email mode logs raw claim tokens when RESEND_API_KEY is unset (dev-only, pre-existing behavior, first live-secret template to ride it). 3. The 5-active-claims throttle is read-then-write (TOCTOU) — soft cap only.

## T6 · Player join-intent polish — redacted status, sharing rows, referral exit — done
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (nothing under src/app/dashboard/, src/components/dashboard/, or the upload wizard — verified via git diff HEAD --stat + untracked list) · rls-boundary-reviewer clear ("no findings": redaction confirmed server-side in the JSON payload, own-row profile fetch RLS-scoped, ?school= param React-escaped with slugified ?ref=, no service-role or env leaks).
**changed:** Player join intent (?intent=join) of the claim flow now matches design screens 4.1–4.3. /api/programs/search redacts via redactForPlayer() in programs-server.ts — player payload carries published directory fields + boolean onAdvantage only (verified on the wire; coach payload byte-identical to before). program-search.tsx renders "On Advantage"/blank under join and forks its bottom exit: join → new /claim/program/referral screen (copyable siteUrl()/claim?ref=<slug> link with Copy button, "Continue to my account" → /dashboard), coach → unchanged /claim/program/new form. /claim/[key]/request prefills the signed-in user's name from their own users row ("From your profile" note), renders the three 4.2 sharing rows from new shared sharing-rows.tsx (exported for T10 reuse), plus a "Keep it personal" secondary → /dashboard; signed-out behavior unchanged (empty editable name). New referral-link.tsx copy control. MAP.md regenerated for the new route.
**follow-ups:** 1. T10 should import JOIN_SHARING_ROWS / <JoinSharingRows /> rather than re-typing the trio (8.2 also has a separate two-column block that is NOT this trio). 2. Coach flow paths (F3.2/F3.4) land on the same /claim/[key]/request route and now see the player-oriented sharing rows — decide whether that copy should be intent-conditional. 3. Design 4.3's "We'll tell you if <school> joins" line was deliberately not shipped (nothing is filed, so nothing could trigger it) — revisit if referral capture is ever wanted; also dropped the stale "through the 2026 pilot" date. 4. Design 4.2's program identity header (roster count) would need a new public projection. 5. Product-level question flagged by reviewers: intent is client-supplied, so omitting it returns the (pre-existing, public) coach payload — confirm the threat model treats owner visibility as UI policy, or move redaction behind auth in a later task.

## T7 · Structured role on invite requests — done
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (nothing under src/app/dashboard/, src/components/dashboard/, or the upload wizard — verified via git diff HEAD --stat + untracked list) · rls-boundary-reviewer clear ("no findings": deny-all posture on program_requests preserved, TS allowlist + identical SQL CHECK as defense in depth, admin read stays behind is_admin gate, claim-roles.ts safely client-shareable — constants and pure functions only).
**changed:** The 6.4 request screen gains an optional "Your role" select (five claim-setup values) between email and note; requestInvite() validates via toClaimRole() server-side and writes program_requests.role (new nullable text column with named CHECK, migration 20260830014449 applied to live DB, verified with valid/roleless/23514-rejected inserts, advisors clean); /admin/claims selects and RequestRow renders the label when present. New shared vocabulary module src/lib/services/programs/claim-roles.ts; setup-form.tsx now imports it instead of its private ROLES copy. Note placeholder no longer asks for the role in prose. Duplicate-request 23505 collapse untouched (unique index excludes role).
**follow-ups:** 1. Invite-request email templates don't mention the role — belongs in the facts row if owners ever get direct notification (T9 wires those sends). 2. program_claims.claimant_role renders via .replace(/_/g," ") in ClaimRow — could use claimRoleLabel() for consistent casing. 3. No "player/team member" option in CLAIM_ROLES, yet join-intent players land on this form — extend vocabulary + CHECK together if wanted.

## T8 · Wire the claim check-email resend button — done
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (nothing under src/app/dashboard/, src/components/dashboard/, or the upload wizard — verified via git diff HEAD --stat + untracked list) · rls-boundary-reviewer clear ("no findings": no new oracle — match vs. no-match identical in content and timing; same Supabase OTP throttle as startClaim, no second unthrottled path; UPDATE touches one row, one column; service-role stays behind "use server").
**changed:** The /claim/check-email Resend button now works: startClaim's inline signInWithOtp extracted into shared sendClaimOtp helper (no behavior change), new resendClaim({programKey,email}) server action re-checks the program is unclaimed, always re-sends the OTP, then extends the matching pending_claims row's expires_at via one conditional-WHERE update (deliberately indistinguishable for missing/expired rows — oracle-safe by design). resend-timer.tsx calls it in a transition: disables while pending, resets the 60s countdown only on success, inline error on failure without clearing the countdown. check-email/page.tsx threads the program URL param as programKey (anticipated by task notes).
**follow-ups:** 1. check-email's "your address" display fallback now also flows into resendClaim as a literal email when ?to= is missing — harmless (Supabase rejects it) but worth graceful degradation. 2. A resend clicked after the pending row expired still mails a link that will fail at completeClaim with no-pending/expired — deliberate oracle-safety trade-off; revisit only with a design that preserves indistinguishability.

## T9 · Wire the claim lifecycle emails — blocked
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` (incl. judging criterion 2's "to the program owner" a drafting error — the reviewed template is requester-facing, wired accordingly) · pipeline-guardrails-reviewer skipped (nothing under src/app/dashboard/, src/components/dashboard/, or the upload wizard — verified via git diff HEAD --stat + untracked list) · rls-boundary-reviewer **FAILED** — 2 findings.
**failed stage & reason:** 5c, rls-boundary-reviewer. (1) Anonymous mail relay + timing oracle in requestInvite: the unauthenticated action mails whatever address was typed (never verified), programs are enumerable without auth (~1,940 via /api/programs/search), and the open-status unique index throttles per program only — so one pass can mail a victim address ~1,940 times. Worse, the send is awaited only on the created branch (duplicate submits return fast, first files run two outbound HTTPS calls) — a reliable timing oracle for "does this (email, program) pair have an open request", the exact oracle resendClaim's own comments in the same file close by sending unconditionally. Fix path: send the receipt only to addresses that are verified or self-confirmed (e.g. gate on a signed-in session's own email, or make timing uniform by always running the same outbound work), and add a cross-program cap per address. (2) Internal→external disclosure: notifyClaimant now emails program_claims.review_notes verbatim to declined claimants under "Reviewer's note", but review-rows.tsx's input still says "Note (optional, kept on the claim)" — admins writing internal commentary would unknowingly ship it to the person being declined. Fix path: either update the admin UI label/affordance to disclose the audience, or add a separate explicit "message to claimant" field and keep review_notes internal.
**stash:** cef91eca05f68f5ef95b410cfc323b218a63442d (`blocked: T9`; restore with `git stash apply <sha>`, not pop)
**live-DB note:** no migration in this task — nothing applied, nothing to mitigate.
**follow-ups (from the implementer, still good):** 1. raiseObjection writes `objected` directly without transition(), so an objected-to claimant hears nothing — decide if they should. 2. resolveRequest has no reviewer-note input, so the decline's "What they said" panel is always absent. 3. docs/email-system.md §8's verified-against stamp is stale. 4. Template copy fixes worth keeping on re-run: the approved email's false "We've let the program's listed contacts know" line, and nullable requesterName.

## T10 · Sharing terms and Not-now on invite acceptance — done
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (nothing under src/app/dashboard/, src/components/dashboard/, or the upload wizard — verified via git diff HEAD --stat + untracked list) · rls-boundary-reviewer clear ("no findings": requestFreshInvite unreachable without a real hashed token, recipient derived only from the row, send strictly under the unique-index insert success branch, no new oracle, inviter name already disclosed by the original invite email, service-role confined).
**changed:** /join/[token] now matches design 8.2/8.3a/9.2a. New src/components/join/join-terms.tsx renders the two-column sharing block ("Your coaches will see" / "Stays yours", six design rows verbatim) above every Join button in the ready/sign_in/sign_up forms, plus a quota footer with the REAL figures (getMonthlyCapSeconds program=75h vs individual=2h — the same source reserveQuota enforces; numbers passed as plain props so splitstep/config never reaches the client bundle). "Not now" is a pure GET link (?not-now=1) rendering the "Nothing was sent" pane — zero writes, invite unconsumed, reopening the link still offers Join. Expired state names the inviter and gains "Ask for a new invite": requestFreshInvite(token) files a program_requests row (idempotent under program_requests_open_unique; repeat clicks cannot re-mail) and best-effort sends new expiredInviteNudgeEmail to the inviter resolved from the row. loadInvite resolves the inviter's display name (address stays server-only). sharing-rows.tsx doc comment corrected (4.2 trio ≠ 8.2 block, contrary to the design caption and the task note).
**follow-ups:** 1. Design's 4.2 caption claiming the row sets are identical contradicts its own frames — raise with the designer. 2. 8.2's pane header micro line "Invited by {name}, {role}" is now buildable (inviter name available on live states) — small follow-on. 3. 8.3b (invite as workspace-switcher badge + Home card for existing accounts) is entirely unbuilt — deferred to its own branch per the plan. 4. Nudge requests land in /admin/claims undistinguished from ordinary invite requests; a coach-facing "someone asked you to resend" surface would be better.

## T11 · Getting-set-up checklist on the personal home empty state — done
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` (incl. weighing both declared deviations as required-by-criteria, not creep) · pipeline-guardrails-reviewer clear ("no findings": wizard/job-request/claim-state/match-analysis untouched; users.hand/backhand feed only the display-label fallback, never attribution or the vendor payload) · rls-boundary-reviewer clear ("no findings": own-row reads/writes under RLS, hardcoded update payload — no mass assignment, plan untouched, allowlisted hand/backhand values, user_preferences RLS makes the probe safe by construction).
**changed:** Zero-match personal home now shows "Getting set up · n of 3" modeled on team first-steps: rows "Account and playing profile" (done when users.hand+backhand set), "Get your first match in" (→ /dashboard/matches/new), "Choose how you're notified…" (→ /dashboard/settings/preferences); fixed-geometry indicators flip without layout shift, derived count, block returns null at 3/3. Row 3's real done-state is **a user_preferences row exists** (every column is defaulted and the row is created only on first save, so existence is the only true "chose preferences" signal — the closest real preference per the task note). The two named door cards remain; the THIRD door card ("Set hand and backhand") was removed as now-redundant with checklist row 1 (criterion names exactly two). Because users.hand/backhand had NO write path anywhere (0/15 live rows populated), Settings › Profile gained Playing hand / Backhand selects with a server-side allowlist — without this, row 1 could never complete. Home page.tsx extends its existing users select and adds the preferences probe in the same Promise.all.
**follow-ups:** 1. The upload wizard collects per-match playerHand/playerBackhand independently — could prefill from the profile (guardrailed file, deliberately untouched). 2. A pure settledSteps() helper + spec in the style of tests/team-roster-progress.spec.ts would make the count/unmount rule directly testable. 3. hand/backhand joining profile-form's FIELDS means every existing user now sees a higher "N fields left" count — real UX side effect to be aware of. 4. users.onboarded_at is populated on all 15 live rows but read nowhere in src — the blocked T1's column; wire or drop when T1 re-runs.

## Triage — author decisions, T1/T2/T4/T9 re-queued (2026-08-29)
The author reviewed all four blocks and flipped them back to `todo`. Binding decisions for the re-runs:
- **T1 / T4:** fix paths as logged in their blocked entries — no product change. T1 replaces handle_new_user's metadata branch (no client-controllable trust) and stamps onboarded_at from the trusted server paths; T4 closes the pending_claims cross-path hijack (all binding columns written by every start path; completion verifies the program the token was issued for).
- **T2:** custom orgs get a REDUCED quota, not the program 75h — "reduced quota and make them pay." Map custom-org workspaces to the individual-tier figure (2h/month shared) until a paid plan raises it; keep a small ownership cap (2 per user) as defense-in-depth; fix the programs SELECT policy per the blocked entry. Payment/upgrade wiring is explicitly OUT of T2's scope — it rides the deferred pricing-tier plan (Pro gating intentionally unenforced today). Note the divergence from design 7.2/7.3's "same 75h" line in the code comment.
- **T9:** receipt email goes to SIGNED-IN requesters only, and only to their own verified account email (signed-out requesters get the on-screen confirmation; kills the relay and the oracle). Decline reasons move to a SEPARATE claimant-facing "message to claimant (they will see this)" field on the admin review row; review_notes stays internal and is never emailed.

## T1 (re-run #2) · Gate first login into two-question onboarding — blocked
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer **FAILED** — 2 findings · rls-boundary-reviewer **FAILED** — 1 finding. The metadata-trust flaw from re-run #1 IS fixed and verified (live handle_new_user stripped of onboarded_at/metadata logic; crafted-metadata negative test left onboarded_at null) — these are NEW, smaller findings surfaced now that the trigger no longer masks them. Gate has no severity triage: any finding blocks.
**failed stage & reason:**
- rls-boundary: prototype-pollution validation gap in `finishOnboarding` (src/app/onboarding/actions.ts) — `const resolution = RESOLUTION[choice]` with `choice="__proto__"` (or constructor/toString/…) resolves truthy via the prototype chain, passing the `if (!resolution)` guard; `role: undefined` is dropped (allowlist intact, no cross-account write) but `onboarded_at` still gets stamped on the caller's OWN row, letting them skip the two-question flow, and the subsequent `redirect(undefined)` throws. Fix: validate with `Object.prototype.hasOwnProperty.call(RESOLUTION, choice)`, a `Map`, or the `.find()`-over-array pattern claim-roles.ts's `toClaimRole()` already uses.
- pipeline-guardrails (2, same shape — "confidently wrong state, nothing looks broken on screen"): (a) claim-actions.ts completeClaim stamps onboarded_at right after finding the pending_claims row but BEFORE the expiry / unknown-program / taken / failed guards — a claimant whose claim then fails is permanently marked onboarded (persona never captured, /onboarding unreachable), and /claim/verify/failed says "Nothing was created" which is no longer true. (b) join-actions.ts createAccountAndAccept stamps right after admin.createUser but BEFORE acceptWithSession confirms — if acceptance fails after account creation, the account is left permanently onboarded with no membership. Fix: move each stamp to fire only on the success path (after all guards pass / after acceptance confirms), or make the failure branches leave onboarded_at null.
**stash:** 611e8e2ee167e1e7ee5166b782afa2441b335753 (`blocked: T1 rerun`; restore with `git stash apply <sha>`, not pop; contains onboarding route + gate + plumbing + migration mirror)
**live-DB note:** migration `20260830035337_onboarded_at_server_stamped` WAS applied to the live DB and is GOOD to keep regardless of this block — it REPLACED the vulnerable trigger from re-run #1 with a clean one (no metadata/onboarded_at logic), fixing the original T1 security hole at the DB layer. onboarded_at exists + backfilled 15/15. No app code reads it while the T1 route is stashed, so the column is inert; the re-run's fixes are all in stashed TS, not the DB. Do NOT re-introduce the metadata branch.
**re-run guidance:** all three findings are small, localized TS fixes on the stashed code — no product decision needed, no new migration. Apply the three fixes above to the stash, re-gate.

## T1 (re-run #3) · Gate first login into two-question onboarding — done
**gate:** mechanical (lint/tsc/test 200/200 incl MAP freshness) pass · task-completion-reviewer `VERDICT: pass` (all five criteria hold after the fixes; success-path stamping satisfies "at creation") · pipeline-guardrails-reviewer clear (verified both premature-stamp findings fixed: claim stamp now after complete_program_claim RPC success past all five failure returns; invite stamp now after acceptWithSession ok) · rls-boundary-reviewer clear (verified prototype-pollution fix real via Object.prototype.hasOwnProperty.call guard — "__proto__"/constructor/etc. rejected before any lookup or write; original metadata-trust property still holds; both admin stamps scoped to server-verified ids; finishOnboarding writes own row role+onboarded_at only, never plan).
**changed:** Restored stash 611e8e2 (the full T1 feature) and applied the three gate fixes on top: (A) src/app/onboarding/actions.ts finishOnboarding guards the RESOLUTION lookup with Object.prototype.hasOwnProperty.call so raw-RPC prototype-chain keys can't stamp onboarded_at; (B) src/lib/services/programs/claim-actions.ts completeClaim stamp moved to after the complete_program_claim RPC succeeds; (C) src/lib/services/programs/join-actions.ts createAccountAndAccept stamp moved to after acceptWithSession succeeds. Feature: dashboard-layout gate redirects null-onboarded_at viewers to /onboarding (not proxy.ts); two-step flow (1.2 persona cards → 1.3 college question + Skip) persisting persona to users.role (play→player/coach→coach/junior→parent); college-yes → /claim/program?intent=join, others → /dashboard; invite- and claim-created accounts stamped server-side on their success paths so they never re-onboard. Migration 20260830035337 (already live from re-run #1's session) replaced the vulnerable metadata trigger with a clean one; onboarded_at backfilled 15/15.
**live-DB note:** no new migration this run — the live 20260830035337 state is correct and unchanged.
**follow-ups:** 1. signInAndAccept never stamps onboarded_at, so a pre-existing un-onboarded account joining via invite sees /onboarding once afterward (pre-existing; one redundant screen; harmless). 2. T3 re-points the coach interim route /claim/program → the team fork; T5 re-points the junior route. 3. users.onboarded_at is now read by the gate — the T11 follow-up noting it was "read nowhere" is resolved.

## T2 (re-run) · Non-college team workspace: schema + creation action — done
**gate:** mechanical (lint/tsc/test 200/200) pass · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer clear (this re-run DID touch the wizard — verified the useUploadMatchWizard change is quota-DISPLAY only: getMonthlyCapSeconds→monthlyCapSecondsFor, job-request.ts zero diff, three vendor attribution inputs untouched; canSubmitVideo single-source intact; provider naming clean) · rls-boundary-reviewer clear (both prior HIGH findings verified closed: old `using(true)` policy DROPPED not OR'd, user_program_role membership disjunct doesn't self-leak, anon sees college-only; per-user cap race-safe via pg_advisory_xact_lock, owner from auth.uid(), org_type can't be 'college', EXECUTE re-granted to authenticated only; roster_public=false a tightening).
**changed:** Author decision applied — custom-org team workspaces draw the INDIVIDUAL 2h monthly quota (not the collegiate 75h) until a paid plan raises it (paid wiring out of scope; marker left on quotaTierFor), PLUS a per-user ownership cap of 2 enforced inside create_custom_program. New migration 20260830050000_custom_org_privacy_and_caps applied live on top of the already-live 20260830000931: (1) replaced programs' `using(true)` SELECT policy with `org_type='college' OR owner_user_id=auth.uid() OR user_program_role(id) is not null` — college stays public, custom orgs member-only; (2) create_custom_program gains the advisory-locked cap (3rd create raises SQLSTATE 54000) + roster_public=false + re-granted EXECUTE to authenticated. App layer split accountTypeFor (ledger, unchanged) from new quotaTierFor/monthlyCapSecondsFor (figure); Workspace.orgType threaded through active-workspace-server, team-home-server, usage-server/actions, resubmit-job (incl. webhook auto-retry), invite-acceptance/join page (invitee quoted the enforced figure), and the wizard's remaining-quota read — so shown cap == enforced cap everywhere. Live verification: non-member/anon see 0 custom rows + all 1941 college rows; 3rd create blocked; custom→2h, collegiate→75h, personal→2h; collegiate /claim states (unclaimed UnivOfNorthGeorgiaM / pending UCLAM / active DartmouthCollegeW / 404) all walked clean; search stays collegiate-only.
**live-DB note:** create_custom_program is now callable again (EXECUTE re-granted) — the earlier runner mitigation revoke is superseded. Both migrations live and mirrored in supabase/migrations/.
**follow-ups:** 1. T3 wires the creation UI and must surface the "limit-reached" (cap) and print the 2h custom-org quota copy. 2. emptyProgramUsage() still hardcodes the 75h program cap in the no-team-card fallback (display-only, pre-existing, unrelated). 3. Paid-plan tier upgrade rides the deferred pricing plan — the marker is on quotaTierFor. 4. /dashboard/team/settings squad selector should hide for custom orgs (null team) — natural T3 scope.
