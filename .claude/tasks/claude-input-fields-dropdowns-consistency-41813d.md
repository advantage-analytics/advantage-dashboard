# Tasks — claude/input-fields-dropdowns-consistency-41813d

> Scope: NARROWED 2026-09-02 by the author to the CLAIM_FIELD family only —
> the nine pages that share one field constant: /onboarding, /join/[token],
> /claim/program, and the six /claim/... setup, request and object pages.
> T1 (the advField helper) is done; T3 and T4 carry the change. Every other
> task is `later` — deferred, not cancelled: the app-wide drift they describe
> is real and documented in work/input-fields-dropdowns-consistency/, and any
> of them can be promoted to `todo` by hand. Generated by the ICM pipeline at
> `work/input-fields-dropdowns-consistency/`.

Run one with `/task-next`. To drain the file, loop a plain-text instruction —
**not** `/loop /task-next`, which a scheduled fire cannot invoke:

> `/loop Read .claude/skills/task-next/SKILL.md and follow it exactly — run one task from this branch's queue; do not add, edit, or reorder tasks; then stop.`

Append freely while it runs: the queue is re-read at the start of every
iteration, and the runner only ever rewrites a task's `status:` line.
Mark a task `next` to jump the queue.

Status values: `todo` (eligible to run), `next` (jump the queue), `doing` /
`done` / `blocked` (written by the runner around a dispatch), and `later`
(deferred — `/task-next`'s picker never selects it, so a loop drain skips
straight past it; promote a task to `todo` by hand once it's actually
ready).

> **Environment, once per fresh checkout.** This worktree has no
> `node_modules` and no real `.env.local`. Any task that lints, tests or
> previews must run `npm ci` first — a real install, because a symlinked
> `node_modules` panics Turbopack.

## T1 · Add the advField() class helper
- **status:** done
- **model:** opus
- **files:** `src/lib/ui/adv-field.ts` (new; modelled file-for-file on `src/lib/ui/adv-button.ts`) — guess; may also touch `src/styles/design-system/colors.css` if `--bg-field` is defined there
- **done when:**
  - [ ] `src/lib/ui/adv-field.ts` exports `advField(kind: "boxed" | "underline" = "boxed", size: "sm" | "md" = "md"): string` plus `AdvFieldKind`/`AdvFieldSize` types. `boxed` resolves to exactly `rounded-[var(--radius-button)]`, `h-9`+`text-[13px]` (md) / `h-8`+`text-[12px]` (sm), `border border-[var(--border-field)]`, `bg-[var(--surface-card)]`, `px-3`, `text-[var(--ink-900)]`, `placeholder:text-[var(--ink-400)]` and a disabled background; `underline` resolves to `h-[34px] border-b border-[var(--border-field)] focus:border-b-2 focus:border-[var(--blue)] bg-transparent text-[13px]` plus the same ink/placeholder. No `rounded-md`/`lg`/`xl` appears anywhere in the file.
  - [ ] The returned strings contain no `focus:*`/`focus-visible:*` shadow, ring or outline utility, and the header comment says why: `src/styles/design-system/focus.css` is imported unlayered, silently discards `focus-visible:*` utilities, and already supplies `--focus-ring-field` to `input`/`select`/`textarea`. It also says consumers of the `underline` kind add `data-focus-ring="none"` themselves, earned because the rule visibly changes on focus.
  - [ ] The header records the two-radius-scale trap: `rounded-md`/`lg`/`xl` resolve to 8/10/14px in this codebase, not the design system's 8/12px, so field radius is written as a token and never as a class name.
  - [ ] Every `var(--*)` the helper emits is defined in `src/styles/design-system/*.css`. `--bg-field` is NOT defined today (SKILL.md lists it only as the literal `#F7F7F7`), so the diff either adds `--bg-field` to `colors.css` in both the light and dark blocks, or the header names the existing token substituted for the disabled background (e.g. `--surface-subtle`) and why.
  - [ ] `npm run lint` passes, and no file other than `src/lib/ui/adv-field.ts` (and `colors.css`, only if the token is added) changes — the helper has no consumers yet.
- **notes:** Foundation for T2–T10 and T12. Confirm each value against the table in `work/input-fields-dropdowns-consistency/02_design/output/design.md` lines ~178–195.

## T2 · Move the three shared primitives and ScoreCell onto the canonical field values
- **status:** later
- **model:** opus
- **needs:** T1
- **files:** `src/components/ui/input.tsx`, `src/components/ui/select.tsx`, `src/components/ui/textarea.tsx`, `src/components/dashboard/matches/new-match-wizard/ScoreCell.tsx` — guess, all verified present
- **done when:**
  - [ ] `input.tsx`, `textarea.tsx` and `select.tsx`'s `SelectTrigger` contain no `rounded-md`: each field's radius is `rounded-[var(--radius-button)]`, its text size is `text-[13px]` (replacing `text-xs`/`text-sm`), `Input` stays `h-9`, and `SelectTrigger`'s tiers read `data-[size=default]:h-9 data-[size=sm]:h-8` (today default is `h-7`).
  - [ ] `select.tsx`'s `SelectContent` panel is `rounded-[var(--radius-dropdown)]` instead of `rounded-md` (brief success criterion 4: opened select panels match the Dropdown spec).
  - [ ] `ScoreCell.tsx` no longer writes `rounded-[6px]`, and still renders at 32px tall, 6px radius and 12px text — the values it has today by override — either by keeping `h-8 text-[12px]` as an explicit small-tier override or via a small-tier prop the primitive now exposes. `!w-7`, `px-0`, `text-center` and `tabular-nums` remain.
  - [ ] Guardrail (`docs/ui-revamp-guardrails.md` §4 items 2–3, set-score ordering and tiebreak game counts): in `ScoreCell.tsx` the only changed line is the `className` template — `value`, `onChange`/`onValueChange`, `onKeyDown`, `inputMode`, `pattern`, `maxLength`, `aria-invalid` and the ref wiring are byte-identical, and neither consumer (`DetailsContent.tsx`, `match-actions/edit-match-dialog.tsx`) is touched.
  - [ ] `npm run lint` passes and `pipeline-guardrails-reviewer` has been run on the diff and reports no finding.
- **notes:** Guardrail-gated — `ScoreCell` is the set-score input. Blast radius is verified small: `Input` has exactly one consumer (`ScoreCell`); `SelectTrigger` and `Textarea` have zero consumers in `src/`. The plan says "delete the redundant `h-8`", but `Input` stays `h-9`, so deleting `h-8` without a small tier grows the cell to 36px — hence the outcome-pinned criterion. `input.tsx`'s `focus-visible:border-[#E5E5E5]` is a border-colour utility, not a ring, so focus.css does not discard it; leave it. Screenshot the score grid in both consumers before/after and confirm the cells are unchanged in size and position.

## T3 · Convert CLAIM_FIELD and the program-search box to advField("boxed")
- **status:** done
- **model:** sonnet
- **needs:** T1
- **files:** `src/components/claim/claim-shell.tsx` (`CLAIM_FIELD`, ~line 267), `src/components/claim/program-search.tsx` (search-box wrapper ~line 109) — guess, both verified present
- **done when:**
  - [ ] `CLAIM_FIELD` in `claim-shell.tsx` is defined as `advField("boxed")` plus `w-full` (imported from `@/lib/ui/adv-field`) and no longer contains `h-[38px]` or `rounded-[var(--radius-element)]`; `CLAIM_LABEL` and every other export in the file are unchanged.
  - [ ] In `program-search.tsx` the search-box wrapper (`<div className="flex h-[38px] … rounded-[var(--radius-element)] …">`) is `h-9` with `rounded-[var(--radius-button)]`, and keeps `focus-within:shadow-[var(--focus-ring-field)]`, `border border-[var(--border-field)]` and `bg-[var(--surface-card)]`.
  - [ ] The inner `<input>` in `program-search.tsx` keeps `data-focus-ring="none"` and its comment (`the box above carries it`) — this is the composite-field case, not the underline case — and the results `<ul>` (~line 134) is untouched.
  - [ ] No file other than those two is modified, and `npm run lint` passes.
- **notes:** The most visible diff in the feature: 38px → 36px and 8px → 6px across every claim, join and onboarding field at once. Screenshot `/claim`, `/claim/team/setup` and one `/join/[token]` page before and after; tab through one form and confirm every field still shows the neutral focus ring (focus.css supplies it — nothing in the class string should try to).

## T4 · Verify the six CLAIM_FIELD consumers inherit T3 unchanged
- **status:** done
- **model:** sonnet
- **needs:** T3
- **files:** `src/components/claim/setup-form.tsx`, `src/components/claim/team-setup-form.tsx`, `src/components/claim/unlisted-program-form.tsx`, `src/components/claim/contact-owner-form.tsx`, `src/components/join/join-forms.tsx`, `src/app/onboarding/onboarding-flow.tsx` — guess; all verified present and all consume `CLAIM_FIELD`
- **done when:**
  - [ ] Every `CLAIM_FIELD` usage in the six files has been inspected, and the diff is either empty or each hunk removes only a class that re-specifies a value `advField("boxed")` already sets (an `h-*`, `rounded-*`, `border-*` or `text-[Npx]`) — no other class is added or removed.
  - [ ] The `cursor-pointer` appends on the `<select>`s at `setup-form.tsx:175`, `team-setup-form.tsx:149`, `contact-owner-form.tsx:167` and `onboarding-flow.tsx:354`, and the `text-[var(--ink-400)]` placeholder-state class inside `onboarding-flow.tsx`'s `cn(…)`, are all still present.
  - [ ] `contact-owner-form.tsx:196`'s textarea keeps `h-auto resize-none py-2.5 leading-[1.55]` — a multi-line control must override the field height; the removal rule applies to duplicated values, not to this.
  - [ ] `npm run lint` passes; `git diff --stat` across the six files is empty or shows single-line hunks only.
- **notes:** Expected near-zero diff. Screenshot `/onboarding` and one claim form to confirm the inherited change looks right. A task that lands no diff is still done — say so in the run output rather than inventing an edit.

## T5 · Move the three settings fields to advField("boxed", "sm")
- **status:** later
- **model:** opus
- **needs:** T1, T2
- **files:** `src/components/dashboard/settings/team-settings-form.tsx` (~line 228), `src/app/dashboard/settings/account/page.tsx` (~line 245), `src/components/dashboard/settings/settings-inline-select.tsx` (wrapper ~line 33) — guess, all verified present
- **done when:**
  - [ ] All three controls use `advField("boxed", "sm")` (32px, 6px token) and no `h-[30px]` or `rounded-[6px]` literal remains on them; `team-settings-form.tsx` keeps `w-[190px]` and the account page keeps `w-[220px]`.
  - [ ] The account page's delete-confirmation input keeps `focus:border-[var(--danger)]` — a deliberate destructive-confirm affordance.
  - [ ] `settings-inline-select.tsx` keeps `focus-within:shadow-[var(--focus-ring-field)]` on the wrapper and the `absolute inset-0 … opacity-0` `<select>`, and that `<select>` does NOT gain `data-focus-ring="none"` — an invisible select has no second ring to suppress.
  - [ ] No other class string in the three files changes (the `border-0 border-b …` triggers at `team-settings-form.tsx` ~lines 175/190 are the underline family and out of scope here); `npm run lint` passes.
- **notes:** The inline-select wrapper is a `<div>` taking field classes — the helper's `placeholder:` utility is inert there and harmless. Screenshot `/dashboard/settings/team` and `/dashboard/settings/account`; tab to the delete-confirmation field and confirm the danger border still fires alongside the neutral ring.

## T6 · Move the schedule score cells to advField("boxed", "sm")
- **status:** later
- **model:** opus
- **needs:** T1, T2
- **files:** `src/components/dashboard/schedule/score-entry.tsx` (`ScoreCell` ~line 179, `TiebreakCell` ~line 199), `src/components/dashboard/schedule/single-score-entry.tsx` (~line 157) — guess, both verified present
- **done when:**
  - [ ] All three cell inputs use `advField("boxed", "sm")` (32px, 6px token) with `w-[26px] px-0 text-center tabular` retained — `px-0` must override the helper's `px-3` or a 26px cell leaves 2px for its digit — and no `h-[30px]`, `rounded-[6px]` or `#EAECF0` literal remains on them.
  - [ ] The existing per-cell text overrides (`text-[11px] text-[#525252]` on tiebreak cells; `text-[13px] text-[#0D0D0D]` plus placeholder colour on game cells) are preserved or consciously tokenised — none is silently dropped to the helper's 12px.
  - [ ] Guardrail (`docs/ui-revamp-guardrails.md` §4 items 2–3): in both files only `className` strings change — `value`, `onChange` (including the `replace(/[^0-9]/g, "")` sanitiser), `inputMode`, `maxLength`, `placeholder`, `aria-label`, the `small` branch, and every prop/handler in the parent that orders sets or carries tiebreak fields are byte-identical.
  - [ ] The `min-w-[160px] border-b …` name input at `score-entry.tsx:97` is untouched (underline family).
  - [ ] `npm run lint` passes and `pipeline-guardrails-reviewer` has been run on the diff and reports no finding.
- **notes:** Guardrail-gated — these are score inputs. Screenshot a schedule result row before/after; enter a score (including a tiebreak) and confirm it still records with the same values.

## T7 · Move the statistics match-selector selects to advField("boxed", "sm")
- **status:** later
- **model:** opus
- **needs:** T1, T2
- **files:** `src/components/dashboard/statistics/match-selector.tsx` (two `<select>`s ~lines 104 and 112) — guess, verified present
- **done when:**
  - [ ] Both `<select>`s use `advField("boxed", "sm")` and contain no `rounded-lg` (10px here, not the 8px the name implies), no `#F7F7F7` and no `#F3F3F3`.
  - [ ] Their border and background are tokens, not hex: border `var(--border-field)` via the helper; background is either the helper's `--surface-card` or the field-background token T1 settled on (`--bg-field` if T1 defined it, otherwise the substitute T1's header names).
  - [ ] The checkbox at ~line 141 (`w-3.5 h-3.5 rounded accent-[#3B82F6]`) is untouched (out of scope per the brief), as are the panel at ~line 93 (`rounded-[14px]`) and the pill trigger at ~line 67.
  - [ ] Only `className` strings change — `value`/`onChange` on both selects and the filter state they drive are byte-identical — and `npm run lint` passes.
- **notes:** Screenshot `/dashboard/statistics` with the selector open; change a filter and confirm the chart recomputes. The `focus-visible:border-[#E5E5E5]` on each select is a border-colour utility (not discarded by focus.css); dropping it is fine since the helper carries none, but say so in the run output.

## T8 · Bring FieldCell's cell geometry to the field token and border
- **status:** later
- **model:** opus
- **needs:** T1, T2
- **files:** `src/components/dashboard/matches/new-match-wizard/FieldCell.tsx` (`cellCls`/`cellRestCls`/`cellOpenCls` ~lines 29–31; text cell ~line 468) — guess, verified present. Renders in `DetailsContent.tsx`, `schedule/static/dual-build-step.tsx` and `schedule/static/static-tournament-builder.tsx`.
- **done when:**
  - [ ] `cellCls` reads `rounded-[var(--radius-button)]` instead of `rounded-[8px]`, and the `#F3F3F3` border literals in `cellRestCls`, `cellOpenCls` and the text cell's `focus-within:border-[#F3F3F3]` are `var(--border-field)`.
  - [ ] The popover option row (~line 150, `rounded-[8px] px-2.5 py-2`) and `floatMenuCls` are untouched — they are the dropdown surface, not the field.
  - [ ] No `data-focus-ring` attribute is added anywhere in `FieldCell.tsx`: the inner controls keep focus.css's neutral ring, and the cell's own `focus-within` change (transparent → hairline border on `#FAFAFA`) is too faint to stand as the one visible indicator focus.css's underline exception requires.
  - [ ] The children stay `bg-transparent … outline-none` with no border or radius of their own, and neither `dual-build-step.tsx` nor `static-tournament-builder.tsx` is modified — the cell owns the geometry for both builders.
  - [ ] `npm run lint` passes.
- **notes:** Widest blast radius per line changed in the feature. Screenshot all three surfaces before/after: the new-match wizard details step, `/dashboard/team/schedule/new/dual` and `/dashboard/team/schedule/new/tournament`. `--border-field` is `--ink-200` (#E5E5EA), so hover/open borders will read one step darker than today's `#F3F3F3` — that is the canonical border, not a regression.

## T9 · Move the matches-page float panels to the dropdown radius token
- **status:** later
- **model:** sonnet
- **needs:** T1
- **files:** `src/components/dashboard/matches/matches-page-content.tsx` (sort menu ~line 350, page-size menu ~line 955) — guess, verified present
- **done when:**
  - [ ] The two float panels (`absolute … min-w-[160px] rounded-xl border px-1.5 py-1.5` and `absolute … min-w-[56px] rounded-xl border px-1 py-1`) read `rounded-[var(--radius-dropdown)]` (12px) and no `rounded-xl` (14px here) remains in the file.
  - [ ] The 28px chrome search — the `h-7 … rounded-[var(--radius-element)]` label ~line 816 and its inner `<input>` with `data-focus-ring="none"` ~line 839 — is byte-identical; it is the design system's Header carve-out, not a field.
  - [ ] Every other `h-7` control and `rounded-[var(--radius-element)]` in the file is unchanged; the diff is exactly two class-string hunks.
  - [ ] `npm run lint` passes.
- **notes:** Screenshot the matches page with the sort menu and the filter panel open; confirm the search control is visually unchanged. The search is added to T15's allowlist rather than converted.

## T10 · Move the admin review-row inputs to advField("boxed", "sm")
- **status:** later
- **model:** sonnet
- **needs:** T1, T2
- **files:** `src/components/admin/review-rows.tsx` (two `<input>`s ~lines 163 and 181) — guess, verified present
- **done when:**
  - [ ] Both inputs' className is `advField("boxed", "sm")` plus `w-full`, and neither contains `--border-medium` (the dropdown/modal border) any more — the border is `var(--border-field)` via the helper.
  - [ ] The `h-8` and `rounded-[6px]` literals are gone (the helper supplies both); `focus:border-[var(--ink-900)]` may stay or go, and if it stays it is the only utility appended after `w-full`.
  - [ ] Only the two `className` strings change — `value`, `onChange`, `placeholder`, `aria-*`, and the `BTN`/card constants (which legitimately use `--border-medium`) are untouched.
  - [ ] `npm run lint` passes.
- **notes:** Smallest diff in the plan. Screenshot `/admin/claims` with a row expanded.

## T11 · Audit the underline family and record what differs from the 34px baseline
- **status:** later
- **model:** fable
- **needs:** T1
- **files:** read-only: `src/components/auth/form-field.tsx`, `src/components/dashboard/settings/settings-card.tsx`, `src/components/dashboard/settings/profile-form.tsx`, `src/components/dashboard/team/player-fields.tsx`, `src/components/dashboard/matches/match-actions/edit-match-dialog.tsx`, `src/components/dashboard/matches/new-match-wizard/DetailsContent.tsx`. Writes `work/input-fields-dropdowns-consistency/05_build/references/underline-audit.md` (new) — guess
- **done when:**
  - [ ] `work/input-fields-dropdowns-consistency/05_build/references/underline-audit.md` exists with one row per underline control in the six files — at least the 12 sites carrying `data-focus-ring="none"` today (form-field 1, settings-card 1, profile-form 1, player-fields 1, edit-match-dialog 6, DetailsContent 2) plus `settings-card.tsx`'s `UnderlineSelect` if it is a separate control — with columns: file:line, control, mechanism (`border-b` vs `h-[1px]` div), rest weight, focus weight, height (explicit class, or computed from text size + padding), text size, focus mechanism, `data-focus-ring` present.
  - [ ] Every row is marked `differs` (naming the property — height, text size or rule weight — and the baseline value it should take: 34px, 1px→2px, 13px) or `keep`, and every `keep` that is not at baseline states why (e.g. the 16–18px player-name and title inputs are display-size fields by design).
  - [ ] The file ends with three explicit lists — the rows T12 must change across the four non-guarded files, the rows T13 must change in `edit-match-dialog.tsx`, and the rows T14 must change in `DetailsContent.tsx` — each possibly empty, such that a downstream subagent can act without re-reading the six files.
  - [ ] Every `differs` row says the fix is a class-string change only and that its mechanism (`border-b` vs `h-[1px]` div) stays — rewriting the mechanism is out of scope.
  - [ ] `git diff --stat` shows only the new markdown file; nothing under `src/` changes.
- **notes:** The deliverable is the table, not code. Baseline is what `settings-card`, `profile-form` and `player-fields` already share: `h-[34px]`, `border-b` → `border-b-2 border-[var(--blue)]`, `text-[13px]`. The two guarded files are 855 and 940 lines — read them in ranges around the sites listed in `files:` rather than whole. `focus.css:48` and `SKILL.md:1109` cite `lineup-editor.tsx`/`field-row.tsx`, which do not exist; ignore those references.

## T12 · Align the non-guarded underline fields the audit flagged
- **status:** later
- **model:** opus
- **needs:** T11
- **files:** whichever of `src/components/auth/form-field.tsx`, `src/components/dashboard/settings/settings-card.tsx`, `src/components/dashboard/settings/profile-form.tsx`, `src/components/dashboard/team/player-fields.tsx` the audit's T12 list names — guess
- **done when:**
  - [ ] Every hunk in the diff corresponds to a row in `underline-audit.md`'s T12 list, changes only height / text-size / rule-weight classes to the values that row names, and no file outside that list is touched; if the list is empty the diff is empty and the task closes as done.
  - [ ] Each touched control keeps its existing mechanism — no `h-[1px]`/`h-[2px]` div is rewritten to `border-b`/`border-b-2` or vice versa.
  - [ ] Every existing `data-focus-ring="none"` (one per file today) and its comment remain, and none is added anywhere.
  - [ ] Each touched control still carries an on-focus rule change in its class strings (`group-focus-within:h-[2px]`, `focus:border-b-2`, or the `focused ? … border-b-2` branch) — the opt-out stays earned.
  - [ ] `npm run lint` passes.
- **notes:** Screenshot `/login`, `/dashboard/settings/profile` and `/dashboard/team/roster/[playerId]`; tab through each form and confirm every field still shows a visible focus change. `advField("underline")` from T1 may be used where it reproduces the row's target exactly, but is not required.

## T13 · Align the flagged underline geometry in edit-match-dialog.tsx
- **status:** later
- **model:** opus
- **needs:** T11
- **files:** `src/components/dashboard/matches/match-actions/edit-match-dialog.tsx` only (855 lines; six `data-focus-ring="none"` sites at ~392, 582, 590, 598, 616, 807) — guess, verified present
- **done when:**
  - [ ] Every hunk corresponds to a row in `underline-audit.md`'s T13 list and changes only height / text-size / rule-weight classes to the values that row names; if the list is empty the diff is empty and the task closes as done.
  - [ ] Guardrail (`docs/ui-revamp-guardrails.md` §4): only `className` strings change — no `value`, `defaultValue`, `name`, `onChange`, `onBlur`, `onKeyDown`, state hook, prop or JSX-ordering line moves; the `ScoreCell` usages and the `UnderlineField`/`PlayerRow` handlers are byte-identical.
  - [ ] All six `data-focus-ring="none"` attributes and their comments remain, none is added, and the `h-[1px]`→`h-[2px]` rule mechanism is not rewritten.
  - [ ] `pipeline-guardrails-reviewer` has been run on the diff and reports no finding.
  - [ ] `npm run lint` passes and `DetailsContent.tsx` is not touched in this task.
- **notes:** Guardrail-gated, one file per run by design — do not open `DetailsContent.tsx` here (that is T14). Read the file in ranges around the audit's rows, not whole. Screenshot the edit-match dialog before/after and tab through it.

## T14 · Align the flagged underline geometry in DetailsContent.tsx
- **status:** later
- **model:** opus
- **needs:** T11
- **files:** `src/components/dashboard/matches/new-match-wizard/DetailsContent.tsx` only (940 lines; two `data-focus-ring="none"` sites at ~580 and ~678, the player-name inputs) — guess, verified present
- **done when:**
  - [ ] Every hunk corresponds to a row in `underline-audit.md`'s T14 list and changes only height / text-size / rule-weight classes to the values that row names; if the list is empty the diff is empty and the task closes as done.
  - [ ] Guardrail (`docs/ui-revamp-guardrails.md` §3.1 and §4): only `className` strings change — the end-at-video-start control (`initialTopPlayerIsPlayer1`), set-score ordering, tiebreak game counts, `fixedCamera`, `adScoring`, and every `value`/`name`/`defaultValue`/handler/state line are byte-identical.
  - [ ] Both `data-focus-ring="none"` attributes and their comments remain, none is added, and the `h-[1px]`→`h-[2px]` rule mechanism is not rewritten; the boxed `editorInputCls` at ~line 163 is untouched.
  - [ ] `pipeline-guardrails-reviewer` has been run on the diff and reports no finding.
  - [ ] `npm run lint` passes and `edit-match-dialog.tsx` is not touched in this task.
- **notes:** Guardrail-gated, one file per run by design — do not open `edit-match-dialog.tsx` here (that is T13). Read in ranges around the audit's rows. Open the wizard, fill a match and confirm the submitted payload is unchanged; screenshot the details step before/after.

## T15 · Add the field-geometry invariant test
- **status:** later
- **model:** opus
- **needs:** T2, T3, T4, T5, T6, T7, T8, T9, T10
- **files:** `tests/field-geometry.spec.ts` (new; pattern: `tests/generate-map.spec.ts`) — guess
- **done when:**
  - [ ] `tests/field-geometry.spec.ts` exists, reads files with `node:fs` and uses no `page`/browser fixture; it walks every `.tsx` under `src/app` and `src/components` and inspects the opening tag of `<input>`, `<select>`, `<textarea>`, `<SelectTrigger>` and `<SelectPrimitive.Trigger>` (the last so `ui/select.tsx`'s own definition is covered).
  - [ ] The tag-extent parser tolerates multi-line tags and `=>` inside attribute values — a naive `[^>]*` stops at the arrow in `onChange={(e) => …}` and finds 2 sites in today's tree where the correct scan finds 5 — and it flags `rounded-md`, `rounded-lg`, `rounded-xl` and bare `rounded` in those tags' class strings, but not `rounded-[…]` arbitrary values or `rounded-full`.
  - [ ] The failure message names `file:line`, the tag and the offending class, and states that `rounded-md`/`lg`/`xl` resolve to 8/10/14px in this codebase rather than the design system's values, so field radius must be written as `rounded-[var(--radius-button)]`.
  - [ ] An allowlist with a comment per entry, keyed by file path plus a stable class fragment (not a bare line number): the `accent-[#3B82F6]` checkbox in `statistics/match-selector.tsx` (out of scope per the brief) and the matches-page 28px chrome search in `matches/matches-page-content.tsx` (Header carve-out) — nothing else.
  - [ ] The spec includes a self-check that feeds the scanner an inline fixture containing `<input className="rounded-md">` and asserts it is flagged, so an empty match set can never pass silently; `npm test` passes on the converted tree.
- **notes:** Must run last among the boxed tasks — before T2 and T7 land it fails on `ui/input.tsx`, `ui/textarea.tsx` and `match-selector.tsx`. Do not add a snapshot harness — visual regression stays a screenshot review.
