# Run log — claude/full-name-title-case-2efeba

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add `titleCaseName` and its offline spec — done
- **gate:** mechanical — `npm run lint` clean, `npx tsc --noEmit` clean, `npm test` 313 passed. Completion review — `VERDICT: pass`, every criterion traced to code and to a specific spec assertion rather than to the implementer's report. Guardrails — `rls-boundary-reviewer` ran (the diff touches `src/lib/data/`) and explicitly found nothing: the file has zero imports, adds no query, no service-role client, no migration, and `titleCaseName` is imported nowhere but its own spec. `pipeline-guardrails-reviewer` skipped — the diff touches no file under `src/app/dashboard/`, `src/components/dashboard/`, or the upload wizard.
- **changed:** `src/lib/data/person-name.ts` gains `titleCaseName`, purely additive beside `normalizedPersonName`, with five private helpers and two regexes. Rules R1 → R1b → R2/R2a, first match wins. New `tests/person-name-display.spec.ts` — 12 offline tests in 3 describes, each closing on must-not-change assertions per the house convention. Nothing calls the function yet, so no screen changes.
- **follow-ups:** 1. Nothing calls `titleCaseName` outside the claim/join flow this branch is wiring; the roster list and the merge dialog's confirm-name display are the natural next callers, but that is visible UI and wants its own task with the guardrails doc read first. 2. If a caller ever stores a title-cased value rather than only rendering it, the `\s+`-vs-`btrim` whitespace divergence documented in the doc comment becomes a live matching bug — worth a guard or a "display only" naming convention at that point. 3. The implementer flagged that R1b uppercases a given name spelled only from `i`/`v`/`x` (`Vivi` → `VIVI`); documented as known-and-accepted rather than patched.

## T2 · Wire the two choke points and correct the TypeScript-side stale comments — blocked
- **gate:** mechanical — `npm run lint` clean, `npx tsc --noEmit` clean, `npm test` 313 passed, `npm run build` succeeded. Completion review — **`VERDICT: needs-work`**, criterion 4 not met. Guardrails — `rls-boundary-reviewer` ran (the diff touches `src/lib/data/` and `src/app/api/`) and explicitly found nothing: `redactForPlayer` is unchanged and structurally cannot forward `ownerDisplay`, the `?intent=join` branch changed by a comment only, and title-casing re-cases a string its recipient already received rather than widening what any client gets. `pipeline-guardrails-reviewer` skipped — no file under `src/app/dashboard/`, `src/components/dashboard/`, or the upload wizard.
- **why blocked:** Criteria 4 and 5 of this task cannot both be satisfied, and the defect is in the task, not the diff. Criterion 4 requires `grep -rn "First L\.\|D\. Wu\|Elena V\." src/` to return nothing; the one remaining hit is `src/components/claim/contact-owner-form.tsx:45` — `/** "Elena V." — named so the confirmation can say who was told. */` — and criterion 5 requires every file under `src/components/claim/` to be untouched. The task author (stage 04) listed only `programs-server.ts:7-8` and `:29` as what criterion 4 "covers", so the third occurrence was not known when the list was written. The implementer honoured criterion 5, left the hit, and reported the conflict rather than quietly widening scope — the correct call. Criteria 1, 2, 3 and 5 were each verified met.
- **to unblock:** a person amends one line of the task, then resets `status:` to `todo`. Either narrow criterion 4 to the two `programs-server.ts` occurrences it names, and let `contact-owner-form.tsx` be its own task; or add that one file to `files:` and drop it from criterion 5's untouched list. The stashed work is correct under either choice and needs no rewriting.
- **stash:** 57a5bd90bb72302d58bac06cbce302ee6b55b6dc — recover with `git stash apply 57a5bd9`, not `pop` (the stash stack is shared across worktrees).
- **changed:** nothing landed in history. The stash holds two behavioural lines — `toResult` title-casing `ownerDisplay` with `|| null` intact, and `displayName` title-casing the composed name and still returning `null` when both parts are blank — plus three doc-comment corrections in `programs-server.ts` (header and the `ownerDisplay` doc) and `route.ts`.
