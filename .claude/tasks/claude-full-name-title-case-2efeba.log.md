# Run log — claude/full-name-title-case-2efeba

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add `titleCaseName` and its offline spec — done
- **gate:** mechanical — `npm run lint` clean, `npx tsc --noEmit` clean, `npm test` 313 passed. Completion review — `VERDICT: pass`, every criterion traced to code and to a specific spec assertion rather than to the implementer's report. Guardrails — `rls-boundary-reviewer` ran (the diff touches `src/lib/data/`) and explicitly found nothing: the file has zero imports, adds no query, no service-role client, no migration, and `titleCaseName` is imported nowhere but its own spec. `pipeline-guardrails-reviewer` skipped — the diff touches no file under `src/app/dashboard/`, `src/components/dashboard/`, or the upload wizard.
- **changed:** `src/lib/data/person-name.ts` gains `titleCaseName`, purely additive beside `normalizedPersonName`, with five private helpers and two regexes. Rules R1 → R1b → R2/R2a, first match wins. New `tests/person-name-display.spec.ts` — 12 offline tests in 3 describes, each closing on must-not-change assertions per the house convention. Nothing calls the function yet, so no screen changes.
- **follow-ups:** 1. Nothing calls `titleCaseName` outside the claim/join flow this branch is wiring; the roster list and the merge dialog's confirm-name display are the natural next callers, but that is visible UI and wants its own task with the guardrails doc read first. 2. If a caller ever stores a title-cased value rather than only rendering it, the `\s+`-vs-`btrim` whitespace divergence documented in the doc comment becomes a live matching bug — worth a guard or a "display only" naming convention at that point. 3. The implementer flagged that R1b uppercases a given name spelled only from `i`/`v`/`x` (`Vivi` → `VIVI`); documented as known-and-accepted rather than patched.
