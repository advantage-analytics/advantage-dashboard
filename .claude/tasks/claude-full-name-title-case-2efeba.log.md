# Run log — claude/full-name-title-case-2efeba

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add `titleCaseName` and its offline spec — done
- **gate:** mechanical — `npm run lint` clean, `npx tsc --noEmit` clean, `npm test` 313 passed. Completion review — `VERDICT: pass`, every criterion traced to code and to a specific spec assertion rather than to the implementer's report. Guardrails — `rls-boundary-reviewer` ran (the diff touches `src/lib/data/`) and explicitly found nothing: the file has zero imports, adds no query, no service-role client, no migration, and `titleCaseName` is imported nowhere but its own spec. `pipeline-guardrails-reviewer` skipped — the diff touches no file under `src/app/dashboard/`, `src/components/dashboard/`, or the upload wizard.
- **changed:** `src/lib/data/person-name.ts` gains `titleCaseName`, purely additive beside `normalizedPersonName`, with five private helpers and two regexes. Rules R1 → R1b → R2/R2a, first match wins. New `tests/person-name-display.spec.ts` — 12 offline tests in 3 describes, each closing on must-not-change assertions per the house convention. Nothing calls the function yet, so no screen changes.
- **follow-ups:** 1. Nothing calls `titleCaseName` outside the claim/join flow this branch is wiring; the roster list and the merge dialog's confirm-name display are the natural next callers, but that is visible UI and wants its own task with the guardrails doc read first. 2. If a caller ever stores a title-cased value rather than only rendering it, the `\s+`-vs-`btrim` whitespace divergence documented in the doc comment becomes a live matching bug — worth a guard or a "display only" naming convention at that point. 3. The implementer flagged that R1b uppercases a given name spelled only from `i`/`v`/`x` (`Vivi` → `VIVI`); documented as known-and-accepted rather than patched.

## T2 · Wire the two choke points and correct the TypeScript-side stale comments — blocked
- **gate:** mechanical — `npm run lint` clean, `npx tsc --noEmit` clean, `npm test` 313 passed, `npm run build` succeeded. Completion review — **`VERDICT: needs-work`**, criterion 4 not met. Guardrails — `rls-boundary-reviewer` ran (the diff touches `src/lib/data/` and `src/app/api/`) and explicitly found nothing: `redactForPlayer` is unchanged and structurally cannot forward `ownerDisplay`, the `?intent=join` branch changed by a comment only, and title-casing re-cases a string its recipient already received rather than widening what any client gets. `pipeline-guardrails-reviewer` skipped — no file under `src/app/dashboard/`, `src/components/dashboard/`, or the upload wizard.
- **why blocked:** Criteria 4 and 5 of this task cannot both be satisfied, and the defect is in the task, not the diff. Criterion 4 requires `grep -rn "First L\.\|D\. Wu\|Elena V\." src/` to return nothing; the one remaining hit is `src/components/claim/contact-owner-form.tsx:45` — `/** "Elena V." — named so the confirmation can say who was told. */` — and criterion 5 requires every file under `src/components/claim/` to be untouched. The task author (stage 04) listed only `programs-server.ts:7-8` and `:29` as what criterion 4 "covers", so the third occurrence was not known when the list was written. The implementer honoured criterion 5, left the hit, and reported the conflict rather than quietly widening scope — the correct call. Criteria 1, 2, 3 and 5 were each verified met.
- **to unblock:** a person amends one line of the task, then resets `status:` to `todo`. Either narrow criterion 4 to the two `programs-server.ts` occurrences it names, and let `contact-owner-form.tsx` be its own task; or add that one file to `files:` and drop it from criterion 5's untouched list. The stashed work is correct under either choice and needs no rewriting.
- **stash:** 57a5bd90bb72302d58bac06cbce302ee6b55b6dc — recover with `git stash apply 57a5bd9`, not `pop` (the stash stack is shared across worktrees).
- **changed:** nothing landed in history. The stash holds two behavioural lines — `toResult` title-casing `ownerDisplay` with `|| null` intact, and `displayName` title-casing the composed name and still returning `null` when both parts are blank — plus three doc-comment corrections in `programs-server.ts` (header and the `ownerDisplay` doc) and `route.ts`.

## T3 · Migration: both definer functions return the full name, with corrected comments — done
- **gate:** mechanical — `npm run lint` clean, `npx tsc --noEmit` clean, `npm test` 313 passed, no TypeScript file changed. Completion review — `VERDICT: pass`; it diffed both function bodies against the migrations that last defined them and confirmed only the owner expression moved. Guardrails — `rls-boundary-reviewer` ran (the diff touches `supabase/migrations/`) and found nothing beyond the approved intent: `security definer` / `stable` / `search_path` pin all retained, grants no wider than before, no column added to either return, and `search_programs` keeps its `org_type = 'college'` filter in both query branches. `pipeline-guardrails-reviewer` skipped — no dashboard or upload-wizard file in the diff.
- **caller verification:** the runner's subagent returned a classifier security warning, so the live database was checked directly rather than taken on report. Confirmed against live after the apply: both functions `prosecdef = true`, `provolatile = 's'`, `proconfig = search_path=""`; signatures and `owner_display text` unchanged; the full-name expression present in both and neither `left(u.first_name, 1)` nor `left(u.last_name, 1)` remaining; the `case when u.id is null then null` wrapper intact; `search_programs` retaining both `org_type` references; grants `anon`/`authenticated`/`postgres`/`service_role` with no PUBLIC. The file itself holds exactly eight statements — two `create or replace`, two `revoke`, two `grant`, two `comment` — and no data or drop statement.
- **changed:** new `supabase/migrations/20260902091500_full_owner_name.sql`. Owner expression in both functions becomes `btrim(coalesce(u.first_name, '') || ' ' || coalesce(u.last_name, ''))` — `program_public_status` was `first_name || ' ' || left(last_name, 1) || '.'`, `search_programs` was `left(first_name, 1) || '. ' || last_name`. Both `comment on function` texts now record the decision: the full name is returned, still never an address, the page is reachable by anonymous visitors, and the surname is deliberate — not to be abbreviated back. Applied live; ZZ Test Program now reads `Clajerson Gimena` through both functions, where the dropdown previously read `C. Gimena`.
- **follow-ups:** 1. The file is named `20260902091500` but was recorded live as version `20260902161134` — `apply_migration` assigned its own timestamp. Harmless here (this repo does not use the folder as the sync mechanism), but local `supabase migration list` will read the file as unapplied and could try to reapply it. 2. Live also carries `20260902153430 claim_roles_vocabulary` and `20260902161208 claim_roles_drop_operations`, which exist in neither this worktree nor the main checkout — almost certainly a concurrent session in another worktree, noted only so a later reader is not surprised by them. 3. `20260817074759`'s header still describes screen F3.3 as reading "Elena V. manages Advantage here"; if the design spec still shows the abbreviation it is now out of step with the product decision.

## T2 · Wire the two choke points and correct the TypeScript-side stale comments — done (second attempt)
- **gate:** mechanical — `npm run lint` clean, `npx tsc --noEmit` clean, `npm test` 313 passed, `npm run build` succeeded. Completion review — `VERDICT: pass` against the amended criterion 4, every criterion re-checked fresh rather than carried over from the first attempt, including running the scoped grep itself. Guardrails — `rls-boundary-reviewer` ran (the diff touches `src/lib/data/` and `src/app/api/`) and found nothing: `redactForPlayer` unchanged and still omitting `ownerDisplay` entirely, the `?intent=join` branch changed by comment only, and it traced every consumer of `toResult()` and `displayName()` to confirm the reach is exactly the approved spillover list and no wider. `pipeline-guardrails-reviewer` skipped — no dashboard or upload-wizard file in the diff.
- **deviation from the runner's procedure, deliberate:** no subagent was dispatched for this attempt. The first attempt's work was stashed on the failed gate, and the human restored it verbatim after amending criterion 4, so the code already existed and had already been reviewed against criteria 1, 2, 3 and 5. Re-dispatching would have regenerated reviewed code and risked different comment wording for no gain. The gate was run in full on the restored diff instead.
- **why the first attempt blocked, and what changed:** criteria 4 and 5 could not both hold — the repo-wide grep's last hit was `src/components/claim/contact-owner-form.tsx:45`, in a directory criterion 5 required untouched. The human scoped criterion 4 to `src/lib/data/programs-server.ts` (commit `ca4c62d`), which is the file this task actually owns. The `"Elena V."` comment in `contact-owner-form.tsx` is still there and is now nobody's task — see follow-ups.
- **changed:** two behavioural lines — `toResult` title-casing `ownerDisplay` with `|| null` intact so a blank stays `null`, and `displayName` title-casing the composed name and still returning `null` when both parts are blank. Three doc-comment corrections: the `programs-server.ts` file header and its `ownerDisplay` doc, and the `route.ts` header sentence, each of which stated the old abbreviation as a promise. The `?intent=join` paragraph below it is byte-for-byte unchanged.
- **follow-ups:** 1. `src/components/claim/contact-owner-form.tsx:45` still reads `/** "Elena V." — named so the confirmation can say who was told. */`, which now describes behaviour the product no longer has. It was deliberately excluded from this task's scope and belongs in its own task under branch-scope discipline. 2. The stash that held this work (`57a5bd9`) was dropped once the work landed here.
