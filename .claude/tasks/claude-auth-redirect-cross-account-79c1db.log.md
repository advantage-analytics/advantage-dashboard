# Run log — claude/auth-redirect-cross-account-79c1db

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Honor a clamped `next` on both login paths — done
- **gate:** mechanical pass (lint, tsc, test, build all exit 0) · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (no files under src/app/dashboard/, src/components/dashboard/ or the upload wizard) · rls-boundary-reviewer skipped (no files under src/lib/supabase/, src/lib/data/, src/app/api/ or supabase/migrations/). The task subagent (opus) was interrupted after its edits; the gate and commit were run in-session at the author's request.
- **changed:** `src/app/(auth)/login/page.tsx` reads `?next=` once, clamps it with `safeNext`, and renders `LoginForm` inside a `<Suspense>` boundary. `src/components/auth/login-form.tsx` takes a `next` prop, re-clamps it, and uses the result for both `router.push` after password sign-in and the Google `redirectTo` (`/callback?next=<encoded>`), defaulting to `/dashboard`. New `tests/safe-next.spec.ts` pins the five refusal cases and one preserved invite path. Still to confirm by hand: the Supabase redirect allow-list accepts `/callback` with an arbitrary `next` value.

## T2 · Route join sign-in and wrong-account through `/login?next=` — done
- **gate:** mechanical pass (lint, tsc, test exit 0; 250 specs) · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (no files under src/app/dashboard/, src/components/dashboard/ or the upload wizard) · rls-boundary-reviewer skipped (no files under src/lib/supabase/, src/lib/data/, src/app/api/ or supabase/migrations/).
- **changed:** `src/app/join/[token]/page.tsx` redirects the `sign_in` state to `/login?next=<encoded /join/token>` for both the plain and `?not-now=1` requests and no longer imports `JoinSignIn`; `ready` and `sign_up` keep their declined → `NothingSent` branches. `src/components/join/join-forms.tsx` deletes `JoinSignIn`, rewrites `JoinWrongAccount`'s copy ("Sign out, sign in as {invitedEmail}, and you'll come straight back here." / button "Sign out and sign in"), and adds one "Sign in with Google instead" link to `JoinSignUp` after `NotNowLink`. `src/lib/services/programs/join-actions.ts` deletes `signInAndAccept`, rewrites its header for the login redirect, and makes `signOutForInvite` redirect to `/login?next=` after signing out. `src/components/join/join-terms.tsx` corrects the stale remark about which form carries the extra link (permitted by the task notes). No email is ever passed to `/login`.
- **follow-ups:** 1. `resolveJoinState`'s `sign_in` variant still carries `role`, `programName`, `inviterName` and `email` that nothing renders now — see whether that payload can shrink after T3. 2. `/login` has no notion of why someone was sent there; a future task could render an eyebrow when `next` points at `/join/`, without passing the email. 3. `src/app/claim/verify/failed/page.tsx` still says "Sign in … and open the link again" in two places — the same round trip this task removed; its own branch per the branch-scope rule.
