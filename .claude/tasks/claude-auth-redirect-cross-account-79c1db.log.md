# Run log — claude/auth-redirect-cross-account-79c1db

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Honor a clamped `next` on both login paths — done
- **gate:** mechanical pass (lint, tsc, test, build all exit 0) · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (no files under src/app/dashboard/, src/components/dashboard/ or the upload wizard) · rls-boundary-reviewer skipped (no files under src/lib/supabase/, src/lib/data/, src/app/api/ or supabase/migrations/). The task subagent (opus) was interrupted after its edits; the gate and commit were run in-session at the author's request.
- **changed:** `src/app/(auth)/login/page.tsx` reads `?next=` once, clamps it with `safeNext`, and renders `LoginForm` inside a `<Suspense>` boundary. `src/components/auth/login-form.tsx` takes a `next` prop, re-clamps it, and uses the result for both `router.push` after password sign-in and the Google `redirectTo` (`/callback?next=<encoded>`), defaulting to `/dashboard`. New `tests/safe-next.spec.ts` pins the five refusal cases and one preserved invite path. Still to confirm by hand: the Supabase redirect allow-list accepts `/callback` with an arbitrary `next` value.

## T2 · Route join sign-in and wrong-account through `/login?next=` — done
- **gate:** mechanical pass (lint, tsc, test exit 0; 250 specs) · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (no files under src/app/dashboard/, src/components/dashboard/ or the upload wizard) · rls-boundary-reviewer skipped (no files under src/lib/supabase/, src/lib/data/, src/app/api/ or supabase/migrations/).
- **changed:** `src/app/join/[token]/page.tsx` redirects the `sign_in` state to `/login?next=<encoded /join/token>` for both the plain and `?not-now=1` requests and no longer imports `JoinSignIn`; `ready` and `sign_up` keep their declined → `NothingSent` branches. `src/components/join/join-forms.tsx` deletes `JoinSignIn`, rewrites `JoinWrongAccount`'s copy ("Sign out, sign in as {invitedEmail}, and you'll come straight back here." / button "Sign out and sign in"), and adds one "Sign in with Google instead" link to `JoinSignUp` after `NotNowLink`. `src/lib/services/programs/join-actions.ts` deletes `signInAndAccept`, rewrites its header for the login redirect, and makes `signOutForInvite` redirect to `/login?next=` after signing out. `src/components/join/join-terms.tsx` corrects the stale remark about which form carries the extra link (permitted by the task notes). No email is ever passed to `/login`.
- **follow-ups:** 1. `resolveJoinState`'s `sign_in` variant still carries `role`, `programName`, `inviterName` and `email` that nothing renders now — see whether that payload can shrink after T3. 2. `/login` has no notion of why someone was sent there; a future task could render an eyebrow when `next` points at `/join/`, without passing the email. 3. `src/app/claim/verify/failed/page.tsx` still says "Sign in … and open the link again" in two places — the same round trip this task removed; its own branch per the branch-scope rule.

## T3 · Stamp `onboarded_at` in `acceptInvite` — done
- **gate:** mechanical pass (lint, tsc, test exit 0) · task-completion-reviewer `VERDICT: pass` · pipeline-guardrails-reviewer skipped (no files under src/app/dashboard/, src/components/dashboard/ or the upload wizard) · rls-boundary-reviewer skipped (no files under src/lib/supabase/, src/lib/data/, src/app/api/ or supabase/migrations/).
- **changed:** `acceptInvite` in `src/lib/services/programs/join-actions.ts` now creates one server client, passes it into `acceptWithSession`, and after a confirmed accept reads the session's user id and stamps `users.onboarded_at` where null via the admin client — same shape and same error message as the existing stamp in `createAccountAndAccept`. A stamp failure is logged and does not alter the result or the redirect. Doc comment added explaining why the stamp lives here. One hunk, confined to `acceptInvite`.

## T4 · Add the pending-invite read and accept-by-id migration — done
- **gate:** mechanical pass (lint, tsc, test exit 0) · task-completion-reviewer `VERDICT: pass` — criterion 4's "no new advisor finding naming either function" judged under the reading that excludes the `authenticated_security_definer_function_executable` lint, which criterion 1's own `security definer` + `grant execute to authenticated` mandate produces and which every exposed RPC in the project carries (the literal reading would fail; recorded so the call is visible) · rls-boundary-reviewer ran and found nothing to flag (checked cross-account read and accept, disclosure through return values and error paths, `is distinct from` null handling, search-path under `security definer`, the delegation re-running the token function's own checks, and grant/policy scope) · pipeline-guardrails-reviewer skipped (no dashboard or wizard files).
- **changed:** new `supabase/migrations/20260902032248_pending_invites.sql`, applied to the live project (recorded as `20260902032650 pending_invites`). Adds `public.pending_program_invites()` (the invitee's only window onto `program_invites`: their own confirmed address, unaccepted, unexpired, not already a member; never `token_hash` or the email) and `public.accept_pending_invite(p_invite_id)` (refuses `not_found`, `unconfirmed`, `wrong_address` with a null `program_id`, then delegates to `accept_program_invite` with the row's own hash, writing nothing itself), both `security definer` with an empty search path, executable by `authenticated` only; plus a partial index on `lower(email)` for open rows. No policy or table-grant changes. Live smoke: anon refused with `42501`; a random id as an authenticated user returns `not_found`; an open invite for another address returns `wrong_address` with no writes.
- **follow-ups:** 1. `list_migrations` shows two live migrations from 2026-09-01 (`account_deletion_retains_program_data`, `program_audit_log_allow_account_deleted`) that this worktree's `supabase/migrations/` lacks; the integration branch should end up with those files. 2. `anon` still holds Supabase-default table privileges on `program_invites` (closed by RLS); a hardening pass like `20260818041110_revoke_public_grants_on_server_only_tables` could revoke them. 3. The RLS reviewer notes that an unconfirmed caller supplying a real invite id learns the row exists (gets `unconfirmed`, not `not_found`); consistent with the migration's stated threat model that ids are not secrets, but the header's "nothing about the row is disclosed" wording is about content, not existence.

## T5 · Server plumbing: pending-invite loader, outcome mapping, accept action — done
- **gate:** mechanical pass (lint, tsc, test exit 0) · task-completion-reviewer `VERDICT: pass` (every mapped column checked against T4's function signature; `acceptWithSession` body untouched; the new action takes one argument and derives program and role server-side) · rls-boundary-reviewer ran and found nothing (traced every importer of the three files for a service-role escape — the only client import is type-only; admin writes keyed on the session user's own id; crafted or foreign invite ids yield only the database function's statuses; logs carry `{ message }` only) · pipeline-guardrails-reviewer skipped (no dashboard or wizard files). The task subagent (fable) ended without delivering its final report after launching its own reviewer; the gate above is the runner's.
- **changed:** new `src/lib/data/pending-invites-server.ts` with `PendingInvite` and `getPendingInvites()` (session-client RPC to `pending_program_invites`, names via `programDisplayName` and `displayName`, `[]` on error). `invite-acceptance.ts` exports `displayName`, adds `unconfirmed` to `AcceptOutcome`, and adds `acceptPendingWithSession()` mirroring `acceptWithSession()`. `join-actions.ts` adds the `unconfirmed` sentence to `describe()` and `acceptPendingInvite(inviteId)`: UUID guard before any database call, RPC through the session, then admin-client stamps of `onboarded_at` and of `role` (read back from the membership row the function wrote, mapped through a guarded const record, only where null), `activate()`, redirect to the team page. No hunk inside any existing action.
- **follow-ups:** 1. Neither `getPendingInvites` nor `acceptPendingInvite` has a caller yet; when T7–T10 wire them, confirm the client component imports the action only as a server-action reference (as `join-forms.tsx` does for `acceptInvite`), never through a module path that could pull server code into a bundle.

## T6 · Live-database spec for the pending-invite functions — done
- **gate:** mechanical pass (lint, tsc, test exit 0; 259 specs, the nine new ones all passed, none skipped) · task-completion-reviewer `VERDICT: pass` (first attempt lost to a network error mid-review, re-run to a full verdict) · pipeline-guardrails-reviewer and rls-boundary-reviewer skipped (only `tests/` touched). The runner confirmed on the live database that no `pend-inv` marker rows remain in `programs`, `program_invites`, `auth.users` or `public.users` after the run.
- **changed:** new `tests/pending-invites.spec.ts`, modelled on the join-request staff-read spec: under a per-run marker it builds three confirmed logins, four programs with generous seats, an owner seat and a pre-existing membership for the invitee, and three invitations for the invitee's address on separate programs (the open-invite unique index forbids two unaccepted rows sharing a program), the live one stored upper-cased to exercise the `lower()` comparison. Nine tests in the task's order: the invitee's read returns exactly the live row with the ten columns and no `token_hash`; a stranger reads nothing; anon is refused with `42501`; the stranger's accept returns `wrong_address` with no seat; the invitee's accept returns `ok` and lands the seat, the stamps and the `invite.accepted` audit row; a second accept returns `already_used`; the read is then empty; the expired and already-member ids were never offered; and a real token hashed the mailer's way still accepts through the token function. `afterAll` deletes audit, invites, players, members, programs and the auth users.
- **follow-ups:** 1. No automated coverage for `accept_program_invite`'s `no_seats`, `player_gone` or `already_claimed` statuses; a sibling live spec could fill a program and archive a targeted profile. 2. The unconfirmed branch stays hand-checked; the only automatable route is a forged-claims SQL session, not a real login. 3. PostgREST's mixed-shape bulk insert sending `null` rather than a column default has now bitten two specs; a line in `tests/fixtures/live-db.ts`'s header would spare the third.
