# Run log — claude/roster-new-player-form-abafe7

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Add neutral icons to the roster drawer's Options menu — done

- **gate:** mechanical pass (lint/typecheck/tests), completion review pass
- **changed:** In `src/components/dashboard/team/player-drawer.tsx`'s `MemberMenu`, added leading Lucide glyphs (`Upload`, `Pencil`, `Trash2`) to the three Options-popover rows, each `size-[13px]` `strokeWidth={1.5}` `aria-hidden` `text-[var(--ink-400)]` in a shared `shrink-0` column with `gap-2.5` to the text. No gating/behaviour logic touched.

## T2 · Drop the event/school suffix from the drawer's recent-match rows — done

- **gate:** mechanical pass (lint/typecheck/tests), completion review pass
- **changed:** Removed the ` · {match.event}` suffix from the drawer's recent-match row in `player-drawer.tsx`; removed `RosterRecentMatch.event`, `DbMatchRow.tournament_name`, the select column, and the map assignment in `team-roster-server.ts`. `tests/team-roster-ids.spec.ts`'s fixture at ~163 builds an unrelated `DbRecentMatch` type from `team-home-server.ts` and needed no change — verified independently, not just taken on the subagent's word.

## T3 · Never send a localhost link from a deployed build — done

- **gate:** mechanical pass (lint/typecheck/tests), completion review pass
- **changed:** `siteUrl()` in `src/lib/site-url.ts` now falls back through `VERCEL_ENV=production → VERCEL_PROJECT_PRODUCTION_URL` and `VERCEL_ENV=preview → VERCEL_URL` before ever reaching `http://localhost:3000`, and warns once via `console.warn` when the localhost fallback fires under `NODE_ENV=production`. `layout.tsx`'s `metadataBase` and the checkout route's redirect base now call `siteUrl()` instead of holding their own private fallback. Added `tests/site-url.spec.ts` (6 tests) pinning the behaviour. Note for the author: `NEXT_PUBLIC_SITE_URL` still needs to be set on Vercel (Preview and Production) — this task only stops the worst case when it's missing.

## T4 · Email the program owner when someone requests to join — done

- **gate:** mechanical pass (lint/typecheck/tests), completion review pass
- **changed:** Added `joinRequestOwnerNoticeEmail` template (`invite-request.ts`) and a new `getProgramOwner(programId)` helper (`src/lib/services/programs/program-owner.ts`, admin-client lookup on `program_members.role = 'owner'`, returns `null` if none) exported for T5. `fileRequest()` in `claim-actions.ts` now reports whether it created a genuinely new row; `requestInvite()` sends the owner notice inside `after()` (from `next/server`) only on that `created: true` branch, so a resubmit never double-mails the owner and the caller's return value/timing is unaffected either way. `index.ts`'s trigger table and qualification paragraph updated to describe both emails. A failed send only `console.warn`s, never fails the request.
- **follow-ups:** Owner-only recipient was a deliberate scope call (per the author's original wording) — the template's header comment flags that coaches/staff could be added as a widen-the-recipient-list follow-up if that decision changes.

## T5 · Email the program owner when an invitee accepts and joins — done

- **gate:** mechanical pass (lint/typecheck/tests), completion review pass
- **changed:** Added `memberJoinedOwnerEmail` template (`invite-request.ts`) and `notifyOwnerOfJoin()` in `join-actions.ts`, which reuses T4's `getProgramOwner` (no second lookup), skips the owner's own acceptance, and logs-but-doesn't-fail on a bad send. All three accept paths (`acceptInvite`, `acceptPendingInvite`, `createAccountAndAccept`) call it once, only after a successful accept, and before `finishJoin()`'s throwing `redirect()`. `adoptMembership` now returns the joined role read back from the `program_members` row (never from an argument) so the notice can name it. `index.ts` exports the template and gains a "Member joined" trigger-table row.
- **follow-ups:** No confirmation email to the joiner themselves, and no notice to the specific coach/staff member who sent the invite (only the owner) — both would extend `notifyOwnerOfJoin`'s recipient list if wanted later.

## T6 · Migration: `restore_program_player` + `program_former_players` — done

- **gate:** mechanical pass (lint/typecheck/tests), completion review pass
- **changed:** New `supabase/migrations/20260913031528_restore_program_player.sql`, **applied live** via the Supabase MCP and verified (`pg_proc`, `routine_privileges`, constraint def). `restore_program_player(p_player_id)` mirrors `archive_program_player`: staff-only, silent on a non-archived/merged/unknown id, `23505` on a live-row email collision (excluding itself), and for a claimed profile runs the seat check (`22023` "no seats left — free one in Settings › Teams first") _before_ clearing `archived_at`, then re-inserts the `program_members` seat on-conflict-do-nothing; every restore writes a `player.restored` audit row. `program_former_players(p_program_id)` is a `user_program_ids()`-gated SECURITY DEFINER read returning archived, unmerged profiles with a program-scoped `match_count`. The audit-log allowlist was re-created from the **live** list — which already carried `event.deleted`, absent from every file in the folder — plus `player.restored`. Rolled-back behavioural probes on ZZ Test Program (13 archived rows) exercised every branch; nothing leaked.
- **follow-ups:** 1. `program_former_players.match_count` counts `player1_id or player2_id` scoped to the program; if T7/T8 ever show it beside the drawer's recent-matches list, confirm the two count the same way (the drawer may read via `visible_match_ids()`). 2. ZZ Test Program's 13 archived rows (some 0-match, ≥1 claimed) are a ready fixture for the plan's live verification of T8 — no seeding needed. 3. Loop paused mid-T6 for a design discussion: "Leave team" (self-leave that un-claims the profile) was decided as a separate follow-up on its own branch — see memory `project_player_removal_and_leave_model`.

## T7 · Restore server action, former-players loader and prop plumbing — blocked

- **gate:** mechanical FAIL (tests); completion review not run
- **reason:** `tests/team-loading.spec.ts` ×3 — the harness mocks `team-roster-server` with only `getRosterData` (L114-121); the page now also imports `getFormerPlayers`, so the mock hands back `undefined` and `RosterContent` throws before `Promise.allSettled` starts. Caused by this diff; the fix is one line in the spec's mock (`getFormerPlayers: async () => []`) but the task's `files:` did not name the spec and the gate is fail-closed. `tests/teams-management.spec.ts:149` also failed ("Only the owner can change who can upload team matches", a coach calling `update_program_settings`) — live-DB test, no RPC touched by this diff; see the follow-up below for the clean-tree check. Subagent's own typecheck + lint were clean.
- **stash:** `c9d054b2e539322b13a5be995d7779cafbeed875` (`git stash apply <sha>`, not pop — six files: `roster-actions.ts`, `team-roster-server.ts`, `roster/page.tsx`, `roster-header-buttons.tsx`, `roster-day-zero.tsx`, `add-player-dialog.tsx`; `roster-day-zero.tsx` is a second `RosterHeaderButtons` call site the task's guess missed, correctly included)
- **follow-ups:** 1. To re-run: reset `status:` to `todo`, add `tests/team-loading.spec.ts` to `files:` with a criterion that its `team-roster-server` mock also exports `getFormerPlayers`, and apply the stash first so the subagent starts from the near-complete diff. 2. **Confirmed on the clean tree** (`npx playwright test tests/teams-management.spec.ts` at HEAD `251aa76`: same single failure). `teams-management.spec.ts:149` is a live-DB regression — `update_program_settings` now refuses a coach flipping `players_can_upload` ("Only the owner can change who can upload team matches") while the spec expects a coach may. T6's full-suite gate passed at `251aa76` ~25 min earlier, so the live function changed in between (other worktrees are active on this machine). Until the live function or the spec is corrected, **every remaining task's mechanical gate fails** — its own task, not part of T7. The loop was stopped for this reason rather than blocking T9 on a failure it cannot cause.

**Update:** the owning session (`team-home-empty-space-commit-5a6699`, branch `claude/profile-account-plan-design-3b943f`) fixed `tests/teams-management.spec.ts` in its own commit `2d06a1f`/`6de5d58` — the spec now asserts a coach's flip is refused (`INSUFFICIENT_PRIVILEGE`), matching their `upload_policy_owner_only` migration. Cherry-picked onto this branch as `018bb25` (8 test-fixture files for the same `events_policy`/`canManageTeamSchedule` rename), amended once to fix Prettier on 3 of those files. Full suite green again at `018bb25`. Note: this branch's own `supabase/migrations/` still does not contain the `upload_policy_owner_only` migration itself (only the shared live database has it) — flagged and tracked, see T10's log entry below.

## T9 · Say in the drawer's Remove row that matches are kept and the player can be restored — done

- **gate:** mechanical pass (lint/typecheck/tests), completion review pass
- **changed:** In `src/components/dashboard/team/player-drawer.tsx`'s `MemberMenu`, the Remove row now stacks a description under its "Remove from roster" label — in the same label+description shape the "Can send video" row uses — reading "Their matches stay. Adding them again offers to restore this profile." — only when `member.profileId` is non-null (the archive branch); a seat-only row (`removeMember` branch) renders unchanged. To support the two-line stack, the button moved from `items-center` to `items-start` (both branches, for visual consistency) and `hover:text-[var(--danger)]` moved from the button to a `group`/`group-hover:` pairing on the label span — the completion reviewer verified this preserves the exact hover behaviour rather than silently dropping it.

## T10 · `program_former_players`: fold a claimed profile's pre-claim matches and drop the per-row subquery — blocked

- **gate:** mechanical PASS (lint/typecheck/tests); completion review FAIL (`VERDICT: needs-work`)
- **reason:** The completion reviewer confirmed every file-content criterion met — signature/return shape, no correlated subquery, the `IN (pp.id, pp.claimed_by_user_id)` fold (correctly null-safe: an unmatched `NULL` inside `IN` is `unknown`, which a `LEFT JOIN ... ON` treats as no-match, not an error or a spurious match), every T6 guarantee preserved verbatim, `20260913031528` byte-identical. It marked the task `needs-work` for exactly one reason: the "applied live and verified" criterion cannot be checked by `task-completion-reviewer`, whose tool grant is `Read, Grep, Glob, Bash` — no Supabase MCP access — so it correctly declined to restate the implementing subagent's `execute_sql`/`apply_migration` numbers as independently confirmed. This is a structural gap in the reviewer's tools, not a defect found in the migration; per `SKILL.md`'s fail-closed rule ("anything other than a literal `VERDICT: pass` … including `VERDICT: needs-work`" is a failure), the orchestrator did not override it.
- **stash:** `bb73eb5c52ce15a3dbd07f9d60d6c4c7f714c745` (`git stash apply <sha>`, not pop — one file: `supabase/migrations/20260913060000_program_former_players_fold_claimed_ids.sql`)
- **follow-ups:** 1. The migration itself is almost certainly correct — the implementing subagent's own live probes (quoted in the T10 dispatch transcript) already showed `pg_get_functiondef` matching the new body, `routine_privileges` unchanged, and a 13-row before/after match on ZZ Test Program with no count regressions. Before re-running this task, either (a) grant `task-completion-reviewer` Supabase MCP tools so migration tasks can actually clear this gate, or (b) have the orchestrator re-verify the live state itself and record that verification directly in this log entry as the basis for a manual pass, since the reviewer structurally cannot. 2. This same gap will recur for any future migration task — worth a `/task-add` on the pipeline/skill itself, not this branch's queue, to decide (a) or (b) once rather than per-task.
