# Run log — codex/dual-match-workflow

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Persist schedule outcome records — blocked

**gate:** Mechanical: `npm run lint` passed; `npx tsc --noEmit` passed; `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` failed (582 passed, 67 skipped, 9 failed). Browser failures were Chromium launch failures: `bootstrap_check_in ... Permission denied (1100)` in the sandbox. Credentials were explicitly disabled to prevent fixture writes to an unverified database. Gate logs: `/tmp/dual-match-t1-gates.69uqTP/`. Completion review and RLS review were not dispatched because the mechanical gate failed first; pipeline guardrails were not applicable (no dashboard/upload changes). No tasks were passed over before selecting T1.

**changed:** An unapplied additive migration and six opt-in development-database tests were drafted, then stashed; no implementation code landed. Live `list_tables` verified relevant parent columns/checks/FKs, but `execute_sql` and `list_branches` returned `Insufficient scope`. The development target remains unidentified, so the required live uniqueness/role/isolation proof was not performed. The targeted DB suite reported six skipped, not verified. No database writes or production migrations occurred. User-approved routing mapped `fable` to `gpt-6-astra`; queue task contents and routing labels were preserved.

**stash:** `ae24bc4ac0fd6ed04c4e75eb82b800a7122679c8` (`blocked: T1`), containing `supabase/migrations/20260910120000_add_program_event_outcomes.sql` and `tests/schedule-outcomes-db.spec.ts`. Recovery requires an approved development target with schema access and a browser-capable test environment, then explicit requeueing of T1 after restoring its draft.

## T4 · Define schedule role capabilities — done

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` all passed. The gates ran with approved browser-capable execution permissions; live database fixtures were disabled to avoid writes to an unverified target. Logs: `/tmp/dual-match-t4-gates.M3ntXd/`. Focused capability tests: six passed; changed-file Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`, every criterion met, no scope creep. Pipeline guardrails skipped because no dashboard/upload files changed. RLS reviewer skipped because the tracked/untracked diff contains only pure workspace helpers and tests, with no database, query, API, or Supabase changes. T2 was passed over waiting on T1; T3 was passed over waiting on T2.

**changed:** Added named Schedule capability helpers and a shared capability object. All team roles can view; owner/coach/staff can create/edit/score; only owner/coach can delete. Personal workspaces receive no team Schedule capabilities, and player upload entitlement does not grant scheduled-line writes. Added tests for every team role, personal workspace exclusion, and upload independence. Caller integration remains with later tasks. User-approved routing used Sol for implementation and Terra for completion review; no task contents or ordering changed.

## T7 · Wire Schedule capabilities and remove Import — blocked

**gate:** Mechanical: `npm run lint` and `npx tsc --noEmit` passed. `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` failed (596 passed, 61 skipped, one failed). `tests/schedule-static-copy.spec.ts:248` still asserts that StaticSchedule draws `Import`, contradicting this task's explicit removal criterion. Browser-capable execution succeeded; live database fixtures were disabled. Logs: `/tmp/dual-match-t7-gates.idknm7/`. Targeted formatting/lint/typecheck and `git diff --check` passed. Completion review and pipeline guardrails were not dispatched because the mechanical gate failed first; RLS review was not applicable (no database/API/Supabase changes or new queries). Dependency scan passed over T2 waiting on T1, T3 waiting on T2, T5 waiting on T3, and T6 waiting on T5.

**changed:** Drafted capability plumbing from SchedulePage through StaticSchedule to EventDrawer and removed the inert Schedule Import button. Preserved the separate upload entitlement, Matches import, and existing drawer layout; no T8 work. The three-file implementation was stashed, not committed. The old copy test was not updated by the worker. Recovery needs restoration of the draft, an updated test that asserts Schedule Import is absent while Matches import remains available, and explicit requeueing before the gates run again. User-approved routing used Sol. Task contents and ordering remained unchanged.

**stash:** `1824acbef9891777d56fe1d16ff03b0b92075a6d` (`blocked: T7`), containing the three planned source files only. No database writes occurred.

## T14 · Clarify tournament roster inclusion and draws — blocked

**gate:** Mechanical: `npm run lint` and `npx tsc --noEmit` passed. `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` failed (597 passed, 61 skipped, two failed). The real-component Next browser fixture's create case did not update after inclusion and timed out waiting for `Seed for Ana Vasquez` at `tests/schedule-tournament-field.spec.ts:190`; the edit case's layout assertion used the wrong enclosing element and failed at line 252 (draw edge 250.34375 versus lock x=24). Client hydration is the worker's suspected create-fixture issue, not a verified product diagnosis. Browser-capable execution was used and live database credentials were disabled. Logs: `/tmp/dual-match-t14-gates.VCcI95/`. Focused ESLint/typecheck, `git diff --check`, and all 19 schedule copy tests passed. Completion and pipeline reviews were not dispatched because the mechanical gate failed first. RLS review was not applicable: no database/API/Supabase edits or new queries. Passed over T2 waiting on T1, T3 on T2, T5 on T3, T6 on T5, T8 on T7, T9 on T6 and T8, T10 on T6, T11 on T10, T12 on T11, and T13 on T12.

**changed:** Drafted explicit athlete inclusion, descriptions for the existing Main draw/Qualifying choices, preserved stable-ID entry construction and settled locks, and updated the intentional copy assertion. Extracted the existing payload builder for a real-component create/edit browser fixture. All three task files were stashed rather than committed. No live writes or production migrations ran; temporary browser-fixture directories were absent at the final status check. Recovery requires fixing the browser fixture/interaction and its layout locator, restoring the stash, and explicitly requeueing T14 before rerunning gates. User-approved routing used Sol; task contents and ordering were unchanged.

**stash:** `c7f562489d7f46015cdf20da564ee0d57ea5e23d` (`blocked: T14`), containing `src/components/dashboard/schedule/static/static-tournament-builder.tsx`, `tests/schedule-static-copy.spec.ts`, and `tests/schedule-tournament-field.spec.ts`.

## T1 · Persist schedule outcome records — done

**gate:** Retry explicitly requested by the user after opening Docker. Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' SCHEDULE_OUTCOMES_DEV_URL='' npm test` passed (597 passed, 67 skipped). Separately, all six real signed-in Auth/REST outcome tests passed against the verified isolated local Supabase stack (928ms), with no skips. A negative remote-URL guard check refused execution before constructing clients. Changed-test Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`. RLS reviewer: explicit pass with no findings; its stalled turn was resumed to obtain the final report. Pipeline guardrails skipped because no dashboard/upload code changed. Logs and local runner: `/tmp/advantage-t1-gates.A5O6F3/`. No dependency scan was needed because the user selected T1 explicitly.

**changed:** Restored the original T1 draft without dropping its stash and completed the additive outcome migration, localhost-only DB spec, and reduced local test schema fixture. Outcomes retain actor/time, enforce dual/round uniqueness and event/program scope, and allow member reads with owner/coach/staff create/clear; immutable provenance cannot be supplied by authenticated callers. Legacy forfeits remain untouched. Created an unlinked Supabase CLI 2.110.0 / PostgreSQL 17.6 test stack at `/tmp/advantage-t1-supabase.nVvVfW` (API `http://127.0.0.1:55321`, database port 55322), separate from the existing `crosses-ios` stack. Applied the fixture and migration only inside `supabase_db_advantage-t1-supabase.nVvVfW` using Docker psql; CLI `db query --file` rejected multiple statements without applying them. Pre-migration, post-migration, and post-test synthetic history snapshots for programs/events/entries/matches/stats/points/shots are byte-identical (SHA256 `b18e54ef8580827e0fd8a92a0f61497dfd3165bc8ed4769acf9dcd5610fd6c53`). Cleanup left zero test auth users, memberships, and outcomes; local security/performance advisors reported no warning/error findings. The local stack is stopped with its volume retained after verification; `.env.local`, production, the original stashes, and the paused queue automation are untouched. User-approved routing used Astra for implementation and Terra for both reviews; task descriptions/order were preserved. T2 is now eligible, but this run stops after T1.

**follow-ups:** 1. Before any production rollout, verify the existing membership SELECT policy and parent triggers/FK actions using sufficient read-only catalog access or an authoritative schema-only export. Live `list_tables` verified parent columns/checks/FK targets, but remote policy SQL still returns `Insufficient scope`; the documented reduced local fixture is not a production policy/trigger clone. 2. The current CLI binds development ports on all interfaces despite a loopback Docker network option; keep this synthetic-only stack stopped when not testing, or verify effective loopback binding before leaving it running.

## T2 · Resolve outcome state in the schedule domain — done

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' SCHEDULE_OUTCOMES_DEV_URL='' npm test` passed (614 passed, 67 skipped). Browser-capable execution was used; production database credentials were explicitly disabled. Focused outcome/legacy-forfeit/tournament-run coverage: 40 passed. Changed-file Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`, all criteria met. Pipeline guardrails skipped because no dashboard/upload files changed. RLS review skipped because the tracked/untracked diff contains only pure domain types/derivation, status labels, and tests; no database/API/Supabase changes or new queries. Logs: `/tmp/dual-match-t2-gates.JL8nVi/`. No unmet dependencies were passed over: T2 was first eligible and T1 was done.

**changed:** Added typed outcomes and a played/non-played/unanswered result union, explicit round-scoped resolution and winner/state helpers, neutral default/withdrawal labels, and outcome-aware dual scoring, video eligibility, and analysis coverage. Existing signatures remain compatible and legacy forfeits retain their fallback and precedence. Tests exercise every kind/side for duals and tournament rounds, clear-to-unanswered, mixed played/outcome rounds, legacy results, and singles/doubles arithmetic. No UI or data-access wiring, database writes, or production changes. User-approved routing used Astra for implementation and Terra for completion review. Resumed the existing Codex heartbeat at its saved 10-minute interval; one task per run, no overlapping tasks, and pause when none are eligible. Task descriptions and ordering were preserved; T3 is now eligible and this invocation stops after T2.

**follow-ups:** 1. T3 must populate `outcomes`; tournament consumers should use the explicit round-scoped resolution/state/video helpers. 2. Clearing a legacy result must also clear `entry.forfeit`, since it remains a deliberate fallback during rollout. These are integration notes for existing tasks, not new queue entries.

## T3 · Load outcomes with team schedule data — done

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' SCHEDULE_OUTCOMES_DEV_URL='' npm test` passed (617 passed, 67 skipped). Focused loader tests: three passed; changed-file Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`, every criterion met. RLS reviewer: explicit no findings, with cookie-client and program/event/entry scope preserved. Pipeline guardrails reviewer: explicit no findings; run conservatively for the changed upload-queue projection although no dashboard UI files changed. Both applicable guardrails ran; none skipped. Logs: `/tmp/dual-match-t3-gates.qqHiQr/`. A read-only smoke query against the retained isolated local Supabase stack accepted the exact outcome columns and filters and returned zero rows; this verifies query/schema compatibility, not authenticated RLS. The stack was stopped again with its volume retained. No unmet dependencies were passed over: T3 was first eligible and T2 was done.

**changed:** Added one batched, workspace-scoped outcome read alongside matches, associates by program/event/entry and preserves independent sorted rounds. Outcome-only entries remain in schedule data without fabricated match rows; only actual match IDs reach analysis loading. The shared upload-queue projection excludes outcome-only rounds from waiting lists and totals while retaining played sibling tournament rounds. Added loader tests for cross-event/program isolation, outcome-only visibility, single-event filters, and analysis/upload exclusions. Existing cookie-client and cached schedule entry points remain intact. No database writes, production migrations, or `.env.local` changes occurred. User-approved routing used Sol for implementation and Terra for all reviews; task descriptions/order were preserved. T5 is now eligible; this invocation stops after T3.

## T5 · Authorize outcome writes and score conflicts — done

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' SCHEDULE_OUTCOMES_DEV_URL='' SCHEDULE_OUTCOMES_LOCAL_FIXTURE='' npm test` passed (622 passed, 76 skipped). Separately, five actual-action tests with mocked request dependencies, nine local database tests including six deterministic competing-write races under READ COMMITTED/REPEATABLE READ, and all six existing T1 database regressions passed. Changed-file Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`, every criterion met. RLS reviewer: explicit pass, no findings. Pipeline guardrails skipped because no dashboard or upload-wizard files changed. Mechanical logs: `/tmp/dual-match-t5-gates.3GxGTZ/`; local evidence: `/tmp/dual-match-t5-{actions,db-tests,t1-regression,typecheck,advisors,cleanup,remote-guard,stop}.log`. Local security advisors reported no issues; the remote-target guard refused execution. No dependencies were passed over: T5 was first eligible with T3 and T4 done.

**changed:** Added staff-authorized set/clear outcome action and invoker RPC, clear-before-change enforcement, exact-round score conflict checks, refreshes, and actionable serialization errors. Private trigger-only integrity checks serialize outcome/match/legacy-forfeit writes against the entry row, preserve independent tournament rounds, and leave unrelated historical match metadata updates available. Added an additive migration, action/database tests, and a reduced local fixture extension. Applied SQL only to the retained isolated local Supabase test stack; no production writes or `.env.local` changes. Cleanup left zero T5 programs/auth users; historical sentinels and T1 regression history checks were unchanged. Stack stopped with its volume preserved. Local tests use explicitly documented assumed parent policies, not a production policy/trigger clone; sufficient read-only production catalog verification remains a rollout prerequisite. User-approved routing used Astra for implementation and Terra for both reviews. Existing 10-minute heartbeat remains active, without duplication. Only the queue status changed; task descriptions and ordering are untouched. This invocation stops after T5.

## T6 · Delete eligible events with server enforcement — done

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' SCHEDULE_OUTCOMES_DEV_URL='' SCHEDULE_OUTCOMES_LOCAL_FIXTURE='' npm test` passed (624 passed, 78 skipped). Changed-file Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`, every criterion met. RLS reviewer: explicit pass, no findings. Pipeline guardrails skipped because no dashboard or upload-wizard files changed. Mechanical logs: `/tmp/dual-match-t6-gates.RcWlLN/`. Worker focused checks: four action/local database tests passed, including six races (both orders for match/outcome/legacy writes), and 13 action/capability regression tests passed; local security advisors reported no issues. Worker evidence is tool output rather than persisted files: focused session `87043`/chunk `8b1078`, regressions `52620`/`92cb1d`, advisors `713a0e`, stop `76880`/`265aab`. No unmet dependencies were passed over: T6 was first eligible with T5 done.

**changed:** Added owner/coach-only `deleteEvent`, an invoker RPC scoped to the active program, and a private trigger enforcing the same role/dependency rules for direct authenticated deletes. Entry locking/versioning protects competing writes; matches, outcomes, and legacy forfeits refuse deletion. The audit record and deletion are atomic, with rollback tested when audit insertion fails, and both Schedule paths are revalidated. Read-only remote catalog access now succeeds: actual FK cascade/SET NULL actions, audit constraints, grants, policies, and triggers were verified before implementation. Local fixtures reproduce the relevant verified rules but remain a reduced schema, not a full production clone. New SQL was applied only to the isolated local stack; synthetic transaction/race fixtures were cleaned up and historical sentinels stayed unchanged. Stack stopped with volumes preserved; no production writes or `.env.local` changes. User-approved routing used Astra for implementation and Terra for both reviews. Task descriptions/order were untouched, and this invocation stops after T6.

**follow-ups:** 1. Wire `deleteEvent(eventId)` into the existing drawer-menu task T9 when its dependencies clear; no extra queue task is needed. 2. The live catalog exposes pre-existing authenticated/anon `TRUNCATE` grants on schedule/audit tables; consider separate hardening review. T6 did not grant or expand them.

## T10 · Validate lineup identities and pairs — done

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' SCHEDULE_OUTCOMES_DEV_URL='' SCHEDULE_OUTCOMES_LOCAL_FIXTURE='' SCHEDULE_DELETE_LOCAL_CONTAINER='' npm test` passed (631 passed, 78 skipped). Focused lineup/entry-plan/outcome-action tests: 23 passed. Changed-file Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`, every criterion met, no scope creep. Pipeline guardrails skipped because no dashboard or upload-wizard files changed. RLS review skipped because the tracked/untracked diff contains only pure validation/settled helpers, action preflight wiring, and tests; no new or changed queries, schema, API, Supabase client, or authorization boundaries. Logs: `/tmp/dual-match-t10-gates.N4zm6b/`, `/tmp/t10-tests.log`, `/tmp/t10-format.log`. Dependency scan passed over T8 waiting on blocked T7, and T9 waiting on T8 (T6 is done).

**changed:** Added stable-ID lineup validation used before create writes and by update reconciliation. Repeated identities within a line and exact duplicate doubles pairs, including reversed order, return slot-specific refusals with no invalid writes. Distinct same-name IDs, singles-plus-doubles participation, and different partner combinations remain allowed. Settled-entry protection preserves existing match/legacy-forfeit checks and includes saved outcomes on duals and tournament rounds. No UI, database operations, production changes, or `.env.local` edits. User-approved routing used Astra for implementation and Terra for completion review. Task descriptions and ordering were preserved; this invocation stops after T10.

## T11 · Choose doubles partners by roster identity — blocked

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' SCHEDULE_OUTCOMES_DEV_URL='' SCHEDULE_OUTCOMES_LOCAL_FIXTURE='' SCHEDULE_DELETE_LOCAL_CONTAINER='' npm test` passed (633 passed, 78 skipped). `git diff --check` passed. Completion reviewer: `VERDICT: needs-work`; the browser acceptance criterion is not met. `tests/schedule-doubles-picker.spec.ts:98-111` renders static markup with a noninteractive replacement for MenuSelect at lines 62-74; same-name and singles-plus-doubles coverage at lines 113-134 is pure payload logic rather than browser interaction. A hydrated real-picker/draft test must select, clear and edit partners and demonstrate same-name identities and permitted singles-plus-doubles participation. Pipeline guardrails applies to the UI diff but was not run because the earlier completion gate failed. RLS review skipped because no database/API/Supabase boundary or query changed. Logs: `/tmp/dual-match-t11-gates.qz1XXH/`. Dependency scan passed over T8 waiting on blocked T7 and T9 waiting on T8 (T6 is done).

**changed:** Preserved all failed implementation and test changes in stash `f490bb211d430c5e6eeb949ba9344defc28e5496`, excluding queue bookkeeping. The stash contains explicit MenuSelect partner choices, same-athlete and duplicate/reversed-pair prevention, stable identity/label draft handling, the necessary new-dual-flow edit-seed adapter, and incomplete browser coverage. No task source changes were committed. Production, the local database, `.env.local`, and existing stashes were untouched. User-approved routing used Sol for implementation and Terra for completion review. Only T11's status changed in the queue; task descriptions and ordering were preserved. This invocation stops after T11.

## T16 · Add result choice to inline scoring — blocked

**gate:** Formal mechanical gate stopped at `npm run lint` (exit 2): ESLint cannot find plugin `react-hooks` for rule `react-hooks/set-state-in-effect`. The new `tests/fixtures/tsx-transpile-loader.cjs` is matched by the repository's plain-JS rules and global hook-rule overrides, but Next's plugin registration at `node_modules/eslint-config-next/dist/index.js:112` matches js/jsx/mjs/ts/tsx/mts/cts, not cjs. Correct the fixture loader/config compatibility and rerun the full gate on an authorized retry. Formal typecheck and full tests did not run after this first failure; completion and applicable pipeline guardrails review were not run. RLS review was not applicable: no data-access, schema, API or Supabase boundary changed. The worker separately reported typecheck, 25 focused tests (including three hydrated browser cases), targeted ESLint, Prettier and diff checks passing; these do not substitute for the failed full lint gate. Failure log: `/tmp/dual-match-t16-gates.E36Rtq/lint.log`. Dependency scan passed over T8 waiting on T7, T9 waiting on T8, T12 waiting on T11, T13 waiting on T12, and T15 waiting on T13.

**changed:** Preserved all T16 source and browser-fixture changes in stash `31045af33d7776333bf8a19a8357f7d52b87664d`, excluding runner bookkeeping. The stash adds a shared result selector, outcome/clear submission including legacy clearing, round-specific inline state, and a hydrated real-component browser harness with action/navigation mocks. No implementation changes were committed. Production, local databases, `.env.local`, and previous stashes were untouched. User-approved routing used Sol for implementation. Only T16's queue status changed; task descriptions/order remain untouched. The existing queue heartbeat was paused because no eligible tasks remain: the queue is incomplete and waits on blocked T7, T11, T14 and T16. This invocation stops after T16.

## T7 · Wire Schedule capabilities and remove Import — done

**gate:** User-authorized retry restored stash `1824acbef9891777d56fe1d16ff03b0b92075a6d` without dropping it. Mechanical: `npm run lint`, `npx tsc --noEmit`, and the full production-credential-disabled `npm test` passed (631 passed, 78 skipped); `git diff --check` passed. Completion reviewer: `VERDICT: pass`, all criteria met with no scope creep. Pipeline guardrails reviewer: explicit no findings; the upload-wizard attribution inputs were untouched, the Schedule remained team-workspace scoped, staff writes and one-off upload entitlement stayed separate, and no analysis-state or provider-name invariant changed. RLS review skipped because the diff changes capability presentation/plumbing and a source-copy regression test only; no database, query, API, Supabase client, or authorization implementation changed. Logs: `/tmp/dual-match-t7-retry-gates.TXWYeS/`.

**changed:** Supplied the named Schedule capability object from the server page through `StaticSchedule` to `EventDrawer`, retained staff-only create/edit/score presentation and the independent `canUploadForProgram` one-off path, and removed the inert Schedule Import action. Updated the copy regression to require Schedule Import's absence while independently preserving the Matches SwingVision entry point. No production/database writes, migrations, `.env.local` changes, or stash deletions occurred. User-approved routing used Sol for implementation and Terra for both reviews. Task descriptions and ordering remain unchanged; the retry sequence continues with T16 on the next loop run.
