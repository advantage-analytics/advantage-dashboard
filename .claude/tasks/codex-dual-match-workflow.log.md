# Run log — codex/dual-match-workflow

Written by `/task-next`. Do not hand-edit — the queue file is yours, this one
is the runner's. Newest entries at the bottom.

## T1 · Persist schedule outcome records — blocked

**gate:** Mechanical: `npm run lint` passed; `npx tsc --noEmit` passed; `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` failed (582 passed, 67 skipped, 9 failed). Browser failures were Chromium launch failures: `bootstrap_check_in ... Permission denied (1100)` in the sandbox. Credentials were explicitly disabled to prevent fixture writes to an unverified database. Gate logs: `/tmp/dual-match-t1-gates.69uqTP/`. Completion review and RLS review were not dispatched because the mechanical gate failed first; pipeline guardrails were not applicable (no dashboard/upload changes). No tasks were passed over before selecting T1.

**changed:** An unapplied additive migration and six opt-in development-database tests were drafted, then stashed; no implementation code landed. Live `list_tables` verified relevant parent columns/checks/FKs, but `execute_sql` and `list_branches` returned `Insufficient scope`. The development target remains unidentified, so the required live uniqueness/role/isolation proof was not performed. The targeted DB suite reported six skipped, not verified. No database writes or production migrations occurred. User-approved routing mapped `fable` to `gpt-6-astra`; queue task contents and routing labels were preserved.

**stash:** `ae24bc4ac0fd6ed04c4e75eb82b800a7122679c8` (`blocked: T1`), containing `supabase/migrations/20260910120000_add_program_event_outcomes.sql` and `tests/schedule-outcomes-db.spec.ts`. Recovery requires an approved development target with schema access and a browser-capable test environment, then explicit requeueing of T1 after restoring its draft.

## T4 · Define schedule role capabilities — done

**gate:** Mechanical: `npm run lint`, `npx tsc --noEmit`, and `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` all passed. The gates ran with approved browser-capable execution permissions; live database fixtures were disabled to avoid writes to an unverified target. Logs: `/tmp/dual-match-t4-gates.M3ntXd/`. Focused capability tests: six passed; changed-file Prettier and `git diff --check` passed. Completion reviewer: `VERDICT: pass`, every criterion met, no scope creep. Pipeline guardrails skipped because no dashboard/upload files changed. RLS reviewer skipped because the tracked/untracked diff contains only pure workspace helpers and tests, with no database, query, API, or Supabase changes. T2 was passed over waiting on T1; T3 was passed over waiting on T2.

**changed:** Added named Schedule capability helpers and a shared capability object. All team roles can view; owner/coach/staff can create/edit/score; only owner/coach can delete. Personal workspaces receive no team Schedule capabilities, and player upload entitlement does not grant scheduled-line writes. Added tests for every team role, personal workspace exclusion, and upload independence. Caller integration remains with later tasks. User-approved routing used Sol for implementation and Terra for completion review; no task contents or ordering changed.

## T7 · Wire Schedule capabilities and remove Import — blocked

**gate:** Mechanical: `npm run lint` and `npx tsc --noEmit` passed. `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` failed (596 passed, 61 skipped, one failed). `tests/schedule-static-copy.spec.ts:248` still asserts that StaticSchedule draws `Import`, contradicting this task's explicit removal criterion. Browser-capable execution succeeded; live database fixtures were disabled. Logs: `/tmp/dual-match-t7-gates.idknm7/`. Targeted formatting/lint/typecheck and `git diff --check` passed. Completion review and pipeline guardrails were not dispatched because the mechanical gate failed first; RLS review was not applicable (no database/API/Supabase changes or new queries). Dependency scan passed over T2 waiting on T1, T3 waiting on T2, T5 waiting on T3, and T6 waiting on T5.

**changed:** Drafted capability plumbing from SchedulePage through StaticSchedule to EventDrawer and removed the inert Schedule Import button. Preserved the separate upload entitlement, Matches import, and existing drawer layout; no T8 work. The three-file implementation was stashed, not committed. The old copy test was not updated by the worker. Recovery needs restoration of the draft, an updated test that asserts Schedule Import is absent while Matches import remains available, and explicit requeueing before the gates run again. User-approved routing used Sol. Task contents and ordering remained unchanged.

**stash:** `1824acbef9891777d56fe1d16ff03b0b92075a6d` (`blocked: T7`), containing the three planned source files only. No database writes occurred.

## T14 · Clarify tournament roster inclusion and draws — blocked

**gate:** Mechanical: `npm run lint` and `npx tsc --noEmit` passed. `NEXT_PUBLIC_SUPABASE_URL='' NEXT_PUBLIC_SUPABASE_ANON_KEY='' SUPABASE_SERVICE_ROLE_KEY='' npm test` failed (597 passed, 61 skipped, two failed). The real-component Next browser fixture's create case did not update after inclusion and timed out waiting for `Seed for Ana Vasquez` at `tests/schedule-tournament-field.spec.ts:190`; the edit case's layout assertion used the wrong enclosing element and failed at line 252 (draw edge 250.34375 versus lock x=24). Client hydration is the worker's suspected create-fixture issue, not a verified product diagnosis. Browser-capable execution was used and live database credentials were disabled. Logs: `/tmp/dual-match-t14-gates.VCcI95/`. Focused ESLint/typecheck, `git diff --check`, and all 19 schedule copy tests passed. Completion and pipeline reviews were not dispatched because the mechanical gate failed first. RLS review was not applicable: no database/API/Supabase edits or new queries. Passed over T2 waiting on T1, T3 on T2, T5 on T3, T6 on T5, T8 on T7, T9 on T6 and T8, T10 on T6, T11 on T10, T12 on T11, and T13 on T12.

**changed:** Drafted explicit athlete inclusion, descriptions for the existing Main draw/Qualifying choices, preserved stable-ID entry construction and settled locks, and updated the intentional copy assertion. Extracted the existing payload builder for a real-component create/edit browser fixture. All three task files were stashed rather than committed. No live writes or production migrations ran; temporary browser-fixture directories were absent at the final status check. Recovery requires fixing the browser fixture/interaction and its layout locator, restoring the stash, and explicitly requeueing T14 before rerunning gates. User-approved routing used Sol; task contents and ordering were unchanged.

**stash:** `c7f562489d7f46015cdf20da564ee0d57ea5e23d` (`blocked: T14`), containing `src/components/dashboard/schedule/static/static-tournament-builder.tsx`, `tests/schedule-static-copy.spec.ts`, and `tests/schedule-tournament-field.spec.ts`.
