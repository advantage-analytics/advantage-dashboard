/**
 * Platform Audit Pa2's "quiet body" claim: 14px/300 on a 1.4 line.
 *
 * A size step over the 12px evidence beneath it rather than display type, so
 * the largest type on Home's first screen stays a KPI number and not a
 * sentence. Shared by the Focus card and the serve-placement strip — the two
 * cards that state a claim — because the setting is what makes them read as
 * one voice; two copies of the string is how they stop.
 */
export const HOME_CLAIM_CLASS =
  "text-[14px] font-light leading-[1.4] text-[var(--ink-900)]";
