# Workspace: events-lineups

- **Feature:** Events & Lineups — copy the `Events & Lineups.dc.html` design
  from the Claude Design project, UI only
- **Scaffolded on branch:** claude/new-session-c3f1ab
- **Date:** 2026-08-31
- **Supersedes:** an earlier `events-lineups` run scoped to designs 5a/5b only
  (the schedule day-zero empty states). That run reached sign-off but was
  never landed; the human abandoned it in favour of this wider scope. Its
  code is already committed on this branch — see `7c930c5`, `4658b5c` and the
  commits below them.

Pipeline rules and invariants: see `.claude/pipeline/CONTEXT.md`.
