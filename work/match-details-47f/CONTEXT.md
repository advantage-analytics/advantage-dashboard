# Workspace: match-details-47f

- **Feature**: Implement frame 47f of the Claude Design artboard "Match Details
  Final" on the `dashboard/matches/[matchId]` route — exact to the design, wired
  to real data where it is tangible, everything else copied as drawn and flagged
  for later. Round 46 (+ the 47a header) of the same artboard already shipped;
  this is the delta 47f adds on top of it.
- **Branch**: claude/match-details-implementation-97c1ee (in its worktree,
  reset onto `splitstep-integration` @ 475f940 before scaffolding)
- **Scaffolded**: 2026-09-02

Rules and invariants: see `.claude/pipeline/CONTEXT.md`. One stage per
`/feature-next match-details-47f` invocation; the human edits `output/`
between runs.
