# Brief seed (verbatim from /feature-new invocation)

> when I make a new schedule event, it adds a another with the breadcrumbs
> instead of putting it in the current breadcrumb. Remove the redundant
> header

Edit or extend freely before running `/feature-next schedule-breadcrumb-header`.
